// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

// Retrieve the binding value, as type QemuTests
func (r *Binding) AsQemuTests() *QemuTests { // qemu-tests (../../../daggerverse/qemu/tests/main.go:19:6)
	q := r.query.Select("asQemuTests")

	return &QemuTests{
		query: q,
	}
}

// Create or update a binding of type QemuTests in the environment
func (r *Env) WithQemuTestsInput(name string, value *QemuTests, description string) *Env { // qemu-tests (../../../daggerverse/qemu/tests/main.go:19:6)
	assertNotNil("value", value)
	q := r.query.Select("withQemuTestsInput")
	q = q.Arg("name", name)
	q = q.Arg("value", value)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Declare a desired QemuTests output to be assigned in the environment
func (r *Env) WithQemuTestsOutput(name string, description string) *Env { // qemu-tests (../../../daggerverse/qemu/tests/main.go:19:6)
	q := r.query.Select("withQemuTestsOutput")
	q = q.Arg("name", name)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

type QemuTests struct { // qemu-tests (../../../daggerverse/qemu/tests/main.go:19:6)
	query *querybuilder.Selection

	all                              *Void
	bareMetal                        *Void
	bareMetalBootsAndCapturesSerial  *Void
	bareMetalExitCodeNonZeroOnFail   *Void
	bareMetalExitCodeZeroOnPass      *Void
	bareMetalRejectsNilFirmware      *Void
	bareMetalRejectsUnknownArch      *Void
	boot                             *Void
	defaultsBootArm64Kernel          *Void
	diskRejectsNilImage              *Void
	endpointRejectsUnforwardedPort   *Void
	endpointReturnsForwardedHostPort *Void
	firmware                         *Void
	id                               *ID
	linuxBootReachesUserspace        *Void
	linuxRejectsNilKernel            *Void
	networking                       *Void
	runCapturesFirmwareSerial        *Void
	runShouldNotBeCached             *Void
	serialLogMaterializesFile        *Void
	serviceForwardedPortReachable    *Void
	stopHaltsService                 *Void
	validation                       *Void
	waitForLineMatchesFirmwareSerial *Void
}

func (r *QemuTests) WithGraphQLQuery(q *querybuilder.Selection) *QemuTests {
	return &QemuTests{
		query: q,
	}
}

// QemuTestsAllOpts contains options for QemuTests.All
type QemuTestsAllOpts struct {
	Parallel int // qemu-tests (../../../daggerverse/qemu/tests/main.go:30:2)
}

// All runs every qemu test as a convenience for local `dagger call all`
// invocations. CI does NOT call All: each sub-aggregator below carries its own
// `+check` directive, so GH Actions schedules each onto its own runner in
// parallel — running All on top would double-bill the same work.
func (r *QemuTests) All(ctx context.Context, opts ...QemuTestsAllOpts) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:27:1)
	if r.all != nil {
		return nil
	}
	q := r.query.Select("all")
	for i := len(opts) - 1; i >= 0; i-- {
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// QemuTestsBareMetalOpts contains options for QemuTests.BareMetal
type QemuTestsBareMetalOpts struct {
	Parallel int // qemu-tests (../../../daggerverse/qemu/tests/baremetal.go:24:2)
}

// BareMetal runs the bare-metal firmware + semihosting tests: boot a tiny
// freestanding Cortex-M3 firmware (built reproducibly via the zig module) on
// the MCU-class lm3s6965evb machine and assert that semihosting SYS_WRITE0
// reaches the serial console and SYS_EXIT surfaces as a guest exit code. These
// are fast TCG boots (the firmware writes a marker and exits immediately).
func (r *QemuTests) BareMetal(ctx context.Context, opts ...QemuTestsBareMetalOpts) error { // qemu-tests (../../../daggerverse/qemu/tests/baremetal.go:21:1)
	if r.bareMetal != nil {
		return nil
	}
	q := r.query.Select("bareMetal")
	for i := len(opts) - 1; i >= 0; i-- {
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// BareMetalBootsAndCapturesSerial verifies a semihosting firmware's SYS_WRITE0
// marker reaches the serial console through RunStatus, Run, and WaitForLine.
func (r *QemuTests) BareMetalBootsAndCapturesSerial(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/baremetal.go:59:1)
	if r.bareMetalBootsAndCapturesSerial != nil {
		return nil
	}
	q := r.query.Select("bareMetalBootsAndCapturesSerial")

	return q.Execute(ctx)
}

// BareMetalExitCodeNonZeroOnFail verifies a firmware that calls SYS_EXIT with a
// non-zero code yields the corresponding non-zero RunStatus().ExitCode.
func (r *QemuTests) BareMetalExitCodeNonZeroOnFail(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/baremetal.go:125:1)
	if r.bareMetalExitCodeNonZeroOnFail != nil {
		return nil
	}
	q := r.query.Select("bareMetalExitCodeNonZeroOnFail")

	return q.Execute(ctx)
}

// BareMetalExitCodeZeroOnPass verifies a firmware that calls SYS_EXIT(0) yields
// RunStatus().ExitCode == 0.
func (r *QemuTests) BareMetalExitCodeZeroOnPass(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/baremetal.go:99:1)
	if r.bareMetalExitCodeZeroOnPass != nil {
		return nil
	}
	q := r.query.Select("bareMetalExitCodeZeroOnPass")

	return q.Execute(ctx)
}

// BareMetalRejectsNilFirmware verifies a nil firmware is rejected. The Dagger
// SDK binding panics via assertNotNil before the call leaves the test module;
// recover and assert the panic mentions the rejected argument.
func (r *QemuTests) BareMetalRejectsNilFirmware(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/baremetal.go:153:1)
	if r.bareMetalRejectsNilFirmware != nil {
		return nil
	}
	q := r.query.Select("bareMetalRejectsNilFirmware")

	return q.Execute(ctx)
}

// BareMetalRejectsUnknownArch verifies BareMetal errors for an arch with no
// bare-metal (MCU) profile — e.g. a SoC arch like X86_64 that Linux/Disk
// accept but BareMetal does not.
func (r *QemuTests) BareMetalRejectsUnknownArch(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/baremetal.go:174:1)
	if r.bareMetalRejectsUnknownArch != nil {
		return nil
	}
	q := r.query.Select("bareMetalRejectsUnknownArch")

	return q.Execute(ctx)
}

// QemuTestsBootOpts contains options for QemuTests.Boot
type QemuTestsBootOpts struct {
	Parallel int // qemu-tests (../../../daggerverse/qemu/tests/main.go:100:2)
}

// Boot runs the real-kernel boot tests against the Alpine aarch64 netboot
// kernel (slow TCG boots).
func (r *QemuTests) Boot(ctx context.Context, opts ...QemuTestsBootOpts) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:97:1)
	if r.boot != nil {
		return nil
	}
	q := r.query.Select("boot")
	for i := len(opts) - 1; i >= 0; i-- {
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// DefaultsBootArm64Kernel verifies the documented defaults boot a working VM:
// arch=AARCH64 with empty machine/cpu must resolve to virt + cortex-a53 and
// reach userspace.
func (r *QemuTests) DefaultsBootArm64Kernel(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:432:1)
	if r.defaultsBootArm64Kernel != nil {
		return nil
	}
	q := r.query.Select("defaultsBootArm64Kernel")

	return q.Execute(ctx)
}

// DiskRejectsNilImage verifies a nil disk image is rejected (assertNotNil
// panic, as above).
func (r *QemuTests) DiskRejectsNilImage(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:251:1)
	if r.diskRejectsNilImage != nil {
		return nil
	}
	q := r.query.Select("diskRejectsNilImage")

	return q.Execute(ctx)
}

// EndpointRejectsUnforwardedPort verifies Endpoint errors for a port that was
// not in the machine's tcpPorts.
func (r *QemuTests) EndpointRejectsUnforwardedPort(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:271:1)
	if r.endpointRejectsUnforwardedPort != nil {
		return nil
	}
	q := r.query.Select("endpointRejectsUnforwardedPort")

	return q.Execute(ctx)
}

// EndpointReturnsForwardedHostPort verifies Endpoint returns host:port for a
// forwarded port.
func (r *QemuTests) EndpointReturnsForwardedHostPort(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:292:1)
	if r.endpointReturnsForwardedHostPort != nil {
		return nil
	}
	q := r.query.Select("endpointReturnsForwardedHostPort")

	return q.Execute(ctx)
}

// QemuTestsFirmwareOpts contains options for QemuTests.Firmware
type QemuTestsFirmwareOpts struct {
	Parallel int // qemu-tests (../../../daggerverse/qemu/tests/main.go:77:2)
}

// Firmware runs the run-to-completion serial-capture tests against a tiny
// custom-init initramfs (fast under TCG).
func (r *QemuTests) Firmware(ctx context.Context, opts ...QemuTestsFirmwareOpts) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:74:1)
	if r.firmware != nil {
		return nil
	}
	q := r.query.Select("firmware")
	for i := len(opts) - 1; i >= 0; i-- {
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// A unique identifier for this QemuTests.
func (r *QemuTests) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *QemuTests) XXX_GraphQLType() string {
	return "QemuTests"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *QemuTests) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *QemuTests) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *QemuTests) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *QemuTests) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = QemuTests{query: selectNode(dag.query, id, "QemuTests")}
	return nil
}

// LinuxBootReachesUserspace verifies a real Alpine aarch64 kernel hands off to
// PID 1 in userspace: the kernel logs the /init handoff and userspace then runs
// a working `uname` syscall.
func (r *QemuTests) LinuxBootReachesUserspace(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:459:1)
	if r.linuxBootReachesUserspace != nil {
		return nil
	}
	q := r.query.Select("linuxBootReachesUserspace")

	return q.Execute(ctx)
}

// LinuxRejectsNilKernel verifies a nil kernel is rejected. The Dagger SDK
// binding panics via assertNotNil before the call leaves the test module;
// recover and assert the panic mentions the rejected argument.
func (r *QemuTests) LinuxRejectsNilKernel(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:231:1)
	if r.linuxRejectsNilKernel != nil {
		return nil
	}
	q := r.query.Select("linuxRejectsNilKernel")

	return q.Execute(ctx)
}

// QemuTestsNetworkingOpts contains options for QemuTests.Networking
type QemuTestsNetworkingOpts struct {
	Parallel int // qemu-tests (../../../daggerverse/qemu/tests/main.go:122:2)
}

// Networking runs the service-bind end-to-end tests: boot a guest that brings up
// networking and serves a forwarded port, then prove reachability and teardown
// through WithServiceBinding. These are slow TCG boots plus real networking.
func (r *QemuTests) Networking(ctx context.Context, opts ...QemuTestsNetworkingOpts) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:119:1)
	if r.networking != nil {
		return nil
	}
	q := r.query.Select("networking")
	for i := len(opts) - 1; i >= 0; i-- {
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// RunCapturesFirmwareSerial verifies Run boots the firmware to completion and
// returns its serial console.
func (r *QemuTests) RunCapturesFirmwareSerial(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:340:1)
	if r.runCapturesFirmwareSerial != nil {
		return nil
	}
	q := r.query.Select("runCapturesFirmwareSerial")

	return q.Execute(ctx)
}

// RunShouldNotBeCached verifies two Run calls on one Machine re-execute QEMU
// rather than cache-hitting: each boot emits fresh /dev/urandom bytes, so the
// two serial consoles must differ.
func (r *QemuTests) RunShouldNotBeCached(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:401:1)
	if r.runShouldNotBeCached != nil {
		return nil
	}
	q := r.query.Select("runShouldNotBeCached")

	return q.Execute(ctx)
}

// SerialLogMaterializesFile verifies SerialLog stages the serial console as a
// readable *dagger.File.
func (r *QemuTests) SerialLogMaterializesFile(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:381:1)
	if r.serialLogMaterializesFile != nil {
		return nil
	}
	q := r.query.Select("serialLogMaterializesFile")

	return q.Execute(ctx)
}

// ServiceForwardedPortReachable boots a guest that brings up networking and
// listens on a runtime-minted port, forwards it over slirp hostfwd, binds the
// machine into a fresh consumer container, and asserts the port is reachable —
// proving hostfwd + slirp + WithServiceBinding wiring end-to-end.
func (r *QemuTests) ServiceForwardedPortReachable(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:487:1)
	if r.serviceForwardedPortReachable != nil {
		return nil
	}
	q := r.query.Select("serviceForwardedPortReachable")

	return q.Execute(ctx)
}

// StopHaltsService proves Stop'sbacking service (the postgres EndpointShouldNotBeCached lifecycle analog).
// The guest serves a per-boot identity token. We pin the service up with an
// explicit Start so it stays one instance across reads, read the token, Stop
// the machine, then read again — the second read re-binds and, because Stop
// killed the original VM, gets a *fresh* boot with a different token. A no-op
// Stop would leave the pinned instance serving the same token, so an unchanged
// token fails the test. This is robust to fast TCG boots (it relies on the
// restart, not on out-racing it); the per-read nonce keeps the two reads from
// cache-colliding.
func (r *QemuTests) StopHaltsService(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:522:1)
	if r.stopHaltsService != nil {
		return nil
	}
	q := r.query.Select("stopHaltsService")

	return q.Execute(ctx)
}

// QemuTestsValidationOpts contains options for QemuTests.Validation
type QemuTestsValidationOpts struct {
	Parallel int // qemu-tests (../../../daggerverse/qemu/tests/main.go:54:2)
}

// Validation runs the pure input-rejection and accessor tests. None boot a
// guest, so they're fast and safe to fan out unbounded.
func (r *QemuTests) Validation(ctx context.Context, opts ...QemuTestsValidationOpts) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:51:1)
	if r.validation != nil {
		return nil
	}
	q := r.query.Select("validation")
	for i := len(opts) - 1; i >= 0; i-- {
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// WaitForLineMatchesFirmwareSerial verifies WaitForLine returns the console
// once the marker line appears.
func (r *QemuTests) WaitForLineMatchesFirmwareSerial(ctx context.Context) error { // qemu-tests (../../../daggerverse/qemu/tests/main.go:362:1)
	if r.waitForLineMatchesFirmwareSerial != nil {
		return nil
	}
	q := r.query.Select("waitForLineMatchesFirmwareSerial")

	return q.Execute(ctx)
}

// AsNode returns this QemuTests as a Node.
// This is a local type conversion — no GraphQL call.
func (r *QemuTests) AsNode() Node {
	return &NodeClient{
		query: r.query,
	}
}

// Tests for the qemu daggerverse module. Each test is exposed as a standalone
// dagger function so it can be invoked individually during TDD; All wires them
// up for parallel execution under `dagger call all`. The four sub-aggregators
// (Validation, Firmware, Boot, Networking) each carry `+check` so CI schedules
// them onto their own runners.
func (r *Query) QemuTests() *QemuTests { // qemu-tests (../../../daggerverse/qemu/tests/main.go:19:6)
	q := r.query.Select("qemuTests")

	return &QemuTests{
		query: q,
	}
}
