// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

// Retrieve the binding value, as type KafkaConsumerTests
func (r *Binding) AsKafkaConsumerTests() *KafkaConsumerTests { // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:34:6)
	q := r.query.Select("asKafkaConsumerTests")

	return &KafkaConsumerTests{
		query: q,
	}
}

// Create or update a binding of type KafkaConsumerTests in the environment
func (r *Env) WithKafkaConsumerTestsInput(name string, value *KafkaConsumerTests, description string) *Env { // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:34:6)
	assertNotNil("value", value)
	q := r.query.Select("withKafkaConsumerTestsInput")
	q = q.Arg("name", name)
	q = q.Arg("value", value)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Declare a desired KafkaConsumerTests output to be assigned in the environment
func (r *Env) WithKafkaConsumerTestsOutput(name string, description string) *Env { // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:34:6)
	q := r.query.Select("withKafkaConsumerTestsOutput")
	q = q.Arg("name", name)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

type KafkaConsumerTests struct { // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:34:6)
	query *querybuilder.Selection

	all             *Void
	goAppCi         *Void
	id              *ID
	mtlsAvroConsume *Void
	tlsAvroConsume  *Void
}

func (r *KafkaConsumerTests) WithGraphQLQuery(q *querybuilder.Selection) *KafkaConsumerTests {
	return &KafkaConsumerTests{
		query: q,
	}
}

// KafkaConsumerTestsAllOpts contains options for KafkaConsumerTests.All
type KafkaConsumerTestsAllOpts struct {
	Source *Directory // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:105:2)

	// Default: "4.2.0"
	KafkaImageTag string // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:107:2)
}

// All runs the suite sequentially, for local `dagger call all`. CI runs only
// GoAppCi (the sole +check); the integration round-trip is blocked by #147.
func (r *KafkaConsumerTests) All(ctx context.Context, opts ...KafkaConsumerTestsAllOpts) error { // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:101:1)
	if r.all != nil {
		return nil
	}
	q := r.query.Select("all")
	for i := len(opts) - 1; i >= 0; i-- {
		// `source` optional argument
		if !querybuilder.IsZeroValue(opts[i].Source) {
			q = q.Arg("source", opts[i].Source)
		}
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaConsumerTestsGoAppCiOpts contains options for KafkaConsumerTests.GoAppCi
type KafkaConsumerTestsGoAppCiOpts struct {
	Source *Directory // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:53:2)
}

// GoAppCi builds the example through the z5labs GoApp archetype: fmt, vet,
// golangci-lint, `go test -race`, and a multi-arch build. GoApp.Ci requires a
// git working tree, so the loaded source is wrapped with gitFixture first.
func (r *KafkaConsumerTests) GoAppCi(ctx context.Context, opts ...KafkaConsumerTestsGoAppCiOpts) error { // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:49:1)
	if r.goAppCi != nil {
		return nil
	}
	q := r.query.Select("goAppCi")
	for i := len(opts) - 1; i >= 0; i-- {
		// `source` optional argument
		if !querybuilder.IsZeroValue(opts[i].Source) {
			q = q.Arg("source", opts[i].Source)
		}
	}

	return q.Execute(ctx)
}

// A unique identifier for this KafkaConsumerTests.
func (r *KafkaConsumerTests) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *KafkaConsumerTests) XXX_GraphQLType() string {
	return "KafkaConsumerTests"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *KafkaConsumerTests) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *KafkaConsumerTests) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *KafkaConsumerTests) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *KafkaConsumerTests) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = KafkaConsumerTests{query: selectNode(dag.query, id, "KafkaConsumerTests")}
	return nil
}

// KafkaConsumerTestsMtlsAvroConsumeOpts contains options for KafkaConsumerTests.MtlsAvroConsume
type KafkaConsumerTestsMtlsAvroConsumeOpts struct {
	Source *Directory // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:77:2)

	// Default: "4.2.0"
	KafkaImageTag string // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:79:2)
}

// MtlsAvroConsume is the recommended-posture integration test: the whole stack
// runs with mutual TLS on both the broker and the Schema Registry hops.
//
// NOT a +check: it reproduces #147 (SchemaRegistry.BindTo alias unresolvable
// from WithExec) and fails at `KafkaSchemaRegistry.bindTo`. Run it on demand
// with `dagger call mtls-avro-consume`; promote back to +check once #147 lands.
func (r *KafkaConsumerTests) MtlsAvroConsume(ctx context.Context, opts ...KafkaConsumerTestsMtlsAvroConsumeOpts) error { // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:73:1)
	if r.mtlsAvroConsume != nil {
		return nil
	}
	q := r.query.Select("mtlsAvroConsume")
	for i := len(opts) - 1; i >= 0; i-- {
		// `source` optional argument
		if !querybuilder.IsZeroValue(opts[i].Source) {
			q = q.Arg("source", opts[i].Source)
		}
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaConsumerTestsTLSAvroConsumeOpts contains options for KafkaConsumerTests.TLSAvroConsume
type KafkaConsumerTestsTLSAvroConsumeOpts struct {
	Source *Directory // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:92:2)

	// Default: "4.2.0"
	KafkaImageTag string // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:94:2)
}

// TlsAvroConsume is the server-TLS (trust-only) variant, runnable on demand.
// Like MtlsAvroConsume it currently reproduces #147 and is not a
func (r *KafkaConsumerTests) TLSAvroConsume(ctx context.Context, opts ...KafkaConsumerTestsTLSAvroConsumeOpts) error { // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:88:1)
	if r.tlsAvroConsume != nil {
		return nil
	}
	q := r.query.Select("tlsAvroConsume")
	for i := len(opts) - 1; i >= 0; i-- {
		// `source` optional argument
		if !querybuilder.IsZeroValue(opts[i].Source) {
			q = q.Arg("source", opts[i].Source)
		}
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// AsNode returns this KafkaConsumerTests as a Node.
// This is a local type conversion — no GraphQL call.
func (r *KafkaConsumerTests) AsNode() Node {
	return &NodeClient{
		query: r.query,
	}
}

// Package main is the kafka-consumer-example tests Dagger module. It exercises
// the runnable example under examples/kafka-consumer/ end to end:
//
//   - GoAppCi builds it through the z5labs GoApp archetype (fmt/vet/lint/test
//     -race + multi-arch build). This is the only +check — it runs in CI.
//   - MtlsAvroConsume / TlsAvroConsume stand up a TLS (or mTLS) Kafka cluster, a
//     TLS/mTLS Schema Registry, and an OpenTelemetry collector wired to Tempo/
//     Mimir/Loki, produce framed Avro records, run the example consumer against
//     the stack, and assert it both decoded the records and exported telemetry.
//
// The integration tests are BLOCKED by a known kafka-module bug — #147:
// SchemaRegistry.BindTo's advertised alias is not resolvable from a WithExec
// process (the service handle detaches when it rides on the cross-module
// SchemaRegistry object). MtlsAvroConsume fails at exactly `KafkaSchemaRegistry.
// bindTo`, so it is deliberately NOT a +check (it would be red-by-#147, not
// red-by-#150). It is kept runnable via `dagger call mtls-avro-consume` as a
// faithful reproduction of the end-to-end user experience that triggers #147,
// and should be promoted back to +check once #147 lands. See the example's
// README for details.
//
// The example source is loaded as a contextual argument (+defaultPath), so the
// +check function runs under `dagger check` with no CLI arguments.
func (r *Query) KafkaConsumerTests() *KafkaConsumerTests { // kafka-consumer-tests (../../../examples/kafka-consumer/tests/main.go:34:6)
	q := r.query.Select("kafkaConsumerTests")

	return &KafkaConsumerTests{
		query: q,
	}
}
