// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

// Retrieve the binding value, as type KafkaTests
func (r *Binding) AsKafkaTests() *KafkaTests { // kafka-tests (../../../daggerverse/kafka/tests/main.go:39:6)
	q := r.query.Select("asKafkaTests")

	return &KafkaTests{
		query: q,
	}
}

// Create or update a binding of type KafkaTests in the environment
func (r *Env) WithKafkaTestsInput(name string, value *KafkaTests, description string) *Env { // kafka-tests (../../../daggerverse/kafka/tests/main.go:39:6)
	assertNotNil("value", value)
	q := r.query.Select("withKafkaTestsInput")
	q = q.Arg("name", name)
	q = q.Arg("value", value)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Declare a desired KafkaTests output to be assigned in the environment
func (r *Env) WithKafkaTestsOutput(name string, description string) *Env { // kafka-tests (../../../daggerverse/kafka/tests/main.go:39:6)
	q := r.query.Select("withKafkaTestsOutput")
	q = q.Arg("name", name)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

type KafkaTests struct { // kafka-tests (../../../daggerverse/kafka/tests/main.go:39:6)
	query *querybuilder.Selection

	all                                                *Void
	apacheClusterMtlsRoundTrip                         *Void
	apacheClusterProduceListTopicsRoundTrip            *Void
	apacheClusterTlsRoundTrip                          *Void
	apacheJvm                                          *Void
	apicurioSchemaRegistryRegisterLookupRoundTrip      *Void
	apicurioSchemaRegistryTlsRegisterLookupRoundTrip   *Void
	autoCreateTopicsDisabled                           *Void
	avroBytesFieldRoundTrip                            *Void
	avroConsumeUnframedErrors                          *Void
	avroFramedProduceConsumeRoundTrip                  *Void
	avroMtlsFramedProduceConsumeRoundTrip              *Void
	avroSerializeRequiresSchemaId                      *Void
	avroTlsFramedProduceConsumeRoundTrip               *Void
	bindBrokersExposesBothListeners                    *Void
	clusterClientCanListTopicsOnFreshCluster           *Void
	confluent                                          *Void
	confluentClusterMtlsRoundTrip                      *Void
	confluentClusterProduceListTopicsRoundTrip         *Void
	confluentClusterTlsRoundTrip                       *Void
	confluentSchemaRegistryMtlsRegisterLookupRoundTrip *Void
	confluentSchemaRegistryTlsRegisterLookupRoundTrip  *Void
	consumerGroupOnSingleBrokerWorks                   *Void
	createAndDeleteTopicRoundTrip                      *Void
	dedicatedControllerAndBrokerProduceConsume         *Void
	describeConsumerGroupReportsLag                    *Void
	describeTopicReportsPartitionsAndConfigs           *Void
	fiveControllerQuorumAccepted                       *Void
	id                                                 *ID
	internalListenersAreEncrypted                      *Void
	invalidControllerCountIsRejected                   *Void
	karapaceSchemaRegistryRegisterLookupRoundTrip      *Void
	karapaceSchemaRegistryTlsRegisterLookupRoundTrip   *Void
	listConsumerGroupsReportsCommittedGroup            *Void
	mtlsRequiresClientCert                             *Void
	mtlsRoundTrip                                      *Void
	native                                             *Void
	oneControllerTwoBrokersReplicationFactorTwo        *Void
	plaintextSecurityProfilesAreNonNil                 *Void
	produceConsumeRoundTripBase64                      *Void
	produceConsumeRoundTripHex                         *Void
	produceConsumeRoundTripRaw                         *Void
	produceRejectsUnknownEncoding                      *Void
	propertiesFileContainsBootstrapAndSecurityProtocol *Void
	propertiesFileContainsMtlsSettings                 *Void
	propertiesFileContainsTlsSettings                  *Void
	redpanda                                           *Void
	redpandaClusterProduceListTopicsRoundTrip          *Void
	redpandaClusterTlsRoundTrip                        *Void
	redpandaSchemaRegistryBundledStopIsNoOp            *Void
	redpandaSchemaRegistryRegisterLookupRoundTrip      *Void
	redpandaSchemaRegistryTlsRegisterLookupRoundTrip   *Void
	schemaRegistry                                     *Void
	schemaRegistryFramedProduceConsumeRoundTrip        *Void
	schemaRegistryJsonframedProduceConsumeRoundTrip    *Void
	schemaRegistryJsonserializeRejectsMalformedInput   *Void
	schemaRegistryPlaintextConsumeUnframed             *Void
	schemaRegistryRegisterLookupRoundTrip              *Void
	schemaRegistryRejectsClusterModeMismatch           *Void
	singleNodeClusterStarts                            *Void
	threeControllerQuorumProduceConsume                *Void
	tlsClientWithWrongCaFails                          *Void
	tlsClusterStarts                                   *Void
	tlsRoundTrip                                       *Void
}

func (r *KafkaTests) WithGraphQLQuery(q *querybuilder.Selection) *KafkaTests {
	return &KafkaTests{
		query: q,
	}
}

// KafkaTestsAllOpts contains options for KafkaTests.All
type KafkaTestsAllOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/main.go:63:2)

	// Default: "8.2.0"
	ConfluentImageTag string // kafka-tests (../../../daggerverse/kafka/tests/main.go:65:2)

	// Default: "v26.1.7"
	RedpandaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/main.go:67:2)

	Parallel int // kafka-tests (../../../daggerverse/kafka/tests/main.go:69:2)
}

// All runs every kafka round-trip test as a convenience for local
// `dagger call all` invocations that want the entire suite in one shot.
// CI does NOT call All: each per-distro group below carries its own
// `+check` directive, so GH Actions schedules each onto its own runner
// in parallel — running All on top would double-bill the same work.
//
// kafkaImageTag picks the tag every spawned Apache cluster runs against —
// applied to both the apache/kafka-native image (ApacheNativeCluster) and
// the apache/kafka JVM image (ApacheCluster). confluentImageTag is the
// independent knob for the cp-kafka tests (Confluent Platform versioning
// is not aligned with Apache's release numbering). redpandaImageTag is
// the independent knob for the redpandadata/redpanda tests.
//
// parallel is the concurrency cap applied at both levels — how many
// groups run at once and how many tests run at once within each group.
// Defaults to 0 (unbounded). Pass any positive integer for a specific
// cap.
func (r *KafkaTests) All(ctx context.Context, opts ...KafkaTestsAllOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/main.go:60:1)
	if r.all != nil {
		return nil
	}
	q := r.query.Select("all")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
		// `confluentImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].ConfluentImageTag) {
			q = q.Arg("confluentImageTag", opts[i].ConfluentImageTag)
		}
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsApacheClusterMtlsRoundTripOpts contains options for KafkaTests.ApacheClusterMtlsRoundTrip
type KafkaTestsApacheClusterMtlsRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_apache.go:237:2)
}

// ApacheClusterMtlsRoundTrip is the MTLS happy-path round-trip for
// Kafka.ApacheCluster. Mirrors MtlsRoundTrip but on the JVM image to
// rule out image-specific differences in how client-cert challenge is
// handled.
func (r *KafkaTests) ApacheClusterMtlsRoundTrip(ctx context.Context, opts ...KafkaTestsApacheClusterMtlsRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_apache.go:234:1)
	if r.apacheClusterMtlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("apacheClusterMtlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsApacheClusterProduceListTopicsRoundTripOpts contains options for KafkaTests.ApacheClusterProduceListTopicsRoundTrip
type KafkaTestsApacheClusterProduceListTopicsRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_apache.go:119:2)
}

// ApacheClusterProduceListTopicsRoundTrip is the PLAINTEXT happy-path
// smoke test for Kafka.ApacheCluster (the JVM image variant): produce a
// single raw record, then call ListTopics and assert the freshly-created
// topic shows up. Together these prove the JVM image's data plane and
// control plane both work; the env-var contract matches
// ApacheNativeCluster so this single test pins down "JVM image actually
// serves traffic".
func (r *KafkaTests) ApacheClusterProduceListTopicsRoundTrip(ctx context.Context, opts ...KafkaTestsApacheClusterProduceListTopicsRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_apache.go:116:1)
	if r.apacheClusterProduceListTopicsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("apacheClusterProduceListTopicsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsApacheClusterTLSRoundTripOpts contains options for KafkaTests.ApacheClusterTLSRoundTrip
type KafkaTestsApacheClusterTLSRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_apache.go:167:2)
}

// ApacheClusterTlsRoundTrip is the TLS happy-path round-trip for
// Kafka.ApacheCluster. Mirrors TlsRoundTrip but on the JVM image to
// rule out image-specific differences in keystore mounts, hostname
// verification, and SSL listener bring-up.
func (r *KafkaTests) ApacheClusterTLSRoundTrip(ctx context.Context, opts ...KafkaTestsApacheClusterTLSRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_apache.go:164:1)
	if r.apacheClusterTlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("apacheClusterTlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsApacheJvmOpts contains options for KafkaTests.ApacheJvm
type KafkaTestsApacheJvmOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/main.go:311:2)

	Parallel int // kafka-tests (../../../daggerverse/kafka/tests/main.go:313:2)
}

// ApacheJVM runs the three apache/kafka JVM-image round-trip tests.
// Each test owns a fresh ApacheCluster, so the group holds no shared
// clusters of its own.
func (r *KafkaTests) ApacheJvm(ctx context.Context, opts ...KafkaTestsApacheJvmOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/main.go:308:1)
	if r.apacheJvm != nil {
		return nil
	}
	q := r.query.Select("apacheJvm")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsApicurioSchemaRegistryRegisterLookupRoundTripOpts contains options for KafkaTests.ApicurioSchemaRegistryRegisterLookupRoundTrip
type KafkaTestsApicurioSchemaRegistryRegisterLookupRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:156:2)
}

// ApicurioSchemaRegistryRegisterLookupRoundTrip is the PLAINTEXT happy-path
// test for Kafka.ApicurioSchemaRegistry: stand an
// apicurio-registry-kafkasql up next to a fresh cluster, then exercise
// register → lookup-by-id → lookup-latest-by-subject → list-subjects →
// set/get-compatibility → delete against its Confluent-compatible REST
// surface — mirroring SchemaRegistryRegisterLookupRoundTrip to prove the
// shared *SchemaRegistryClient drives Apicurio unchanged.
func (r *KafkaTests) ApicurioSchemaRegistryRegisterLookupRoundTrip(ctx context.Context, opts ...KafkaTestsApicurioSchemaRegistryRegisterLookupRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:153:1)
	if r.apicurioSchemaRegistryRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("apicurioSchemaRegistryRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsApicurioSchemaRegistryTLSRegisterLookupRoundTripOpts contains options for KafkaTests.ApicurioSchemaRegistryTLSRegisterLookupRoundTrip
type KafkaTestsApicurioSchemaRegistryTLSRegisterLookupRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:1386:2)
}

// ApicurioSchemaRegistryTlsRegisterLookupRoundTrip drives the Apicurio TLS
// path (Quarkus HTTPS REST + kafkasql SSL storage) end-to-end.
func (r *KafkaTests) ApicurioSchemaRegistryTLSRegisterLookupRoundTrip(ctx context.Context, opts ...KafkaTestsApicurioSchemaRegistryTLSRegisterLookupRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:1383:1)
	if r.apicurioSchemaRegistryTlsRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("apicurioSchemaRegistryTlsRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsAutoCreateTopicsDisabledOpts contains options for KafkaTests.AutoCreateTopicsDisabled
type KafkaTestsAutoCreateTopicsDisabledOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:1174:2)
}

// AutoCreateTopicsDisabled produces to a topic that was never created and
// asserts the call errors out. With KAFKA_AUTO_CREATE_TOPICS_ENABLE=false on
// the broker, the produce path must surface a topic-not-found error rather
// than silently auto-creating, so producer typos can't pass tests.
func (r *KafkaTests) AutoCreateTopicsDisabled(ctx context.Context, opts ...KafkaTestsAutoCreateTopicsDisabledOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:1171:1)
	if r.autoCreateTopicsDisabled != nil {
		return nil
	}
	q := r.query.Select("autoCreateTopicsDisabled")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsAvroBytesFieldRoundTripOpts contains options for KafkaTests.AvroBytesFieldRoundTrip
type KafkaTestsAvroBytesFieldRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:1067:2)
}

// AvroBytesFieldRoundTrip pins the Avro-spec JSON encoding of a bytes field.
// Per the Avro specification, the JSON encoding of bytes (and fixed) is a
// string whose characters are the byte values as Unicode code points 0-255 —
// one character per byte, NOT base64. The input value here contains byte 0xFF
// (code point U+00FF), which is outside the base64 alphabet, so a round-trip
// that preserves it byte-for-byte proves the one-char-per-byte mapping and
// would be impossible under a base64 interpretation.
func (r *KafkaTests) AvroBytesFieldRoundTrip(ctx context.Context, opts ...KafkaTestsAvroBytesFieldRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:1064:1)
	if r.avroBytesFieldRoundTrip != nil {
		return nil
	}
	q := r.query.Select("avroBytesFieldRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsAvroConsumeUnframedErrorsOpts contains options for KafkaTests.AvroConsumeUnframedErrors
type KafkaTestsAvroConsumeUnframedErrorsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:761:2)
}

// AvroConsumeUnframedErrors pins the negative consume path: a record produced
// without a Confluent wire header, consumed with valueDeserializeAs="AVRO" +
// schemaRegistryAware=true, must error out pointing at the missing header. The
// header check fires before any schema lookup, so the registry service never
// has to start.
func (r *KafkaTests) AvroConsumeUnframedErrors(ctx context.Context, opts ...KafkaTestsAvroConsumeUnframedErrorsOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:758:1)
	if r.avroConsumeUnframedErrors != nil {
		return nil
	}
	q := r.query.Select("avroConsumeUnframedErrors")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsAvroFramedProduceConsumeRoundTripOpts contains options for KafkaTests.AvroFramedProduceConsumeRoundTrip
type KafkaTestsAvroFramedProduceConsumeRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:820:2)
}

// AvroFramedProduceConsumeRoundTrip is the happy-path data round-trip for AVRO
// serde: register an Avro record schema to get an id, Produce a JSON document
// with valueSerializeAs="AVRO"then framed), and Consume it back with valueDeserializeAs="AVRO" +
// schemaRegistryAware=true. The asserted invariant is byte-equality of the
// consumed value to the canonical JSON form of the original input, proving the
// JSON->Avro-binary->JSON pipeline preserves the datum.
func (r *KafkaTests) AvroFramedProduceConsumeRoundTrip(ctx context.Context, opts ...KafkaTestsAvroFramedProduceConsumeRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:817:1)
	if r.avroFramedProduceConsumeRoundTrip != nil {
		return nil
	}
	q := r.query.Select("avroFramedProduceConsumeRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsAvroMtlsFramedProduceConsumeRoundTripOpts contains options for KafkaTests.AvroMtlsFramedProduceConsumeRoundTrip
type KafkaTestsAvroMtlsFramedProduceConsumeRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:944:2)
}

// AvroMtlsFramedProduceConsumeRoundTrip is the mTLS counterpart: the same
// round-trip where the kafka-wire client and the REST schema-resolution client
// each present their own leaf (both signed by the single CA). It proves the
// threaded RegistrySecurity profile also carries a client key store through to
// the avro path, not just a trust store.
func (r *KafkaTests) AvroMtlsFramedProduceConsumeRoundTrip(ctx context.Context, opts ...KafkaTestsAvroMtlsFramedProduceConsumeRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:941:1)
	if r.avroMtlsFramedProduceConsumeRoundTrip != nil {
		return nil
	}
	q := r.query.Select("avroMtlsFramedProduceConsumeRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// AvroSerializeRequiresSchemaID pins the up-front validation contract of
// valueSerializeAs="AVRO": Produce must reject a zero schema id before any
// broker or registry I/O. dag.Kafka().Client(...) builds without I/O, so no
// cluster boots — the failure is purely the missing-id guard on the AVRO
// serializer.
func (r *KafkaTests) AvroSerializeRequiresSchemaID(ctx context.Context) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:731:1)
	if r.avroSerializeRequiresSchemaId != nil {
		return nil
	}
	q := r.query.Select("avroSerializeRequiresSchemaId")

	return q.Execute(ctx)
}

// KafkaTestsAvroTLSFramedProduceConsumeRoundTripOpts contains options for KafkaTests.AvroTLSFramedProduceConsumeRoundTrip
type KafkaTestsAvroTLSFramedProduceConsumeRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:922:2)
}

// AvroTlsFramedProduceConsumeRoundTrip is the TLS counterpart of
// AvroFramedProduceConsumeRoundTrip: it stands up a TLS cluster and a TLS
// cp-schema-registry rooted at one CA and drives the same JSON->Avro-binary->
// JSON round-trip, but every hop is encrypted — the kafka-wire client speaks
// TLS to the brokers and the Produce/Consume avro path resolves the schema
// over HTTPS via the threaded RegistrySecurity profile (#141). Without that
// profile the resolution would fail with "serves HTTPS but client is
// PLAINTEXT", so a green round-trip proves the profile reaches Client(...).
func (r *KafkaTests) AvroTLSFramedProduceConsumeRoundTrip(ctx context.Context, opts ...KafkaTestsAvroTLSFramedProduceConsumeRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:919:1)
	if r.avroTlsFramedProduceConsumeRoundTrip != nil {
		return nil
	}
	q := r.query.Select("avroTlsFramedProduceConsumeRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsBindBrokersExposesBothListenersOpts contains options for KafkaTests.BindBrokersExposesBothListeners
type KafkaTestsBindBrokersExposesBothListenersOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:1130:2)
}

// BindBrokersExposesBothListeners binds the cluster's brokers into a
// vanilla alpine container and asserts that both the host-facing client
// port (9092) and the inter-broker port (19092) are reachable from inside
// that container — together they cover the dual-listener contract
// (PLAINTEXT_HOST:9092 for clients, PLAINTEXT:19092 for inter-broker).
func (r *KafkaTests) BindBrokersExposesBothListeners(ctx context.Context, opts ...KafkaTestsBindBrokersExposesBothListenersOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:1127:1)
	if r.bindBrokersExposesBothListeners != nil {
		return nil
	}
	q := r.query.Select("bindBrokersExposesBothListeners")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsClusterClientCanListTopicsOnFreshClusterOpts contains options for KafkaTests.ClusterClientCanListTopicsOnFreshCluster
type KafkaTestsClusterClientCanListTopicsOnFreshClusterOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:672:2)
}

// ClusterClientCanListTopicsOnFreshCluster opens a franz-go-backed Client
// against a fresh cluster and asserts that ListTopics returns without error.
// A fresh KRaft cluster has no user topics, so the result may be empty —
// but the call itself must succeed, which proves module-runtime networking
// can reach the started broker service.
func (r *KafkaTests) ClusterClientCanListTopicsOnFreshCluster(ctx context.Context, opts ...KafkaTestsClusterClientCanListTopicsOnFreshClusterOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:669:1)
	if r.clusterClientCanListTopicsOnFreshCluster != nil {
		return nil
	}
	q := r.query.Select("clusterClientCanListTopicsOnFreshCluster")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsConfluentOpts contains options for KafkaTests.Confluent
type KafkaTestsConfluentOpts struct {

	// Default: "8.2.0"
	ConfluentImageTag string // kafka-tests (../../../daggerverse/kafka/tests/main.go:343:2)

	Parallel int // kafka-tests (../../../daggerverse/kafka/tests/main.go:345:2)
}

// Confluent runs the three confluentinc/cp-kafka round-trip tests.
// Each test owns a fresh ConfluentCluster.
func (r *KafkaTests) Confluent(ctx context.Context, opts ...KafkaTestsConfluentOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/main.go:340:1)
	if r.confluent != nil {
		return nil
	}
	q := r.query.Select("confluent")
	for i := len(opts) - 1; i >= 0; i-- {
		// `confluentImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].ConfluentImageTag) {
			q = q.Arg("confluentImageTag", opts[i].ConfluentImageTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsConfluentClusterMtlsRoundTripOpts contains options for KafkaTests.ConfluentClusterMtlsRoundTrip
type KafkaTestsConfluentClusterMtlsRoundTripOpts struct {

	// Default: "8.2.0"
	ConfluentImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_confluent.go:236:2)
}

// ConfluentClusterMtlsRoundTrip is the MTLS happy-path round-trip for
// Kafka.ConfluentCluster. Mirrors MtlsRoundTrip but on cp-kafka to rule
// out distro-specific differences in how client-cert challenge is
// handled.
func (r *KafkaTests) ConfluentClusterMtlsRoundTrip(ctx context.Context, opts ...KafkaTestsConfluentClusterMtlsRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_confluent.go:233:1)
	if r.confluentClusterMtlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("confluentClusterMtlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `confluentImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].ConfluentImageTag) {
			q = q.Arg("confluentImageTag", opts[i].ConfluentImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsConfluentClusterProduceListTopicsRoundTripOpts contains options for KafkaTests.ConfluentClusterProduceListTopicsRoundTrip
type KafkaTestsConfluentClusterProduceListTopicsRoundTripOpts struct {

	// Default: "8.2.0"
	ConfluentImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_confluent.go:118:2)
}

// ConfluentClusterProduceListTopicsRoundTrip is the PLAINTEXT happy-path
// smoke test for Kafka.ConfluentCluster (the cp-kafka image variant):
// produce a single raw record, then call ListTopics and assert the
// freshly-created topic shows up. Confluent Platform's cp-kafka image
// uses the same `KAFKA_*` Scala-wrapper contract as Apache, so this
// single test pins down "cp-kafka actually serves traffic".
func (r *KafkaTests) ConfluentClusterProduceListTopicsRoundTrip(ctx context.Context, opts ...KafkaTestsConfluentClusterProduceListTopicsRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_confluent.go:115:1)
	if r.confluentClusterProduceListTopicsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("confluentClusterProduceListTopicsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `confluentImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].ConfluentImageTag) {
			q = q.Arg("confluentImageTag", opts[i].ConfluentImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsConfluentClusterTLSRoundTripOpts contains options for KafkaTests.ConfluentClusterTLSRoundTrip
type KafkaTestsConfluentClusterTLSRoundTripOpts struct {

	// Default: "8.2.0"
	ConfluentImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_confluent.go:166:2)
}

// ConfluentClusterTlsRoundTrip is the TLS happy-path round-trip for
// Kafka.ConfluentCluster. Mirrors TlsRoundTrip but on cp-kafka to rule
// out distro-specific differences in keystore mounts, hostname
// verification, and SSL listener bring-up.
func (r *KafkaTests) ConfluentClusterTLSRoundTrip(ctx context.Context, opts ...KafkaTestsConfluentClusterTLSRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_confluent.go:163:1)
	if r.confluentClusterTlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("confluentClusterTlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `confluentImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].ConfluentImageTag) {
			q = q.Arg("confluentImageTag", opts[i].ConfluentImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsConfluentSchemaRegistryMtlsRegisterLookupRoundTripOpts contains options for KafkaTests.ConfluentSchemaRegistryMtlsRegisterLookupRoundTrip
type KafkaTestsConfluentSchemaRegistryMtlsRegisterLookupRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:1367:2)
}

// ConfluentSchemaRegistryMtlsRegisterLookupRoundTrip is the mTLS counterpart:
// the REST endpoint requires a client certificate and the registry presents
// its own leaf to the mTLS broker for the kafkastore connection, all rooted at
// one CA.
func (r *KafkaTests) ConfluentSchemaRegistryMtlsRegisterLookupRoundTrip(ctx context.Context, opts ...KafkaTestsConfluentSchemaRegistryMtlsRegisterLookupRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:1364:1)
	if r.confluentSchemaRegistryMtlsRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("confluentSchemaRegistryMtlsRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsConfluentSchemaRegistryTLSRegisterLookupRoundTripOpts contains options for KafkaTests.ConfluentSchemaRegistryTLSRegisterLookupRoundTrip
type KafkaTestsConfluentSchemaRegistryTLSRegisterLookupRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:1346:2)
}

// ConfluentSchemaRegistryTlsRegisterLookupRoundTrip is the canonical TLS test:
// a TLS cp-schema-registry terminates HTTPS on its REST endpoint and talks SSL
// to a TLS cluster's brokers for the `_schemas` topic, and the round-trip
// succeeds over an HTTPS client verifying the registry cert against the
// shared CA.
func (r *KafkaTests) ConfluentSchemaRegistryTLSRegisterLookupRoundTrip(ctx context.Context, opts ...KafkaTestsConfluentSchemaRegistryTLSRegisterLookupRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:1343:1)
	if r.confluentSchemaRegistryTlsRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("confluentSchemaRegistryTlsRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsConsumerGroupOnSingleBrokerWorksOpts contains options for KafkaTests.ConsumerGroupOnSingleBrokerWorks
type KafkaTestsConsumerGroupOnSingleBrokerWorksOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:1212:2)
}

// ConsumerGroupOnSingleBrokerWorks produces one record then consumes it back
// through a consumer group on a 1-broker cluster. A successful round-trip
// proves __consumer_offsets was created at the broker's configured
// replication factor (1, after the system-topic env vars take effect).
// Without KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=1 the broker would refuse
// to create __consumer_offsets at the upstream default RF=3 and the group
// join would hang or error.
func (r *KafkaTests) ConsumerGroupOnSingleBrokerWorks(ctx context.Context, opts ...KafkaTestsConsumerGroupOnSingleBrokerWorksOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:1209:1)
	if r.consumerGroupOnSingleBrokerWorks != nil {
		return nil
	}
	q := r.query.Select("consumerGroupOnSingleBrokerWorks")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsCreateAndDeleteTopicRoundTripOpts contains options for KafkaTests.CreateAndDeleteTopicRoundTrip
type KafkaTestsCreateAndDeleteTopicRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:700:2)
}

// CreateAndDeleteTopicRoundTrip exercises the create/list/delete cycle to
// confirm kadm wiring. The topic name is randomized so the test is
// repeatable against the same cluster and never collides with leftovers.
func (r *KafkaTests) CreateAndDeleteTopicRoundTrip(ctx context.Context, opts ...KafkaTestsCreateAndDeleteTopicRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:697:1)
	if r.createAndDeleteTopicRoundTrip != nil {
		return nil
	}
	q := r.query.Select("createAndDeleteTopicRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsDedicatedControllerAndBrokerProduceConsumeOpts contains options for KafkaTests.DedicatedControllerAndBrokerProduceConsume
type KafkaTestsDedicatedControllerAndBrokerProduceConsumeOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:1112:2)
}

// DedicatedControllerAndBrokerProduceConsume verifies that the split
// controller+broker topology (introduced this increment) still supports a
// full produce/consume round-trip — i.e. the broker correctly joined the
// controller quorum over its WithServiceBinding alias.
func (r *KafkaTests) DedicatedControllerAndBrokerProduceConsume(ctx context.Context, opts ...KafkaTestsDedicatedControllerAndBrokerProduceConsumeOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:1109:1)
	if r.dedicatedControllerAndBrokerProduceConsume != nil {
		return nil
	}
	q := r.query.Select("dedicatedControllerAndBrokerProduceConsume")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsDescribeConsumerGroupReportsLagOpts contains options for KafkaTests.DescribeConsumerGroupReportsLag
type KafkaTestsDescribeConsumerGroupReportsLagOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_introspection.go:225:2)
}

// DescribeConsumerGroupReportsLag produces five records, consumes three of
// them through a committing consumer group, and asserts DescribeConsumerGroup
// reports the group in the Empty state with committed-offset lag of 2 (end
// offset 5 minus committed offset 3) on the single partition.
func (r *KafkaTests) DescribeConsumerGroupReportsLag(ctx context.Context, opts ...KafkaTestsDescribeConsumerGroupReportsLagOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_introspection.go:222:1)
	if r.describeConsumerGroupReportsLag != nil {
		return nil
	}
	q := r.query.Select("describeConsumerGroupReportsLag")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsDescribeTopicReportsPartitionsAndConfigsOpts contains options for KafkaTests.DescribeTopicReportsPartitionsAndConfigs
type KafkaTestsDescribeTopicReportsPartitionsAndConfigsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_introspection.go:100:2)
}

// DescribeTopicReportsPartitionsAndConfigs creates a 3-partition RF=1 topic
// and asserts DescribeTopic reports the derived partition count / replication
// factor, one partition entry per partition, and a non-empty topic-level
// config set (proving the configs path is wired).
func (r *KafkaTests) DescribeTopicReportsPartitionsAndConfigs(ctx context.Context, opts ...KafkaTestsDescribeTopicReportsPartitionsAndConfigsOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_introspection.go:97:1)
	if r.describeTopicReportsPartitionsAndConfigs != nil {
		return nil
	}
	q := r.query.Select("describeTopicReportsPartitionsAndConfigs")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsFiveControllerQuorumAcceptedOpts contains options for KafkaTests.FiveControllerQuorumAccepted
type KafkaTestsFiveControllerQuorumAcceptedOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:961:2)
}

// FiveControllerQuorumAccepted proves the constructor accepts a five-voter
// quorum (odd, > 3) and returns a *Cluster: resolving BootstrapServers forces
// the server-side constructor — validation, internal-CA minting of a leaf per
// controller, and the full container graph — to run without booting the
// containers, so the accept path is exercised cheaply. The 3-controller
// end-to-end quorum is covered by ThreeControllerQuorumProduceConsume.
func (r *KafkaTests) FiveControllerQuorumAccepted(ctx context.Context, opts ...KafkaTestsFiveControllerQuorumAcceptedOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:958:1)
	if r.fiveControllerQuorumAccepted != nil {
		return nil
	}
	q := r.query.Select("fiveControllerQuorumAccepted")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// A unique identifier for this KafkaTests.
func (r *KafkaTests) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *KafkaTests) XXX_GraphQLType() string {
	return "KafkaTests"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *KafkaTests) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *KafkaTests) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *KafkaTests) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *KafkaTests) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = KafkaTests{query: selectNode(dag.query, id, "KafkaTests")}
	return nil
}

// KafkaTestsInternalListenersAreEncryptedOpts contains options for KafkaTests.InternalListenersAreEncrypted
type KafkaTestsInternalListenersAreEncryptedOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:448:2)
}

// InternalListenersAreEncrypted spins up a 1+2 cluster with TLS on the
// external listener and creates an RF=2 topic. A successful produce →
// consume round-trip proves replication traffic flowed over the (always
// mTLS) INTERNAL inter-broker listener: without working internal mTLS,
// the second broker would never become an in-sync replica and the produce
// (with default acks=all-isr) would stall.
func (r *KafkaTests) InternalListenersAreEncrypted(ctx context.Context, opts ...KafkaTestsInternalListenersAreEncryptedOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:445:1)
	if r.internalListenersAreEncrypted != nil {
		return nil
	}
	q := r.query.Select("internalListenersAreEncrypted")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsInvalidControllerCountIsRejectedOpts contains options for KafkaTests.InvalidControllerCountIsRejected
type KafkaTestsInvalidControllerCountIsRejectedOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:932:2)
}

// InvalidControllerCountIsRejected pins the voter-count policy: a KRaft
// quorum wants an odd voter count for a clean majority, so even controller
// counts are rejected, and a sub-1 count is nonsensical. Both must fail at
// construction time with a clear error rather than spinning up a broken
// topology. (Controllers=0 can't be exercised from the Go SDK — Dagger drops
// the zero value and applies the
func (r *KafkaTests) InvalidControllerCountIsRejected(ctx context.Context, opts ...KafkaTestsInvalidControllerCountIsRejectedOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:929:1)
	if r.invalidControllerCountIsRejected != nil {
		return nil
	}
	q := r.query.Select("invalidControllerCountIsRejected")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsKarapaceSchemaRegistryRegisterLookupRoundTripOpts contains options for KafkaTests.KarapaceSchemaRegistryRegisterLookupRoundTrip
type KafkaTestsKarapaceSchemaRegistryRegisterLookupRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:284:2)
}

// KarapaceSchemaRegistryRegisterLookupRoundTrip is the PLAINTEXT happy-path
// test for Kafka.KarapaceSchemaRegistry: stand a Karapace service up next to
// a fresh cluster, then exercise register → lookup-by-id →
// lookup-latest-by-subject → list-subjects → set/get-compatibility → delete
// against its Confluent-compatible REST surface — mirroring
// SchemaRegistryRegisterLookupRoundTrip to prove the shared
// *SchemaRegistryClient drives Karapace unchanged.
func (r *KafkaTests) KarapaceSchemaRegistryRegisterLookupRoundTrip(ctx context.Context, opts ...KafkaTestsKarapaceSchemaRegistryRegisterLookupRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:281:1)
	if r.karapaceSchemaRegistryRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("karapaceSchemaRegistryRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsKarapaceSchemaRegistryTLSRegisterLookupRoundTripOpts contains options for KafkaTests.KarapaceSchemaRegistryTLSRegisterLookupRoundTrip
type KafkaTestsKarapaceSchemaRegistryTLSRegisterLookupRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:1405:2)
}

// KarapaceSchemaRegistryTlsRegisterLookupRoundTrip drives the Karapace TLS
// path (PEM REST listener + aiokafka SSL storage) end-to-end.
func (r *KafkaTests) KarapaceSchemaRegistryTLSRegisterLookupRoundTrip(ctx context.Context, opts ...KafkaTestsKarapaceSchemaRegistryTLSRegisterLookupRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:1402:1)
	if r.karapaceSchemaRegistryTlsRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("karapaceSchemaRegistryTlsRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsListConsumerGroupsReportsCommittedGroupOpts contains options for KafkaTests.ListConsumerGroupsReportsCommittedGroup
type KafkaTestsListConsumerGroupsReportsCommittedGroupOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_introspection.go:163:2)
}

// ListConsumerGroupsReportsCommittedGroup produces a record, consumes it back
// through a committing consumer group, and asserts the group then appears in
// ListConsumerGroups. A fresh cluster reports no groups, so the group's
// presence proves the join + commit reached __consumer_offsets.
func (r *KafkaTests) ListConsumerGroupsReportsCommittedGroup(ctx context.Context, opts ...KafkaTestsListConsumerGroupsReportsCommittedGroupOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_introspection.go:160:1)
	if r.listConsumerGroupsReportsCommittedGroup != nil {
		return nil
	}
	q := r.query.Select("listConsumerGroupsReportsCommittedGroup")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsMtlsRequiresClientCertOpts contains options for KafkaTests.MtlsRequiresClientCert
type KafkaTestsMtlsRequiresClientCertOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:314:2)
}

// MtlsRequiresClientCert points a TLS-only client (no keystore) at an
// MTLS broker and asserts the handshake fails. Confirms the broker's
// client.auth=required setting is actually being honoured.
func (r *KafkaTests) MtlsRequiresClientCert(ctx context.Context, opts ...KafkaTestsMtlsRequiresClientCertOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:311:1)
	if r.mtlsRequiresClientCert != nil {
		return nil
	}
	q := r.query.Select("mtlsRequiresClientCert")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsMtlsRoundTripOpts contains options for KafkaTests.MtlsRoundTrip
type KafkaTestsMtlsRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:344:2)
}

// MtlsRoundTrip produces and consumes a single record over a mutual-TLS
// external listener. The broker presents its cert (signed by the server
// CA) and demands a client cert in return; the test client presents one
// signed by an independent client CA the broker is configured to trust.
func (r *KafkaTests) MtlsRoundTrip(ctx context.Context, opts ...KafkaTestsMtlsRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:341:1)
	if r.mtlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("mtlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsNativeOpts contains options for KafkaTests.Native
type KafkaTestsNativeOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/main.go:187:2)

	Parallel int // kafka-tests (../../../daggerverse/kafka/tests/main.go:189:2)
}

// Native runs every apache/kafka-native test as one group. It boots
// the three shared ApacheNativeClusters up front, fans the shared-cluster
// and fresh-cluster native tests across a par pool capped at parallel,
// and tears the shared clusters down on return.
func (r *KafkaTests) Native(ctx context.Context, opts ...KafkaTestsNativeOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/main.go:184:1)
	if r.native != nil {
		return nil
	}
	q := r.query.Select("native")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsOneControllerTwoBrokersReplicationFactorTwoOpts contains options for KafkaTests.OneControllerTwoBrokersReplicationFactorTwo
type KafkaTestsOneControllerTwoBrokersReplicationFactorTwoOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:1028:2)
}

// OneControllerTwoBrokersReplicationFactorTwo spins up a 1+2 cluster and
// creates a replication-factor-2 topic so the produce path forces inter-
// broker replication. A successful round-trip proves brokers can reach
// each other over the engine network without explicit peer bindings.
func (r *KafkaTests) OneControllerTwoBrokersReplicationFactorTwo(ctx context.Context, opts ...KafkaTestsOneControllerTwoBrokersReplicationFactorTwoOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:1025:1)
	if r.oneControllerTwoBrokersReplicationFactorTwo != nil {
		return nil
	}
	q := r.query.Select("oneControllerTwoBrokersReplicationFactorTwo")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

func (r *KafkaTests) PlaintextSecurityProfilesAreNonNil(ctx context.Context) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:618:1)
	if r.plaintextSecurityProfilesAreNonNil != nil {
		return nil
	}
	q := r.query.Select("plaintextSecurityProfilesAreNonNil")

	return q.Execute(ctx)
}

// KafkaTestsProduceConsumeRoundTripBase64Opts contains options for KafkaTests.ProduceConsumeRoundTripBase64
type KafkaTestsProduceConsumeRoundTripBase64Opts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:836:2)
}

// ProduceConsumeRoundTripBase64 round-trips the same kind of binary payload
// through standard base64 (with padding).
func (r *KafkaTests) ProduceConsumeRoundTripBase64(ctx context.Context, opts ...KafkaTestsProduceConsumeRoundTripBase64Opts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:833:1)
	if r.produceConsumeRoundTripBase64 != nil {
		return nil
	}
	q := r.query.Select("produceConsumeRoundTripBase64")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsProduceConsumeRoundTripHexOpts contains options for KafkaTests.ProduceConsumeRoundTripHex
type KafkaTestsProduceConsumeRoundTripHexOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:821:2)
}

// ProduceConsumeRoundTripHex round-trips a binary payload through hex
// encoding. The non-UTF-8 bytes (including 0x00) verify that hex transports
// arbitrary binary safely.
func (r *KafkaTests) ProduceConsumeRoundTripHex(ctx context.Context, opts ...KafkaTestsProduceConsumeRoundTripHexOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:818:1)
	if r.produceConsumeRoundTripHex != nil {
		return nil
	}
	q := r.query.Select("produceConsumeRoundTripHex")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsProduceConsumeRoundTripRawOpts contains options for KafkaTests.ProduceConsumeRoundTripRaw
type KafkaTestsProduceConsumeRoundTripRawOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:754:2)
}

// ProduceConsumeRoundTripRaw produces a single record with raw-encoded key
// and value, then consumes it back and asserts byte equality. The raw
// encoding round-trips Go strings verbatim, so the assertion is direct
// string equality.
func (r *KafkaTests) ProduceConsumeRoundTripRaw(ctx context.Context, opts ...KafkaTestsProduceConsumeRoundTripRawOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:751:1)
	if r.produceConsumeRoundTripRaw != nil {
		return nil
	}
	q := r.query.Select("produceConsumeRoundTripRaw")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsProduceRejectsUnknownEncodingOpts contains options for KafkaTests.ProduceRejectsUnknownEncoding
type KafkaTestsProduceRejectsUnknownEncodingOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:851:2)
}

// ProduceRejectsUnknownEncoding verifies that a Produce call with a bogus
// encoding name fails fast rather than silently misbehaving.
func (r *KafkaTests) ProduceRejectsUnknownEncoding(ctx context.Context, opts ...KafkaTestsProduceRejectsUnknownEncodingOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:848:1)
	if r.produceRejectsUnknownEncoding != nil {
		return nil
	}
	q := r.query.Select("produceRejectsUnknownEncoding")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsPropertiesFileContainsBootstrapAndSecurityProtocolOpts contains options for KafkaTests.PropertiesFileContainsBootstrapAndSecurityProtocol
type KafkaTestsPropertiesFileContainsBootstrapAndSecurityProtocolOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:892:2)
}

// PropertiesFileContainsBootstrapAndSecurityProtocol verifies that the
// rendered Java client.properties file carries the bootstrap.servers list
// and a plaintext security.protocol entry — enough for the Apache Kafka
// CLI tools to pick up the connection settings.
func (r *KafkaTests) PropertiesFileContainsBootstrapAndSecurityProtocol(ctx context.Context, opts ...KafkaTestsPropertiesFileContainsBootstrapAndSecurityProtocolOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:889:1)
	if r.propertiesFileContainsBootstrapAndSecurityProtocol != nil {
		return nil
	}
	q := r.query.Select("propertiesFileContainsBootstrapAndSecurityProtocol")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsPropertiesFileContainsMtlsSettingsOpts contains options for KafkaTests.PropertiesFileContainsMtlsSettings
type KafkaTestsPropertiesFileContainsMtlsSettingsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:270:2)
}

// PropertiesFileContainsMtlsSettings verifies that mTLS mode also renders
// the ssl.keystore.* triple referencing a keystore.p12 sidecar.
func (r *KafkaTests) PropertiesFileContainsMtlsSettings(ctx context.Context, opts ...KafkaTestsPropertiesFileContainsMtlsSettingsOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:267:1)
	if r.propertiesFileContainsMtlsSettings != nil {
		return nil
	}
	q := r.query.Select("propertiesFileContainsMtlsSettings")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsPropertiesFileContainsTLSSettingsOpts contains options for KafkaTests.PropertiesFileContainsTLSSettings
type KafkaTestsPropertiesFileContainsTLSSettingsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:231:2)
}

// PropertiesFileContainsTlsSettings verifies the rendered Java
// client.properties carries security.protocol=SSL plus an ssl.truststore.*
// triple referencing a sidecar PKCS#12 file by basename.
func (r *KafkaTests) PropertiesFileContainsTLSSettings(ctx context.Context, opts ...KafkaTestsPropertiesFileContainsTLSSettingsOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:228:1)
	if r.propertiesFileContainsTlsSettings != nil {
		return nil
	}
	q := r.query.Select("propertiesFileContainsTlsSettings")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsRedpandaOpts contains options for KafkaTests.Redpanda
type KafkaTestsRedpandaOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/main.go:376:2)

	Parallel int // kafka-tests (../../../daggerverse/kafka/tests/main.go:378:2)
}

// Redpanda runs the redpandadata/redpanda round-trip tests — the two
// Kafka-wire round-trips, the PLAINTEXT and TLS bundled-Schema-Registry
// round-trips, and the bundled-registry Stop-is-a-no-op lifecycle test.
func (r *KafkaTests) Redpanda(ctx context.Context, opts ...KafkaTestsRedpandaOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/main.go:373:1)
	if r.redpanda != nil {
		return nil
	}
	q := r.query.Select("redpanda")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsRedpandaClusterProduceListTopicsRoundTripOpts contains options for KafkaTests.RedpandaClusterProduceListTopicsRoundTrip
type KafkaTestsRedpandaClusterProduceListTopicsRoundTripOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_redpanda.go:64:2)
}

// RedpandaClusterProduceListTopicsRoundTrip is the PLAINTEXT happy-path
// round-trip for Kafka.RedpandaCluster: spin up a single-node Redpanda,
// create a topic, produce one record, then assert the freshly-created
// topic shows up in ListTopics. Pins down "redpanda actually serves
// Kafka-wire traffic on the external listener".
func (r *KafkaTests) RedpandaClusterProduceListTopicsRoundTrip(ctx context.Context, opts ...KafkaTestsRedpandaClusterProduceListTopicsRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_redpanda.go:61:1)
	if r.redpandaClusterProduceListTopicsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("redpandaClusterProduceListTopicsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsRedpandaClusterTLSRoundTripOpts contains options for KafkaTests.RedpandaClusterTLSRoundTrip
type KafkaTestsRedpandaClusterTLSRoundTripOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_redpanda.go:113:2)
}

// RedpandaClusterTlsRoundTrip is the TLS happy-path round-trip for
// Kafka.RedpandaCluster: spin up Redpanda with kafka_api_tls.enabled=true
// using PEM cert/key/CA mounted into /etc/redpanda/certs, then produce
// and consume one record over the TLS listener with the franz-go client
// verifying the broker leaf against the matching truststore.
func (r *KafkaTests) RedpandaClusterTLSRoundTrip(ctx context.Context, opts ...KafkaTestsRedpandaClusterTLSRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_redpanda.go:110:1)
	if r.redpandaClusterTlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("redpandaClusterTlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsRedpandaSchemaRegistryBundledStopIsNoOpOpts contains options for KafkaTests.RedpandaSchemaRegistryBundledStopIsNoOp
type KafkaTestsRedpandaSchemaRegistryBundledStopIsNoOpOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_redpanda.go:285:2)
}

// RedpandaSchemaRegistryBundledStopIsNoOp pins the bundled-registry lifecycle
// contract: a bundled *SchemaRegistry shares the broker service, so sr.Stop
// must be a no-op — the cluster owns teardown via cluster.Stop. This runs a
// register/lookup round-trip, calls sr.Stop, then exercises the registry
// again through the same handle. If sr.Stop had torn down the shared broker
// service, that follow-up call would fail.
func (r *KafkaTests) RedpandaSchemaRegistryBundledStopIsNoOp(ctx context.Context, opts ...KafkaTestsRedpandaSchemaRegistryBundledStopIsNoOpOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_redpanda.go:282:1)
	if r.redpandaSchemaRegistryBundledStopIsNoOp != nil {
		return nil
	}
	q := r.query.Select("redpandaSchemaRegistryBundledStopIsNoOp")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsRedpandaSchemaRegistryRegisterLookupRoundTripOpts contains options for KafkaTests.RedpandaSchemaRegistryRegisterLookupRoundTrip
type KafkaTestsRedpandaSchemaRegistryRegisterLookupRoundTripOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_redpanda.go:236:2)
}

// RedpandaSchemaRegistryRegisterLookupRoundTrip is the PLAINTEXT happy-path
// test for RedpandaCluster.SchemaRegistry: `rpk redpanda start` runs a Schema
// Registry inside the broker process on :8081, so this registers a schema
// against cluster.SchemaRegistry() and asserts the lookup-by-id round-trip —
// proving the bundled SR is reachable and interchangeable with the
// separate-container ConfluentSchemaRegistry. The SR service is the broker
// itself, so cluster.Stop tears it down — sr.Stop is a no-op for a bundled
// registry.
func (r *KafkaTests) RedpandaSchemaRegistryRegisterLookupRoundTrip(ctx context.Context, opts ...KafkaTestsRedpandaSchemaRegistryRegisterLookupRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_redpanda.go:233:1)
	if r.redpandaSchemaRegistryRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("redpandaSchemaRegistryRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsRedpandaSchemaRegistryTLSRegisterLookupRoundTripOpts contains options for KafkaTests.RedpandaSchemaRegistryTLSRegisterLookupRoundTrip
type KafkaTestsRedpandaSchemaRegistryTLSRegisterLookupRoundTripOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_redpanda.go:259:2)
}

// RedpandaSchemaRegistryTlsRegisterLookupRoundTrip is the TLS-cluster
// counterpart of RedpandaSchemaRegistryRegisterLookupRoundTrip. A TLS
// Redpanda cluster terminates HTTPS on its bundled Schema Registry REST
// endpoint, reusing the broker's server leaf (configured through the
// separately-rendered redpanda.yaml schema_registry_api_tls block), so this
// exercises that YAML path end-to-end and drives register/lookup over HTTPS,
// verifying the SR cert against the cluster CA truststore.
func (r *KafkaTests) RedpandaSchemaRegistryTLSRegisterLookupRoundTrip(ctx context.Context, opts ...KafkaTestsRedpandaSchemaRegistryTLSRegisterLookupRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_redpanda.go:256:1)
	if r.redpandaSchemaRegistryTlsRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("redpandaSchemaRegistryTlsRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsSchemaRegistryOpts contains options for KafkaTests.SchemaRegistry
type KafkaTestsSchemaRegistryOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/main.go:108:2)

	Parallel int // kafka-tests (../../../daggerverse/kafka/tests/main.go:110:2)
}

// SchemaRegistry runs the Schema Registry tests — ConfluentSchemaRegistry,
// ApicurioSchemaRegistry, and KarapaceSchemaRegistry — as one group. Each test
// owns the cluster (and, for the round-trips, the registry service) it boots,
// so the group's only lifetime guarantee is that both are torn down once it
// returns.
func (r *KafkaTests) SchemaRegistry(ctx context.Context, opts ...KafkaTestsSchemaRegistryOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/main.go:105:1)
	if r.schemaRegistry != nil {
		return nil
	}
	q := r.query.Select("schemaRegistry")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsSchemaRegistryFramedProduceConsumeRoundTripOpts contains options for KafkaTests.SchemaRegistryFramedProduceConsumeRoundTrip
type KafkaTestsSchemaRegistryFramedProduceConsumeRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:410:2)
}

// SchemaRegistryFramedProduceConsumeRoundTrip exercises the data path of the
// Client with Confluent wire-format framing: register a schema to get an ID,
// produce a record whose value is framed with that ID, then consume with
// schemaRegistryAware=true and assert the parsed ID matches and the value
// bytes are stripped back to the original payload.
func (r *KafkaTests) SchemaRegistryFramedProduceConsumeRoundTrip(ctx context.Context, opts ...KafkaTestsSchemaRegistryFramedProduceConsumeRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:407:1)
	if r.schemaRegistryFramedProduceConsumeRoundTrip != nil {
		return nil
	}
	q := r.query.Select("schemaRegistryFramedProduceConsumeRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsSchemaRegistryJsonframedProduceConsumeRoundTripOpts contains options for KafkaTests.SchemaRegistryJsonframedProduceConsumeRoundTrip
type KafkaTestsSchemaRegistryJsonframedProduceConsumeRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:635:2)
}

// SchemaRegistryJSONFramedProduceConsumeRoundTrip composes the framing
// primitive (valueSchemaID) with the JSON serde (valueSerializeAs /
// valueDeserializeAs). A JSON document is registered as a JSON-schema-typed
// subject, produced with both opts on, then consumed with both opts on; the
// asserted invariant is byte-equality of the consumed value to the canonical
// JSON form of the original input — proving frame strip and JSON validation
// compose without corrupting the payload.
func (r *KafkaTests) SchemaRegistryJsonframedProduceConsumeRoundTrip(ctx context.Context, opts ...KafkaTestsSchemaRegistryJsonframedProduceConsumeRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:632:1)
	if r.schemaRegistryJsonframedProduceConsumeRoundTrip != nil {
		return nil
	}
	q := r.query.Select("schemaRegistryJsonframedProduceConsumeRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// SchemaRegistryJSONSerializeRejectsMalformedInput pins the up-front
// validation contract of valueSerializeAs="JSON": Produce must reject a
// malformed JSON payload before any broker I/O. dag.Kafka().Client(...)
// builds without I/O, so no cluster boots — the failure is purely a
// payload-validation failure on the canonicalising serializer.
func (r *KafkaTests) SchemaRegistryJsonserializeRejectsMalformedInput(ctx context.Context) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:604:1)
	if r.schemaRegistryJsonserializeRejectsMalformedInput != nil {
		return nil
	}
	q := r.query.Select("schemaRegistryJsonserializeRejectsMalformedInput")

	return q.Execute(ctx)
}

// KafkaTestsSchemaRegistryPlaintextConsumeUnframedOpts contains options for KafkaTests.SchemaRegistryPlaintextConsumeUnframed
type KafkaTestsSchemaRegistryPlaintextConsumeUnframedOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:505:2)
}

// SchemaRegistryPlaintextConsumeUnframed verifies the negative path: a record
// produced without framing, consumed with schemaRegistryAware=true, must
// surface ValueSchemaID=0 and pass the value bytes through unchanged.
func (r *KafkaTests) SchemaRegistryPlaintextConsumeUnframed(ctx context.Context, opts ...KafkaTestsSchemaRegistryPlaintextConsumeUnframedOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:502:1)
	if r.schemaRegistryPlaintextConsumeUnframed != nil {
		return nil
	}
	q := r.query.Select("schemaRegistryPlaintextConsumeUnframed")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsSchemaRegistryRegisterLookupRoundTripOpts contains options for KafkaTests.SchemaRegistryRegisterLookupRoundTrip
type KafkaTestsSchemaRegistryRegisterLookupRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:28:2)
}

// SchemaRegistryRegisterLookupRoundTrip is the PLAINTEXT happy-path test
// for Kafka.ConfluentSchemaRegistry: stand a cp-schema-registry up next to
// a fresh cluster, then exercise register → lookup-by-id →
// lookup-latest-by-subject → list-subjects → set/get-compatibility →
// delete against it — covering every SchemaRegistryClient operation.
func (r *KafkaTests) SchemaRegistryRegisterLookupRoundTrip(ctx context.Context, opts ...KafkaTestsSchemaRegistryRegisterLookupRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:25:1)
	if r.schemaRegistryRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("schemaRegistryRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsSchemaRegistryRejectsClusterModeMismatchOpts contains options for KafkaTests.SchemaRegistryRejectsClusterModeMismatch
type KafkaTestsSchemaRegistryRejectsClusterModeMismatchOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:580:2)
}

// SchemaRegistryRejectsClusterModeMismatch pins the mode-match contract: a
// registry's security profile must match its backing cluster's client-listener
// mode, so the registry's kafka-storage connection authenticates against the
// broker. Here a PLAINTEXT registry security is paired with a TLS cluster and
// must be rejected with an error naming both modes. The constructor errors
// before any service boots, so the TLS cluster never has to start.
func (r *KafkaTests) SchemaRegistryRejectsClusterModeMismatch(ctx context.Context, opts ...KafkaTestsSchemaRegistryRejectsClusterModeMismatchOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_schema_registry.go:577:1)
	if r.schemaRegistryRejectsClusterModeMismatch != nil {
		return nil
	}
	q := r.query.Select("schemaRegistryRejectsClusterModeMismatch")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsSingleNodeClusterStartsOpts contains options for KafkaTests.SingleNodeClusterStarts
type KafkaTestsSingleNodeClusterStartsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:638:2)
}

// SingleNodeClusterStarts spins up the smallest split-role cluster (one
// controller + one broker) and forces the server-side Cluster constructor
// to run by resolving BootstrapServers, asserting only that the broker
// hostname is non-empty. End-to-end reachability is covered by sibling
// tests that exercise ListTopics / produce / consume.
func (r *KafkaTests) SingleNodeClusterStarts(ctx context.Context, opts ...KafkaTestsSingleNodeClusterStartsOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:635:1)
	if r.singleNodeClusterStarts != nil {
		return nil
	}
	q := r.query.Select("singleNodeClusterStarts")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsThreeControllerQuorumProduceConsumeOpts contains options for KafkaTests.ThreeControllerQuorumProduceConsume
type KafkaTestsThreeControllerQuorumProduceConsumeOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:995:2)
}

// ThreeControllerQuorumProduceConsume stands up a real three-node KRaft
// controller quorum (plus one broker) and drives a produce → consume
// round-trip through it, mirroring the single-controller round-trip. A
// successful round-trip proves the three controllers formed a quorum and
// elected a leader over their (always-mTLS) CONTROLLER listeners — which in
// turn proves the internal CA minted a verifying leaf for every controller
// host and that all three discovered each other over session-wide DNS with
// no controller-to-controller WithServiceBinding. Cluster.Stop then tears
// down all three controller services plus the broker.
func (r *KafkaTests) ThreeControllerQuorumProduceConsume(ctx context.Context, opts ...KafkaTestsThreeControllerQuorumProduceConsumeOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:992:1)
	if r.threeControllerQuorumProduceConsume != nil {
		return nil
	}
	q := r.query.Select("threeControllerQuorumProduceConsume")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsTLSClientWithWrongCaFailsOpts contains options for KafkaTests.TLSClientWithWrongCaFails
type KafkaTestsTLSClientWithWrongCaFailsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:416:2)
}

// TlsClientWithWrongCaFails verifies that pointing the client at a
// truststore for an unrelated CA fails the handshake — i.e. the broker is
// genuinely presenting a cert chained to its own CA, not skipping
// verification.
func (r *KafkaTests) TLSClientWithWrongCaFails(ctx context.Context, opts ...KafkaTestsTLSClientWithWrongCaFailsOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:413:1)
	if r.tlsClientWithWrongCaFails != nil {
		return nil
	}
	q := r.query.Select("tlsClientWithWrongCaFails")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsTLSClusterStartsOpts contains options for KafkaTests.TLSClusterStarts
type KafkaTestsTLSClusterStartsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:592:2)
}

// TlsClusterStarts forces the lazy Cluster construction to run under
// TlsServerSecurity and confirms BootstrapServers reports a non-empty,
// non-zero-port broker address. No client connection attempted — this
// proves caller's CA loads, leaf signing succeeds, the keystore mounts,
// and the broker doesn't crash on startup.
func (r *KafkaTests) TLSClusterStarts(ctx context.Context, opts ...KafkaTestsTLSClusterStartsOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:589:1)
	if r.tlsClusterStarts != nil {
		return nil
	}
	q := r.query.Select("tlsClusterStarts")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// KafkaTestsTLSRoundTripOpts contains options for KafkaTests.TLSRoundTrip
type KafkaTestsTLSRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:518:2)
}

// TlsRoundTrip produces and consumes a single record over a TLS-only
// external listener with TlsClientSecurity holding the CA's truststore.
// Exercises: SAN matching the bootstrap address, kgo dialer + TLS, broker
// SSL listener, end-to-end encryption.
func (r *KafkaTests) TLSRoundTrip(ctx context.Context, opts ...KafkaTestsTLSRoundTripOpts) error { // kafka-tests (../../../daggerverse/kafka/tests/tests_native.go:515:1)
	if r.tlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("tlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// AsNode returns this KafkaTests as a Node.
// This is a local type conversion — no GraphQL call.
func (r *KafkaTests) AsNode() Node {
	return &NodeClient{
		query: r.query,
	}
}

// Tests for the kafka daggerverse module. Each test is exposed as a standalone
// dagger function so it can be invoked individually during TDD; All wires them
// up for grouped, parallel execution under `dagger call all`.
//
// File map (all `package main`, surfaced as one Dagger module):
//
//   - main.go            — Tests struct, the All() orchestrator, and the four
//     per-distro group functions (nativeTests,
//     apacheJVMTests, confluentTests, redpandaTests).
//   - helpers.go         — cross-cutting scaffolding shared across distros:
//     newClusterId, freshCa, randHex, randomTopicName,
//     contains.
//   - tests_native.go    — ApacheNativeCluster (apache/kafka-native) cluster
//     helpers (freshCluster / freshTlsCluster /
//     freshMtlsCluster) + every test that drives the
//     GraalVM image (the bulk of the suite, including
//     shared roundTripBinaryOn).
//   - tests_apache.go    — ApacheCluster (apache/kafka JVM) cluster helpers
//   - the three Apache-JVM round-trip tests.
//   - tests_confluent.go — ConfluentCluster (confluentinc/cp-kafka) cluster
//     helpers + the three cp-kafka round-trip tests.
//   - tests_redpanda.go  — RedpandaCluster (redpandadata/redpanda) cluster
//     helpers + the two Redpanda Kafka-wire round-trip
//     tests, the PLAINTEXT + TLS bundled-Schema-
//     Registry round-trips, and the bundled-registry
//     Stop-is-a-no-op lifecycle test.
//   - tests_schema_registry.go — ConfluentSchemaRegistry tests: the
//     register/lookup/delete round-trip and the
//     non-PLAINTEXT-cluster rejection.
func (r *Query) KafkaTests() *KafkaTests { // kafka-tests (../../../daggerverse/kafka/tests/main.go:39:6)
	q := r.query.Select("kafkaTests")

	return &KafkaTests{
		query: q,
	}
}
