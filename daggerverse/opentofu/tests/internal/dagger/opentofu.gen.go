// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

// Retrieve the binding value, as type Opentofu
func (r *Binding) AsOpentofu() *Opentofu { // opentofu (../../../../../daggerverse/opentofu/main.go:48:6)
	q := r.query.Select("asOpentofu")

	return &Opentofu{
		query: q,
	}
}

// Retrieve the binding value, as type OpentofuConfig
func (r *Binding) AsOpentofuConfig() *OpentofuConfig { // opentofu (../../../../../daggerverse/opentofu/config.go:70:6)
	q := r.query.Select("asOpentofuConfig")

	return &OpentofuConfig{
		query: q,
	}
}

// Create or update a binding of type OpentofuConfig in the environment
func (r *Env) WithOpentofuConfigInput(name string, value *OpentofuConfig, description string) *Env { // opentofu (../../../../../daggerverse/opentofu/config.go:70:6)
	assertNotNil("value", value)
	q := r.query.Select("withOpentofuConfigInput")
	q = q.Arg("name", name)
	q = q.Arg("value", value)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Declare a desired OpentofuConfig output to be assigned in the environment
func (r *Env) WithOpentofuConfigOutput(name string, description string) *Env { // opentofu (../../../../../daggerverse/opentofu/config.go:70:6)
	q := r.query.Select("withOpentofuConfigOutput")
	q = q.Arg("name", name)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Create or update a binding of type Opentofu in the environment
func (r *Env) WithOpentofuInput(name string, value *Opentofu, description string) *Env { // opentofu (../../../../../daggerverse/opentofu/main.go:48:6)
	assertNotNil("value", value)
	q := r.query.Select("withOpentofuInput")
	q = q.Arg("name", name)
	q = q.Arg("value", value)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Declare a desired Opentofu output to be assigned in the environment
func (r *Env) WithOpentofuOutput(name string, description string) *Env { // opentofu (../../../../../daggerverse/opentofu/main.go:48:6)
	q := r.query.Select("withOpentofuOutput")
	q = q.Arg("name", name)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Opentofu wraps the tofu CLI as Dagger functions. Construct via New(); call
// Container() for the assembled image, or Config(source) to bind a root
// module and reach the lifecycle functions.
type Opentofu struct { // opentofu (../../../../../daggerverse/opentofu/main.go:48:6)
	query *querybuilder.Selection

	id      *ID
	version *string
}

func (r *Opentofu) WithGraphQLQuery(q *querybuilder.Selection) *Opentofu {
	return &Opentofu{
		query: q,
	}
}

// Config binds a root module directory to the toolchain. source is the whole
// tree, not a lone file: tofu resolves sub-modules, .tfvars files and the
// dependency lock file relative to the root module.
func (r *Opentofu) Config(source *Directory) *OpentofuConfig { // opentofu (../../../../../daggerverse/opentofu/main.go:129:1)
	assertNotNil("source", source)
	q := r.query.Select("config")
	q = q.Arg("source", source)

	return &OpentofuConfig{
		query: q,
	}
}

// Container returns the assembled tofu image: the base plus git, CA
// certificates and the tofu binary. This is the escape hatch for every
// subcommand this module does not wrap — `container with-exec` keeps tofu's
// long tail reachable.
func (r *Opentofu) Container() *Container { // opentofu (../../../../../daggerverse/opentofu/main.go:96:1)
	q := r.query.Select("container")

	return &Container{
		query: q,
	}
}

// A unique identifier for this Opentofu.
func (r *Opentofu) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *Opentofu) XXX_GraphQLType() string {
	return "Opentofu"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *Opentofu) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *Opentofu) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *Opentofu) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *Opentofu) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = Opentofu{query: selectNode(dag.query, id, "Opentofu")}
	return nil
}

// Version returns the OpenTofu release the assembled container ships, as
// reported by `tofu version` — the first line only, so the trailing
// `on <os>_<arch>` line stays out of the value.
func (r *Opentofu) Version(ctx context.Context) (string, error) { // opentofu (../../../../../daggerverse/opentofu/main.go:115:1)
	if r.version != nil {
		return *r.version, nil
	}
	q := r.query.Select("version")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// AsNode returns this Opentofu as a Node.
// This is a local type conversion — no GraphQL call.
func (r *Opentofu) AsNode() Node {
	return &NodeClient{
		query: r.query,
	}
}

// Config is a bound root module plus the settings that apply to nearly every
// tofu subcommand. It is immutable: every With* returns a copy.
//
// Variables, credentials and backend settings are hoisted here as chained
// modifiers rather than repeated as optional parameters across eight
// lifecycle signatures.
type OpentofuConfig struct { // opentofu (../../../../../daggerverse/opentofu/config.go:70:6)
	query *querybuilder.Selection

	fmt      *string
	id       *ID
	outputs  *string
	show     *string
	validate *Void
}
type WithOpentofuConfigFunc func(r *OpentofuConfig) *OpentofuConfig

// With calls the provided function with current OpentofuConfig.
//
// This is useful for reusability and readability by not breaking the calling chain.
func (r *OpentofuConfig) With(f WithOpentofuConfigFunc) *OpentofuConfig {
	return f(r)
}

func (r *OpentofuConfig) WithGraphQLQuery(q *querybuilder.Selection) *OpentofuConfig {
	return &OpentofuConfig{
		query: q,
	}
}

// OpentofuConfigApplyOpts contains options for OpentofuConfig.Apply
type OpentofuConfigApplyOpts struct {
	//
	// A saved plan from Plan. Without it, Apply plans and applies in one run.
	//
	Plan *File // opentofu (../../../../../daggerverse/opentofu/config.go:391:2)
	//
	// Limit the apply to these resource addresses (`-target`). Rejected
	// alongside a saved plan, which already fixes what it changes.
	//
	Targets []string // opentofu (../../../../../daggerverse/opentofu/config.go:395:2)
}

// Apply realises the configuration and returns terraform.tfstate (in
// file-carried mode), outputs.json and apply.log.
//
// Pass the plan.tfplan emitted by Plan to apply exactly that plan; without
// one, Apply plans and applies in a single run. A saved plan already carries
// its variables and target set, so neither is re-sent with it.
//
// A non-zero tofu exit is an error, and Dagger drops a function's value when
// its error is non-nil — so a partially failed apply forfeits the state it
// produced in file-carried mode. That is deliberate: the alternative, always
// returning the directory with an exit code inside it, turns a failed apply
// into a silent green whenever a caller forgets to look.
func (r *OpentofuConfig) Apply(opts ...OpentofuConfigApplyOpts) *Directory { // opentofu (../../../../../daggerverse/opentofu/config.go:387:1)
	q := r.query.Select("apply")
	for i := len(opts) - 1; i >= 0; i-- {
		// `plan` optional argument
		if !querybuilder.IsZeroValue(opts[i].Plan) {
			q = q.Arg("plan", opts[i].Plan)
		}
		// `targets` optional argument
		if !querybuilder.IsZeroValue(opts[i].Targets) {
			q = q.Arg("targets", opts[i].Targets)
		}
	}

	return &Directory{
		query: q,
	}
}

// OpentofuConfigDestroyOpts contains options for OpentofuConfig.Destroy
type OpentofuConfigDestroyOpts struct {
	//
	// Limit the destroy to these resource addresses (`-target`).
	//
	Targets []string // opentofu (../../../../../daggerverse/opentofu/config.go:430:2)
}

// Destroy tears down everything the state tracks and returns the post-destroy
// terraform.tfstate (in file-carried mode), outputs.json and destroy.log.
//
// It is rejected outright when there is neither state to destroy nor a
// backend to read it from: tofu would happily report "0 destroyed" against an
// empty state, which reads as success while leaving the real infrastructure
// untouched.
func (r *OpentofuConfig) Destroy(opts ...OpentofuConfigDestroyOpts) *Directory { // opentofu (../../../../../daggerverse/opentofu/config.go:426:1)
	q := r.query.Select("destroy")
	for i := len(opts) - 1; i >= 0; i-- {
		// `targets` optional argument
		if !querybuilder.IsZeroValue(opts[i].Targets) {
			q = q.Arg("targets", opts[i].Targets)
		}
	}

	return &Directory{
		query: q,
	}
}

// Fmt reports formatting drift with `tofu fmt -check -diff -recursive`. It
// returns the diff and fails when anything needs rewriting, so it is usable
// as a CI gate; rewriting in place is a separate function.
//
// A failing run carries the diff in the error rather than the return value:
// Dagger drops a function's value whenever its error is non-nil.
func (r *OpentofuConfig) Fmt(ctx context.Context) (string, error) { // opentofu (../../../../../daggerverse/opentofu/config.go:235:1)
	if r.fmt != nil {
		return *r.fmt, nil
	}
	q := r.query.Select("fmt")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// A unique identifier for this OpentofuConfig.
func (r *OpentofuConfig) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *OpentofuConfig) XXX_GraphQLType() string {
	return "OpentofuConfig"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *OpentofuConfig) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *OpentofuConfig) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *OpentofuConfig) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *OpentofuConfig) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = OpentofuConfig{query: selectNode(dag.query, id, "OpentofuConfig")}
	return nil
}

// Init runs a full `tofu init` — backend included — and returns the root
// module with the dependency lock file init produced.
//
// .terraform/ is stripped from the result: with the shared provider cache in
// play its provider entries are symlinks into a cache volume that does not
// exist outside this container, so carrying them out would hand the caller
// dangling links. The lock file is the portable artifact of an init, and it
// is what a repo commits.
func (r *OpentofuConfig) Init() *Directory { // opentofu (../../../../../daggerverse/opentofu/config.go:293:1)
	q := r.query.Select("init")

	return &Directory{
		query: q,
	}
}

// Outputs returns the root module's output values as JSON
// (`tofu output -json`).
func (r *OpentofuConfig) Outputs(ctx context.Context) (string, error) { // opentofu (../../../../../daggerverse/opentofu/config.go:464:1)
	if r.outputs != nil {
		return *r.outputs, nil
	}
	q := r.query.Select("outputs")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// OpentofuConfigPlanOpts contains options for OpentofuConfig.Plan
type OpentofuConfigPlanOpts struct {
	//
	// Plan the destruction of all remote objects (`-destroy`).
	//
	Destroy bool // opentofu (../../../../../daggerverse/opentofu/config.go:316:2)
	//
	// Limit the plan to these resource addresses (`-target`).
	//
	Targets []string // opentofu (../../../../../daggerverse/opentofu/config.go:319:2)
}

// Plan produces a saved plan and everything needed to read it, in a single
// tofu run: plan.tfplan (the saved plan Apply consumes), plan.json
// (`tofu show -json`), plan.txt (the human-readable rendering) and changes
// (`none` or `changes`, from `-detailed-exitcode`).
//
// One run, not two: underobtain the JSON form could legitimately disagree with the first. The JSON
// and text renderings are derived from the saved plan file, so they describe
// exactly the plan that was made.
func (r *OpentofuConfig) Plan(opts ...OpentofuConfigPlanOpts) *Directory { // opentofu (../../../../../daggerverse/opentofu/config.go:312:1)
	q := r.query.Select("plan")
	for i := len(opts) - 1; i >= 0; i-- {
		// `destroy` optional argument
		if !querybuilder.IsZeroValue(opts[i].Destroy) {
			q = q.Arg("destroy", opts[i].Destroy)
		}
		// `targets` optional argument
		if !querybuilder.IsZeroValue(opts[i].Targets) {
			q = q.Arg("targets", opts[i].Targets)
		}
	}

	return &Directory{
		query: q,
	}
}

// Show returns the human-readable rendering of the current state
// (`tofu show`).
func (r *OpentofuConfig) Show(ctx context.Context) (string, error) { // opentofu (../../../../../daggerverse/opentofu/config.go:472:1)
	if r.show != nil {
		return *r.show, nil
	}
	q := r.query.Select("show")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// Validate checks the configuration for internal consistency with
// `tofu validate`.
//
// It initialises with `-backend=false` first, so a configuration declaring a
// remote backend validates without any credentials and without touching the
// backend at all. Providers are still installed, because validation needs
// their schemas.
func (r *OpentofuConfig) Validate(ctx context.Context) error { // opentofu (../../../../../daggerverse/opentofu/config.go:263:1)
	if r.validate != nil {
		return nil
	}
	q := r.query.Select("validate")

	return q.Execute(ctx)
}

// WithBackendConfig sets one backend setting (`-backend-config=name=value`),
// merged into the configuration's own backend block at init.
//
// Selecting a remote backend is mutually exclusive with WithState: they are
// two different answers to where state lives, and combining them is rejected
// rather than silently resolved.
func (r *OpentofuConfig) WithBackendConfig(name string, value string) *OpentofuConfig { // opentofu (../../../../../daggerverse/opentofu/config.go:174:1)
	q := r.query.Select("withBackendConfig")
	q = q.Arg("name", name)
	q = q.Arg("value", value)

	return &OpentofuConfig{
		query: q,
	}
}

// WithBackendConfigFile adds a backend settings file
// (`-backend-config=<file>`). Like WithBackendConfig, it is mutually
// exclusive with WithState.
func (r *OpentofuConfig) WithBackendConfigFile(file *File) *OpentofuConfig { // opentofu (../../../../../daggerverse/opentofu/config.go:184:1)
	assertNotNil("file", file)
	q := r.query.Select("withBackendConfigFile")
	q = q.Arg("file", file)

	return &OpentofuConfig{
		query: q,
	}
}

// WithEnvVariable sets a plain environment variable on every tofu exec — the
// escape hatch for the non-sensitive knobs tofu reads from the environment
// (TF_LOG, TF_CLI_ARGS_*, provider region settings, ...). Credentials belong
// in WithSecretVariable.
func (r *OpentofuConfig) WithEnvVariable(name string, value string) *OpentofuConfig { // opentofu (../../../../../daggerverse/opentofu/config.go:150:1)
	q := r.query.Select("withEnvVariable")
	q = q.Arg("name", name)
	q = q.Arg("value", value)

	return &OpentofuConfig{
		query: q,
	}
}

// WithSecretVar sets an input variable from a secret.
//
// The value is bound as the container environment variable TF_VAR_<name>
// rather than passed as `-var name=value`, so the plaintext never enters
// argv, the CLI log, or a saved plan's command line. tofu still marks the
// variable's own value in the plan unless the variable is declared
// `sensitive = true`, which a configuration handling secrets should do.
func (r *OpentofuConfig) WithSecretVar(name string, value *Secret) *OpentofuConfig { // opentofu (../../../../../daggerverse/opentofu/config.go:130:1)
	assertNotNil("value", value)
	q := r.query.Select("withSecretVar")
	q = q.Arg("name", name)
	q = q.Arg("value", value)

	return &OpentofuConfig{
		query: q,
	}
}

// WithSecretVariable binds a secret as an environment variable on every tofu
// exec. This is how provider credentials reach tofu — AWS_ACCESS_KEY_ID,
// AWS_SECRET_ACCESS_KEY, TF_TOKEN_app_terraform_io and friends — as
// *dagger.Secret, never as a string.
func (r *OpentofuConfig) WithSecretVariable(name string, value *Secret) *OpentofuConfig { // opentofu (../../../../../daggerverse/opentofu/config.go:161:1)
	assertNotNil("value", value)
	q := r.query.Select("withSecretVariable")
	q = q.Arg("name", name)
	q = q.Arg("value", value)

	return &OpentofuConfig{
		query: q,
	}
}

// WithState supplies the state file for file-carried mode: state is written
// into the container, and every mutating operation hands the resulting
// terraform.tfstate back out in its output directory. Fully hermetic, no
// backend required, and the caller owns persistence.
//
// Omit it entirely for a first apply against an empty state.
func (r *OpentofuConfig) WithState(state *File) *OpentofuConfig { // opentofu (../../../../../daggerverse/opentofu/config.go:219:1)
	assertNotNil("state", state)
	q := r.query.Select("withState")
	q = q.Arg("state", state)

	return &OpentofuConfig{
		query: q,
	}
}

// WithVar sets an input variable (`-var name=value`).
//
// It takes a name and a value rather than a map because Dagger functions
// cannot accept map parameters. Use WithSecretVar for anything sensitive:
// a value passed here lands in argv and in the plan.
func (r *OpentofuConfig) WithVar(name string, value string) *OpentofuConfig { // opentofu (../../../../../daggerverse/opentofu/config.go:116:1)
	q := r.query.Select("withVar")
	q = q.Arg("name", name)
	q = q.Arg("value", value)

	return &OpentofuConfig{
		query: q,
	}
}

// WithVarFile adds a variable definitions file (`-var-file`). The file is
// staged outside the root module so it cannot collide with a file the
// configuration owns; tofu is pointed at the staged path.
func (r *OpentofuConfig) WithVarFile(file *File) *OpentofuConfig { // opentofu (../../../../../daggerverse/opentofu/config.go:140:1)
	assertNotNil("file", file)
	q := r.query.Select("withVarFile")
	q = q.Arg("file", file)

	return &OpentofuConfig{
		query: q,
	}
}

// WithWorkspace selects a tofu workspace, creating it when it does not exist
// (`tofu workspace select -or-create`). With the local backend this moves the
// state to terraform.tfstate.d/<name>/terraform.tfstate, which is where
// WithState writes and where Apply reads the emitted state from.
func (r *OpentofuConfig) WithWorkspace(name string) *OpentofuConfig { // opentofu (../../../../../daggerverse/opentofu/config.go:194:1)
	q := r.query.Select("withWorkspace")
	q = q.Arg("name", name)

	return &OpentofuConfig{
		query: q,
	}
}

// WithoutPluginCache disables the shared provider cache, so every init
// downloads its providers afresh.
//
// This is a Without* modifier rather than a `pluginCache bool` parameter
// defaulting to true, because a `false from the Go SDK: the zero value is dropped before it reaches the
// engine.
func (r *OpentofuConfig) WithoutPluginCache() *OpentofuConfig { // opentofu (../../../../../daggerverse/opentofu/config.go:207:1)
	q := r.query.Select("withoutPluginCache")

	return &OpentofuConfig{
		query: q,
	}
}

// AsNode returns this OpentofuConfig as a Node.
// This is a local type conversion — no GraphQL call.
func (r *OpentofuConfig) AsNode() Node {
	return &NodeClient{
		query: r.query,
	}
}

// OpentofuOpts contains options for Query.Opentofu
type OpentofuOpts struct {
	//
	// Container registry hosting the opentofu/opentofu image.
	//
	//
	// Default: "ghcr.io"
	Registry string // opentofu (../../../../../daggerverse/opentofu/main.go:62:2)
	//
	// OpenTofu release to install. The -minimal suffix is appended when
	// absent, so "1.12.5" and "1.12.5-minimal" select the same image.
	//
	//
	// Default: "1.12.5"
	Version string // opentofu (../../../../../daggerverse/opentofu/main.go:66:2)
	//
	// Base image the binary is copied onto. It must be Alpine-family: the
	// module runs `apk add git ca-certificates` on it, because module sources
	// (git) and the provider registry (TLS) need both and the -minimal image
	// carries neither.
	//
	//
	// Default: "alpine:3.22"
	Base string // opentofu (../../../../../daggerverse/opentofu/main.go:72:2)
}

// New returns an Opentofu module whose tofu binary comes from
// <registry>/opentofu/opentofu:<version>-minimal, placed onto base.
func (r *Query) Opentofu(opts ...OpentofuOpts) *Opentofu { // opentofu (../../../../../daggerverse/opentofu/main.go:59:1)
	q := r.query.Select("opentofu")
	for i := len(opts) - 1; i >= 0; i-- {
		// `registry` optional argument
		if !querybuilder.IsZeroValue(opts[i].Registry) {
			q = q.Arg("registry", opts[i].Registry)
		}
		// `version` optional argument
		if !querybuilder.IsZeroValue(opts[i].Version) {
			q = q.Arg("version", opts[i].Version)
		}
		// `base` optional argument
		if !querybuilder.IsZeroValue(opts[i].Base) {
			q = q.Arg("base", opts[i].Base)
		}
	}

	return &Opentofu{
		query: q,
	}
}
