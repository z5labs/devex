// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

// GrafanaStackExamples is the module's main object: a namespace for the
// grafana-stack usage recipes.
type GrafanaStackExamples struct { // grafana-stack-examples (../../../../../../:0:0)
	query *querybuilder.Selection

	fullStackWithGrafana *string
	grafanaWithDashboard *string
	id                   *ID
	lokiOnly             *string
	mimirOnly            *string
	tempoOnly            *string
}

func (r *GrafanaStackExamples) WithGraphQLQuery(q *querybuilder.Selection) *GrafanaStackExamples {
	return &GrafanaStackExamples{
		query: q,
	}
}

// FullStackWithGrafana wires all three backends into one Grafana with a
// provisioned datasource each and returns the Grafana UI endpoint (log in
// with admin / admin). This is the recipe to copy for a local observability
// sandbox: the WithXDatasource builders bind each backend into Grafana's
// network and register it, so no datasource has to be clicked together in the
// UI after startup.
func (r *GrafanaStackExamples) FullStackWithGrafana(ctx context.Context) (string, error) {
	if r.fullStackWithGrafana != nil {
		return *r.fullStackWithGrafana, nil
	}
	q := r.query.Select("fullStackWithGrafana")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// GrafanaWithDashboard is FullStackWithGrafana plus a sample dashboard
// registered via WithDashboard, and returns the Grafana UI endpoint (log in
// with admin / admin, then find "OTel Overview" in the dashboard list). It
// shows the contract dashboard provisioning relies on: every panel addresses
// its datasource by uid, and that uid is the name the datasource was
// registered under — so a dashboard authored against "loki" / "tempo" /
// "mimir" resolves the moment Grafana starts.
func (r *GrafanaStackExamples) GrafanaWithDashboard(ctx context.Context) (string, error) {
	if r.grafanaWithDashboard != nil {
		return *r.grafanaWithDashboard, nil
	}
	q := r.query.Select("grafanaWithDashboard")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// A unique identifier for this GrafanaStackExamples.
func (r *GrafanaStackExamples) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *GrafanaStackExamples) XXX_GraphQLType() string {
	return "GrafanaStackExamples"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *GrafanaStackExamples) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *GrafanaStackExamples) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *GrafanaStackExamples) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *GrafanaStackExamples) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = GrafanaStackExamples{query: selectNode(dag.query, id, "GrafanaStackExamples")}
	return nil
}

// LokiOnly spins up a lone Loki on its defaults and returns the URL of its
// OTLP/HTTP logs receiver. This is the smallest useful stack: point an
// OpenTelemetry log exporter at the returned endpoint and records land in
// Loki, no Grafana required.
func (r *GrafanaStackExamples) LokiOnly(ctx context.Context) (string, error) {
	if r.lokiOnly != nil {
		return *r.lokiOnly, nil
	}
	q := r.query.Select("lokiOnly")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// MimirOnly spins up a lone Mimir in monolithic mode and returns the URL of
// its OTLP/HTTP metrics receiver. Multitenancy is off in the default config,
// so an exporter can push to the returned endpoint without an X-Scope-OrgID
// header.
func (r *GrafanaStackExamples) MimirOnly(ctx context.Context) (string, error) {
	if r.mimirOnly != nil {
		return *r.mimirOnly, nil
	}
	q := r.query.Select("mimirOnly")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// TempoOnly spins up a lone Tempo and returns the address of its OTLP/gRPC
// trace receiver. Note the shape difference from the Loki and Mimir recipes:
// gRPC callers want a bare host:port with no URL scheme, which is exactly
// what OtlpGrpcEndpoint hands back.
func (r *GrafanaStackExamples) TempoOnly(ctx context.Context) (string, error) {
	if r.tempoOnly != nil {
		return *r.tempoOnly, nil
	}
	q := r.query.Select("tempoOnly")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

func (r *Query) GrafanaStackExamples() *GrafanaStackExamples { // grafana-stack-examples (../../../../../../:0:0)
	q := r.query.Select("grafanaStackExamples")

	return &GrafanaStackExamples{
		query: q,
	}
}
