// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

// Retrieve the binding value, as type Kicad
func (r *Binding) AsKicad() *Kicad { // kicad (../../../../../daggerverse/kicad/main.go:61:6)
	q := r.query.Select("asKicad")

	return &Kicad{
		query: q,
	}
}

// Retrieve the binding value, as type KicadPcb
func (r *Binding) AsKicadPcb() *KicadPcb { // kicad (../../../../../daggerverse/kicad/pcb.go:14:6)
	q := r.query.Select("asKicadPcb")

	return &KicadPcb{
		query: q,
	}
}

// Retrieve the binding value, as type KicadProject
func (r *Binding) AsKicadProject() *KicadProject { // kicad (../../../../../daggerverse/kicad/main.go:122:6)
	q := r.query.Select("asKicadProject")

	return &KicadProject{
		query: q,
	}
}

// Retrieve the binding value, as type KicadSch
func (r *Binding) AsKicadSch() *KicadSch { // kicad (../../../../../daggerverse/kicad/sch.go:10:6)
	q := r.query.Select("asKicadSch")

	return &KicadSch{
		query: q,
	}
}

// Create or update a binding of type Kicad in the environment
func (r *Env) WithKicadInput(name string, value *Kicad, description string) *Env { // kicad (../../../../../daggerverse/kicad/main.go:61:6)
	assertNotNil("value", value)
	q := r.query.Select("withKicadInput")
	q = q.Arg("name", name)
	q = q.Arg("value", value)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Declare a desired Kicad output to be assigned in the environment
func (r *Env) WithKicadOutput(name string, description string) *Env { // kicad (../../../../../daggerverse/kicad/main.go:61:6)
	q := r.query.Select("withKicadOutput")
	q = q.Arg("name", name)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Create or update a binding of type KicadPcb in the environment
func (r *Env) WithKicadPcbInput(name string, value *KicadPcb, description string) *Env { // kicad (../../../../../daggerverse/kicad/pcb.go:14:6)
	assertNotNil("value", value)
	q := r.query.Select("withKicadPcbInput")
	q = q.Arg("name", name)
	q = q.Arg("value", value)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Declare a desired KicadPcb output to be assigned in the environment
func (r *Env) WithKicadPcbOutput(name string, description string) *Env { // kicad (../../../../../daggerverse/kicad/pcb.go:14:6)
	q := r.query.Select("withKicadPcbOutput")
	q = q.Arg("name", name)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Create or update a binding of type KicadProject in the environment
func (r *Env) WithKicadProjectInput(name string, value *KicadProject, description string) *Env { // kicad (../../../../../daggerverse/kicad/main.go:122:6)
	assertNotNil("value", value)
	q := r.query.Select("withKicadProjectInput")
	q = q.Arg("name", name)
	q = q.Arg("value", value)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Declare a desired KicadProject output to be assigned in the environment
func (r *Env) WithKicadProjectOutput(name string, description string) *Env { // kicad (../../../../../daggerverse/kicad/main.go:122:6)
	q := r.query.Select("withKicadProjectOutput")
	q = q.Arg("name", name)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Create or update a binding of type KicadSch in the environment
func (r *Env) WithKicadSchInput(name string, value *KicadSch, description string) *Env { // kicad (../../../../../daggerverse/kicad/sch.go:10:6)
	assertNotNil("value", value)
	q := r.query.Select("withKicadSchInput")
	q = q.Arg("name", name)
	q = q.Arg("value", value)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Declare a desired KicadSch output to be assigned in the environment
func (r *Env) WithKicadSchOutput(name string, description string) *Env { // kicad (../../../../../daggerverse/kicad/sch.go:10:6)
	q := r.query.Select("withKicadSchOutput")
	q = q.Arg("name", name)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Kicad wraps kicad-cli as Dagger functions. Construct via New(); call
// Container() for the raw image, or Project(source) to reach the typed
// pcb/sch helpers.
type Kicad struct { // kicad (../../../../../daggerverse/kicad/main.go:61:6)
	query *querybuilder.Selection

	id      *ID
	version *string
}

func (r *Kicad) WithGraphQLQuery(q *querybuilder.Selection) *Kicad {
	return &Kicad{
		query: q,
	}
}

// Container returns the bare kicad image. This is the escape hatch for every
// subcommand this module does not wrap — kicad-cli's long tail of exotic and
// legacy exports stays reachable via `container with-exec`.
func (r *Kicad) Container() *Container { // kicad (../../../../../daggerverse/kicad/main.go:91:1)
	q := r.query.Select("container")

	return &Container{
		query: q,
	}
}

// A unique identifier for this Kicad.
func (r *Kicad) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *Kicad) XXX_GraphQLType() string {
	return "Kicad"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *Kicad) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *Kicad) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *Kicad) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *Kicad) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = Kicad{query: selectNode(dag.query, id, "Kicad")}
	return nil
}

// Project binds a KiCad project directory to the toolchain. source is the
// whole project tree, not a single file, because kicad-cli resolves
// sub-sheets, footprint libraries and drawing sheets relative to it.
func (r *Kicad) Project(source *Directory) *KicadProject { // kicad (../../../../../daggerverse/kicad/main.go:112:1)
	assertNotNil("source", source)
	q := r.query.Select("project")
	q = q.Arg("source", source)

	return &KicadProject{
		query: q,
	}
}

// Version returns the KiCad release the pinned image ships, as reported by
// `kicad-cli version`.
func (r *Kicad) Version(ctx context.Context) (string, error) { // kicad (../../../../../daggerverse/kicad/main.go:99:1)
	if r.version != nil {
		return *r.version, nil
	}
	q := r.query.Select("version")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// AsNode returns this Kicad as a Node.
// This is a local type conversion — no GraphQL call.
func (r *Kicad) AsNode() Node {
	return &NodeClient{
		query: r.query,
	}
}

// Pcb is a board selected within a Project. Every method here execs
// kicad-cli, so each carries a session cache directive; selecting the board
// itself is pure config and carries none.
type KicadPcb struct { // kicad (../../../../../daggerverse/kicad/pcb.go:14:6)
	query *querybuilder.Selection

	drc *Void
	id  *ID
}

func (r *KicadPcb) WithGraphQLQuery(q *querybuilder.Selection) *KicadPcb {
	return &KicadPcb{
		query: q,
	}
}

// KicadPcbDrcOpts contains options for KicadPcb.Drc
type KicadPcbDrcOpts struct {
	//
	// Violation levels to report: all, error, warning or exclusions.
	//
	//
	// Default: "error"
	Severity string // kicad (../../../../../daggerverse/kicad/pcb.go:36:2)
	//
	// Also check the board against the schematic (footprints, nets, values).
	//
	SchematicParity bool // kicad (../../../../../daggerverse/kicad/pcb.go:39:2)
	//
	// Refill copper zones before checking.
	//
	RefillZones bool // kicad (../../../../../daggerverse/kicad/pcb.go:42:2)
}

// Drc runs the Design Rule Check and returns a non-nil error listing the
// violations when the board fails, nil when it is clean.
//
// It returns a bare error rather than (report, error) because Dagger drops a
// function's value whenever it also returns a non-nil error: a
// report-returning signature would leave the violation list unreachable on
// exactly the failure path that needs it. `--exit-code-violations` exits 5 on
// violations, which Expect=ReturnTypeAny keeps on the value path so the
// report can be read back and folded into the message.
func (r *KicadPcb) Drc(ctx context.Context, opts ...KicadPcbDrcOpts) error { // kicad (../../../../../daggerverse/kicad/pcb.go:32:1)
	if r.drc != nil {
		return nil
	}
	q := r.query.Select("drc")
	for i := len(opts) - 1; i >= 0; i-- {
		// `severity` optional argument
		if !querybuilder.IsZeroValue(opts[i].Severity) {
			q = q.Arg("severity", opts[i].Severity)
		}
		// `schematicParity` optional argument
		if !querybuilder.IsZeroValue(opts[i].SchematicParity) {
			q = q.Arg("schematicParity", opts[i].SchematicParity)
		}
		// `refillZones` optional argument
		if !querybuilder.IsZeroValue(opts[i].RefillZones) {
			q = q.Arg("refillZones", opts[i].RefillZones)
		}
	}

	return q.Execute(ctx)
}

// KicadPcbDrillOpts contains options for KicadPcb.Drill
type KicadPcbDrillOpts struct {
	//
	// Drill file format: excellon or gerber.
	//
	//
	// Default: "excellon"
	Format string // kicad (../../../../../daggerverse/kicad/pcb.go:110:2)
	//
	// Excellon output units: mm or in.
	//
	//
	// Default: "mm"
	Units string // kicad (../../../../../daggerverse/kicad/pcb.go:113:2)
	//
	// Drill origin: absolute or plot.
	//
	//
	// Default: "absolute"
	Origin string // kicad (../../../../../daggerverse/kicad/pcb.go:116:2)
	//
	// Emit independent files for plated and non-plated holes.
	//
	SeparatePlatedHoles bool // kicad (../../../../../daggerverse/kicad/pcb.go:119:2)
	//
	// Also emit a drill map.
	//
	GenerateMap bool // kicad (../../../../../daggerverse/kicad/pcb.go:122:2)
}

// Drill generates the board's drill files and returns them as a directory.
func (r *KicadPcb) Drill(opts ...KicadPcbDrillOpts) *Directory { // kicad (../../../../../daggerverse/kicad/pcb.go:106:1)
	q := r.query.Select("drill")
	for i := len(opts) - 1; i >= 0; i-- {
		// `format` optional argument
		if !querybuilder.IsZeroValue(opts[i].Format) {
			q = q.Arg("format", opts[i].Format)
		}
		// `units` optional argument
		if !querybuilder.IsZeroValue(opts[i].Units) {
			q = q.Arg("units", opts[i].Units)
		}
		// `origin` optional argument
		if !querybuilder.IsZeroValue(opts[i].Origin) {
			q = q.Arg("origin", opts[i].Origin)
		}
		// `separatePlatedHoles` optional argument
		if !querybuilder.IsZeroValue(opts[i].SeparatePlatedHoles) {
			q = q.Arg("separatePlatedHoles", opts[i].SeparatePlatedHoles)
		}
		// `generateMap` optional argument
		if !querybuilder.IsZeroValue(opts[i].GenerateMap) {
			q = q.Arg("generateMap", opts[i].GenerateMap)
		}
	}

	return &Directory{
		query: q,
	}
}

// KicadPcbGerbersOpts contains options for KicadPcb.Gerbers
type KicadPcbGerbersOpts struct {
	//
	// Untranslated layer names to plot, e.g. F.Cu, B.Cu. Empty plots all.
	//
	Layers []string // kicad (../../../../../daggerverse/kicad/pcb.go:72:2)
	//
	// Gerber coordinate precision: 5 or 6.
	//
	//
	// Default: 6
	Precision int // kicad (../../../../../daggerverse/kicad/pcb.go:75:2)
	//
	// Check and refill zones before plotting.
	//
	CheckZones bool // kicad (../../../../../daggerverse/kicad/pcb.go:78:2)
}

// Gerbers plots the board's Gerber files and returns them as a directory. An
// empty layers list plots every layer the board defines, plus the .gbrjob
// file that ties them together.
func (r *KicadPcb) Gerbers(opts ...KicadPcbGerbersOpts) *Directory { // kicad (../../../../../daggerverse/kicad/pcb.go:68:1)
	q := r.query.Select("gerbers")
	for i := len(opts) - 1; i >= 0; i-- {
		// `layers` optional argument
		if !querybuilder.IsZeroValue(opts[i].Layers) {
			q = q.Arg("layers", opts[i].Layers)
		}
		// `precision` optional argument
		if !querybuilder.IsZeroValue(opts[i].Precision) {
			q = q.Arg("precision", opts[i].Precision)
		}
		// `checkZones` optional argument
		if !querybuilder.IsZeroValue(opts[i].CheckZones) {
			q = q.Arg("checkZones", opts[i].CheckZones)
		}
	}

	return &Directory{
		query: q,
	}
}

// A unique identifier for this KicadPcb.
func (r *KicadPcb) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *KicadPcb) XXX_GraphQLType() string {
	return "KicadPcb"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *KicadPcb) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *KicadPcb) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *KicadPcb) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *KicadPcb) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = KicadPcb{query: selectNode(dag.query, id, "KicadPcb")}
	return nil
}

// KicadPcbIpc2581Opts contains options for KicadPcb.Ipc2581
type KicadPcbIpc2581Opts struct {
	//
	// IPC-2581 standard revision: B or C.
	//
	//
	// Default: "C"
	Version string // kicad (../../../../../daggerverse/kicad/pcb.go:256:2)
	//
	// Units: mm or in.
	//
	//
	// Default: "mm"
	Units string // kicad (../../../../../daggerverse/kicad/pcb.go:259:2)

	// Default: "board.xml"
	OutputName string // kicad (../../../../../daggerverse/kicad/pcb.go:261:2)
}

// Ipc2581 exports the board in IPC-2581 format — a single XML file carrying
// the fabrication and assembly data that would otherwise be spread across
// Gerbers, drill files and a BOM.
func (r *KicadPcb) Ipc2581(opts ...KicadPcbIpc2581Opts) *File { // kicad (../../../../../daggerverse/kicad/pcb.go:252:1)
	q := r.query.Select("ipc2581")
	for i := len(opts) - 1; i >= 0; i-- {
		// `version` optional argument
		if !querybuilder.IsZeroValue(opts[i].Version) {
			q = q.Arg("version", opts[i].Version)
		}
		// `units` optional argument
		if !querybuilder.IsZeroValue(opts[i].Units) {
			q = q.Arg("units", opts[i].Units)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbPdfOpts contains options for KicadPcb.Pdf
type KicadPcbPdfOpts struct {

	// Default: "board.pdf"
	OutputName string // kicad (../../../../../daggerverse/kicad/pcb.go:300:2)
}

// Pdf plots the given layers into a single PDF.
//
// Pdf and PdfPerLayer are split into file- and directory-returning functions
// rather than one function taking a mode flag: a Dagger function has exactly
// one return type, so modelling `--mode-single` vs `--mode-separate` as a
// parameter would force *Directory onto the common single-file case.
func (r *KicadPcb) Pdf(layers []string, opts ...KicadPcbPdfOpts) *File { // kicad (../../../../../daggerverse/kicad/pcb.go:295:1)
	q := r.query.Select("pdf")
	for i := len(opts) - 1; i >= 0; i-- {
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}
	q = q.Arg("layers", layers)

	return &File{
		query: q,
	}
}

// PdfPerLayer plots each layer into its own PDF and returns the directory.
func (r *KicadPcb) PdfPerLayer(layers []string) *Directory { // kicad (../../../../../daggerverse/kicad/pcb.go:326:1)
	q := r.query.Select("pdfPerLayer")
	q = q.Arg("layers", layers)

	return &Directory{
		query: q,
	}
}

// KicadPcbPosOpts contains options for KicadPcb.Pos
type KicadPcbPosOpts struct {
	//
	// Board side to include: front, back or both.
	//
	//
	// Default: "both"
	Side string // kicad (../../../../../daggerverse/kicad/pcb.go:163:2)
	//
	// Output format: ascii, csv or gerber.
	//
	//
	// Default: "ascii"
	Format string // kicad (../../../../../daggerverse/kicad/pcb.go:166:2)
	//
	// Output units for the ascii and csv formats: in or mm.
	//
	//
	// Default: "in"
	Units string // kicad (../../../../../daggerverse/kicad/pcb.go:169:2)
	//
	// Include only SMD footprints.
	//
	SmdOnly bool // kicad (../../../../../daggerverse/kicad/pcb.go:172:2)
	//
	// Name of the produced file. Named outputName, not output, because -o
	// collides with the Dagger CLI's own top-level flag.
	//
	//
	// Default: "pos.pos"
	OutputName string // kicad (../../../../../daggerverse/kicad/pcb.go:176:2)
}

// Pos generates the component position (pick-and-place) file.
func (r *KicadPcb) Pos(opts ...KicadPcbPosOpts) *File { // kicad (../../../../../daggerverse/kicad/pcb.go:159:1)
	q := r.query.Select("pos")
	for i := len(opts) - 1; i >= 0; i-- {
		// `side` optional argument
		if !querybuilder.IsZeroValue(opts[i].Side) {
			q = q.Arg("side", opts[i].Side)
		}
		// `format` optional argument
		if !querybuilder.IsZeroValue(opts[i].Format) {
			q = q.Arg("format", opts[i].Format)
		}
		// `units` optional argument
		if !querybuilder.IsZeroValue(opts[i].Units) {
			q = q.Arg("units", opts[i].Units)
		}
		// `smdOnly` optional argument
		if !querybuilder.IsZeroValue(opts[i].SmdOnly) {
			q = q.Arg("smdOnly", opts[i].SmdOnly)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbStepOpts contains options for KicadPcb.Step
type KicadPcbStepOpts struct {
	//
	// Export the bare board, with no component models.
	//
	BoardOnly bool // kicad (../../../../../daggerverse/kicad/pcb.go:217:2)
	//
	// Exclude models for components flagged Do Not Populate.
	//
	ExcludeDnp bool // kicad (../../../../../daggerverse/kicad/pcb.go:220:2)

	// Default: "board.step"
	OutputName string // kicad (../../../../../daggerverse/kicad/pcb.go:222:2)
}

// Step exports the board as a STEP model. The default `10.0` image is the
// slim variant, which ships no 3D component models — pass boardOnly to make
// that explicit, or use the `-full` image tag for populated assemblies.
func (r *KicadPcb) Step(opts ...KicadPcbStepOpts) *File { // kicad (../../../../../daggerverse/kicad/pcb.go:213:1)
	q := r.query.Select("step")
	for i := len(opts) - 1; i >= 0; i-- {
		// `boardOnly` optional argument
		if !querybuilder.IsZeroValue(opts[i].BoardOnly) {
			q = q.Arg("boardOnly", opts[i].BoardOnly)
		}
		// `excludeDnp` optional argument
		if !querybuilder.IsZeroValue(opts[i].ExcludeDnp) {
			q = q.Arg("excludeDnp", opts[i].ExcludeDnp)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbSvgOpts contains options for KicadPcb.Svg
type KicadPcbSvgOpts struct {

	// Default: "board.svg"
	OutputName string // kicad (../../../../../daggerverse/kicad/pcb.go:356:2)
}

// Svg plots the given layers into a single SVG.
func (r *KicadPcb) Svg(layers []string, opts ...KicadPcbSvgOpts) *File { // kicad (../../../../../daggerverse/kicad/pcb.go:351:1)
	q := r.query.Select("svg")
	for i := len(opts) - 1; i >= 0; i-- {
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}
	q = q.Arg("layers", layers)

	return &File{
		query: q,
	}
}

// SvgPerLayer plots each layer into its own SVG and returns the directory.
func (r *KicadPcb) SvgPerLayer(layers []string) *Directory { // kicad (../../../../../daggerverse/kicad/pcb.go:382:1)
	q := r.query.Select("svgPerLayer")
	q = q.Arg("layers", layers)

	return &Directory{
		query: q,
	}
}

// AsNode returns this KicadPcb as a Node.
// This is a local type conversion — no GraphQL call.
func (r *KicadPcb) AsNode() Node {
	return &NodeClient{
		query: r.query,
	}
}

// Project is a KiCad project tree plus the options that apply to nearly every
// kicad-cli subcommand. It is immutable: every With* returns a copy.
type KicadProject struct { // kicad (../../../../../daggerverse/kicad/main.go:122:6)
	query *querybuilder.Selection

	id *ID
}
type WithKicadProjectFunc func(r *KicadProject) *KicadProject

// With calls the provided function with current KicadProject.
//
// This is useful for reusability and readability by not breaking the calling chain.
func (r *KicadProject) With(f WithKicadProjectFunc) *KicadProject {
	return f(r)
}

func (r *KicadProject) WithGraphQLQuery(q *querybuilder.Selection) *KicadProject {
	return &KicadProject{
		query: q,
	}
}

// A unique identifier for this KicadProject.
func (r *KicadProject) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *KicadProject) XXX_GraphQLType() string {
	return "KicadProject"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *KicadProject) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *KicadProject) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *KicadProject) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *KicadProject) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = KicadProject{query: selectNode(dag.query, id, "KicadProject")}
	return nil
}

// Jobset runs a .kicad_jobset file and returns the project directory with
// everything the jobset produced, so a project that already declares its
// output set in-repo can generate the whole fabrication package in one call.
//
// The jobset's outputs are written relative to the project, which is why the
// whole tree comes back rather than a lone output folder: the jobset — not
// this module — decides where its artifacts land.
func (r *KicadProject) Jobset(path string) *Directory { // kicad (../../../../../daggerverse/kicad/main.go:201:1)
	q := r.query.Select("jobset")
	q = q.Arg("path", path)

	return &Directory{
		query: q,
	}
}

// KicadProjectPcbOpts contains options for KicadProject.Pcb
type KicadProjectPcbOpts struct {
	//
	// Project-relative path to the .kicad_pcb; empty auto-discovers.
	//
	Path string // kicad (../../../../../daggerverse/kicad/main.go:176:2)
}

// Pcb selects a board within the project. An empty path auto-discovers the
// single *.kicad_pcb in the tree and errors when there are zero or more than
// one; discovery is deferred to the exec so the error surfaces on the call
// that needed the board.
func (r *KicadProject) Pcb(opts ...KicadProjectPcbOpts) *KicadPcb { // kicad (../../../../../daggerverse/kicad/main.go:173:1)
	q := r.query.Select("pcb")
	for i := len(opts) - 1; i >= 0; i-- {
		// `path` optional argument
		if !querybuilder.IsZeroValue(opts[i].Path) {
			q = q.Arg("path", opts[i].Path)
		}
	}

	return &KicadPcb{
		query: q,
	}
}

// KicadProjectSchOpts contains options for KicadProject.Sch
type KicadProjectSchOpts struct {
	//
	// Project-relative path to the .kicad_sch; empty auto-discovers.
	//
	Path string // kicad (../../../../../daggerverse/kicad/main.go:187:2)
}

// Sch selects a schematic within the project. An empty path auto-discovers
// the single *.kicad_sch in the tree, ignoring the sub-sheets of a
// hierarchical design, and errors when there are zero or more than one root.
func (r *KicadProject) Sch(opts ...KicadProjectSchOpts) *KicadSch { // kicad (../../../../../daggerverse/kicad/main.go:184:1)
	q := r.query.Select("sch")
	for i := len(opts) - 1; i >= 0; i-- {
		// `path` optional argument
		if !querybuilder.IsZeroValue(opts[i].Path) {
			q = q.Arg("path", opts[i].Path)
		}
	}

	return &KicadSch{
		query: q,
	}
}

// WithDrawingSheet overrides the project's drawing sheet with the supplied
// .kicad_wks file (`--drawing-sheet`). It applies to the plotting exports;
// subcommands that do not accept the flag ignore it.
func (r *KicadProject) WithDrawingSheet(sheet *File) *KicadProject { // kicad (../../../../../daggerverse/kicad/main.go:163:1)
	assertNotNil("sheet", sheet)
	q := r.query.Select("withDrawingSheet")
	q = q.Arg("sheet", sheet)

	return &KicadProject{
		query: q,
	}
}

// WithVar sets a KiCad text variable, overriding or adding to the ones the
// project file declares (kicad-cli's `--define-var name=value`).
//
// It takes a name and a value rather than a map because Dagger functions
// cannot accept map parameters. Validation is deferred to the exec: builder
// methods have no error return, so a bad name surfaces when the export or
// check that would have used it runs.
func (r *KicadProject) WithVar(name string, value string) *KicadProject { // kicad (../../../../../daggerverse/kicad/main.go:144:1)
	q := r.query.Select("withVar")
	q = q.Arg("name", name)
	q = q.Arg("value", value)

	return &KicadProject{
		query: q,
	}
}

// WithVariant selects a KiCad assembly variant (`--variant`). It applies to
// the exports that support variants; checks (drc, erc) and drill files ignore
// it because kicad-cli does not accept the flag there.
func (r *KicadProject) WithVariant(variant string) *KicadProject { // kicad (../../../../../daggerverse/kicad/main.go:154:1)
	q := r.query.Select("withVariant")
	q = q.Arg("variant", variant)

	return &KicadProject{
		query: q,
	}
}

// AsNode returns this KicadProject as a Node.
// This is a local type conversion — no GraphQL call.
func (r *KicadProject) AsNode() Node {
	return &NodeClient{
		query: r.query,
	}
}

// Sch is a schematic selected within a Project.
type KicadSch struct { // kicad (../../../../../daggerverse/kicad/sch.go:10:6)
	query *querybuilder.Selection

	erc *Void
	id  *ID
}

func (r *KicadSch) WithGraphQLQuery(q *querybuilder.Selection) *KicadSch {
	return &KicadSch{
		query: q,
	}
}

// KicadSchBomOpts contains options for KicadSch.Bom
type KicadSchBomOpts struct {
	//
	// Ordered list of fields to export. Generated fields such as QUANTITY,
	// ITEM_NUMBER and DNP may be used alongside symbol fields.
	//
	//
	// Default: "Reference,Value,Footprint,QUANTITY,DNP"
	Fields string // kicad (../../../../../daggerverse/kicad/sch.go:53:2)
	//
	// Fields to group references by when their values match.
	//
	GroupBy string // kicad (../../../../../daggerverse/kicad/sch.go:56:2)
	//
	// Field name to sort by.
	//
	//
	// Default: "Reference"
	SortField string // kicad (../../../../../daggerverse/kicad/sch.go:59:2)
	//
	// Exclude symbols marked Do Not Populate.
	//
	ExcludeDnp bool // kicad (../../../../../daggerverse/kicad/sch.go:62:2)

	// Default: "bom.csv"
	OutputName string // kicad (../../../../../daggerverse/kicad/sch.go:64:2)
}

// Bom exports the Bill of Materials as CSV.
func (r *KicadSch) Bom(opts ...KicadSchBomOpts) *File { // kicad (../../../../../daggerverse/kicad/sch.go:48:1)
	q := r.query.Select("bom")
	for i := len(opts) - 1; i >= 0; i-- {
		// `fields` optional argument
		if !querybuilder.IsZeroValue(opts[i].Fields) {
			q = q.Arg("fields", opts[i].Fields)
		}
		// `groupBy` optional argument
		if !querybuilder.IsZeroValue(opts[i].GroupBy) {
			q = q.Arg("groupBy", opts[i].GroupBy)
		}
		// `sortField` optional argument
		if !querybuilder.IsZeroValue(opts[i].SortField) {
			q = q.Arg("sortField", opts[i].SortField)
		}
		// `excludeDnp` optional argument
		if !querybuilder.IsZeroValue(opts[i].ExcludeDnp) {
			q = q.Arg("excludeDnp", opts[i].ExcludeDnp)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadSchErcOpts contains options for KicadSch.Erc
type KicadSchErcOpts struct {
	//
	// Violation levels to report: all, error, warning or exclusions.
	//
	//
	// Default: "error"
	Severity string // kicad (../../../../../daggerverse/kicad/sch.go:30:2)
}

// Erc runs the Electrical Rule Check and returns a non-nil error listing the
// violations when the schematic fails, nil when it is clean.
//
// Like Pcb.Drc it returns a bare error: Dagger drops a function's value when
// it also returns a non-nil error, so a (report, error) signature would hide
// the violation list on the failure path. `--exit-code-violations` exits 5,
// which Expect=ReturnTypeAny keeps on the value path.
func (r *KicadSch) Erc(ctx context.Context, opts ...KicadSchErcOpts) error { // kicad (../../../../../daggerverse/kicad/sch.go:26:1)
	if r.erc != nil {
		return nil
	}
	q := r.query.Select("erc")
	for i := len(opts) - 1; i >= 0; i-- {
		// `severity` optional argument
		if !querybuilder.IsZeroValue(opts[i].Severity) {
			q = q.Arg("severity", opts[i].Severity)
		}
	}

	return q.Execute(ctx)
}

// A unique identifier for this KicadSch.
func (r *KicadSch) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *KicadSch) XXX_GraphQLType() string {
	return "KicadSch"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *KicadSch) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *KicadSch) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *KicadSch) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *KicadSch) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = KicadSch{query: selectNode(dag.query, id, "KicadSch")}
	return nil
}

// KicadSchNetlistOpts contains options for KicadSch.Netlist
type KicadSchNetlistOpts struct {
	//
	// Netlist format: kicadsexpr, kicadxml, cadstar, orcadpcb2, spice,
	// spicemodel, pads or allegro.
	//
	//
	// Default: "kicadsexpr"
	Format string // kicad (../../../../../daggerverse/kicad/sch.go:100:2)

	// Default: "netlist.net"
	OutputName string // kicad (../../../../../daggerverse/kicad/sch.go:102:2)
}

// Netlist exports the schematic's netlist.
func (r *KicadSch) Netlist(opts ...KicadSchNetlistOpts) *File { // kicad (../../../../../daggerverse/kicad/sch.go:95:1)
	q := r.query.Select("netlist")
	for i := len(opts) - 1; i >= 0; i-- {
		// `format` optional argument
		if !querybuilder.IsZeroValue(opts[i].Format) {
			q = q.Arg("format", opts[i].Format)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadSchPdfOpts contains options for KicadSch.Pdf
type KicadSchPdfOpts struct {

	// Default: "schematic.pdf"
	OutputName string // kicad (../../../../../daggerverse/kicad/sch.go:134:2)
}

// Pdf plots the schematic to a single multi-page PDF — one page per sheet of
// a hierarchical design.
func (r *KicadSch) Pdf(opts ...KicadSchPdfOpts) *File { // kicad (../../../../../daggerverse/kicad/sch.go:131:1)
	q := r.query.Select("pdf")
	for i := len(opts) - 1; i >= 0; i-- {
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// Svg plots the schematic to SVG, one file per sheet, and returns the
// directory. Unlike Pcb.Svg there is no single-file counterpart: kicad-cli
// always plots schematics per sheet.
func (r *KicadSch) Svg() *Directory { // kicad (../../../../../daggerverse/kicad/sch.go:158:1)
	q := r.query.Select("svg")

	return &Directory{
		query: q,
	}
}

// AsNode returns this KicadSch as a Node.
// This is a local type conversion — no GraphQL call.
func (r *KicadSch) AsNode() Node {
	return &NodeClient{
		query: r.query,
	}
}

// KicadOpts contains options for Query.Kicad
type KicadOpts struct {
	//
	// Container registry hosting the kicad/kicad image.
	//
	//
	// Default: "docker.io"
	Registry string // kicad (../../../../../daggerverse/kicad/main.go:72:2)
	//
	// Image tag for kicad/kicad.
	//
	//
	// Default: "10.0"
	Tag string // kicad (../../../../../daggerverse/kicad/main.go:75:2)
}

// New returns a Kicad module backed by <registry>/kicad/kicad:<tag>.
func (r *Query) Kicad(opts ...KicadOpts) *Kicad { // kicad (../../../../../daggerverse/kicad/main.go:69:1)
	q := r.query.Select("kicad")
	for i := len(opts) - 1; i >= 0; i-- {
		// `registry` optional argument
		if !querybuilder.IsZeroValue(opts[i].Registry) {
			q = q.Arg("registry", opts[i].Registry)
		}
		// `tag` optional argument
		if !querybuilder.IsZeroValue(opts[i].Tag) {
			q = q.Arg("tag", opts[i].Tag)
		}
	}

	return &Kicad{
		query: q,
	}
}
