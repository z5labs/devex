// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

// Kicad wraps kicad-cli as Dagger functions. Construct via New(); call
// Container() for the raw image, or Project(source) to reach the typed
// pcb/sch helpers.
type Kicad struct { // kicad (../../../../:0:0)
	query *querybuilder.Selection

	id      *ID
	version *string
}

func (r *Kicad) WithGraphQLQuery(q *querybuilder.Selection) *Kicad {
	return &Kicad{
		query: q,
	}
}

// Ci returns a new pipeline builder bound to the supplied project source. The
// board and schematic are auto-discovered per stage, exactly as a bare
// Project(source).Pcb()/Sch() call would.
func (r *Kicad) Ci(source *Directory) *KicadCi {
	assertNotNil("source", source)
	q := r.query.Select("ci")
	q = q.Arg("source", source)

	return &KicadCi{
		query: q,
	}
}

// Container returns the bare kicad image. This is the escape hatch for every
// subcommand this module does not wrap — kicad-cli's long tail of exotic and
// legacy exports stays reachable via `container with-exec`.
func (r *Kicad) Container() *Container {
	q := r.query.Select("container")

	return &Container{
		query: q,
	}
}

// Fp binds a footprint library directory (a .pretty folder, or any directory
// of .kicad_mod files) to the toolchain for the `fp` command family.
func (r *Kicad) Fp(source *Directory) *KicadFp {
	assertNotNil("source", source)
	q := r.query.Select("fp")
	q = q.Arg("source", source)

	return &KicadFp{
		query: q,
	}
}

// A unique identifier for this Kicad.
func (r *Kicad) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *Kicad) XXX_GraphQLType() string {
	return "Kicad"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *Kicad) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *Kicad) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *Kicad) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *Kicad) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = Kicad{query: selectNode(dag.query, id, "Kicad")}
	return nil
}

// Project binds a KiCad project directory to the toolchain. source is the
// whole project tree, not a single file, because kicad-cli resolves
// sub-sheets, footprint libraries and drawing sheets relative to it.
func (r *Kicad) Project(source *Directory) *KicadProject {
	assertNotNil("source", source)
	q := r.query.Select("project")
	q = q.Arg("source", source)

	return &KicadProject{
		query: q,
	}
}

// Sym binds a symbol library file (.kicad_sym) to the toolchain for the `sym`
// command family. It takes a lone *File, not a directory, because a symbol
// library is a single self-contained file — the footprint library's on-disk
// counterpart is a directory, which is why Fp takes a *Directory instead.
func (r *Kicad) Sym(source *File) *KicadSym {
	assertNotNil("source", source)
	q := r.query.Select("sym")
	q = q.Arg("source", source)

	return &KicadSym{
		query: q,
	}
}

// Version returns the KiCad release the pinned image ships, as reported by
// `kicad-cli version`.
func (r *Kicad) Version(ctx context.Context) (string, error) {
	if r.version != nil {
		return *r.version, nil
	}
	q := r.query.Select("version")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// Ci is a chained builder for a standardized KiCad CI pipeline. Construct via
// Kicad.Ci(source); enable check stages and output sets via the With* methods;
// call Run to execute checks-then-outputs, or Check to run only the parallel
// checks.
//
// Stage 1 runs the enabled design-rule checks in parallel (Erc, Drc); errors
// are aggregated. Stage 2 produces the enabled outputs as a single directory
// and Run returns it. Downstream consumers compose that directory into their
// own pipelines (archive, upload to a fab house, attach to a release, ...).
//
// It composes the Project/Pcb/Sch primitives without adding capability of its
// own: every stage is a call the caller could make by hand, bundled into one
// declarative pipeline so a hardware repo's CI is a single `dagger call`.
type KicadCi struct { // kicad (../../../../:0:0)
	query *querybuilder.Selection

	check *Void
	id    *ID
}
type WithKicadCiFunc func(r *KicadCi) *KicadCi

// With calls the provided function with current KicadCi.
//
// This is useful for reusability and readability by not breaking the calling chain.
func (r *KicadCi) With(f WithKicadCiFunc) *KicadCi {
	return f(r)
}

func (r *KicadCi) WithGraphQLQuery(q *querybuilder.Selection) *KicadCi {
	return &KicadCi{
		query: q,
	}
}

// Check runs the enabled check stages (Erc, Drc) in parallel via
// github.com/dagger/dagger/util/parallel and returns the aggregated error. Use
// when callers want to run the checks independently of the outputs (for
// example a PR gate that never needs the fabrication package).
func (r *KicadCi) Check(ctx context.Context) error {
	if r.check != nil {
		return nil
	}
	q := r.query.Select("check")

	return q.Execute(ctx)
}

// A unique identifier for this KicadCi.
func (r *KicadCi) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *KicadCi) XXX_GraphQLType() string {
	return "KicadCi"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *KicadCi) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *KicadCi) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *KicadCi) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *KicadCi) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = KicadCi{query: selectNode(dag.query, id, "KicadCi")}
	return nil
}

// Run executes the pipeline: stage 1 (Check) → stage 2 (outputs). Returns the
// enabled outputs merged into one directory. On stage-1 failure, returns the
// aggregated error from Check and a nil directory (stage 2 is skipped), so a
// failing check short-circuits before any export work.
func (r *KicadCi) Run() *Directory {
	q := r.query.Select("run")

	return &Directory{
		query: q,
	}
}

// KicadCiWithDrcOpts contains options for KicadCi.WithDrc
type KicadCiWithDrcOpts struct {
	SchematicParity bool
}

// WithDrc enables the Design Rule Check stage (Pcb.Drc at severity error).
// Pass schematicParity to also check the board against the schematic
// (footprints, nets, values) — a class of defect plain DRC never looks for.
func (r *KicadCi) WithDrc(opts ...KicadCiWithDrcOpts) *KicadCi {
	q := r.query.Select("withDrc")
	for i := len(opts) - 1; i >= 0; i-- {
		// `schematicParity` optional argument
		if !querybuilder.IsZeroValue(opts[i].SchematicParity) {
			q = q.Arg("schematicParity", opts[i].SchematicParity)
		}
	}

	return &KicadCi{
		query: q,
	}
}

// WithErc enables the Electrical Rule Check stage (Sch.Erc at severity error).
func (r *KicadCi) WithErc() *KicadCi {
	q := r.query.Select("withErc")

	return &KicadCi{
		query: q,
	}
}

// WithFabricationOutputs enables the fabrication package: Gerbers, drill files,
// the pick-and-place position file and the BOM. Run merges them into one
// directory (gerbers/ and drill/ subdirectories, pos.pos and bom.csv at root).
func (r *KicadCi) WithFabricationOutputs() *KicadCi {
	q := r.query.Select("withFabricationOutputs")

	return &KicadCi{
		query: q,
	}
}

// Fp is a footprint library (a .pretty directory of .kicad_mod files) bound to
// the toolchain.
type KicadFp struct { // kicad (../../../../:0:0)
	query *querybuilder.Selection

	id *ID
}

func (r *KicadFp) WithGraphQLQuery(q *querybuilder.Selection) *KicadFp {
	return &KicadFp{
		query: q,
	}
}

// A unique identifier for this KicadFp.
func (r *KicadFp) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *KicadFp) XXX_GraphQLType() string {
	return "KicadFp"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *KicadFp) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *KicadFp) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *KicadFp) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *KicadFp) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = KicadFp{query: selectNode(dag.query, id, "KicadFp")}
	return nil
}

// KicadFpSvgOpts contains options for KicadFp.Svg
type KicadFpSvgOpts struct {
	// Export only this footprint from the library; empty exports all.
	Footprint string
}

// Svg exports the footprint library to SVG, one file per footprint, and
// returns the directory. Pass footprint to export a single footprint by name
// instead of the whole library.
func (r *KicadFp) Svg(opts ...KicadFpSvgOpts) *Directory {
	q := r.query.Select("svg")
	for i := len(opts) - 1; i >= 0; i-- {
		// `footprint` optional argument
		if !querybuilder.IsZeroValue(opts[i].Footprint) {
			q = q.Arg("footprint", opts[i].Footprint)
		}
	}

	return &Directory{
		query: q,
	}
}

// KicadFpUpgradeOpts contains options for KicadFp.Upgrade
type KicadFpUpgradeOpts struct {
	// Resave even when the library is already at the latest format version.
	Force bool
}

// Upgrade resaves the footprint library in the current KiCad format and returns
// the upgraded .pretty directory.
func (r *KicadFp) Upgrade(opts ...KicadFpUpgradeOpts) *Directory {
	q := r.query.Select("upgrade")
	for i := len(opts) - 1; i >= 0; i-- {
		// `force` optional argument
		if !querybuilder.IsZeroValue(opts[i].Force) {
			q = q.Arg("force", opts[i].Force)
		}
	}

	return &Directory{
		query: q,
	}
}

// Pcb is a board selected within a Project. Every method here execs
// kicad-cli, so each carries a session cache directive; selecting the board
// itself is pure config and carries none.
type KicadPcb struct { // kicad (../../../../:0:0)
	query *querybuilder.Selection

	drc *Void
	id  *ID
}

func (r *KicadPcb) WithGraphQLQuery(q *querybuilder.Selection) *KicadPcb {
	return &KicadPcb{
		query: q,
	}
}

// KicadPcbBrepOpts contains options for KicadPcb.Brep
type KicadPcbBrepOpts struct {
	BoardOnly bool

	ExcludeDnp bool

	// Default: "board.brep"
	OutputName string
}

// Brep exports the board as an OpenCASCADE BREP model.
func (r *KicadPcb) Brep(opts ...KicadPcbBrepOpts) *File {
	q := r.query.Select("brep")
	for i := len(opts) - 1; i >= 0; i-- {
		// `boardOnly` optional argument
		if !querybuilder.IsZeroValue(opts[i].BoardOnly) {
			q = q.Arg("boardOnly", opts[i].BoardOnly)
		}
		// `excludeDnp` optional argument
		if !querybuilder.IsZeroValue(opts[i].ExcludeDnp) {
			q = q.Arg("excludeDnp", opts[i].ExcludeDnp)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbDrcOpts contains options for KicadPcb.Drc
type KicadPcbDrcOpts struct {
	// Violation levels to report: all, error, warning or exclusions.
	//
	// Default: "error"
	Severity string
	// Also check the board against the schematic (footprints, nets, values).
	SchematicParity bool
	// Refill copper zones before checking.
	RefillZones bool
}

// Drc runs the Design Rule Check and returns a non-nil error listing the
// violations when the board fails, nil when it is clean.
//
// It returns a bare error rather than (report, error) because Dagger drops a
// function's value whenever it also returns a non-nil error: a
// report-returning signature would leave the violation list unreachable on
// exactly the failure path that needs it. `--exit-code-violations` exits 5 on
// violations, which Expect=ReturnTypeAny keeps on the value path so the
// report can be read back and folded into the message.
func (r *KicadPcb) Drc(ctx context.Context, opts ...KicadPcbDrcOpts) error {
	if r.drc != nil {
		return nil
	}
	q := r.query.Select("drc")
	for i := len(opts) - 1; i >= 0; i-- {
		// `severity` optional argument
		if !querybuilder.IsZeroValue(opts[i].Severity) {
			q = q.Arg("severity", opts[i].Severity)
		}
		// `schematicParity` optional argument
		if !querybuilder.IsZeroValue(opts[i].SchematicParity) {
			q = q.Arg("schematicParity", opts[i].SchematicParity)
		}
		// `refillZones` optional argument
		if !querybuilder.IsZeroValue(opts[i].RefillZones) {
			q = q.Arg("refillZones", opts[i].RefillZones)
		}
	}

	return q.Execute(ctx)
}

// KicadPcbDrillOpts contains options for KicadPcb.Drill
type KicadPcbDrillOpts struct {
	// Drill file format: excellon or gerber.
	//
	// Default: "excellon"
	Format string
	// Excellon output units: mm or in.
	//
	// Default: "mm"
	Units string
	// Drill origin: absolute or plot.
	//
	// Default: "absolute"
	Origin string
	// Emit independent files for plated and non-plated holes.
	SeparatePlatedHoles bool
	// Also emit a drill map.
	GenerateMap bool
}

// Drill generates the board's drill files and returns them as a directory.
func (r *KicadPcb) Drill(opts ...KicadPcbDrillOpts) *Directory {
	q := r.query.Select("drill")
	for i := len(opts) - 1; i >= 0; i-- {
		// `format` optional argument
		if !querybuilder.IsZeroValue(opts[i].Format) {
			q = q.Arg("format", opts[i].Format)
		}
		// `units` optional argument
		if !querybuilder.IsZeroValue(opts[i].Units) {
			q = q.Arg("units", opts[i].Units)
		}
		// `origin` optional argument
		if !querybuilder.IsZeroValue(opts[i].Origin) {
			q = q.Arg("origin", opts[i].Origin)
		}
		// `separatePlatedHoles` optional argument
		if !querybuilder.IsZeroValue(opts[i].SeparatePlatedHoles) {
			q = q.Arg("separatePlatedHoles", opts[i].SeparatePlatedHoles)
		}
		// `generateMap` optional argument
		if !querybuilder.IsZeroValue(opts[i].GenerateMap) {
			q = q.Arg("generateMap", opts[i].GenerateMap)
		}
	}

	return &Directory{
		query: q,
	}
}

// KicadPcbDxfOpts contains options for KicadPcb.Dxf
type KicadPcbDxfOpts struct {
	// Output units: mm or in.
	//
	// Default: "mm"
	Units string

	// Default: "board.dxf"
	OutputName string
}

// Dxf plots the given layers into a single DXF drawing (`--mode-single`).
func (r *KicadPcb) Dxf(layers []string, opts ...KicadPcbDxfOpts) *File {
	q := r.query.Select("dxf")
	for i := len(opts) - 1; i >= 0; i-- {
		// `units` optional argument
		if !querybuilder.IsZeroValue(opts[i].Units) {
			q = q.Arg("units", opts[i].Units)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}
	q = q.Arg("layers", layers)

	return &File{
		query: q,
	}
}

// KicadPcbGencadOpts contains options for KicadPcb.Gencad
type KicadPcbGencadOpts struct {

	// Default: "board.cad"
	OutputName string
}

// Gencad exports the board in GenCAD format — a legacy interchange format still
// used by some test-fixture and assembly houses.
func (r *KicadPcb) Gencad(opts ...KicadPcbGencadOpts) *File {
	q := r.query.Select("gencad")
	for i := len(opts) - 1; i >= 0; i-- {
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbGerbersOpts contains options for KicadPcb.Gerbers
type KicadPcbGerbersOpts struct {
	// Untranslated layer names to plot, e.g. F.Cu, B.Cu. Empty plots all.
	Layers []string
	// Gerber coordinate precision: 5 or 6.
	//
	// Default: 6
	Precision int
	// Check and refill zones before plotting.
	CheckZones bool
}

// Gerbers plots the board's Gerber files and returns them as a directory. An
// empty layers list plots every layer the board defines, plus the .gbrjob
// file that ties them together.
func (r *KicadPcb) Gerbers(opts ...KicadPcbGerbersOpts) *Directory {
	q := r.query.Select("gerbers")
	for i := len(opts) - 1; i >= 0; i-- {
		// `layers` optional argument
		if !querybuilder.IsZeroValue(opts[i].Layers) {
			q = q.Arg("layers", opts[i].Layers)
		}
		// `precision` optional argument
		if !querybuilder.IsZeroValue(opts[i].Precision) {
			q = q.Arg("precision", opts[i].Precision)
		}
		// `checkZones` optional argument
		if !querybuilder.IsZeroValue(opts[i].CheckZones) {
			q = q.Arg("checkZones", opts[i].CheckZones)
		}
	}

	return &Directory{
		query: q,
	}
}

// KicadPcbGlbOpts contains options for KicadPcb.Glb
type KicadPcbGlbOpts struct {
	// Export the bare board, with no component models.
	BoardOnly bool
	// Exclude models for components flagged Do Not Populate.
	ExcludeDnp bool

	// Default: "board.glb"
	OutputName string
}

// Glb exports the board as a binary glTF (GLB) model. Like every 3D export it
// needs the -full image for component models; pass boardOnly for board
// geometry alone on the slim image.
func (r *KicadPcb) Glb(opts ...KicadPcbGlbOpts) *File {
	q := r.query.Select("glb")
	for i := len(opts) - 1; i >= 0; i-- {
		// `boardOnly` optional argument
		if !querybuilder.IsZeroValue(opts[i].BoardOnly) {
			q = q.Arg("boardOnly", opts[i].BoardOnly)
		}
		// `excludeDnp` optional argument
		if !querybuilder.IsZeroValue(opts[i].ExcludeDnp) {
			q = q.Arg("excludeDnp", opts[i].ExcludeDnp)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// A unique identifier for this KicadPcb.
func (r *KicadPcb) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *KicadPcb) XXX_GraphQLType() string {
	return "KicadPcb"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *KicadPcb) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *KicadPcb) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *KicadPcb) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *KicadPcb) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = KicadPcb{query: selectNode(dag.query, id, "KicadPcb")}
	return nil
}

// KicadPcbImportOpts contains options for KicadPcb.Import
type KicadPcbImportOpts struct {
	// Input format hint: auto, pads, altium, eagle, cadstar, fabmaster, pcad
	// or solidworks.
	//
	// Default: "auto"
	Format string

	// Default: "imported.kicad_pcb"
	OutputName string
}

// Import converts a non-KiCad board file into KiCad format and returns the
// produced .kicad_pcb. inputPath names the foreign file within the project
// source; unlike the export commands it is not auto-discovered, since it is not
// a .kicad_pcb and the board selected by Pcb() is irrelevant here.
func (r *KicadPcb) Import(inputPath string, opts ...KicadPcbImportOpts) *File {
	q := r.query.Select("import")
	for i := len(opts) - 1; i >= 0; i-- {
		// `format` optional argument
		if !querybuilder.IsZeroValue(opts[i].Format) {
			q = q.Arg("format", opts[i].Format)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}
	q = q.Arg("inputPath", inputPath)

	return &File{
		query: q,
	}
}

// KicadPcbIpc2581Opts contains options for KicadPcb.Ipc2581
type KicadPcbIpc2581Opts struct {
	// IPC-2581 standard revision: B or C.
	//
	// Default: "C"
	Version string
	// Units: mm or in.
	//
	// Default: "mm"
	Units string

	// Default: "board.xml"
	OutputName string
}

// Ipc2581 exports the board in IPC-2581 format — a single XML file carrying
// the fabrication and assembly data that would otherwise be spread across
// Gerbers, drill files and a BOM.
func (r *KicadPcb) Ipc2581(opts ...KicadPcbIpc2581Opts) *File {
	q := r.query.Select("ipc2581")
	for i := len(opts) - 1; i >= 0; i-- {
		// `version` optional argument
		if !querybuilder.IsZeroValue(opts[i].Version) {
			q = q.Arg("version", opts[i].Version)
		}
		// `units` optional argument
		if !querybuilder.IsZeroValue(opts[i].Units) {
			q = q.Arg("units", opts[i].Units)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbIpcd356Opts contains options for KicadPcb.Ipcd356
type KicadPcbIpcd356Opts struct {

	// Default: "board.d356"
	OutputName string
}

// Ipcd356 generates an IPC-D-356 netlist file, the bare-board electrical test
// format a fab house uses to flying-probe an unpopulated board.
func (r *KicadPcb) Ipcd356(opts ...KicadPcbIpcd356Opts) *File {
	q := r.query.Select("ipcd356")
	for i := len(opts) - 1; i >= 0; i-- {
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbOdbOpts contains options for KicadPcb.Odb
type KicadPcbOdbOpts struct {
	// Archive compression: zip, tgz or none. none writes an uncompressed
	// directory tree rather than a single file.
	//
	// Default: "zip"
	Compression string
	// Units: mm or in.
	//
	// Default: "mm"
	Units string

	// Default: "odb.zip"
	OutputName string
}

// Odb exports the board in ODB++ format as a single compressed archive — the
// fabrication data package many modern fab houses prefer over Gerbers.
func (r *KicadPcb) Odb(opts ...KicadPcbOdbOpts) *File {
	q := r.query.Select("odb")
	for i := len(opts) - 1; i >= 0; i-- {
		// `compression` optional argument
		if !querybuilder.IsZeroValue(opts[i].Compression) {
			q = q.Arg("compression", opts[i].Compression)
		}
		// `units` optional argument
		if !querybuilder.IsZeroValue(opts[i].Units) {
			q = q.Arg("units", opts[i].Units)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbPdfOpts contains options for KicadPcb.Pdf
type KicadPcbPdfOpts struct {

	// Default: "board.pdf"
	OutputName string
}

// Pdf plots the given layers into a single PDF.
//
// Pdf and PdfPerLayer are split into file- and directory-returning functions
// rather than one function taking a mode flag: a Dagger function has exactly
// one return type, so modelling `--mode-single` vs `--mode-separate` as a
// parameter would force *Directory onto the common single-file case.
func (r *KicadPcb) Pdf(layers []string, opts ...KicadPcbPdfOpts) *File {
	q := r.query.Select("pdf")
	for i := len(opts) - 1; i >= 0; i-- {
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}
	q = q.Arg("layers", layers)

	return &File{
		query: q,
	}
}

// KicadPcbPdf3DOpts contains options for KicadPcb.Pdf3D
type KicadPcbPdf3DOpts struct {
	BoardOnly bool

	ExcludeDnp bool

	// Default: "board-3d.pdf"
	OutputName string
}

// Pdf3d exports the board as a 3D PDF (a PDF carrying an embedded U3D model).
// Exposed as `pdf-3d` on the CLI.
func (r *KicadPcb) Pdf3D(opts ...KicadPcbPdf3DOpts) *File {
	q := r.query.Select("pdf3D")
	for i := len(opts) - 1; i >= 0; i-- {
		// `boardOnly` optional argument
		if !querybuilder.IsZeroValue(opts[i].BoardOnly) {
			q = q.Arg("boardOnly", opts[i].BoardOnly)
		}
		// `excludeDnp` optional argument
		if !querybuilder.IsZeroValue(opts[i].ExcludeDnp) {
			q = q.Arg("excludeDnp", opts[i].ExcludeDnp)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// PdfPerLayer plots each layer into its own PDF and returns the directory.
func (r *KicadPcb) PdfPerLayer(layers []string) *Directory {
	q := r.query.Select("pdfPerLayer")
	q = q.Arg("layers", layers)

	return &Directory{
		query: q,
	}
}

// KicadPcbPlyOpts contains options for KicadPcb.Ply
type KicadPcbPlyOpts struct {
	BoardOnly bool

	ExcludeDnp bool

	// Default: "board.ply"
	OutputName string
}

// Ply exports the board as a PLY mesh.
func (r *KicadPcb) Ply(opts ...KicadPcbPlyOpts) *File {
	q := r.query.Select("ply")
	for i := len(opts) - 1; i >= 0; i-- {
		// `boardOnly` optional argument
		if !querybuilder.IsZeroValue(opts[i].BoardOnly) {
			q = q.Arg("boardOnly", opts[i].BoardOnly)
		}
		// `excludeDnp` optional argument
		if !querybuilder.IsZeroValue(opts[i].ExcludeDnp) {
			q = q.Arg("excludeDnp", opts[i].ExcludeDnp)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbPosOpts contains options for KicadPcb.Pos
type KicadPcbPosOpts struct {
	// Board side to include: front, back or both.
	//
	// Default: "both"
	Side string
	// Output format: ascii, csv or gerber.
	//
	// Default: "ascii"
	Format string
	// Output units for the ascii and csv formats: in or mm.
	//
	// Default: "in"
	Units string
	// Include only SMD footprints.
	SmdOnly bool
	// Name of the produced file. Named outputName, not output, because -o
	// collides with the Dagger CLI's own top-level flag.
	//
	// Default: "pos.pos"
	OutputName string
}

// Pos generates the component position (pick-and-place) file.
func (r *KicadPcb) Pos(opts ...KicadPcbPosOpts) *File {
	q := r.query.Select("pos")
	for i := len(opts) - 1; i >= 0; i-- {
		// `side` optional argument
		if !querybuilder.IsZeroValue(opts[i].Side) {
			q = q.Arg("side", opts[i].Side)
		}
		// `format` optional argument
		if !querybuilder.IsZeroValue(opts[i].Format) {
			q = q.Arg("format", opts[i].Format)
		}
		// `units` optional argument
		if !querybuilder.IsZeroValue(opts[i].Units) {
			q = q.Arg("units", opts[i].Units)
		}
		// `smdOnly` optional argument
		if !querybuilder.IsZeroValue(opts[i].SmdOnly) {
			q = q.Arg("smdOnly", opts[i].SmdOnly)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbPsOpts contains options for KicadPcb.Ps
type KicadPcbPsOpts struct {

	// Default: "board.ps"
	OutputName string
}

// Ps plots the given layers into a single PostScript file (`--mode-single`).
func (r *KicadPcb) Ps(layers []string, opts ...KicadPcbPsOpts) *File {
	q := r.query.Select("ps")
	for i := len(opts) - 1; i >= 0; i-- {
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}
	q = q.Arg("layers", layers)

	return &File{
		query: q,
	}
}

// KicadPcbRenderOpts contains options for KicadPcb.Render
type KicadPcbRenderOpts struct {
	// Camera side: top, bottom, left, right, front or back.
	//
	// Default: "top"
	Side string
	// Render quality: basic, high, user or job_settings.
	//
	// Default: "basic"
	Quality string
	// Image width in pixels.
	//
	// Default: 1600
	Width int
	// Image height in pixels.
	//
	// Default: 900
	Height int

	// Default: "render.png"
	OutputName string
}

// Render ray-traces the board's 3D view to a PNG or JPEG image; the output
// format follows the outputName extension. Like the 3D model exports it is
// only meaningful with component models, so it wants the -full image — but
// unlike them it degrades to a bare-board render on the slim image rather than
// failing, because a board-only render is still a useful artifact.
func (r *KicadPcb) Render(opts ...KicadPcbRenderOpts) *File {
	q := r.query.Select("render")
	for i := len(opts) - 1; i >= 0; i-- {
		// `side` optional argument
		if !querybuilder.IsZeroValue(opts[i].Side) {
			q = q.Arg("side", opts[i].Side)
		}
		// `quality` optional argument
		if !querybuilder.IsZeroValue(opts[i].Quality) {
			q = q.Arg("quality", opts[i].Quality)
		}
		// `width` optional argument
		if !querybuilder.IsZeroValue(opts[i].Width) {
			q = q.Arg("width", opts[i].Width)
		}
		// `height` optional argument
		if !querybuilder.IsZeroValue(opts[i].Height) {
			q = q.Arg("height", opts[i].Height)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbStatsOpts contains options for KicadPcb.Stats
type KicadPcbStatsOpts struct {
	// Report format: report (human-readable) or json.
	//
	// Default: "report"
	Format string
	// Report units: mm or in.
	//
	// Default: "mm"
	Units string

	// Default: "stats.txt"
	OutputName string
}

// Stats generates a board statistics report (pad, via, track and area counts).
func (r *KicadPcb) Stats(opts ...KicadPcbStatsOpts) *File {
	q := r.query.Select("stats")
	for i := len(opts) - 1; i >= 0; i-- {
		// `format` optional argument
		if !querybuilder.IsZeroValue(opts[i].Format) {
			q = q.Arg("format", opts[i].Format)
		}
		// `units` optional argument
		if !querybuilder.IsZeroValue(opts[i].Units) {
			q = q.Arg("units", opts[i].Units)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbStepOpts contains options for KicadPcb.Step
type KicadPcbStepOpts struct {
	// Export the bare board, with no component models.
	BoardOnly bool
	// Exclude models for components flagged Do Not Populate.
	ExcludeDnp bool

	// Default: "board.step"
	OutputName string
}

// Step exports the board as a STEP model. The default `10.0` image is the
// slim variant, which ships no 3D component models: a with-models export
// (boardOnly=false) fails there naming the -full image, rather than silently
// emitting a board-only model. Pass boardOnly for board geometry alone, or
// select the -full image for populated assemblies.
func (r *KicadPcb) Step(opts ...KicadPcbStepOpts) *File {
	q := r.query.Select("step")
	for i := len(opts) - 1; i >= 0; i-- {
		// `boardOnly` optional argument
		if !querybuilder.IsZeroValue(opts[i].BoardOnly) {
			q = q.Arg("boardOnly", opts[i].BoardOnly)
		}
		// `excludeDnp` optional argument
		if !querybuilder.IsZeroValue(opts[i].ExcludeDnp) {
			q = q.Arg("excludeDnp", opts[i].ExcludeDnp)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbStlOpts contains options for KicadPcb.Stl
type KicadPcbStlOpts struct {
	BoardOnly bool

	ExcludeDnp bool

	// Default: "board.stl"
	OutputName string
}

// Stl exports the board as an STL mesh.
func (r *KicadPcb) Stl(opts ...KicadPcbStlOpts) *File {
	q := r.query.Select("stl")
	for i := len(opts) - 1; i >= 0; i-- {
		// `boardOnly` optional argument
		if !querybuilder.IsZeroValue(opts[i].BoardOnly) {
			q = q.Arg("boardOnly", opts[i].BoardOnly)
		}
		// `excludeDnp` optional argument
		if !querybuilder.IsZeroValue(opts[i].ExcludeDnp) {
			q = q.Arg("excludeDnp", opts[i].ExcludeDnp)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbStpzOpts contains options for KicadPcb.Stpz
type KicadPcbStpzOpts struct {
	BoardOnly bool

	ExcludeDnp bool

	// Default: "board.stpz"
	OutputName string
}

// Stpz exports the board as a zip-compressed STEP (STPZ) model.
func (r *KicadPcb) Stpz(opts ...KicadPcbStpzOpts) *File {
	q := r.query.Select("stpz")
	for i := len(opts) - 1; i >= 0; i-- {
		// `boardOnly` optional argument
		if !querybuilder.IsZeroValue(opts[i].BoardOnly) {
			q = q.Arg("boardOnly", opts[i].BoardOnly)
		}
		// `excludeDnp` optional argument
		if !querybuilder.IsZeroValue(opts[i].ExcludeDnp) {
			q = q.Arg("excludeDnp", opts[i].ExcludeDnp)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbSvgOpts contains options for KicadPcb.Svg
type KicadPcbSvgOpts struct {

	// Default: "board.svg"
	OutputName string
}

// Svg plots the given layers into a single SVG.
func (r *KicadPcb) Svg(layers []string, opts ...KicadPcbSvgOpts) *File {
	q := r.query.Select("svg")
	for i := len(opts) - 1; i >= 0; i-- {
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}
	q = q.Arg("layers", layers)

	return &File{
		query: q,
	}
}

// SvgPerLayer plots each layer into its own SVG and returns the directory.
func (r *KicadPcb) SvgPerLayer(layers []string) *Directory {
	q := r.query.Select("svgPerLayer")
	q = q.Arg("layers", layers)

	return &Directory{
		query: q,
	}
}

// KicadPcbU3DOpts contains options for KicadPcb.U3D
type KicadPcbU3DOpts struct {
	BoardOnly bool

	ExcludeDnp bool

	// Default: "board.u3d"
	OutputName string
}

// U3d exports the board as a Universal 3D (U3D) model.
func (r *KicadPcb) U3D(opts ...KicadPcbU3DOpts) *File {
	q := r.query.Select("u3D")
	for i := len(opts) - 1; i >= 0; i-- {
		// `boardOnly` optional argument
		if !querybuilder.IsZeroValue(opts[i].BoardOnly) {
			q = q.Arg("boardOnly", opts[i].BoardOnly)
		}
		// `excludeDnp` optional argument
		if !querybuilder.IsZeroValue(opts[i].ExcludeDnp) {
			q = q.Arg("excludeDnp", opts[i].ExcludeDnp)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbUpgradeOpts contains options for KicadPcb.Upgrade
type KicadPcbUpgradeOpts struct {
	// Resave even when the board is already at the latest format version.
	Force bool
}

// Upgrade resaves the board in the current KiCad file format and returns the
// upgraded .kicad_pcb. kicad-cli's `pcb upgrade` rewrites the file in place and
// has no output flag, so the board runs against a writable copy (owned by the
// image's UID-1000 user) rather than the read-only mounted source.
func (r *KicadPcb) Upgrade(opts ...KicadPcbUpgradeOpts) *File {
	q := r.query.Select("upgrade")
	for i := len(opts) - 1; i >= 0; i-- {
		// `force` optional argument
		if !querybuilder.IsZeroValue(opts[i].Force) {
			q = q.Arg("force", opts[i].Force)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbVrmlOpts contains options for KicadPcb.Vrml
type KicadPcbVrmlOpts struct {
	// Acknowledge board-geometry-only output on the slim image, skipping the
	// -full guard. VRML has no board-only mode, so this cannot exclude models
	// on the -full image.
	BoardOnly bool
	// Exclude models for components flagged Do Not Populate.
	ExcludeDnp bool
	// Output units: mm, m, in or tenths.
	//
	// Default: "mm"
	Units string

	// Default: "board.wrl"
	OutputName string
}

// Vrml exports the board as a VRML model. Unlike the other 3D exports
// kicad-cli's `pcb export vrml` has no --board-only flag, so boardOnly here is
// a module-level acknowledgement rather than a kicad-cli switch: it gates the
// -full-image guard only. On the slim image no component models resolve, so
// boardOnly=true yields the board geometry alone; on the -full image the
// models are always embedded regardless, which is why boardOnly cannot suppress
// them for VRML.
func (r *KicadPcb) Vrml(opts ...KicadPcbVrmlOpts) *File {
	q := r.query.Select("vrml")
	for i := len(opts) - 1; i >= 0; i-- {
		// `boardOnly` optional argument
		if !querybuilder.IsZeroValue(opts[i].BoardOnly) {
			q = q.Arg("boardOnly", opts[i].BoardOnly)
		}
		// `excludeDnp` optional argument
		if !querybuilder.IsZeroValue(opts[i].ExcludeDnp) {
			q = q.Arg("excludeDnp", opts[i].ExcludeDnp)
		}
		// `units` optional argument
		if !querybuilder.IsZeroValue(opts[i].Units) {
			q = q.Arg("units", opts[i].Units)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadPcbXaoOpts contains options for KicadPcb.Xao
type KicadPcbXaoOpts struct {
	BoardOnly bool

	ExcludeDnp bool

	// Default: "board.xao"
	OutputName string
}

// Xao exports the board as an XAO model (Salome geometry exchange).
func (r *KicadPcb) Xao(opts ...KicadPcbXaoOpts) *File {
	q := r.query.Select("xao")
	for i := len(opts) - 1; i >= 0; i-- {
		// `boardOnly` optional argument
		if !querybuilder.IsZeroValue(opts[i].BoardOnly) {
			q = q.Arg("boardOnly", opts[i].BoardOnly)
		}
		// `excludeDnp` optional argument
		if !querybuilder.IsZeroValue(opts[i].ExcludeDnp) {
			q = q.Arg("excludeDnp", opts[i].ExcludeDnp)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// Project is a KiCad project tree plus the options that apply to nearly every
// kicad-cli subcommand. It is immutable: every With* returns a copy.
type KicadProject struct { // kicad (../../../../:0:0)
	query *querybuilder.Selection

	id *ID
}
type WithKicadProjectFunc func(r *KicadProject) *KicadProject

// With calls the provided function with current KicadProject.
//
// This is useful for reusability and readability by not breaking the calling chain.
func (r *KicadProject) With(f WithKicadProjectFunc) *KicadProject {
	return f(r)
}

func (r *KicadProject) WithGraphQLQuery(q *querybuilder.Selection) *KicadProject {
	return &KicadProject{
		query: q,
	}
}

// A unique identifier for this KicadProject.
func (r *KicadProject) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *KicadProject) XXX_GraphQLType() string {
	return "KicadProject"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *KicadProject) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *KicadProject) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *KicadProject) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *KicadProject) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = KicadProject{query: selectNode(dag.query, id, "KicadProject")}
	return nil
}

// Jobset runs a .kicad_jobset file and returns the project directory with
// everything the jobset produced, so a project that already declares its
// output set in-repo can generate the whole fabrication package in one call.
//
// The jobset's outputs are written relative to the project, which is why the
// whole tree comes back rather than a lone output folder: the jobset — not
// this module — decides where its artifacts land.
func (r *KicadProject) Jobset(path string) *Directory {
	q := r.query.Select("jobset")
	q = q.Arg("path", path)

	return &Directory{
		query: q,
	}
}

// KicadProjectPcbOpts contains options for KicadProject.Pcb
type KicadProjectPcbOpts struct {
	// Project-relative path to the .kicad_pcb; empty auto-discovers.
	Path string
}

// Pcb selects a board within the project. An empty path auto-discovers the
// single *.kicad_pcb in the tree and errors when there are zero or more than
// one; discovery is deferred to the exec so the error surfaces on the call
// that needed the board.
func (r *KicadProject) Pcb(opts ...KicadProjectPcbOpts) *KicadPcb {
	q := r.query.Select("pcb")
	for i := len(opts) - 1; i >= 0; i-- {
		// `path` optional argument
		if !querybuilder.IsZeroValue(opts[i].Path) {
			q = q.Arg("path", opts[i].Path)
		}
	}

	return &KicadPcb{
		query: q,
	}
}

// KicadProjectSchOpts contains options for KicadProject.Sch
type KicadProjectSchOpts struct {
	// Project-relative path to the .kicad_sch; empty auto-discovers.
	Path string
}

// Sch selects a schematic within the project. An empty path auto-discovers
// the single *.kicad_sch in the tree, ignoring the sub-sheets of a
// hierarchical design, and errors when there are zero or more than one root.
func (r *KicadProject) Sch(opts ...KicadProjectSchOpts) *KicadSch {
	q := r.query.Select("sch")
	for i := len(opts) - 1; i >= 0; i-- {
		// `path` optional argument
		if !querybuilder.IsZeroValue(opts[i].Path) {
			q = q.Arg("path", opts[i].Path)
		}
	}

	return &KicadSch{
		query: q,
	}
}

// WithDrawingSheet overrides the project's drawing sheet with the supplied
// .kicad_wks file (`--drawing-sheet`). It applies to the plotting exports;
// subcommands that do not accept the flag ignore it.
func (r *KicadProject) WithDrawingSheet(sheet *File) *KicadProject {
	assertNotNil("sheet", sheet)
	q := r.query.Select("withDrawingSheet")
	q = q.Arg("sheet", sheet)

	return &KicadProject{
		query: q,
	}
}

// WithVar sets a KiCad text variable, overriding or adding to the ones the
// project file declares (kicad-cli's `--define-var name=value`).
//
// It takes a name and a value rather than a map because Dagger functions
// cannot accept map parameters. Validation is deferred to the exec: builder
// methods have no error return, so a bad name surfaces when the export or
// check that would have used it runs.
func (r *KicadProject) WithVar(name string, value string) *KicadProject {
	q := r.query.Select("withVar")
	q = q.Arg("name", name)
	q = q.Arg("value", value)

	return &KicadProject{
		query: q,
	}
}

// WithVariant selects a KiCad assembly variant (`--variant`). It applies to
// the exports that support variants; checks (drc, erc) and drill files ignore
// it because kicad-cli does not accept the flag there.
//
// The name is validated against the variants the project file declares:
// kicad-cli silently falls back to the default variant when handed an unknown
// name, so an unrecognised variant would otherwise produce a wrong export with
// no signal. Like WithVar, the check is deferred to the exec that uses it,
// because a builder method has no error return.
func (r *KicadProject) WithVariant(variant string) *KicadProject {
	q := r.query.Select("withVariant")
	q = q.Arg("variant", variant)

	return &KicadProject{
		query: q,
	}
}

// Sch is a schematic selected within a Project.
type KicadSch struct { // kicad (../../../../:0:0)
	query *querybuilder.Selection

	erc *Void
	id  *ID
}

func (r *KicadSch) WithGraphQLQuery(q *querybuilder.Selection) *KicadSch {
	return &KicadSch{
		query: q,
	}
}

// KicadSchBomOpts contains options for KicadSch.Bom
type KicadSchBomOpts struct {
	// Ordered list of fields to export. Generated fields such as QUANTITY,
	// ITEM_NUMBER and DNP may be used alongside symbol fields.
	//
	// Default: "Reference,Value,Footprint,QUANTITY,DNP"
	Fields string
	// Fields to group references by when their values match.
	GroupBy string
	// Field name to sort by.
	//
	// Default: "Reference"
	SortField string
	// Exclude symbols marked Do Not Populate.
	ExcludeDnp bool

	// Default: "bom.csv"
	OutputName string
}

// Bom exports the Bill of Materials as CSV. The header row is the field list
// verbatim, because fields is always passed through to kicad-cli — the column
// labels follow whatever the caller asked to export.
func (r *KicadSch) Bom(opts ...KicadSchBomOpts) *File {
	q := r.query.Select("bom")
	for i := len(opts) - 1; i >= 0; i-- {
		// `fields` optional argument
		if !querybuilder.IsZeroValue(opts[i].Fields) {
			q = q.Arg("fields", opts[i].Fields)
		}
		// `groupBy` optional argument
		if !querybuilder.IsZeroValue(opts[i].GroupBy) {
			q = q.Arg("groupBy", opts[i].GroupBy)
		}
		// `sortField` optional argument
		if !querybuilder.IsZeroValue(opts[i].SortField) {
			q = q.Arg("sortField", opts[i].SortField)
		}
		// `excludeDnp` optional argument
		if !querybuilder.IsZeroValue(opts[i].ExcludeDnp) {
			q = q.Arg("excludeDnp", opts[i].ExcludeDnp)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// Dxf plots the schematic to DXF, one file per sheet, and returns the
// directory. Like Svg there is no single-file counterpart: kicad-cli always
// plots schematics per sheet.
func (r *KicadSch) Dxf() *Directory {
	q := r.query.Select("dxf")

	return &Directory{
		query: q,
	}
}

// KicadSchErcOpts contains options for KicadSch.Erc
type KicadSchErcOpts struct {
	// Violation levels to report: all, error, warning or exclusions.
	//
	// Default: "error"
	Severity string
}

// Erc runs the Electrical Rule Check and returns a non-nil error listing the
// violations when the schematic fails, nil when it is clean.
//
// Like Pcb.Drc it returns a bare error: Dagger drops a function's value when
// it also returns a non-nil error, so a (report, error) signature would hide
// the violation list on the failure path. `--exit-code-violations` exits 5,
// which Expect=ReturnTypeAny keeps on the value path.
func (r *KicadSch) Erc(ctx context.Context, opts ...KicadSchErcOpts) error {
	if r.erc != nil {
		return nil
	}
	q := r.query.Select("erc")
	for i := len(opts) - 1; i >= 0; i-- {
		// `severity` optional argument
		if !querybuilder.IsZeroValue(opts[i].Severity) {
			q = q.Arg("severity", opts[i].Severity)
		}
	}

	return q.Execute(ctx)
}

// A unique identifier for this KicadSch.
func (r *KicadSch) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *KicadSch) XXX_GraphQLType() string {
	return "KicadSch"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *KicadSch) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *KicadSch) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *KicadSch) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *KicadSch) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = KicadSch{query: selectNode(dag.query, id, "KicadSch")}
	return nil
}

// KicadSchNetlistOpts contains options for KicadSch.Netlist
type KicadSchNetlistOpts struct {
	// Netlist format: kicadsexpr, kicadxml, cadstar, orcadpcb2, spice,
	// spicemodel, pads or allegro.
	//
	// Default: "kicadsexpr"
	Format string

	// Default: "netlist.net"
	OutputName string
}

// Netlist exports the schematic's netlist.
func (r *KicadSch) Netlist(opts ...KicadSchNetlistOpts) *File {
	q := r.query.Select("netlist")
	for i := len(opts) - 1; i >= 0; i-- {
		// `format` optional argument
		if !querybuilder.IsZeroValue(opts[i].Format) {
			q = q.Arg("format", opts[i].Format)
		}
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// KicadSchPdfOpts contains options for KicadSch.Pdf
type KicadSchPdfOpts struct {

	// Default: "schematic.pdf"
	OutputName string
}

// Pdf plots the schematic to a single multi-page PDF — one page per sheet of
// a hierarchical design.
func (r *KicadSch) Pdf(opts ...KicadSchPdfOpts) *File {
	q := r.query.Select("pdf")
	for i := len(opts) - 1; i >= 0; i-- {
		// `outputName` optional argument
		if !querybuilder.IsZeroValue(opts[i].OutputName) {
			q = q.Arg("outputName", opts[i].OutputName)
		}
	}

	return &File{
		query: q,
	}
}

// Ps plots the schematic to PostScript, one file per sheet, and returns the
// directory.
func (r *KicadSch) Ps() *Directory {
	q := r.query.Select("ps")

	return &Directory{
		query: q,
	}
}

// Svg plots the schematic to SVG, one file per sheet, and returns the
// directory. Unlike Pcb.Svg there is no single-file counterpart: kicad-cli
// always plots schematics per sheet.
func (r *KicadSch) Svg() *Directory {
	q := r.query.Select("svg")

	return &Directory{
		query: q,
	}
}

// Sym is a symbol library (a .kicad_sym file) bound to the toolchain.
type KicadSym struct { // kicad (../../../../:0:0)
	query *querybuilder.Selection

	id *ID
}

func (r *KicadSym) WithGraphQLQuery(q *querybuilder.Selection) *KicadSym {
	return &KicadSym{
		query: q,
	}
}

// A unique identifier for this KicadSym.
func (r *KicadSym) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *KicadSym) XXX_GraphQLType() string {
	return "KicadSym"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *KicadSym) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *KicadSym) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *KicadSym) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *KicadSym) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = KicadSym{query: selectNode(dag.query, id, "KicadSym")}
	return nil
}

// KicadSymSvgOpts contains options for KicadSym.Svg
type KicadSymSvgOpts struct {
	// Export only this symbol from the library; empty exports all.
	Symbol string
}

// Svg exports the symbol library to SVG, one file per symbol unit, and returns
// the directory. Pass symbol to export a single symbol by name instead of the
// whole library.
func (r *KicadSym) Svg(opts ...KicadSymSvgOpts) *Directory {
	q := r.query.Select("svg")
	for i := len(opts) - 1; i >= 0; i-- {
		// `symbol` optional argument
		if !querybuilder.IsZeroValue(opts[i].Symbol) {
			q = q.Arg("symbol", opts[i].Symbol)
		}
	}

	return &Directory{
		query: q,
	}
}

// KicadSymUpgradeOpts contains options for KicadSym.Upgrade
type KicadSymUpgradeOpts struct {
	// Resave even when the library is already at the latest format version.
	Force bool
}

// Upgrade resaves the symbol library in the current KiCad format and returns
// the upgraded .kicad_sym file.
func (r *KicadSym) Upgrade(opts ...KicadSymUpgradeOpts) *File {
	q := r.query.Select("upgrade")
	for i := len(opts) - 1; i >= 0; i-- {
		// `force` optional argument
		if !querybuilder.IsZeroValue(opts[i].Force) {
			q = q.Arg("force", opts[i].Force)
		}
	}

	return &File{
		query: q,
	}
}

// KicadOpts contains options for Query.Kicad
type KicadOpts struct {
	// Container registry hosting the kicad/kicad image.
	//
	// Default: "docker.io"
	Registry string
	// Image tag for kicad/kicad.
	//
	// Default: "10.0"
	Tag string
	// Select the -full image variant, which bundles the 3D component-model
	// libraries the slim image omits. The suffix is appended to tag, so
	// full=true with the default 10.0 tag resolves 10.0-full. Required for any
	// 3D export that includes component models (Step without boardOnly, Glb,
	// Stl, ...); ~1.34GB against the slim image's ~770MB.
	Full bool
}

func (r *Query) Kicad(opts ...KicadOpts) *Kicad { // kicad (../../../../:0:0)
	q := r.query.Select("kicad")
	for i := len(opts) - 1; i >= 0; i-- {
		// `registry` optional argument
		if !querybuilder.IsZeroValue(opts[i].Registry) {
			q = q.Arg("registry", opts[i].Registry)
		}
		// `tag` optional argument
		if !querybuilder.IsZeroValue(opts[i].Tag) {
			q = q.Arg("tag", opts[i].Tag)
		}
		// `full` optional argument
		if !querybuilder.IsZeroValue(opts[i].Full) {
			q = q.Arg("full", opts[i].Full)
		}
	}

	return &Kicad{
		query: q,
	}
}
