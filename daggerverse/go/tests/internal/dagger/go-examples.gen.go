// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

// Retrieve the binding value, as type GoExamples
func (r *Binding) AsGoExamples() *GoExamples { // go-examples (../../../../../daggerverse/go/examples/go/main.go:20:6)
	q := r.query.Select("asGoExamples")

	return &GoExamples{
		query: q,
	}
}

// Create or update a binding of type GoExamples in the environment
func (r *Env) WithGoExamplesInput(name string, value *GoExamples, description string) *Env { // go-examples (../../../../../daggerverse/go/examples/go/main.go:20:6)
	assertNotNil("value", value)
	q := r.query.Select("withGoExamplesInput")
	q = q.Arg("name", name)
	q = q.Arg("value", value)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Declare a desired GoExamples output to be assigned in the environment
func (r *Env) WithGoExamplesOutput(name string, description string) *Env { // go-examples (../../../../../daggerverse/go/examples/go/main.go:20:6)
	q := r.query.Select("withGoExamplesOutput")
	q = q.Arg("name", name)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// GoExamples is the module's main object: a namespace for the go usage
// recipes.
type GoExamples struct { // go-examples (../../../../../daggerverse/go/examples/go/main.go:20:6)
	query *querybuilder.Selection

	id          *ID
	testPackage *string
}

func (r *GoExamples) WithGraphQLQuery(q *querybuilder.Selection) *GoExamples {
	return &GoExamples{
		query: q,
	}
}

// GoExamplesBuildBinaryOpts contains options for GoExamples.BuildBinary
type GoExamplesBuildBinaryOpts struct {
	//
	// The Go source tree to build. Defaults to the built-in sample module.
	//
	Source *Directory // go-examples (../../../../../daggerverse/go/examples/go/main.go:55:2)
	//
	// The package to build, relative to the source root. Must resolve to a
	// single main package, since output names one file.
	//
	//
	// Default: "."
	Pkg string // go-examples (../../../../../daggerverse/go/examples/go/main.go:60:2)
}

// BuildBinary compiles a Go source tree and returns the single produced
// executable as a *dagger.File -- the recipe to reach for when the artifact
// you actually want out of a pipeline is one binary to ship:
//
//	dagger call build-binary export --path ./greeter
//
// Two details worth copying. Build returns the whole /out directory (go
// build can emit one binary per main package), so you select the file you
// want out of it. And passing output pins that filename, which is what makes
// the .File("app") selection below deterministic -- leave output empty and
// go names each binary after its own package instead.
//
// Note the toolchain is never named here: go infers it from the source's
// go.mod `go` directive, so the sample builds on golang:1.23. Pin it
// explicitly with dag.Go(dagger.GoOpts{Version: "1.24"}) when a project
// needs a newer compiler than it declares.
func (r *GoExamples) BuildBinary(opts ...GoExamplesBuildBinaryOpts) *File { // go-examples (../../../../../daggerverse/go/examples/go/main.go:51:1)
	q := r.query.Select("buildBinary")
	for i := len(opts) - 1; i >= 0; i-- {
		// `source` optional argument
		if !querybuilder.IsZeroValue(opts[i].Source) {
			q = q.Arg("source", opts[i].Source)
		}
		// `pkg` optional argument
		if !querybuilder.IsZeroValue(opts[i].Pkg) {
			q = q.Arg("pkg", opts[i].Pkg)
		}
	}

	return &File{
		query: q,
	}
}

// A unique identifier for this GoExamples.
func (r *GoExamples) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *GoExamples) XXX_GraphQLType() string {
	return "GoExamples"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *GoExamples) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *GoExamples) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *GoExamples) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *GoExamples) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = GoExamples{query: selectNode(dag.query, id, "GoExamples")}
	return nil
}

// GoExamplesInstallToolOpts contains options for GoExamples.InstallTool
type GoExamplesInstallToolOpts struct {
	//
	// The tool package to install, pinned to an explicit version.
	//
	//
	// Default: "github.com/google/yamlfmt/cmd/yamlfmt@v0.21.0"
	Pkg string // go-examples (../../../../../daggerverse/go/examples/go/main.go:151:2)
}

// InstallTool `go install`s a command-line tool and returns the compiled
// binary as a *dagger.File, ready to mount into any other container:
//
//	dagger call install-tool export --path ./yamlfmt
//
// This is how you get a Go-distributed tool into a pipeline without building
// an image for it. The returned file is named after the package's last path
// segment (`yamlfmt`), matching go install's own naming rules, and the
// install runs in a source-less container -- so the same tool installed by
// several stages resolves to one shared build.
//
// pkg must pin an explicit version. go rejects `@latest` and bare paths,
// because Install is cached for the session: without a pin the proxy could
// resolve a different version on a later call and the pipeline would
// silently keep serving the first binary it happened to build.
func (r *GoExamples) InstallTool(opts ...GoExamplesInstallToolOpts) *File { // go-examples (../../../../../daggerverse/go/examples/go/main.go:147:1)
	q := r.query.Select("installTool")
	for i := len(opts) - 1; i >= 0; i-- {
		// `pkg` optional argument
		if !querybuilder.IsZeroValue(opts[i].Pkg) {
			q = q.Arg("pkg", opts[i].Pkg)
		}
	}

	return &File{
		query: q,
	}
}

// GoExamplesModuleHygieneOpts contains options for GoExamples.ModuleHygiene
type GoExamplesModuleHygieneOpts struct {
	//
	// The Go source tree to check and tidy. Defaults to the built-in sample
	// module.
	//
	Source *Directory // go-examples (../../../../../daggerverse/go/examples/go/main.go:116:2)
}

// ModuleHygiene runs the three housekeeping checks a Go repo wants before it
// commits -- gofmt, go vet, go mod tidy -- and returns the tidied source
// tree:
//
//	dagger call module-hygiene export --path ./tidied
//
// The ordering is the lesson. Fmt and Vet are gates: each returns an error
// the moment it finds a problem, so an unformatted file or a bad Printf verb
// stops the recipe before anything is rewritten. Only once both pass does
// ModTidy run and hand back /src, which is why the exported directory is
// always a tree that already passed its checks.
//
// Fmt also returns the gofmt diff alongside its error. That output is the
// useful half of the failure, so it is folded into the error message here
// instead of being dropped.
func (r *GoExamples) ModuleHygiene(opts ...GoExamplesModuleHygieneOpts) *Directory { // go-examples (../../../../../daggerverse/go/examples/go/main.go:110:1)
	q := r.query.Select("moduleHygiene")
	for i := len(opts) - 1; i >= 0; i-- {
		// `source` optional argument
		if !querybuilder.IsZeroValue(opts[i].Source) {
			q = q.Arg("source", opts[i].Source)
		}
	}

	return &Directory{
		query: q,
	}
}

// GoExamplesTestPackageOpts contains options for GoExamples.TestPackage
type GoExamplesTestPackageOpts struct {
	//
	// The Go source tree to test. Defaults to the built-in sample module.
	//
	Source *Directory // go-examples (../../../../../daggerverse/go/examples/go/main.go:82:2)
	//
	// The package pattern to test.
	//
	//
	// Default: "./..."
	Pkg string // go-examples (../../../../../daggerverse/go/examples/go/main.go:86:2)
	//
	// Enable the data-race detector (`go test -race`).
	//
	Race bool // go-examples (../../../../../daggerverse/go/examples/go/main.go:90:2)
}

// TestPackage runs `go test ./...` over a source tree and returns the
// combined output, so a failing package shows up as text you can read rather
// than an opaque exit code:
//
//	dagger call test-package
//
// go always passes -count=1, which disables Go's own test cache: inside a
// pipeline you want the tests to actually execute, not replay a previous
// "ok (cached)" line. Set race to add the data-race detector, the same knob
// the Ci builder's WithTest(race) flips.
func (r *GoExamples) TestPackage(ctx context.Context, opts ...GoExamplesTestPackageOpts) (string, error) { // go-examples (../../../../../daggerverse/go/examples/go/main.go:77:1)
	if r.testPackage != nil {
		return *r.testPackage, nil
	}
	q := r.query.Select("testPackage")
	for i := len(opts) - 1; i >= 0; i-- {
		// `source` optional argument
		if !querybuilder.IsZeroValue(opts[i].Source) {
			q = q.Arg("source", opts[i].Source)
		}
		// `pkg` optional argument
		if !querybuilder.IsZeroValue(opts[i].Pkg) {
			q = q.Arg("pkg", opts[i].Pkg)
		}
		// `race` optional argument
		if !querybuilder.IsZeroValue(opts[i].Race) {
			q = q.Arg("race", opts[i].Race)
		}
	}

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// AsNode returns this GoExamples as a Node.
// This is a local type conversion — no GraphQL call.
func (r *GoExamples) AsNode() Node {
	return &NodeClient{
		query: r.query,
	}
}

// GoExamples is the module's main object: a namespace for the go usage
// recipes.
func (r *Query) GoExamples() *GoExamples { // go-examples (../../../../../daggerverse/go/examples/go/main.go:20:6)
	q := r.query.Select("goExamples")

	return &GoExamples{
		query: q,
	}
}
