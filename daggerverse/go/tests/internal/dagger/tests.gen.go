// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

func (r *Query) Tests() *Tests { // tests (../../../../../:0:0)
	q := r.query.Select("tests")

	return &Tests{
		query: q,
	}
}

type Tests struct { // tests (../../../../../:0:0)
	query *querybuilder.Selection

	all                                            *Void
	buildBuildmodeCarchiveProducesArchiveAndHeader *Void
	buildBuildmodeMembersAllProduceOutput          *Void
	buildHelloWritesBinary                         *Void
	buildMultipkgDotSlashEllipsis                  *Void
	buildPlatformCrossCompiles                     *Void
	buildRaceLinksTheDetector                      *Void
	buildRejectsMalformedStamps                    *Void
	buildRejectsRaceWithDisableCgo                 *Void
	buildStampsReachTheBinary                      *Void
	buildStripShrinksTheBinary                     *Void
	buildTagsSelectTaggedFiles                     *Void
	buildTrimpathRemovesSourcePaths                *Void
	ciCheckRunsEnabledChecksAndSkipsBuild          *Void
	ciRunHelloAllStages                            *Void
	ciRunHelloDefaultsProduceModuleNameBinary      *Void
	ciRunVetBadAggregates                          *Void
	ciWithBuildCustomBinaryName                    *Void
	ciWithFmtPasses                                *Void
	ciWithLintAcceptsV2Config                      *Void
	ciWithLintBuildsUnderOlderGoToolchain          *Void
	ciWithLintPasses                               *Void
	ciWithLintRejectsUnreadableVersion             *Void
	ciWithLintRejectsV1Config                      *Void
	ciWithLintRollsBackToV1                        *Void
	ciWithTestPasses                               *Void
	ciWithTestRacePasses                           *Void
	ciWithVetPasses                                *Void
	containerHasGoToolchain                        *Void
	containerInfersVersionFromGoMod                *Void
	cycloneDxDocumentIsCompliant                   *Void
	envContainsGoroot                              *Void
	fmtHelloIsClean                                *Void
	generateHelloProducesFile                      *Void
	id                                             *ID
	installSmallToolReturnsBinary                  *Void
	modDownloadHelloPasses                         *Void
	modTidyHelloIsIdempotent                       *Void
	modVerifyHelloPasses                           *Void
	runHelloPrintsHello                            *Void
	sbomDescribesTheBinaryNotTheSourceTree         *Void
	sbomFormatsAgreeOnComponents                   *Void
	sbomResolvesDependencyLicences                 *Void
	spdxDocumentIsCompliant                        *Void
	testHelloPasses                                *Void
	testMultipkgPkgArgVariants                     *Void
	toolVersionContainsGoVersion                   *Void
	vetHelloPasses                                 *Void
	workInitSucceeds                               *Void
}

func (r *Tests) WithGraphQLQuery(q *querybuilder.Selection) *Tests {
	return &Tests{
		query: q,
	}
}

// TestsAllOpts contains options for Tests.All
type TestsAllOpts struct {
	GoImageTag string

	Parallel int
}

// All runs every go-module test in parallel. goImageTag is forwarded to
// each per-test as the Go toolchain image tag passed to dag.Go(); an empty
// string preserves the module's default behavior (infer from each fixture's
// go.mod for source-bearing tests, fall back to "latest" otherwise).
//
// Note: ContainerInfersVersionFromGoMod intentionally ignores goImageTag —
// it asserts the empty-version inference path against a 1.23 fixture, so a
// caller-supplied override would defeat what the test is verifying.
// CiWithLintBuildsUnderOlderGoToolchain takes no goImageTag at all for the
// same reason: the toolchain it pins is the subject of the assertion.
//
// parallel caps how many tests run concurrently inside this suite. Defaults
// to 0 (unbounded fan-out) — each `dagger check` job runs on its own GH
// Actions runner, so in-runner parallelism is bounded by the VM's
// CPU/memory, not by the scheduler. Pass any positive integer to opt into
// a specific cap.
func (r *Tests) All(ctx context.Context, opts ...TestsAllOpts) error {
	if r.all != nil {
		return nil
	}
	q := r.query.Select("all")
	for i := len(opts) - 1; i >= 0; i-- {
		// `goImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].GoImageTag) {
			q = q.Arg("goImageTag", opts[i].GoImageTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// BuildBuildmodeCArchiveProducesArchiveAndHeader asserts C_ARCHIVE reaches
// `go build -buildmode=c-archive`: the output is the ar archive plus the
// generated C header, and specifically not an executable — which is what the
// same fixture and the same -o would produce with the buildmode left off.
func (r *Tests) BuildBuildmodeCarchiveProducesArchiveAndHeader(ctx context.Context, goImageTag string) error {
	if r.buildBuildmodeCarchiveProducesArchiveAndHeader != nil {
		return nil
	}
	q := r.query.Select("buildBuildmodeCarchiveProducesArchiveAndHeader")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// BuildBuildmodeMembersAllProduceOutput calls Build once per BuildMode member
// with a fixture and an output name that mode can actually satisfy, asserting
// each produces a non-empty artifact. That is what makes every member of the
// enum reachable rather than merely declared: a member the module failed to
// map onto a `-buildmode=` value would fail here.
func (r *Tests) BuildBuildmodeMembersAllProduceOutput(ctx context.Context, goImageTag string) error {
	if r.buildBuildmodeMembersAllProduceOutput != nil {
		return nil
	}
	q := r.query.Select("buildBuildmodeMembersAllProduceOutput")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// BuildHelloWritesBinary builds the hello fixture into /out and asserts the
// produced "hello" binary is non-empty.
func (r *Tests) BuildHelloWritesBinary(ctx context.Context, goImageTag string) error {
	if r.buildHelloWritesBinary != nil {
		return nil
	}
	q := r.query.Select("buildHelloWritesBinary")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// BuildMultipkgDotSlashEllipsis builds the multipkg fixture with the default
// pkg=./... and asserts the produced multipkg binary is non-empty. Only the
// root main package contributes a binary; pkg/foo is a library.
func (r *Tests) BuildMultipkgDotSlashEllipsis(ctx context.Context, goImageTag string) error {
	if r.buildMultipkgDotSlashEllipsis != nil {
		return nil
	}
	q := r.query.Select("buildMultipkgDotSlashEllipsis")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// BuildPlatformCrossCompiles asserts platform reaches GOOS/GOARCH: the
// binary built for linux/arm64 carries the aarch64 machine type in its ELF
// header (e_machine == 0xb7 at offset 18), which an amd64 build does not.
func (r *Tests) BuildPlatformCrossCompiles(ctx context.Context, goImageTag string) error {
	if r.buildPlatformCrossCompiles != nil {
		return nil
	}
	q := r.query.Select("buildPlatformCrossCompiles")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// BuildRaceLinksTheDetector asserts race reaches `go build -race`: the racy
// fixture reports race=on only when the detector is linked in, because
// -race implies the `race` build tag. The binary is run to prove the
// detector is present in the artifact and not merely on the command line.
func (r *Tests) BuildRaceLinksTheDetector(ctx context.Context, goImageTag string) error {
	if r.buildRaceLinksTheDetector != nil {
		return nil
	}
	q := r.query.Select("buildRaceLinksTheDetector")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// BuildRejectsMalformedStamps asserts Build rejects a stamp with no "=" and
// a stamp whose importpath.Name is empty, and that each message names the
// offending element rather than reporting a generic parse failure.
func (r *Tests) BuildRejectsMalformedStamps(ctx context.Context, goImageTag string) error {
	if r.buildRejectsMalformedStamps != nil {
		return nil
	}
	q := r.query.Select("buildRejectsMalformedStamps")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// BuildRejectsRaceWithDisableCgo asserts Build refuses the racepairing up front rather than letting `go build` fail on it, and that the
// message names both inputs so the caller knows which two are in conflict.
func (r *Tests) BuildRejectsRaceWithDisableCgo(ctx context.Context, goImageTag string) error {
	if r.buildRejectsRaceWithDisableCgo != nil {
		return nil
	}
	q := r.query.Select("buildRejectsRaceWithDisableCgo")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// BuildStampsReachTheBinary asserts -X stamps are applied and that a stamp
// value containing "=" arrives unmangled: only the first "=" separates the
// variable name from its value.
func (r *Tests) BuildStampsReachTheBinary(ctx context.Context, goImageTag string) error {
	if r.buildStampsReachTheBinary != nil {
		return nil
	}
	q := r.query.Select("buildStampsReachTheBinary")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// BuildStripShrinksTheBinary asserts strip reaches `go build -ldflags "-s
// -w"`: dropping the symbol table and DWARF info makes the output smaller.
func (r *Tests) BuildStripShrinksTheBinary(ctx context.Context, goImageTag string) error {
	if r.buildStripShrinksTheBinary != nil {
		return nil
	}
	q := r.query.Select("buildStripShrinksTheBinary")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// BuildTagsSelectTaggedFiles asserts tags reaches `go build -tags`: the
// stamped fixture reports flavor=fancy only when the `fancy` tag is set.
func (r *Tests) BuildTagsSelectTaggedFiles(ctx context.Context, goImageTag string) error {
	if r.buildTagsSelectTaggedFiles != nil {
		return nil
	}
	q := r.query.Select("buildTagsSelectTaggedFiles")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// BuildTrimpathRemovesSourcePaths asserts trimpath reaches `go build
// -trimpath`: the build's own /src mount point is recorded in an untrimmed
// binary and absent from a trimmed one.
func (r *Tests) BuildTrimpathRemovesSourcePaths(ctx context.Context, goImageTag string) error {
	if r.buildTrimpathRemovesSourcePaths != nil {
		return nil
	}
	q := r.query.Select("buildTrimpathRemovesSourcePaths")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// CiCheckRunsEnabledChecksAndSkipsBuild configures every With* stage
// against the clean hello fixture and calls Check (not Run), asserting
// no error. To actively prove Check does not invoke the build stage
// internally, WithBuild is configured with a non-existent package path:
// if Check were to call runBuild, `go build ./does-not-exist` would
// fail and surface here as an error. A nil return therefore proves
// both (a) the checks passed and (b) the build was skipped.
func (r *Tests) CiCheckRunsEnabledChecksAndSkipsBuild(ctx context.Context, goImageTag string) error {
	if r.ciCheckRunsEnabledChecksAndSkipsBuild != nil {
		return nil
	}
	q := r.query.Select("ciCheckRunsEnabledChecksAndSkipsBuild")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// CiRunHelloAllStages runs Ci with every stage enabled against the hello
// fixture and asserts a non-empty binary is produced.
func (r *Tests) CiRunHelloAllStages(ctx context.Context, goImageTag string) error {
	if r.ciRunHelloAllStages != nil {
		return nil
	}
	q := r.query.Select("ciRunHelloAllStages")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// CiRunHelloDefaultsProduceModuleNameBinary asserts that Ci.Run with no
// builders configured still produces a binary named after the go.mod
// module path (example.com/hello → "hello").
func (r *Tests) CiRunHelloDefaultsProduceModuleNameBinary(ctx context.Context, goImageTag string) error {
	if r.ciRunHelloDefaultsProduceModuleNameBinary != nil {
		return nil
	}
	q := r.query.Select("ciRunHelloDefaultsProduceModuleNameBinary")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// CiRunVetBadAggregates runs Ci against the vet-bad fixture with both Vet
// and Lint enabled and asserts that stage-1 aggregated BOTH job failures
// rather than short-circuiting on the first. parallel.New concatenates each
// job's raw error (job names appear in trace spans, not the Go-level
// string), so each underlying `withExec` failure surfaces as a separate
// "exit code: 1" line. Counting those occurrences confirms both vet and
// lint ran and both errors were propagated through Run.
func (r *Tests) CiRunVetBadAggregates(ctx context.Context, goImageTag string) error {
	if r.ciRunVetBadAggregates != nil {
		return nil
	}
	q := r.query.Select("ciRunVetBadAggregates")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// CiWithBuildCustomBinaryName configures a custom binary name via WithBuild
// and asserts the produced File carries that name.
func (r *Tests) CiWithBuildCustomBinaryName(ctx context.Context, goImageTag string) error {
	if r.ciWithBuildCustomBinaryName != nil {
		return nil
	}
	q := r.query.Select("ciWithBuildCustomBinaryName")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// CiWithFmtPasses runs Ci with the Fmt stage enabled against the
// gofmt-clean hello fixture and asserts a non-empty binary is produced.
func (r *Tests) CiWithFmtPasses(ctx context.Context, goImageTag string) error {
	if r.ciWithFmtPasses != nil {
		return nil
	}
	q := r.query.Select("ciWithFmtPasses")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// CiWithLintAcceptsV2Config runs the lint stage with a golangci-lint **v2**
// configuration and asserts it passes.
//
// This is the assertion that the default pin is a v2 release. A v1 binary
// does not ignore a v2 config file, it refuses it before running any
// linter — "you are using a configuration file for golangci-lint v2 with
// golangci-lint v1" — so this test fails outright the moment the pin slips
// back across the major boundary, which is the failure adopters hit.
func (r *Tests) CiWithLintAcceptsV2Config(ctx context.Context, goImageTag string) error {
	if r.ciWithLintAcceptsV2Config != nil {
		return nil
	}
	q := r.query.Select("ciWithLintAcceptsV2Config")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// CiWithLintBuildsUnderOlderGoToolchain pins the Go toolchain below what
// golangci-lint v2's own go.mod requires and asserts the lint stage still
// runs.
//
// golangci-lint tracks the newest Go release, and the official golang
// images set GOTOOLCHAIN=local, so building the linter inside a project's
// pinned toolchain fails for any project a release or two behind — exactly
// the repository most likely to be adopting the standard pipeline. The
// stage therefore builds the linter with an unpinned toolchain and only
// *runs* it under the project's. Pinning 1.23 here rather than honouring
// goImageTag is the point of the test; a caller-supplied override would
// defeat it.
func (r *Tests) CiWithLintBuildsUnderOlderGoToolchain(ctx context.Context) error {
	if r.ciWithLintBuildsUnderOlderGoToolchain != nil {
		return nil
	}
	q := r.query.Select("ciWithLintBuildsUnderOlderGoToolchain")

	return q.Execute(ctx)
}

// CiWithLintPasses runs Ci with the Lint stage enabled against the
// clean hello fixture and asserts a non-empty binary is produced.
// Uses the pinned default golangci-lint version.
func (r *Tests) CiWithLintPasses(ctx context.Context, goImageTag string) error {
	if r.ciWithLintPasses != nil {
		return nil
	}
	q := r.query.Select("ciWithLintPasses")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// CiWithLintRejectsUnreadableVersion asserts a version the module cannot
// read a major out of is refused with a message naming it, rather than
// being guessed at and surfacing later as an unresolvable package.
func (r *Tests) CiWithLintRejectsUnreadableVersion(ctx context.Context, goImageTag string) error {
	if r.ciWithLintRejectsUnreadableVersion != nil {
		return nil
	}
	q := r.query.Select("ciWithLintRejectsUnreadableVersion")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// CiWithLintRejectsV1Config is the other half of CiWithLintAcceptsV2Config:
// a v1-dialect config must now fail. Without it, "accepts a v2 config" is
// also satisfied by a binary that accepts everything, and the dialect the
// stage actually requires would stay unpinned.
//
// The assertion is on *how* it fails, not merely that it does. The
// module boundary drops golangci-lint's stderr from the Go error — what
// survives is the exec's exit code — and golangci-lint distinguishes the
// two outcomes there: 3 is "could not load the config", 1 is "the config
// loaded and linters reported issues". Accepting any failure would let a
// v2 binary that merely dislikes the fixture pass for a v1 binary that
// happily loaded a v1 file.
func (r *Tests) CiWithLintRejectsV1Config(ctx context.Context, goImageTag string) error {
	if r.ciWithLintRejectsV1Config != nil {
		return nil
	}
	q := r.query.Select("ciWithLintRejectsV1Config")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// CiWithLintRollsBackToV1 pins a golangci-lint v1 release together with a
// v1-dialect config and asserts the stage passes.
//
// Rolling back is not merely a different `@version`: Go's semantic import
// versioning put v2 on a `/v2` module path, so the package installed has
// to follow the pinned major. This is the test that the derivation works
// on the other side of that boundary — without it, everything below v2 is
// a path nothing exercises.
func (r *Tests) CiWithLintRollsBackToV1(ctx context.Context, goImageTag string) error {
	if r.ciWithLintRollsBackToV1 != nil {
		return nil
	}
	q := r.query.Select("ciWithLintRollsBackToV1")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// CiWithTestPasses runs Ci with the Test stage enabled (no race) against
// hello and asserts a non-empty binary is produced.
func (r *Tests) CiWithTestPasses(ctx context.Context, goImageTag string) error {
	if r.ciWithTestPasses != nil {
		return nil
	}
	q := r.query.Select("ciWithTestPasses")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// CiWithTestRacePasses runs Ci with the Test stage enabled with -race and
// asserts a non-empty binary is produced.
func (r *Tests) CiWithTestRacePasses(ctx context.Context, goImageTag string) error {
	if r.ciWithTestRacePasses != nil {
		return nil
	}
	q := r.query.Select("ciWithTestRacePasses")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// CiWithVetPasses runs Ci with the Vet stage enabled against the vet-clean
// hello fixture and asserts a non-empty binary is produced.
func (r *Tests) CiWithVetPasses(ctx context.Context, goImageTag string) error {
	if r.ciWithVetPasses != nil {
		return nil
	}
	q := r.query.Select("ciWithVetPasses")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// ContainerHasGoToolchain proves the base container is reachable, the source
// is mounted at /src, and the golang image's `go` binary runs. This is the
// canary for every other test — if it fails, the rest can't possibly pass.
func (r *Tests) ContainerHasGoToolchain(ctx context.Context, goImageTag string) error {
	if r.containerHasGoToolchain != nil {
		return nil
	}
	q := r.query.Select("containerHasGoToolchain")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// ContainerInfersVersionFromGoMod asserts that constructing the module with
// New("") and a fixture whose go.mod declares `go 1.23` actually pulls the
// matching golang:1.23 image — i.e. resolveVersion + go.mod parsing wire
// through to the toolchain selection. Catches regressions in go.mod parsing
// or in the fallback path silently using `latest`.
//
// goImageTag is accepted for signature uniformity (All forwards it to
// every test) but deliberately ignored: this test exercises the
// empty-version inference path, so a caller-supplied override would
// defeat what's being verified.
func (r *Tests) ContainerInfersVersionFromGoMod(ctx context.Context, goImageTag string) error {
	if r.containerInfersVersionFromGoMod != nil {
		return nil
	}
	q := r.query.Select("containerInfersVersionFromGoMod")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// CycloneDxDocumentIsCompliant asserts the CycloneDX document is at the
// pinned spec version and carries the same required elements.
func (r *Tests) CycloneDxDocumentIsCompliant(ctx context.Context, goImageTag string) error {
	if r.cycloneDxDocumentIsCompliant != nil {
		return nil
	}
	q := r.query.Select("cycloneDxDocumentIsCompliant")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// EnvContainsGoroot calls dag.Go(dagger.GoOpts{Version: goImageTag}).Env and asserts the output mentions GOROOT
// — the canonical signal that `go env` ran inside the prepared container.
func (r *Tests) EnvContainsGoroot(ctx context.Context, goImageTag string) error {
	if r.envContainsGoroot != nil {
		return nil
	}
	q := r.query.Select("envContainsGoroot")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// FmtHelloIsClean runs Fmt against the gofmt-clean hello fixture and asserts
// the diff is empty.
func (r *Tests) FmtHelloIsClean(ctx context.Context, goImageTag string) error {
	if r.fmtHelloIsClean != nil {
		return nil
	}
	q := r.query.Select("fmtHelloIsClean")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// GenerateHelloProducesFile runs go generate against the hello fixture and
// asserts the //go:generate directive produced out.txt with the expected
// content.
func (r *Tests) GenerateHelloProducesFile(ctx context.Context, goImageTag string) error {
	if r.generateHelloProducesFile != nil {
		return nil
	}
	q := r.query.Select("generateHelloProducesFile")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// A unique identifier for this Tests.
func (r *Tests) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *Tests) XXX_GraphQLType() string {
	return "Tests"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *Tests) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *Tests) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *Tests) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *Tests) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = Tests{query: selectNode(dag.query, id, "Tests")}
	return nil
}

// InstallSmallToolReturnsBinary installs a small public tool (stringer) and
// asserts the returned binary is non-empty. The version is pinned so CI
// doesn't drift with upstream releases. Requires network egress for the
// initial fetch; subsequent runs hit the go-mod-cache.
func (r *Tests) InstallSmallToolReturnsBinary(ctx context.Context, goImageTag string) error {
	if r.installSmallToolReturnsBinary != nil {
		return nil
	}
	q := r.query.Select("installSmallToolReturnsBinary")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// ModDownloadHelloPasses runs ModDownload against the hello fixture and
// asserts no error.
func (r *Tests) ModDownloadHelloPasses(ctx context.Context, goImageTag string) error {
	if r.modDownloadHelloPasses != nil {
		return nil
	}
	q := r.query.Select("modDownloadHelloPasses")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// ModTidyHelloIsIdempotent runs `go mod tidy` against the stdlib-only hello
// fixture and asserts the resulting go.mod is unchanged.
func (r *Tests) ModTidyHelloIsIdempotent(ctx context.Context, goImageTag string) error {
	if r.modTidyHelloIsIdempotent != nil {
		return nil
	}
	q := r.query.Select("modTidyHelloIsIdempotent")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// ModVerifyHelloPasses runs ModVerify against the hello fixture and asserts
// no error.
func (r *Tests) ModVerifyHelloPasses(ctx context.Context, goImageTag string) error {
	if r.modVerifyHelloPasses != nil {
		return nil
	}
	q := r.query.Select("modVerifyHelloPasses")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// RunHelloPrintsHello runs the hello fixture's main and asserts stdout is
// "hello\n".
func (r *Tests) RunHelloPrintsHello(ctx context.Context, goImageTag string) error {
	if r.runHelloPrintsHello != nil {
		return nil
	}
	q := r.query.Select("runHelloPrintsHello")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// SbomDescribesTheBinaryNotTheSourceTree asserts the component list is
// what was linked in rather than what go.mod happens to require.
//
// The distinction is the reason the graph is read out of the compiled
// artifact: a source tree's requirement list includes modules no code
// imports, and a document built from it over-reports. The fixture's
// go.mod requires exactly one module and its binary links exactly that
// one, so the check is that the document holds the linked module and no
// tooling-only entries beside it.
func (r *Tests) SbomDescribesTheBinaryNotTheSourceTree(ctx context.Context, goImageTag string) error {
	if r.sbomDescribesTheBinaryNotTheSourceTree != nil {
		return nil
	}
	q := r.query.Select("sbomDescribesTheBinaryNotTheSourceTree")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// SbomFormatsAgreeOnComponents asserts the two documents describe the
// same component set.
//
// This is the property that a single resolution buys and that two
// independent tools cannot offer at any price: two documents about one
// binary that disagree about a component or a licence are an audit
// finding, and nothing downstream can adjudicate which is right. The
// licences are compared as well as the coordinates, because a component
// set that matches while the licences differ is the same failure wearing
// a different hat.
func (r *Tests) SbomFormatsAgreeOnComponents(ctx context.Context, goImageTag string) error {
	if r.sbomFormatsAgreeOnComponents != nil {
		return nil
	}
	q := r.query.Select("sbomFormatsAgreeOnComponents")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// SbomResolvesDependencyLicences asserts a licence is actually resolved
// and that the declared/concluded distinction is populated rather than
// left at NOASSERTION for everything.
//
// A Go binary carries no licence text, so this is the half of the
// document that can only come from the source: if licence resolution
// silently did nothing, every field below would still be present and
// every one would say NOASSERTION. That is why the assertion is on a
// specific dependency with a known licence rather than on the shape.
func (r *Tests) SbomResolvesDependencyLicences(ctx context.Context, goImageTag string) error {
	if r.sbomResolvesDependencyLicences != nil {
		return nil
	}
	q := r.query.Select("sbomResolvesDependencyLicences")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// SpdxDocumentIsCompliant asserts the SPDX document carries the elements
// a consumer requires, not merely that it parses.
//
// The library guarantees the syntax; nothing guarantees the fields are
// populated, and an SBOM missing a supplier or a unique identifier fails
// the minimum-elements check every regulated consumer runs even though
// it is perfectly well-formed JSON. So this walks the NTIA minimum
// elements one at a time: supplier, component name, version, unique
// identifier, dependency relationship, author of the SBOM data, and
// timestamp.
func (r *Tests) SpdxDocumentIsCompliant(ctx context.Context, goImageTag string) error {
	if r.spdxDocumentIsCompliant != nil {
		return nil
	}
	q := r.query.Select("spdxDocumentIsCompliant")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// TestHelloPasses runs `go test ./...` against the hello fixture and asserts
// the canonical "PASS" marker appears in stdout.
func (r *Tests) TestHelloPasses(ctx context.Context, goImageTag string) error {
	if r.testHelloPasses != nil {
		return nil
	}
	q := r.query.Select("testHelloPasses")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// TestMultipkgPkgArgVariants runs `go test` against the multipkg fixture
// twice — once with pkg=./... (covers the whole module) and once with
// pkg=./pkg/foo (sub-package only) — to confirm the pkg arg shape.
func (r *Tests) TestMultipkgPkgArgVariants(ctx context.Context, goImageTag string) error {
	if r.testMultipkgPkgArgVariants != nil {
		return nil
	}
	q := r.query.Select("testMultipkgPkgArgVariants")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// ToolVersionContainsGoVersion calls dag.Go(dagger.GoOpts{Version: goImageTag}).ToolVersion and asserts the
// output starts with the canonical "go version" prefix.
func (r *Tests) ToolVersionContainsGoVersion(ctx context.Context, goImageTag string) error {
	if r.toolVersionContainsGoVersion != nil {
		return nil
	}
	q := r.query.Select("toolVersionContainsGoVersion")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// VetHelloPasses runs Vet against the hello fixture, which is vet-clean,
// so the call must succeed.
func (r *Tests) VetHelloPasses(ctx context.Context, goImageTag string) error {
	if r.vetHelloPasses != nil {
		return nil
	}
	q := r.query.Select("vetHelloPasses")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}

// WorkInitSucceeds runs `go work init .` against the hello fixture and
// asserts no error. `go work init` is a side-effecting subcommand that
// returns empty stdout on success — the assertion is the absence of error.
func (r *Tests) WorkInitSucceeds(ctx context.Context, goImageTag string) error {
	if r.workInitSucceeds != nil {
		return nil
	}
	q := r.query.Select("workInitSucceeds")
	q = q.Arg("goImageTag", goImageTag)

	return q.Execute(ctx)
}
