// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

func (r *Query) Tests() *Tests { // tests (../../../../../:0:0)
	q := r.query.Select("tests")

	return &Tests{
		query: q,
	}
}

type Tests struct { // tests (../../../../../:0:0)
	query *querybuilder.Selection

	admin                                             *Void
	adminEndpointServesReady                          *Void
	all                                               *Void
	configFileOverridesRendered                       *Void
	customHttpFilterBodyIsSpliced                     *Void
	customListenerBodyIsSpliced                       *Void
	defaultClusterTypeIsStrictDns                     *Void
	dynamicResourcesConflictsWithStatic               *Void
	dynamicResourcesRendersDynamicBootstrap           *Void
	dynamicResourcesRequiresLdsAndCds                 *Void
	id                                                *ID
	l4TcpRoundTrip                                    *Void
	l4TcpTlsRoundTrip                                 *Void
	l4TcpXdsRoundTrip                                 *Void
	l7HttpRoundTrip                                   *Void
	l7HttpXdsRoundTrip                                *Void
	l7HttpsMtlsAcceptsAuthorizedClient                *Void
	l7HttpsMtlsRejectsAnonymousClient                 *Void
	l7HttpsRoundTrip                                  *Void
	mtlsServerSecurityRequiresClientCert              *Void
	mtlsUpstreamSecurityIncludesClientLeaf            *Void
	plaintextServerSecurityRendersNoTransportSocket   *Void
	plaintextUpstreamSecurityRendersNoTransportSocket *Void
	rejectsDuplicateListenerName                      *Void
	rejectsInvalidComponentName                       *Void
	rejectsUnknownClusterReference                    *Void
	rejectsUnknownClusterType                         *Void
	roundTrips                                        *Void
	serviceWithoutConfigFails                         *Void
	tlsServerSecurityRendersDownstreamTlsContext      *Void
	tlsUpstreamSecurityRendersUpstreamTlsContext      *Void
	upstreamMtlsRoundTrip                             *Void
	upstreamTlsRoundTrip                              *Void
	validation                                        *Void
	xdsResourcesMatchStaticResources                  *Void
	xdsResourcesRejectsInvalidResourceSet             *Void
	xdsResourcesRejectsSecureComponents               *Void
}

func (r *Tests) WithGraphQLQuery(q *querybuilder.Selection) *Tests {
	return &Tests{
		query: q,
	}
}

// TestsAdminOpts contains options for Tests.Admin
type TestsAdminOpts struct {

	// Default: "v1.32.1"
	EnvoyTag string

	Parallel int
}

// Admin runs the boot-and-probe tests — Envoy comes up, we hit the
// admin endpoint or assert misconfigurations fail at boot, no upstream
// traffic.
func (r *Tests) Admin(ctx context.Context, opts ...TestsAdminOpts) error {
	if r.admin != nil {
		return nil
	}
	q := r.query.Select("admin")
	for i := len(opts) - 1; i >= 0; i-- {
		// `envoyTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].EnvoyTag) {
			q = q.Arg("envoyTag", opts[i].EnvoyTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// TestsAdminEndpointServesReadyOpts contains options for Tests.AdminEndpointServesReady
type TestsAdminEndpointServesReadyOpts struct {

	// Default: "v1.32.1"
	EnvoyTag string
}

// AdminEndpointServesReady asserts the admin /ready endpoint
// returns HTTP 200 from a fresh probe container service-bound to a
// proxy whose minimal valid config is one HTTP listener wired to one
// cluster.
func (r *Tests) AdminEndpointServesReady(ctx context.Context, opts ...TestsAdminEndpointServesReadyOpts) error {
	if r.adminEndpointServesReady != nil {
		return nil
	}
	q := r.query.Select("adminEndpointServesReady")
	for i := len(opts) - 1; i >= 0; i-- {
		// `envoyTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].EnvoyTag) {
			q = q.Arg("envoyTag", opts[i].EnvoyTag)
		}
	}

	return q.Execute(ctx)
}

// TestsAllOpts contains options for Tests.All
type TestsAllOpts struct {

	// Default: "v1.32.1"
	EnvoyTag string

	Parallel int
}

// All runs every envoy test as a convenience for local `dagger call all`
// invocations. CI does NOT call All: each of the three sub-aggregators
// below (Validation, Admin, RoundTrips) carries its own `directive, so GH Actions schedules each onto its own runner in
// parallel — running All on top would double-bill the same work.
func (r *Tests) All(ctx context.Context, opts ...TestsAllOpts) error {
	if r.all != nil {
		return nil
	}
	q := r.query.Select("all")
	for i := len(opts) - 1; i >= 0; i-- {
		// `envoyTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].EnvoyTag) {
			q = q.Arg("envoyTag", opts[i].EnvoyTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// ConfigFileOverridesRendered asserts that WithConfigFile fully
// replaces the rendered bootstrap; listeners and clusters added via
// WithListener/WithCluster are ignored when an override is set.
func (r *Tests) ConfigFileOverridesRendered(ctx context.Context) error {
	if r.configFileOverridesRendered != nil {
		return nil
	}
	q := r.query.Select("configFileOverridesRendered")

	return q.Execute(ctx)
}

// CustomHttpFilterBodyIsSpliced asserts that a CustomHttpFilter's
// caller-supplied YAML body lands as the filter's typed_config in
// the rendered HCM http_filters chain.
func (r *Tests) CustomHTTPFilterBodyIsSpliced(ctx context.Context) error {
	if r.customHttpFilterBodyIsSpliced != nil {
		return nil
	}
	q := r.query.Select("customHttpFilterBodyIsSpliced")

	return q.Execute(ctx)
}

// CustomListenerBodyIsSpliced asserts that a CustomListener's
// caller-supplied YAML body round-trips verbatim under
// static_resources.listeners with the builder-supplied name keyed
// in.
func (r *Tests) CustomListenerBodyIsSpliced(ctx context.Context) error {
	if r.customListenerBodyIsSpliced != nil {
		return nil
	}
	q := r.query.Select("customListenerBodyIsSpliced")

	return q.Execute(ctx)
}

// DefaultClusterTypeIsStrictDns asserts a cluster built with default
// clusterType renders as `type: STRICT_DNS` in the rendered
// bootstrap YAML.
func (r *Tests) DefaultClusterTypeIsStrictDNS(ctx context.Context) error {
	if r.defaultClusterTypeIsStrictDns != nil {
		return nil
	}
	q := r.query.Select("defaultClusterTypeIsStrictDns")

	return q.Execute(ctx)
}

// DynamicResourcesConflictsWithStatic asserts that mixing
// WithDynamicResources with any of WithListener / WithCluster /
// WithConfigFile on the same Proxy makes ConfigFile() return a
// non-nil error — the two configuration modes are exclusive.
func (r *Tests) DynamicResourcesConflictsWithStatic(ctx context.Context) error {
	if r.dynamicResourcesConflictsWithStatic != nil {
		return nil
	}
	q := r.query.Select("dynamicResourcesConflictsWithStatic")

	return q.Execute(ctx)
}

// DynamicResourcesRendersDynamicBootstrap asserts WithDynamicResources
// renders a bootstrap whose dynamic_resources block points lds/cds at
// the mounted xds directory and which carries no static_resources.
func (r *Tests) DynamicResourcesRendersDynamicBootstrap(ctx context.Context) error {
	if r.dynamicResourcesRendersDynamicBootstrap != nil {
		return nil
	}
	q := r.query.Select("dynamicResourcesRendersDynamicBootstrap")

	return q.Execute(ctx)
}

// DynamicResourcesRequiresLdsAndCds asserts Service() rejects a
// resource directory missing either of the two files the bootstrap's
// dynamic_resources block points at, rather than booting an Envoy
// that silently discovers nothing.
func (r *Tests) DynamicResourcesRequiresLdsAndCds(ctx context.Context) error {
	if r.dynamicResourcesRequiresLdsAndCds != nil {
		return nil
	}
	q := r.query.Select("dynamicResourcesRequiresLdsAndCds")

	return q.Execute(ctx)
}

// A unique identifier for this Tests.
func (r *Tests) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *Tests) XXX_GraphQLType() string {
	return "Tests"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *Tests) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *Tests) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *Tests) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *Tests) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = Tests{query: selectNode(dag.query, id, "Tests")}
	return nil
}

// TestsL4TcpRoundTripOpts contains options for Tests.L4TcpRoundTrip
type TestsL4TcpRoundTripOpts struct {

	// Default: "v1.32.1"
	EnvoyTag string
}

// L4TcpRoundTrip stands up an alpine `nc` echo upstream behind an
// Envoy TcpListener and asserts that bytes sent through Envoy come
// back on the same TCP connection.
func (r *Tests) L4TcpRoundTrip(ctx context.Context, opts ...TestsL4TcpRoundTripOpts) error {
	if r.l4TcpRoundTrip != nil {
		return nil
	}
	q := r.query.Select("l4TcpRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `envoyTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].EnvoyTag) {
			q = q.Arg("envoyTag", opts[i].EnvoyTag)
		}
	}

	return q.Execute(ctx)
}

// TestsL4TcpTLSRoundTripOpts contains options for Tests.L4TcpTLSRoundTrip
type TestsL4TcpTLSRoundTripOpts struct {

	// Default: "v1.32.1"
	EnvoyTag string
}

// L4TcpTlsRoundTrip stands up an nc echo upstream behind an Envoy
// TLS-terminated TcpListener and asserts an openssl s_client probe
// can complete a TLS round-trip with marker bytes echoed back.
func (r *Tests) L4TcpTLSRoundTrip(ctx context.Context, opts ...TestsL4TcpTLSRoundTripOpts) error {
	if r.l4TcpTlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("l4TcpTlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `envoyTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].EnvoyTag) {
			q = q.Arg("envoyTag", opts[i].EnvoyTag)
		}
	}

	return q.Execute(ctx)
}

// TestsL4TcpXdsRoundTripOpts contains options for Tests.L4TcpXdsRoundTrip
type TestsL4TcpXdsRoundTripOpts struct {

	// Default: "v1.32.1"
	EnvoyTag string
}

// L4TcpXdsRoundTrip stands up an alpine `nc` echo upstream behind an
// Envoy TcpListener delivered over file-based xDS and asserts bytes
// sent through Envoy come back on the same TCP connection. Also
// asserts ListenerEndpoint resolves the listener's port out of the
// mounted lds.yaml, since no Listener is registered on the Proxy in
// this mode.
func (r *Tests) L4TcpXdsRoundTrip(ctx context.Context, opts ...TestsL4TcpXdsRoundTripOpts) error {
	if r.l4TcpXdsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("l4TcpXdsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `envoyTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].EnvoyTag) {
			q = q.Arg("envoyTag", opts[i].EnvoyTag)
		}
	}

	return q.Execute(ctx)
}

// TestsL7HttpRoundTripOpts contains options for Tests.L7HttpRoundTrip
type TestsL7HttpRoundTripOpts struct {

	// Default: "v1.32.1"
	EnvoyTag string
}

// L7HttpRoundTrip stands up an HTTP upstream behind an Envoy
// HttpListener and asserts a request through Envoy returns a fresh
// random marker served by the upstream.
func (r *Tests) L7HttpRoundTrip(ctx context.Context, opts ...TestsL7HttpRoundTripOpts) error {
	if r.l7HttpRoundTrip != nil {
		return nil
	}
	q := r.query.Select("l7HttpRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `envoyTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].EnvoyTag) {
			q = q.Arg("envoyTag", opts[i].EnvoyTag)
		}
	}

	return q.Execute(ctx)
}

// TestsL7HttpXdsRoundTripOpts contains options for Tests.L7HttpXdsRoundTrip
type TestsL7HttpXdsRoundTripOpts struct {

	// Default: "v1.32.1"
	EnvoyTag string
}

// L7HttpXdsRoundTrip stands up an HTTP upstream behind an Envoy
// proxy whose listeners and clusters arrive over file-based xDS
// rather than static_resources, and asserts a request through Envoy
// returns a fresh random marker served by the upstream.
func (r *Tests) L7HttpXdsRoundTrip(ctx context.Context, opts ...TestsL7HttpXdsRoundTripOpts) error {
	if r.l7HttpXdsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("l7HttpXdsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `envoyTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].EnvoyTag) {
			q = q.Arg("envoyTag", opts[i].EnvoyTag)
		}
	}

	return q.Execute(ctx)
}

// TestsL7HttpsMtlsAcceptsAuthorizedClientOpts contains options for Tests.L7HttpsMtlsAcceptsAuthorizedClient
type TestsL7HttpsMtlsAcceptsAuthorizedClientOpts struct {

	// Default: "v1.32.1"
	EnvoyTag string
}

// L7HttpsMtlsAcceptsAuthorizedClient asserts that an mTLS-terminated
// listener accepts curl clients that present a leaf signed by the
// configured clientTrustStore CA.
func (r *Tests) L7HttpsMtlsAcceptsAuthorizedClient(ctx context.Context, opts ...TestsL7HttpsMtlsAcceptsAuthorizedClientOpts) error {
	if r.l7HttpsMtlsAcceptsAuthorizedClient != nil {
		return nil
	}
	q := r.query.Select("l7HttpsMtlsAcceptsAuthorizedClient")
	for i := len(opts) - 1; i >= 0; i-- {
		// `envoyTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].EnvoyTag) {
			q = q.Arg("envoyTag", opts[i].EnvoyTag)
		}
	}

	return q.Execute(ctx)
}

// TestsL7HttpsMtlsRejectsAnonymousClientOpts contains options for Tests.L7HttpsMtlsRejectsAnonymousClient
type TestsL7HttpsMtlsRejectsAnonymousClientOpts struct {

	// Default: "v1.32.1"
	EnvoyTag string
}

// L7HttpsMtlsRejectsAnonymousClient asserts that an mTLS-terminated
// HttpListener refuses clients that don't present a cert signed by
// the configured clientTrustStore CA — curl exits non-zero.
func (r *Tests) L7HttpsMtlsRejectsAnonymousClient(ctx context.Context, opts ...TestsL7HttpsMtlsRejectsAnonymousClientOpts) error {
	if r.l7HttpsMtlsRejectsAnonymousClient != nil {
		return nil
	}
	q := r.query.Select("l7HttpsMtlsRejectsAnonymousClient")
	for i := len(opts) - 1; i >= 0; i-- {
		// `envoyTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].EnvoyTag) {
			q = q.Arg("envoyTag", opts[i].EnvoyTag)
		}
	}

	return q.Execute(ctx)
}

// TestsL7HttpsRoundTripOpts contains options for Tests.L7HttpsRoundTrip
type TestsL7HttpsRoundTripOpts struct {

	// Default: "v1.32.1"
	EnvoyTag string
}

// L7HttpsRoundTrip stands up a plaintext HTTP upstream behind an
// Envoy TLS-terminated HttpListener and asserts a client trusting
// the CA can complete an HTTPS round-trip.
func (r *Tests) L7HttpsRoundTrip(ctx context.Context, opts ...TestsL7HttpsRoundTripOpts) error {
	if r.l7HttpsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("l7HttpsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `envoyTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].EnvoyTag) {
			q = q.Arg("envoyTag", opts[i].EnvoyTag)
		}
	}

	return q.Execute(ctx)
}

// MtlsServerSecurityRequiresClientCert asserts that mTLS adds
// require_client_certificate + a validation_context.trusted_ca
// pointing at the listener's trust PEM mount path.
func (r *Tests) MtlsServerSecurityRequiresClientCert(ctx context.Context) error {
	if r.mtlsServerSecurityRequiresClientCert != nil {
		return nil
	}
	q := r.query.Select("mtlsServerSecurityRequiresClientCert")

	return q.Execute(ctx)
}

// MtlsUpstreamSecurityIncludesClientLeaf asserts that mTLS upstream
// renders both validation_context AND tls_certificates referencing
// the cluster keystore PKCS#12 path + env-var password.
func (r *Tests) MtlsUpstreamSecurityIncludesClientLeaf(ctx context.Context) error {
	if r.mtlsUpstreamSecurityIncludesClientLeaf != nil {
		return nil
	}
	q := r.query.Select("mtlsUpstreamSecurityIncludesClientLeaf")

	return q.Execute(ctx)
}

// PlaintextServerSecurityRendersNoTransportSocket asserts that a
// listener built with PlaintextServerSecurity is byte-identical to
// one built with nil security (no transport_socket in either).
func (r *Tests) PlaintextServerSecurityRendersNoTransportSocket(ctx context.Context) error {
	if r.plaintextServerSecurityRendersNoTransportSocket != nil {
		return nil
	}
	q := r.query.Select("plaintextServerSecurityRendersNoTransportSocket")

	return q.Execute(ctx)
}

// PlaintextUpstreamSecurityRendersNoTransportSocket asserts that
// PlaintextUpstreamSecurity renders the same cluster as nil upstream.
func (r *Tests) PlaintextUpstreamSecurityRendersNoTransportSocket(ctx context.Context) error {
	if r.plaintextUpstreamSecurityRendersNoTransportSocket != nil {
		return nil
	}
	q := r.query.Select("plaintextUpstreamSecurityRendersNoTransportSocket")

	return q.Execute(ctx)
}

// RejectsDuplicateListenerName asserts that wiring two listeners
// sharing the same name into a Proxy causes ConfigFile() to return a
// non-nil error.
func (r *Tests) RejectsDuplicateListenerName(ctx context.Context) error {
	if r.rejectsDuplicateListenerName != nil {
		return nil
	}
	q := r.query.Select("rejectsDuplicateListenerName")

	return q.Execute(ctx)
}

// RejectsInvalidComponentName asserts that the typed component
// factories reject names that don't match [A-Za-z0-9_-]+ with a
// non-nil error. AC calls out Envoy.Cluster and Envoy.VirtualHost
// explicitly; RoutePrefix's cluster arg shares the same validator.
func (r *Tests) RejectsInvalidComponentName(ctx context.Context) error {
	if r.rejectsInvalidComponentName != nil {
		return nil
	}
	q := r.query.Select("rejectsInvalidComponentName")

	return q.Execute(ctx)
}

// RejectsUnknownClusterReference asserts a listener whose filter
// chain references a cluster not registered via WithCluster causes
// ConfigFile() to return a non-nil error.
func (r *Tests) RejectsUnknownClusterReference(ctx context.Context) error {
	if r.rejectsUnknownClusterReference != nil {
		return nil
	}
	q := r.query.Select("rejectsUnknownClusterReference")

	return q.Execute(ctx)
}

// RejectsUnknownClusterType asserts Envoy.Cluster rejects clusterType
// values outside {STATIC, STRICT_DNS, LOGICAL_DNS} with a non-nil
// error.
func (r *Tests) RejectsUnknownClusterType(ctx context.Context) error {
	if r.rejectsUnknownClusterType != nil {
		return nil
	}
	q := r.query.Select("rejectsUnknownClusterType")

	return q.Execute(ctx)
}

// TestsRoundTripsOpts contains options for Tests.RoundTrips
type TestsRoundTripsOpts struct {

	// Default: "v1.32.1"
	EnvoyTag string

	Parallel int
}

// RoundTrips runs every L7/L4 + TLS/mTLS round-trip — each spins an
// Envoy proxy plus an upstream service plus a curl client. The
// heaviest of the three groups.
func (r *Tests) RoundTrips(ctx context.Context, opts ...TestsRoundTripsOpts) error {
	if r.roundTrips != nil {
		return nil
	}
	q := r.query.Select("roundTrips")
	for i := len(opts) - 1; i >= 0; i-- {
		// `envoyTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].EnvoyTag) {
			q = q.Arg("envoyTag", opts[i].EnvoyTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// TestsServiceWithoutConfigFailsOpts contains options for Tests.ServiceWithoutConfigFails
type TestsServiceWithoutConfigFailsOpts struct {

	// Default: "v1.32.1"
	EnvoyTag string
}

// ServiceWithoutConfigFails asserts that Service() on a Proxy with
// no listeners, clusters, or override produces a container whose
// admin port never opens (the envoy binary refuses to start without
// -c).
func (r *Tests) ServiceWithoutConfigFails(ctx context.Context, opts ...TestsServiceWithoutConfigFailsOpts) error {
	if r.serviceWithoutConfigFails != nil {
		return nil
	}
	q := r.query.Select("serviceWithoutConfigFails")
	for i := len(opts) - 1; i >= 0; i-- {
		// `envoyTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].EnvoyTag) {
			q = q.Arg("envoyTag", opts[i].EnvoyTag)
		}
	}

	return q.Execute(ctx)
}

// TlsServerSecurityRendersDownstreamTlsContext asserts that an
// HttpListener built with TlsServerSecurity renders a downstream TLS
// transport_socket on its filter chain referencing the listener's
// PKCS#12 mount path and a password env var, with no
// require_client_certificate.
func (r *Tests) TLSServerSecurityRendersDownstreamTLSContext(ctx context.Context) error {
	if r.tlsServerSecurityRendersDownstreamTlsContext != nil {
		return nil
	}
	q := r.query.Select("tlsServerSecurityRendersDownstreamTlsContext")

	return q.Execute(ctx)
}

// TlsUpstreamSecurityRendersUpstreamTlsContext asserts that a
// Cluster built with TlsUpstreamSecurity renders an
// UpstreamTlsContext transport_socket on the cluster with a
// validation_context pointing at the upstream trust PEM path.
func (r *Tests) TLSUpstreamSecurityRendersUpstreamTLSContext(ctx context.Context) error {
	if r.tlsUpstreamSecurityRendersUpstreamTlsContext != nil {
		return nil
	}
	q := r.query.Select("tlsUpstreamSecurityRendersUpstreamTlsContext")

	return q.Execute(ctx)
}

// TestsUpstreamMtlsRoundTripOpts contains options for Tests.UpstreamMtlsRoundTrip
type TestsUpstreamMtlsRoundTripOpts struct {

	// Default: "v1.32.1"
	EnvoyTag string
}

// UpstreamMtlsRoundTrip asserts Envoy presents a client leaf to a
// mTLS upstream that verifies it against its own clientTrust CA.
func (r *Tests) UpstreamMtlsRoundTrip(ctx context.Context, opts ...TestsUpstreamMtlsRoundTripOpts) error {
	if r.upstreamMtlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("upstreamMtlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `envoyTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].EnvoyTag) {
			q = q.Arg("envoyTag", opts[i].EnvoyTag)
		}
	}

	return q.Execute(ctx)
}

// TestsUpstreamTLSRoundTripOpts contains options for Tests.UpstreamTLSRoundTrip
type TestsUpstreamTLSRoundTripOpts struct {

	// Default: "v1.32.1"
	EnvoyTag string
}

// UpstreamTlsRoundTrip stands up a python HTTPS upstream and asserts
// Envoy connects to it over TLS when the cluster's
// UpstreamSecurity truststore matches the upstream's server cert CA.
func (r *Tests) UpstreamTLSRoundTrip(ctx context.Context, opts ...TestsUpstreamTLSRoundTripOpts) error {
	if r.upstreamTlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("upstreamTlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `envoyTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].EnvoyTag) {
			q = q.Arg("envoyTag", opts[i].EnvoyTag)
		}
	}

	return q.Execute(ctx)
}

// TestsValidationOpts contains options for Tests.Validation
type TestsValidationOpts struct {
	Parallel int
}

// Validation runs the pure-render and config-validation tests — no
// Envoy service is booted, just the proxy config-rendering paths and
// the security renderers. The fastest of the three groups.
func (r *Tests) Validation(ctx context.Context, opts ...TestsValidationOpts) error {
	if r.validation != nil {
		return nil
	}
	q := r.query.Select("validation")
	for i := len(opts) - 1; i >= 0; i-- {
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// XdsResourcesMatchStaticResources asserts that the lds.yaml /
// cds.yaml discovery responses rendered by XdsResources.Directory()
// are structurally equivalent to the static_resources block the same
// components produce in static mode — identical apart from the
// per-resource `@type` discriminator that only the xDS shape needs.
func (r *Tests) XdsResourcesMatchStaticResources(ctx context.Context) error {
	if r.xdsResourcesMatchStaticResources != nil {
		return nil
	}
	q := r.query.Select("xdsResourcesMatchStaticResources")

	return q.Execute(ctx)
}

// XdsResourcesRejectsInvalidResourceSet asserts Directory() surfaces
// the same two errors (*Proxy).ConfigFile() does: two listeners
// sharing a name, and a listener whose filter chain references a
// cluster that isn't in the resource set.
func (r *Tests) XdsResourcesRejectsInvalidResourceSet(ctx context.Context) error {
	if r.xdsResourcesRejectsInvalidResourceSet != nil {
		return nil
	}
	q := r.query.Select("xdsResourcesRejectsInvalidResourceSet")

	return q.Execute(ctx)
}

// XdsResourcesRejectsSecureComponents asserts Directory() refuses
// TLS / mTLS listeners and clusters: their rendered resources point
// at key material under /etc/envoy/secrets that only the static
// Service() path mounts, so a proxy fed them through an opaque
// resource directory would boot and then fail every handshake.
func (r *Tests) XdsResourcesRejectsSecureComponents(ctx context.Context) error {
	if r.xdsResourcesRejectsSecureComponents != nil {
		return nil
	}
	q := r.query.Select("xdsResourcesRejectsSecureComponents")

	return q.Execute(ctx)
}
