// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

func (r *Query) Tests() *Tests { // tests (../../../../../:0:0)
	q := r.query.Select("tests")

	return &Tests{
		query: q,
	}
}

type Tests struct { // tests (../../../../../:0:0)
	query *querybuilder.Selection

	all                                                        *Void
	apacheClusterMtlsRoundTrip                                 *Void
	apacheClusterProduceListTopicsRoundTrip                    *Void
	apacheClusterTlsRoundTrip                                  *Void
	apacheJvm                                                  *Void
	apicurioSchemaRegistryRegisterLookupRoundTrip              *Void
	apicurioSchemaRegistryTlsRegisterLookupRoundTrip           *Void
	autoCreateTopicsDisabled                                   *Void
	avroBytesFieldRoundTrip                                    *Void
	avroConsumeUnframedErrors                                  *Void
	avroFramedProduceConsumeRoundTrip                          *Void
	avroMtlsFramedProduceConsumeRoundTrip                      *Void
	avroSerializeRequiresSchemaId                              *Void
	avroTlsFramedProduceConsumeRoundTrip                       *Void
	bindBrokersExposesBothListeners                            *Void
	clusterClientCanListTopicsOnFreshCluster                   *Void
	confluent                                                  *Void
	confluentClusterMtlsRoundTrip                              *Void
	confluentClusterProduceListTopicsRoundTrip                 *Void
	confluentClusterTlsRoundTrip                               *Void
	confluentSchemaRegistryMtlsRegisterLookupRoundTrip         *Void
	confluentSchemaRegistryTlsRegisterLookupRoundTrip          *Void
	consumerGroupOnSingleBrokerWorks                           *Void
	createAndDeleteTopicRoundTrip                              *Void
	dedicatedControllerAndBrokerProduceConsume                 *Void
	describeConsumerGroupReportsLag                            *Void
	describeTopicReportsPartitionsAndConfigs                   *Void
	fiveControllerQuorumAccepted                               *Void
	id                                                         *ID
	internalListenersAreEncrypted                              *Void
	invalidControllerCountIsRejected                           *Void
	karapaceSchemaRegistryRegisterLookupRoundTrip              *Void
	karapaceSchemaRegistryTlsRegisterLookupRoundTrip           *Void
	listConsumerGroupsReportsCommittedGroup                    *Void
	mtlsRequiresClientCert                                     *Void
	mtlsRoundTrip                                              *Void
	native                                                     *Void
	oneControllerTwoBrokersReplicationFactorTwo                *Void
	plaintextSecurityProfilesAreNonNil                         *Void
	produceConsumeRoundTripBase64                              *Void
	produceConsumeRoundTripHex                                 *Void
	produceConsumeRoundTripRaw                                 *Void
	produceRejectsUnknownEncoding                              *Void
	propertiesFileContainsBootstrapAndSecurityProtocol         *Void
	propertiesFileContainsMtlsSettings                         *Void
	propertiesFileContainsTlsSettings                          *Void
	protobufConsumeMessageIndexMismatchErrors                  *Void
	protobufConsumeUnframedErrors                              *Void
	protobufDeserializeRequiresDescriptorSet                   *Void
	protobufDeserializeRequiresSchemaRegistryAware             *Void
	protobufFramedProduceConsumeRoundTrip                      *Void
	protobufKeyFramedProduceConsumeRoundTrip                   *Void
	protobufNonZeroMessageIndexRoundTrip                       *Void
	protobufSerializeRequiresDescriptorSet                     *Void
	protobufSerializeRequiresMessageName                       *Void
	protobufSerializeRequiresSchemaId                          *Void
	redpanda                                                   *Void
	redpandaClusterProduceListTopicsRoundTrip                  *Void
	redpandaClusterTlsRoundTrip                                *Void
	redpandaMultiBrokerBootstrapServersListsEveryNode          *Void
	redpandaMultiBrokerControllerPolicyRejected                *Void
	redpandaMultiBrokerSchemaRegistryRoundTrip                 *Void
	redpandaSchemaRegistryBundledStopIsNoOp                    *Void
	redpandaSchemaRegistryRegisterLookupRoundTrip              *Void
	redpandaSchemaRegistryTlsRegisterLookupRoundTrip           *Void
	redpandaThreeBrokerReplicationFactorThreeProduceConsume    *Void
	redpandaThreeBrokerTlsReplicationFactorThreeProduceConsume *Void
	schemaRegistry                                             *Void
	schemaRegistryFramedProduceConsumeRoundTrip                *Void
	schemaRegistryJsonframedProduceConsumeRoundTrip            *Void
	schemaRegistryJsonserializeRejectsMalformedInput           *Void
	schemaRegistryPlaintextConsumeUnframed                     *Void
	schemaRegistryRegisterLookupRoundTrip                      *Void
	schemaRegistryRejectsClusterModeMismatch                   *Void
	singleNodeClusterStarts                                    *Void
	threeControllerQuorumProduceConsume                        *Void
	tlsClientWithWrongCaFails                                  *Void
	tlsClusterStarts                                           *Void
	tlsRoundTrip                                               *Void
}

func (r *Tests) WithGraphQLQuery(q *querybuilder.Selection) *Tests {
	return &Tests{
		query: q,
	}
}

// TestsAllOpts contains options for Tests.All
type TestsAllOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string

	// Default: "8.2.0"
	ConfluentImageTag string

	// Default: "v26.1.7"
	RedpandaImageTag string

	Parallel int
}

// All runs every kafka round-trip test as a convenience for local
// `dagger call all` invocations that want the entire suite in one shot.
// CI does NOT call All: each per-distro group below carries its own
// `+check` directive, so GH Actions schedules each onto its own runner
// in parallel — running All on top would double-bill the same work.
//
// kafkaImageTag picks the tag every spawned Apache cluster runs against —
// applied to both the apache/kafka-native image (ApacheNativeCluster) and
// the apache/kafka JVM image (ApacheCluster). confluentImageTag is the
// independent knob for the cp-kafka tests (Confluent Platform versioning
// is not aligned with Apache's release numbering). redpandaImageTag is
// the independent knob for the redpandadata/redpanda tests.
//
// parallel is the concurrency cap applied at both levels — how many
// groups run at once and how many tests run at once within each group.
// Defaults to 0 (unbounded). Pass any positive integer for a specific
// cap.
func (r *Tests) All(ctx context.Context, opts ...TestsAllOpts) error {
	if r.all != nil {
		return nil
	}
	q := r.query.Select("all")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
		// `confluentImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].ConfluentImageTag) {
			q = q.Arg("confluentImageTag", opts[i].ConfluentImageTag)
		}
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// TestsApacheClusterMtlsRoundTripOpts contains options for Tests.ApacheClusterMtlsRoundTrip
type TestsApacheClusterMtlsRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ApacheClusterMtlsRoundTrip is the MTLS happy-path round-trip for
// Kafka.ApacheCluster. Mirrors MtlsRoundTrip but on the JVM image to
// rule out image-specific differences in how client-cert challenge is
// handled.
func (r *Tests) ApacheClusterMtlsRoundTrip(ctx context.Context, opts ...TestsApacheClusterMtlsRoundTripOpts) error {
	if r.apacheClusterMtlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("apacheClusterMtlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsApacheClusterProduceListTopicsRoundTripOpts contains options for Tests.ApacheClusterProduceListTopicsRoundTrip
type TestsApacheClusterProduceListTopicsRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ApacheClusterProduceListTopicsRoundTrip is the PLAINTEXT happy-path
// smoke test for Kafka.ApacheCluster (the JVM image variant): produce a
// single raw record, then call ListTopics and assert the freshly-created
// topic shows up. Together these prove the JVM image's data plane and
// control plane both work; the env-var contract matches
// ApacheNativeCluster so this single test pins down "JVM image actually
// serves traffic".
func (r *Tests) ApacheClusterProduceListTopicsRoundTrip(ctx context.Context, opts ...TestsApacheClusterProduceListTopicsRoundTripOpts) error {
	if r.apacheClusterProduceListTopicsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("apacheClusterProduceListTopicsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsApacheClusterTLSRoundTripOpts contains options for Tests.ApacheClusterTLSRoundTrip
type TestsApacheClusterTLSRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ApacheClusterTlsRoundTrip is the TLS happy-path round-trip for
// Kafka.ApacheCluster. Mirrors TlsRoundTrip but on the JVM image to
// rule out image-specific differences in keystore mounts, hostname
// verification, and SSL listener bring-up.
func (r *Tests) ApacheClusterTLSRoundTrip(ctx context.Context, opts ...TestsApacheClusterTLSRoundTripOpts) error {
	if r.apacheClusterTlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("apacheClusterTlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsApacheJvmOpts contains options for Tests.ApacheJvm
type TestsApacheJvmOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string

	Parallel int
}

// ApacheJVM runs the three apache/kafka JVM-image round-trip tests.
// Each test owns a fresh ApacheCluster, so the group holds no shared
// clusters of its own.
func (r *Tests) ApacheJvm(ctx context.Context, opts ...TestsApacheJvmOpts) error {
	if r.apacheJvm != nil {
		return nil
	}
	q := r.query.Select("apacheJvm")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// TestsApicurioSchemaRegistryRegisterLookupRoundTripOpts contains options for Tests.ApicurioSchemaRegistryRegisterLookupRoundTrip
type TestsApicurioSchemaRegistryRegisterLookupRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ApicurioSchemaRegistryRegisterLookupRoundTrip is the PLAINTEXT happy-path
// test for Kafka.ApicurioSchemaRegistry: stand an
// apicurio-registry-kafkasql up next to a fresh cluster, then exercise
// register → lookup-by-id → lookup-latest-by-subject → list-subjects →
// set/get-compatibility → delete against its Confluent-compatible REST
// surface — mirroring SchemaRegistryRegisterLookupRoundTrip to prove the
// shared *SchemaRegistryClient drives Apicurio unchanged.
func (r *Tests) ApicurioSchemaRegistryRegisterLookupRoundTrip(ctx context.Context, opts ...TestsApicurioSchemaRegistryRegisterLookupRoundTripOpts) error {
	if r.apicurioSchemaRegistryRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("apicurioSchemaRegistryRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsApicurioSchemaRegistryTLSRegisterLookupRoundTripOpts contains options for Tests.ApicurioSchemaRegistryTLSRegisterLookupRoundTrip
type TestsApicurioSchemaRegistryTLSRegisterLookupRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ApicurioSchemaRegistryTlsRegisterLookupRoundTrip drives the Apicurio TLS
// path (Quarkus HTTPS REST + kafkasql SSL storage) end-to-end.
func (r *Tests) ApicurioSchemaRegistryTLSRegisterLookupRoundTrip(ctx context.Context, opts ...TestsApicurioSchemaRegistryTLSRegisterLookupRoundTripOpts) error {
	if r.apicurioSchemaRegistryTlsRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("apicurioSchemaRegistryTlsRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsAutoCreateTopicsDisabledOpts contains options for Tests.AutoCreateTopicsDisabled
type TestsAutoCreateTopicsDisabledOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// AutoCreateTopicsDisabled produces to a topic that was never created and
// asserts the call errors out. With KAFKA_AUTO_CREATE_TOPICS_ENABLE=false on
// the broker, the produce path must surface a topic-not-found error rather
// than silently auto-creating, so producer typos can't pass tests.
func (r *Tests) AutoCreateTopicsDisabled(ctx context.Context, opts ...TestsAutoCreateTopicsDisabledOpts) error {
	if r.autoCreateTopicsDisabled != nil {
		return nil
	}
	q := r.query.Select("autoCreateTopicsDisabled")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsAvroBytesFieldRoundTripOpts contains options for Tests.AvroBytesFieldRoundTrip
type TestsAvroBytesFieldRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// AvroBytesFieldRoundTrip pins the Avro-spec JSON encoding of a bytes field.
// Per the Avro specification, the JSON encoding of bytes (and fixed) is a
// string whose characters are the byte values as Unicode code points 0-255 —
// one character per byte, NOT base64. The input value here contains byte 0xFF
// (code point U+00FF), which is outside the base64 alphabet, so a round-trip
// that preserves it byte-for-byte proves the one-char-per-byte mapping and
// would be impossible under a base64 interpretation.
func (r *Tests) AvroBytesFieldRoundTrip(ctx context.Context, opts ...TestsAvroBytesFieldRoundTripOpts) error {
	if r.avroBytesFieldRoundTrip != nil {
		return nil
	}
	q := r.query.Select("avroBytesFieldRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsAvroConsumeUnframedErrorsOpts contains options for Tests.AvroConsumeUnframedErrors
type TestsAvroConsumeUnframedErrorsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// AvroConsumeUnframedErrors pins the negative consume path: a record produced
// without a Confluent wire header, consumed with valueDeserializeAs="AVRO" +
// schemaRegistryAware=true, must error out pointing at the missing header. The
// header check fires before any schema lookup, so the registry service never
// has to start.
func (r *Tests) AvroConsumeUnframedErrors(ctx context.Context, opts ...TestsAvroConsumeUnframedErrorsOpts) error {
	if r.avroConsumeUnframedErrors != nil {
		return nil
	}
	q := r.query.Select("avroConsumeUnframedErrors")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsAvroFramedProduceConsumeRoundTripOpts contains options for Tests.AvroFramedProduceConsumeRoundTrip
type TestsAvroFramedProduceConsumeRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// AvroFramedProduceConsumeRoundTrip is the happy-path data round-trip for AVRO
// serde: register an Avro record schema to get an id, Produce a JSON document
// with valueSerializeAs="AVRO"then framed), and Consume it back with valueDeserializeAs="AVRO" +
// schemaRegistryAware=true. The asserted invariant is byte-equality of the
// consumed value to the canonical JSON form of the original input, proving the
// JSON->Avro-binary->JSON pipeline preserves the datum.
func (r *Tests) AvroFramedProduceConsumeRoundTrip(ctx context.Context, opts ...TestsAvroFramedProduceConsumeRoundTripOpts) error {
	if r.avroFramedProduceConsumeRoundTrip != nil {
		return nil
	}
	q := r.query.Select("avroFramedProduceConsumeRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsAvroMtlsFramedProduceConsumeRoundTripOpts contains options for Tests.AvroMtlsFramedProduceConsumeRoundTrip
type TestsAvroMtlsFramedProduceConsumeRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// AvroMtlsFramedProduceConsumeRoundTrip is the mTLS counterpart: the same
// round-trip where the kafka-wire client and the REST schema-resolution client
// each present their own leaf (both signed by the single CA). It proves the
// threaded RegistrySecurity profile also carries a client key store through to
// the avro path, not just a trust store.
func (r *Tests) AvroMtlsFramedProduceConsumeRoundTrip(ctx context.Context, opts ...TestsAvroMtlsFramedProduceConsumeRoundTripOpts) error {
	if r.avroMtlsFramedProduceConsumeRoundTrip != nil {
		return nil
	}
	q := r.query.Select("avroMtlsFramedProduceConsumeRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// AvroSerializeRequiresSchemaID pins the up-front validation contract of
// valueSerializeAs="AVRO": Produce must reject a zero schema id before any
// broker or registry I/O. dag.Kafka().Client(...) builds without I/O, so no
// cluster boots — the failure is purely the missing-id guard on the AVRO
// serializer.
func (r *Tests) AvroSerializeRequiresSchemaID(ctx context.Context) error {
	if r.avroSerializeRequiresSchemaId != nil {
		return nil
	}
	q := r.query.Select("avroSerializeRequiresSchemaId")

	return q.Execute(ctx)
}

// TestsAvroTLSFramedProduceConsumeRoundTripOpts contains options for Tests.AvroTLSFramedProduceConsumeRoundTrip
type TestsAvroTLSFramedProduceConsumeRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// AvroTlsFramedProduceConsumeRoundTrip is the TLS counterpart of
// AvroFramedProduceConsumeRoundTrip: it stands up a TLS cluster and a TLS
// cp-schema-registry rooted at one CA and drives the same JSON->Avro-binary->
// JSON round-trip, but every hop is encrypted — the kafka-wire client speaks
// TLS to the brokers and the Produce/Consume avro path resolves the schema
// over HTTPS via the threaded RegistrySecurity profile (#141). Without that
// profile the resolution would fail with "serves HTTPS but client is
// PLAINTEXT", so a green round-trip proves the profile reaches Client(...).
func (r *Tests) AvroTLSFramedProduceConsumeRoundTrip(ctx context.Context, opts ...TestsAvroTLSFramedProduceConsumeRoundTripOpts) error {
	if r.avroTlsFramedProduceConsumeRoundTrip != nil {
		return nil
	}
	q := r.query.Select("avroTlsFramedProduceConsumeRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsBindBrokersExposesBothListenersOpts contains options for Tests.BindBrokersExposesBothListeners
type TestsBindBrokersExposesBothListenersOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// BindBrokersExposesBothListeners binds the cluster's brokers into a
// vanilla alpine container and asserts that both the host-facing client
// port (9092) and the inter-broker port (19092) are reachable from inside
// that container — together they cover the dual-listener contract
// (PLAINTEXT_HOST:9092 for clients, PLAINTEXT:19092 for inter-broker).
func (r *Tests) BindBrokersExposesBothListeners(ctx context.Context, opts ...TestsBindBrokersExposesBothListenersOpts) error {
	if r.bindBrokersExposesBothListeners != nil {
		return nil
	}
	q := r.query.Select("bindBrokersExposesBothListeners")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsClusterClientCanListTopicsOnFreshClusterOpts contains options for Tests.ClusterClientCanListTopicsOnFreshCluster
type TestsClusterClientCanListTopicsOnFreshClusterOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ClusterClientCanListTopicsOnFreshCluster opens a franz-go-backed Client
// against a fresh cluster and asserts that ListTopics returns without error.
// A fresh KRaft cluster has no user topics, so the result may be empty —
// but the call itself must succeed, which proves module-runtime networking
// can reach the started broker service.
func (r *Tests) ClusterClientCanListTopicsOnFreshCluster(ctx context.Context, opts ...TestsClusterClientCanListTopicsOnFreshClusterOpts) error {
	if r.clusterClientCanListTopicsOnFreshCluster != nil {
		return nil
	}
	q := r.query.Select("clusterClientCanListTopicsOnFreshCluster")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsConfluentOpts contains options for Tests.Confluent
type TestsConfluentOpts struct {

	// Default: "8.2.0"
	ConfluentImageTag string

	Parallel int
}

// Confluent runs the three confluentinc/cp-kafka round-trip tests.
// Each test owns a fresh ConfluentCluster.
func (r *Tests) Confluent(ctx context.Context, opts ...TestsConfluentOpts) error {
	if r.confluent != nil {
		return nil
	}
	q := r.query.Select("confluent")
	for i := len(opts) - 1; i >= 0; i-- {
		// `confluentImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].ConfluentImageTag) {
			q = q.Arg("confluentImageTag", opts[i].ConfluentImageTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// TestsConfluentClusterMtlsRoundTripOpts contains options for Tests.ConfluentClusterMtlsRoundTrip
type TestsConfluentClusterMtlsRoundTripOpts struct {

	// Default: "8.2.0"
	ConfluentImageTag string
}

// ConfluentClusterMtlsRoundTrip is the MTLS happy-path round-trip for
// Kafka.ConfluentCluster. Mirrors MtlsRoundTrip but on cp-kafka to rule
// out distro-specific differences in how client-cert challenge is
// handled.
func (r *Tests) ConfluentClusterMtlsRoundTrip(ctx context.Context, opts ...TestsConfluentClusterMtlsRoundTripOpts) error {
	if r.confluentClusterMtlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("confluentClusterMtlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `confluentImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].ConfluentImageTag) {
			q = q.Arg("confluentImageTag", opts[i].ConfluentImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsConfluentClusterProduceListTopicsRoundTripOpts contains options for Tests.ConfluentClusterProduceListTopicsRoundTrip
type TestsConfluentClusterProduceListTopicsRoundTripOpts struct {

	// Default: "8.2.0"
	ConfluentImageTag string
}

// ConfluentClusterProduceListTopicsRoundTrip is the PLAINTEXT happy-path
// smoke test for Kafka.ConfluentCluster (the cp-kafka image variant):
// produce a single raw record, then call ListTopics and assert the
// freshly-created topic shows up. Confluent Platform's cp-kafka image
// uses the same `KAFKA_*` Scala-wrapper contract as Apache, so this
// single test pins down "cp-kafka actually serves traffic".
func (r *Tests) ConfluentClusterProduceListTopicsRoundTrip(ctx context.Context, opts ...TestsConfluentClusterProduceListTopicsRoundTripOpts) error {
	if r.confluentClusterProduceListTopicsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("confluentClusterProduceListTopicsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `confluentImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].ConfluentImageTag) {
			q = q.Arg("confluentImageTag", opts[i].ConfluentImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsConfluentClusterTLSRoundTripOpts contains options for Tests.ConfluentClusterTLSRoundTrip
type TestsConfluentClusterTLSRoundTripOpts struct {

	// Default: "8.2.0"
	ConfluentImageTag string
}

// ConfluentClusterTlsRoundTrip is the TLS happy-path round-trip for
// Kafka.ConfluentCluster. Mirrors TlsRoundTrip but on cp-kafka to rule
// out distro-specific differences in keystore mounts, hostname
// verification, and SSL listener bring-up.
func (r *Tests) ConfluentClusterTLSRoundTrip(ctx context.Context, opts ...TestsConfluentClusterTLSRoundTripOpts) error {
	if r.confluentClusterTlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("confluentClusterTlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `confluentImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].ConfluentImageTag) {
			q = q.Arg("confluentImageTag", opts[i].ConfluentImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsConfluentSchemaRegistryMtlsRegisterLookupRoundTripOpts contains options for Tests.ConfluentSchemaRegistryMtlsRegisterLookupRoundTrip
type TestsConfluentSchemaRegistryMtlsRegisterLookupRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ConfluentSchemaRegistryMtlsRegisterLookupRoundTrip is the mTLS counterpart:
// the REST endpoint requires a client certificate and the registry presents
// its own leaf to the mTLS broker for the kafkastore connection, all rooted at
// one CA.
func (r *Tests) ConfluentSchemaRegistryMtlsRegisterLookupRoundTrip(ctx context.Context, opts ...TestsConfluentSchemaRegistryMtlsRegisterLookupRoundTripOpts) error {
	if r.confluentSchemaRegistryMtlsRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("confluentSchemaRegistryMtlsRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsConfluentSchemaRegistryTLSRegisterLookupRoundTripOpts contains options for Tests.ConfluentSchemaRegistryTLSRegisterLookupRoundTrip
type TestsConfluentSchemaRegistryTLSRegisterLookupRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ConfluentSchemaRegistryTlsRegisterLookupRoundTrip is the canonical TLS test:
// a TLS cp-schema-registry terminates HTTPS on its REST endpoint and talks SSL
// to a TLS cluster's brokers for the `_schemas` topic, and the round-trip
// succeeds over an HTTPS client verifying the registry cert against the
// shared CA.
func (r *Tests) ConfluentSchemaRegistryTLSRegisterLookupRoundTrip(ctx context.Context, opts ...TestsConfluentSchemaRegistryTLSRegisterLookupRoundTripOpts) error {
	if r.confluentSchemaRegistryTlsRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("confluentSchemaRegistryTlsRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsConsumerGroupOnSingleBrokerWorksOpts contains options for Tests.ConsumerGroupOnSingleBrokerWorks
type TestsConsumerGroupOnSingleBrokerWorksOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ConsumerGroupOnSingleBrokerWorks produces one record then consumes it back
// through a consumer group on a 1-broker cluster. A successful round-trip
// proves __consumer_offsets was created at the broker's configured
// replication factor (1, after the system-topic env vars take effect).
// Without KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=1 the broker would refuse
// to create __consumer_offsets at the upstream default RF=3 and the group
// join would hang or error.
func (r *Tests) ConsumerGroupOnSingleBrokerWorks(ctx context.Context, opts ...TestsConsumerGroupOnSingleBrokerWorksOpts) error {
	if r.consumerGroupOnSingleBrokerWorks != nil {
		return nil
	}
	q := r.query.Select("consumerGroupOnSingleBrokerWorks")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsCreateAndDeleteTopicRoundTripOpts contains options for Tests.CreateAndDeleteTopicRoundTrip
type TestsCreateAndDeleteTopicRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// CreateAndDeleteTopicRoundTrip exercises the create/list/delete cycle to
// confirm kadm wiring. The topic name is randomized so the test is
// repeatable against the same cluster and never collides with leftovers.
func (r *Tests) CreateAndDeleteTopicRoundTrip(ctx context.Context, opts ...TestsCreateAndDeleteTopicRoundTripOpts) error {
	if r.createAndDeleteTopicRoundTrip != nil {
		return nil
	}
	q := r.query.Select("createAndDeleteTopicRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsDedicatedControllerAndBrokerProduceConsumeOpts contains options for Tests.DedicatedControllerAndBrokerProduceConsume
type TestsDedicatedControllerAndBrokerProduceConsumeOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// DedicatedControllerAndBrokerProduceConsume verifies that the split
// controller+broker topology (introduced this increment) still supports a
// full produce/consume round-trip — i.e. the broker correctly joined the
// controller quorum over its WithServiceBinding alias.
func (r *Tests) DedicatedControllerAndBrokerProduceConsume(ctx context.Context, opts ...TestsDedicatedControllerAndBrokerProduceConsumeOpts) error {
	if r.dedicatedControllerAndBrokerProduceConsume != nil {
		return nil
	}
	q := r.query.Select("dedicatedControllerAndBrokerProduceConsume")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsDescribeConsumerGroupReportsLagOpts contains options for Tests.DescribeConsumerGroupReportsLag
type TestsDescribeConsumerGroupReportsLagOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// DescribeConsumerGroupReportsLag produces five records, consumes three of
// them through a committing consumer group, and asserts DescribeConsumerGroup
// reports the group in the Empty state with committed-offset lag of 2 (end
// offset 5 minus committed offset 3) on the single partition.
func (r *Tests) DescribeConsumerGroupReportsLag(ctx context.Context, opts ...TestsDescribeConsumerGroupReportsLagOpts) error {
	if r.describeConsumerGroupReportsLag != nil {
		return nil
	}
	q := r.query.Select("describeConsumerGroupReportsLag")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsDescribeTopicReportsPartitionsAndConfigsOpts contains options for Tests.DescribeTopicReportsPartitionsAndConfigs
type TestsDescribeTopicReportsPartitionsAndConfigsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// DescribeTopicReportsPartitionsAndConfigs creates a 3-partition RF=1 topic
// and asserts DescribeTopic reports the derived partition count / replication
// factor, one partition entry per partition, and a non-empty topic-level
// config set (proving the configs path is wired).
func (r *Tests) DescribeTopicReportsPartitionsAndConfigs(ctx context.Context, opts ...TestsDescribeTopicReportsPartitionsAndConfigsOpts) error {
	if r.describeTopicReportsPartitionsAndConfigs != nil {
		return nil
	}
	q := r.query.Select("describeTopicReportsPartitionsAndConfigs")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsFiveControllerQuorumAcceptedOpts contains options for Tests.FiveControllerQuorumAccepted
type TestsFiveControllerQuorumAcceptedOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// FiveControllerQuorumAccepted proves the constructor accepts a five-voter
// quorum (odd, > 3) and returns a *Cluster: resolving BootstrapServers forces
// the server-side constructor — validation, internal-CA minting of a leaf per
// controller, and the full container graph — to run without booting the
// containers, so the accept path is exercised cheaply. The 3-controller
// end-to-end quorum is covered by ThreeControllerQuorumProduceConsume.
func (r *Tests) FiveControllerQuorumAccepted(ctx context.Context, opts ...TestsFiveControllerQuorumAcceptedOpts) error {
	if r.fiveControllerQuorumAccepted != nil {
		return nil
	}
	q := r.query.Select("fiveControllerQuorumAccepted")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// A unique identifier for this Tests.
func (r *Tests) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *Tests) XXX_GraphQLType() string {
	return "Tests"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *Tests) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *Tests) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *Tests) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *Tests) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = Tests{query: selectNode(dag.query, id, "Tests")}
	return nil
}

// TestsInternalListenersAreEncryptedOpts contains options for Tests.InternalListenersAreEncrypted
type TestsInternalListenersAreEncryptedOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// InternalListenersAreEncrypted spins up a 1+2 cluster with TLS on the
// external listener and creates an RF=2 topic. A successful produce →
// consume round-trip proves replication traffic flowed over the (always
// mTLS) INTERNAL inter-broker listener: without working internal mTLS,
// the second broker would never become an in-sync replica and the produce
// (with default acks=all-isr) would stall.
func (r *Tests) InternalListenersAreEncrypted(ctx context.Context, opts ...TestsInternalListenersAreEncryptedOpts) error {
	if r.internalListenersAreEncrypted != nil {
		return nil
	}
	q := r.query.Select("internalListenersAreEncrypted")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsInvalidControllerCountIsRejectedOpts contains options for Tests.InvalidControllerCountIsRejected
type TestsInvalidControllerCountIsRejectedOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// InvalidControllerCountIsRejected pins the voter-count policy: a KRaft
// quorum wants an odd voter count for a clean majority, so even controller
// counts are rejected, and a sub-1 count is nonsensical. Both must fail at
// construction time with a clear error rather than spinning up a broken
// topology. (Controllers=0 can't be exercised from the Go SDK — Dagger drops
// the zero value and applies the
func (r *Tests) InvalidControllerCountIsRejected(ctx context.Context, opts ...TestsInvalidControllerCountIsRejectedOpts) error {
	if r.invalidControllerCountIsRejected != nil {
		return nil
	}
	q := r.query.Select("invalidControllerCountIsRejected")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsKarapaceSchemaRegistryRegisterLookupRoundTripOpts contains options for Tests.KarapaceSchemaRegistryRegisterLookupRoundTrip
type TestsKarapaceSchemaRegistryRegisterLookupRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// KarapaceSchemaRegistryRegisterLookupRoundTrip is the PLAINTEXT happy-path
// test for Kafka.KarapaceSchemaRegistry: stand a Karapace service up next to
// a fresh cluster, then exercise register → lookup-by-id →
// lookup-latest-by-subject → list-subjects → set/get-compatibility → delete
// against its Confluent-compatible REST surface — mirroring
// SchemaRegistryRegisterLookupRoundTrip to prove the shared
// *SchemaRegistryClient drives Karapace unchanged.
func (r *Tests) KarapaceSchemaRegistryRegisterLookupRoundTrip(ctx context.Context, opts ...TestsKarapaceSchemaRegistryRegisterLookupRoundTripOpts) error {
	if r.karapaceSchemaRegistryRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("karapaceSchemaRegistryRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsKarapaceSchemaRegistryTLSRegisterLookupRoundTripOpts contains options for Tests.KarapaceSchemaRegistryTLSRegisterLookupRoundTrip
type TestsKarapaceSchemaRegistryTLSRegisterLookupRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// KarapaceSchemaRegistryTlsRegisterLookupRoundTrip drives the Karapace TLS
// path (PEM REST listener + aiokafka SSL storage) end-to-end.
func (r *Tests) KarapaceSchemaRegistryTLSRegisterLookupRoundTrip(ctx context.Context, opts ...TestsKarapaceSchemaRegistryTLSRegisterLookupRoundTripOpts) error {
	if r.karapaceSchemaRegistryTlsRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("karapaceSchemaRegistryTlsRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsListConsumerGroupsReportsCommittedGroupOpts contains options for Tests.ListConsumerGroupsReportsCommittedGroup
type TestsListConsumerGroupsReportsCommittedGroupOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ListConsumerGroupsReportsCommittedGroup produces a record, consumes it back
// through a committing consumer group, and asserts the group then appears in
// ListConsumerGroups. A fresh cluster reports no groups, so the group's
// presence proves the join + commit reached __consumer_offsets.
func (r *Tests) ListConsumerGroupsReportsCommittedGroup(ctx context.Context, opts ...TestsListConsumerGroupsReportsCommittedGroupOpts) error {
	if r.listConsumerGroupsReportsCommittedGroup != nil {
		return nil
	}
	q := r.query.Select("listConsumerGroupsReportsCommittedGroup")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsMtlsRequiresClientCertOpts contains options for Tests.MtlsRequiresClientCert
type TestsMtlsRequiresClientCertOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// MtlsRequiresClientCert points a TLS-only client (no keystore) at an
// MTLS broker and asserts the handshake fails. Confirms the broker's
// client.auth=required setting is actually being honoured.
func (r *Tests) MtlsRequiresClientCert(ctx context.Context, opts ...TestsMtlsRequiresClientCertOpts) error {
	if r.mtlsRequiresClientCert != nil {
		return nil
	}
	q := r.query.Select("mtlsRequiresClientCert")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsMtlsRoundTripOpts contains options for Tests.MtlsRoundTrip
type TestsMtlsRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// MtlsRoundTrip produces and consumes a single record over a mutual-TLS
// external listener. The broker presents its cert (signed by the server
// CA) and demands a client cert in return; the test client presents one
// signed by an independent client CA the broker is configured to trust.
func (r *Tests) MtlsRoundTrip(ctx context.Context, opts ...TestsMtlsRoundTripOpts) error {
	if r.mtlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("mtlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsNativeOpts contains options for Tests.Native
type TestsNativeOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string

	Parallel int
}

// Native runs every apache/kafka-native test as one group. It boots
// the three shared ApacheNativeClusters up front, fans the shared-cluster
// and fresh-cluster native tests across a par pool capped at parallel,
// and tears the shared clusters down on return.
func (r *Tests) Native(ctx context.Context, opts ...TestsNativeOpts) error {
	if r.native != nil {
		return nil
	}
	q := r.query.Select("native")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// TestsOneControllerTwoBrokersReplicationFactorTwoOpts contains options for Tests.OneControllerTwoBrokersReplicationFactorTwo
type TestsOneControllerTwoBrokersReplicationFactorTwoOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// OneControllerTwoBrokersReplicationFactorTwo spins up a 1+2 cluster and
// creates a replication-factor-2 topic so the produce path forces inter-
// broker replication. A successful round-trip proves brokers can reach
// each other over the engine network without explicit peer bindings.
func (r *Tests) OneControllerTwoBrokersReplicationFactorTwo(ctx context.Context, opts ...TestsOneControllerTwoBrokersReplicationFactorTwoOpts) error {
	if r.oneControllerTwoBrokersReplicationFactorTwo != nil {
		return nil
	}
	q := r.query.Select("oneControllerTwoBrokersReplicationFactorTwo")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

func (r *Tests) PlaintextSecurityProfilesAreNonNil(ctx context.Context) error {
	if r.plaintextSecurityProfilesAreNonNil != nil {
		return nil
	}
	q := r.query.Select("plaintextSecurityProfilesAreNonNil")

	return q.Execute(ctx)
}

// TestsProduceConsumeRoundTripBase64Opts contains options for Tests.ProduceConsumeRoundTripBase64
type TestsProduceConsumeRoundTripBase64Opts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ProduceConsumeRoundTripBase64 round-trips the same kind of binary payload
// through standard base64 (with padding).
func (r *Tests) ProduceConsumeRoundTripBase64(ctx context.Context, opts ...TestsProduceConsumeRoundTripBase64Opts) error {
	if r.produceConsumeRoundTripBase64 != nil {
		return nil
	}
	q := r.query.Select("produceConsumeRoundTripBase64")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsProduceConsumeRoundTripHexOpts contains options for Tests.ProduceConsumeRoundTripHex
type TestsProduceConsumeRoundTripHexOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ProduceConsumeRoundTripHex round-trips a binary payload through hex
// encoding. The non-UTF-8 bytes (including 0x00) verify that hex transports
// arbitrary binary safely.
func (r *Tests) ProduceConsumeRoundTripHex(ctx context.Context, opts ...TestsProduceConsumeRoundTripHexOpts) error {
	if r.produceConsumeRoundTripHex != nil {
		return nil
	}
	q := r.query.Select("produceConsumeRoundTripHex")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsProduceConsumeRoundTripRawOpts contains options for Tests.ProduceConsumeRoundTripRaw
type TestsProduceConsumeRoundTripRawOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ProduceConsumeRoundTripRaw produces a single record with raw-encoded key
// and value, then consumes it back and asserts byte equality. The raw
// encoding round-trips Go strings verbatim, so the assertion is direct
// string equality.
func (r *Tests) ProduceConsumeRoundTripRaw(ctx context.Context, opts ...TestsProduceConsumeRoundTripRawOpts) error {
	if r.produceConsumeRoundTripRaw != nil {
		return nil
	}
	q := r.query.Select("produceConsumeRoundTripRaw")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsProduceRejectsUnknownEncodingOpts contains options for Tests.ProduceRejectsUnknownEncoding
type TestsProduceRejectsUnknownEncodingOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ProduceRejectsUnknownEncoding verifies that a Produce call with a bogus
// encoding name fails fast rather than silently misbehaving.
func (r *Tests) ProduceRejectsUnknownEncoding(ctx context.Context, opts ...TestsProduceRejectsUnknownEncodingOpts) error {
	if r.produceRejectsUnknownEncoding != nil {
		return nil
	}
	q := r.query.Select("produceRejectsUnknownEncoding")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsPropertiesFileContainsBootstrapAndSecurityProtocolOpts contains options for Tests.PropertiesFileContainsBootstrapAndSecurityProtocol
type TestsPropertiesFileContainsBootstrapAndSecurityProtocolOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// PropertiesFileContainsBootstrapAndSecurityProtocol verifies that the
// rendered Java client.properties file carries the bootstrap.servers list
// and a plaintext security.protocol entry — enough for the Apache Kafka
// CLI tools to pick up the connection settings.
func (r *Tests) PropertiesFileContainsBootstrapAndSecurityProtocol(ctx context.Context, opts ...TestsPropertiesFileContainsBootstrapAndSecurityProtocolOpts) error {
	if r.propertiesFileContainsBootstrapAndSecurityProtocol != nil {
		return nil
	}
	q := r.query.Select("propertiesFileContainsBootstrapAndSecurityProtocol")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsPropertiesFileContainsMtlsSettingsOpts contains options for Tests.PropertiesFileContainsMtlsSettings
type TestsPropertiesFileContainsMtlsSettingsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// PropertiesFileContainsMtlsSettings verifies that mTLS mode also renders
// the ssl.keystore.* triple referencing a keystore.p12 sidecar.
func (r *Tests) PropertiesFileContainsMtlsSettings(ctx context.Context, opts ...TestsPropertiesFileContainsMtlsSettingsOpts) error {
	if r.propertiesFileContainsMtlsSettings != nil {
		return nil
	}
	q := r.query.Select("propertiesFileContainsMtlsSettings")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsPropertiesFileContainsTLSSettingsOpts contains options for Tests.PropertiesFileContainsTLSSettings
type TestsPropertiesFileContainsTLSSettingsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// PropertiesFileContainsTlsSettings verifies the rendered Java
// client.properties carries security.protocol=SSL plus an ssl.truststore.*
// triple referencing a sidecar PKCS#12 file by basename.
func (r *Tests) PropertiesFileContainsTLSSettings(ctx context.Context, opts ...TestsPropertiesFileContainsTLSSettingsOpts) error {
	if r.propertiesFileContainsTlsSettings != nil {
		return nil
	}
	q := r.query.Select("propertiesFileContainsTlsSettings")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsProtobufConsumeMessageIndexMismatchErrorsOpts contains options for Tests.ProtobufConsumeMessageIndexMismatchErrors
type TestsProtobufConsumeMessageIndexMismatchErrorsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ProtobufConsumeMessageIndexMismatchErrors pins the guard that makes the
// message-index array load-bearing rather than decorative. A record produced
// as devex.kafka.test.User (index [0]) but consumed as devex.kafka.test.Event
// (index [1]) must be rejected: protobuf wire bytes are not self-describing,
// so decoding one message type's bytes against another's descriptor would
// otherwise succeed and hand back silent garbage.
func (r *Tests) ProtobufConsumeMessageIndexMismatchErrors(ctx context.Context, opts ...TestsProtobufConsumeMessageIndexMismatchErrorsOpts) error {
	if r.protobufConsumeMessageIndexMismatchErrors != nil {
		return nil
	}
	q := r.query.Select("protobufConsumeMessageIndexMismatchErrors")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsProtobufConsumeUnframedErrorsOpts contains options for Tests.ProtobufConsumeUnframedErrors
type TestsProtobufConsumeUnframedErrorsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ProtobufConsumeUnframedErrors pins the negative consume path: a record
// produced without a Confluent wire header, consumed with
// valueDeserializeAs="PROTOBUF"pointing at the missing header rather than trying to parse arbitrary bytes
// as a protobuf message. No registry is started — PROTOBUF decoding never
// needs one, and the header check fires before the descriptor set is even
// read.
func (r *Tests) ProtobufConsumeUnframedErrors(ctx context.Context, opts ...TestsProtobufConsumeUnframedErrorsOpts) error {
	if r.protobufConsumeUnframedErrors != nil {
		return nil
	}
	q := r.query.Select("protobufConsumeUnframedErrors")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// ProtobufDeserializeRequiresDescriptorSet mirrors the produce-side guard on
// the consume path. It must fail before a broker connection is opened, which
// the unroutable bootstrap address proves: a missing guard would surface as a
// dial timeout rather than a validation error.
func (r *Tests) ProtobufDeserializeRequiresDescriptorSet(ctx context.Context) error {
	if r.protobufDeserializeRequiresDescriptorSet != nil {
		return nil
	}
	q := r.query.Select("protobufDeserializeRequiresDescriptorSet")

	return q.Execute(ctx)
}

// ProtobufDeserializeRequiresSchemaRegistryAware pins that PROTOBUF consume
// needs schemaRegistryAware=true: without it the 5-byte header is never
// stripped, so neither the wire id nor the message-index array that follows it
// is reachable.
func (r *Tests) ProtobufDeserializeRequiresSchemaRegistryAware(ctx context.Context) error {
	if r.protobufDeserializeRequiresSchemaRegistryAware != nil {
		return nil
	}
	q := r.query.Select("protobufDeserializeRequiresSchemaRegistryAware")

	return q.Execute(ctx)
}

// TestsProtobufFramedProduceConsumeRoundTripOpts contains options for Tests.ProtobufFramedProduceConsumeRoundTrip
type TestsProtobufFramedProduceConsumeRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ProtobufFramedProduceConsumeRoundTrip is the happy-path data round-trip for
// PROTOBUF serde: register user.proto to get a schema id, Produce a JSON
// document with valueSerializeAs="PROTOBUF" + the descriptor set + the message
// name (so it is protobuf-encoded, then framed with the header *and* the
// message-index array), and Consume it back with valueDeserializeAs="PROTOBUF"
//
// The asserted invariant is byte-equality of the consumed value to the
// canonical JSON form of the original input, proving the
// JSON->protobuf-binary->JSON pipeline preserves the datum. devex.kafka.test.User
// is the first message in the file, so this exercises the single-zero-byte
// shortcut form of the message-index array.
func (r *Tests) ProtobufFramedProduceConsumeRoundTrip(ctx context.Context, opts ...TestsProtobufFramedProduceConsumeRoundTripOpts) error {
	if r.protobufFramedProduceConsumeRoundTrip != nil {
		return nil
	}
	q := r.query.Select("protobufFramedProduceConsumeRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsProtobufKeyFramedProduceConsumeRoundTripOpts contains options for Tests.ProtobufKeyFramedProduceConsumeRoundTrip
type TestsProtobufKeyFramedProduceConsumeRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ProtobufKeyFramedProduceConsumeRoundTrip drives PROTOBUF serde on the *key*
// rather than the value. Key and value are plumbed through independent
// protoMessages instances, independent validation calls, and independent
// framing branches, so the value round-trip alone would not catch a
// copy-paste slip on the key side. Both fields are encoded here — with
// different message types, so a crossed wire between them fails rather than
// coincidentally passing.
func (r *Tests) ProtobufKeyFramedProduceConsumeRoundTrip(ctx context.Context, opts ...TestsProtobufKeyFramedProduceConsumeRoundTripOpts) error {
	if r.protobufKeyFramedProduceConsumeRoundTrip != nil {
		return nil
	}
	q := r.query.Select("protobufKeyFramedProduceConsumeRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsProtobufNonZeroMessageIndexRoundTripOpts contains options for Tests.ProtobufNonZeroMessageIndexRoundTrip
type TestsProtobufNonZeroMessageIndexRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ProtobufNonZeroMessageIndexRoundTrip drives the same pipeline against
// devex.kafka.test.Event, the *second* top-level message in user.proto. Its
// Confluent message-index path is [1] rather than [0], so the frame carries
// the length-prefixed varint form of the index array instead of the
// single-zero-byte shortcut. Without a correctly written and re-read index the
// consume side would either mis-parse the payload or reject the record, so a
// green round-trip here is what proves the index round-trips rather than being
// accidentally skipped.
func (r *Tests) ProtobufNonZeroMessageIndexRoundTrip(ctx context.Context, opts ...TestsProtobufNonZeroMessageIndexRoundTripOpts) error {
	if r.protobufNonZeroMessageIndexRoundTrip != nil {
		return nil
	}
	q := r.query.Select("protobufNonZeroMessageIndexRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// ProtobufSerializeRequiresDescriptorSet pins the up-front validation contract
// of valueSerializeAs="PROTOBUF": Produce must reject a missing descriptor set
// before any broker, registry, or file I/O. dag.Kafka().Client(...) builds
// without I/O and the bootstrap address is unroutable, so a nil error here
// would mean the guard never fired.
func (r *Tests) ProtobufSerializeRequiresDescriptorSet(ctx context.Context) error {
	if r.protobufSerializeRequiresDescriptorSet != nil {
		return nil
	}
	q := r.query.Select("protobufSerializeRequiresDescriptorSet")

	return q.Execute(ctx)
}

// ProtobufSerializeRequiresMessageName is the sibling guard: a descriptor set
// alone is not enough, because a FileDescriptorSet can hold many message types
// and nothing in it says which one the payload is.
func (r *Tests) ProtobufSerializeRequiresMessageName(ctx context.Context) error {
	if r.protobufSerializeRequiresMessageName != nil {
		return nil
	}
	q := r.query.Select("protobufSerializeRequiresMessageName")

	return q.Execute(ctx)
}

// ProtobufSerializeRequiresSchemaID pins that PROTOBUF mode also demands a
// positive schema id. The id is not optional the way it arguably is for a bare
// JSON payload: the Confluent message-index array lives inside the frame, so
// an unframed Protobuf record has nowhere to record which message type it
// holds and no consumer could decode it.
func (r *Tests) ProtobufSerializeRequiresSchemaID(ctx context.Context) error {
	if r.protobufSerializeRequiresSchemaId != nil {
		return nil
	}
	q := r.query.Select("protobufSerializeRequiresSchemaId")

	return q.Execute(ctx)
}

// TestsRedpandaOpts contains options for Tests.Redpanda
type TestsRedpandaOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string

	Parallel int
}

// Redpanda runs the redpandadata/redpanda round-trip tests — the two
// Kafka-wire round-trips, the PLAINTEXT and TLS bundled-Schema-Registry
// round-trips, and the bundled-registry Stop-is-a-no-op lifecycle test.
func (r *Tests) Redpanda(ctx context.Context, opts ...TestsRedpandaOpts) error {
	if r.redpanda != nil {
		return nil
	}
	q := r.query.Select("redpanda")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// TestsRedpandaClusterProduceListTopicsRoundTripOpts contains options for Tests.RedpandaClusterProduceListTopicsRoundTrip
type TestsRedpandaClusterProduceListTopicsRoundTripOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string
}

// RedpandaClusterProduceListTopicsRoundTrip is the PLAINTEXT happy-path
// round-trip for Kafka.RedpandaCluster: spin up a single-node Redpanda,
// create a topic, produce one record, then assert the freshly-created
// topic shows up in ListTopics. Pins down "redpanda actually serves
// Kafka-wire traffic on the external listener".
func (r *Tests) RedpandaClusterProduceListTopicsRoundTrip(ctx context.Context, opts ...TestsRedpandaClusterProduceListTopicsRoundTripOpts) error {
	if r.redpandaClusterProduceListTopicsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("redpandaClusterProduceListTopicsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsRedpandaClusterTLSRoundTripOpts contains options for Tests.RedpandaClusterTLSRoundTrip
type TestsRedpandaClusterTLSRoundTripOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string
}

// RedpandaClusterTlsRoundTrip is the TLS happy-path round-trip for
// Kafka.RedpandaCluster: spin up Redpanda with kafka_api_tls.enabled=true
// using PEM cert/key/CA mounted into /etc/redpanda/certs, then produce
// and consume one record over the TLS listener with the franz-go client
// verifying the broker leaf against the matching truststore.
func (r *Tests) RedpandaClusterTLSRoundTrip(ctx context.Context, opts ...TestsRedpandaClusterTLSRoundTripOpts) error {
	if r.redpandaClusterTlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("redpandaClusterTlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsRedpandaMultiBrokerBootstrapServersListsEveryNodeOpts contains options for Tests.RedpandaMultiBrokerBootstrapServersListsEveryNode
type TestsRedpandaMultiBrokerBootstrapServersListsEveryNodeOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string
}

// RedpandaMultiBrokerBootstrapServersListsEveryNode proves the constructor
// accepts brokers=3 and returns a cluster advertising all three broker
// bootstrap addresses. Resolving BootstrapServers forces the server-side
// constructor (validation + the full container graph, including the per-node
// seed list) to run without booting the containers, so the accept path is
// exercised cheaply — the real three-node Raft round-trip is covered by
// RedpandaThreeBrokerReplicationFactorThreeProduceConsume.
func (r *Tests) RedpandaMultiBrokerBootstrapServersListsEveryNode(ctx context.Context, opts ...TestsRedpandaMultiBrokerBootstrapServersListsEveryNodeOpts) error {
	if r.redpandaMultiBrokerBootstrapServersListsEveryNode != nil {
		return nil
	}
	q := r.query.Select("redpandaMultiBrokerBootstrapServersListsEveryNode")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsRedpandaMultiBrokerControllerPolicyRejectedOpts contains options for Tests.RedpandaMultiBrokerControllerPolicyRejected
type TestsRedpandaMultiBrokerControllerPolicyRejectedOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string
}

// RedpandaMultiBrokerControllerPolicyRejected pins the two construction-time
// policies Redpanda enforces: `controllers != 1` is rejected (Redpanda has no
// separate controller role) and `brokers < 1` is rejected. Resolving
// BootstrapServers forces the server-side constructor to run its validation
// without booting any container, so both rejections are exercised cheaply.
func (r *Tests) RedpandaMultiBrokerControllerPolicyRejected(ctx context.Context, opts ...TestsRedpandaMultiBrokerControllerPolicyRejectedOpts) error {
	if r.redpandaMultiBrokerControllerPolicyRejected != nil {
		return nil
	}
	q := r.query.Select("redpandaMultiBrokerControllerPolicyRejected")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsRedpandaMultiBrokerSchemaRegistryRoundTripOpts contains options for Tests.RedpandaMultiBrokerSchemaRegistryRoundTrip
type TestsRedpandaMultiBrokerSchemaRegistryRoundTripOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string
}

// RedpandaMultiBrokerSchemaRegistryRoundTrip proves Redpanda's bundled Schema
// Registry still works against a multi-node cluster: it stands up a three-node
// cluster, registers a schema against cluster.SchemaRegistry() (which points
// at node 0, whose service cascades the whole cluster online), and asserts the
// lookup-by-id round-trip. The `_schemas` topic itself is replicated across the
// cluster, so a successful round-trip proves the registry reaches a formed
// multi-node Raft group.
func (r *Tests) RedpandaMultiBrokerSchemaRegistryRoundTrip(ctx context.Context, opts ...TestsRedpandaMultiBrokerSchemaRegistryRoundTripOpts) error {
	if r.redpandaMultiBrokerSchemaRegistryRoundTrip != nil {
		return nil
	}
	q := r.query.Select("redpandaMultiBrokerSchemaRegistryRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsRedpandaSchemaRegistryBundledStopIsNoOpOpts contains options for Tests.RedpandaSchemaRegistryBundledStopIsNoOp
type TestsRedpandaSchemaRegistryBundledStopIsNoOpOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string
}

// RedpandaSchemaRegistryBundledStopIsNoOp pins the bundled-registry lifecycle
// contract: a bundled *SchemaRegistry shares the broker service, so sr.Stop
// must be a no-op — the cluster owns teardown via cluster.Stop. This runs a
// register/lookup round-trip, calls sr.Stop, then exercises the registry
// again through the same handle. If sr.Stop had torn down the shared broker
// service, that follow-up call would fail.
func (r *Tests) RedpandaSchemaRegistryBundledStopIsNoOp(ctx context.Context, opts ...TestsRedpandaSchemaRegistryBundledStopIsNoOpOpts) error {
	if r.redpandaSchemaRegistryBundledStopIsNoOp != nil {
		return nil
	}
	q := r.query.Select("redpandaSchemaRegistryBundledStopIsNoOp")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsRedpandaSchemaRegistryRegisterLookupRoundTripOpts contains options for Tests.RedpandaSchemaRegistryRegisterLookupRoundTrip
type TestsRedpandaSchemaRegistryRegisterLookupRoundTripOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string
}

// RedpandaSchemaRegistryRegisterLookupRoundTrip is the PLAINTEXT happy-path
// test for RedpandaCluster.SchemaRegistry: `rpk redpanda start` runs a Schema
// Registry inside the broker process on :8081, so this registers a schema
// against cluster.SchemaRegistry() and asserts the lookup-by-id round-trip —
// proving the bundled SR is reachable and interchangeable with the
// separate-container ConfluentSchemaRegistry. The SR service is the broker
// itself, so cluster.Stop tears it down — sr.Stop is a no-op for a bundled
// registry.
func (r *Tests) RedpandaSchemaRegistryRegisterLookupRoundTrip(ctx context.Context, opts ...TestsRedpandaSchemaRegistryRegisterLookupRoundTripOpts) error {
	if r.redpandaSchemaRegistryRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("redpandaSchemaRegistryRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsRedpandaSchemaRegistryTLSRegisterLookupRoundTripOpts contains options for Tests.RedpandaSchemaRegistryTLSRegisterLookupRoundTrip
type TestsRedpandaSchemaRegistryTLSRegisterLookupRoundTripOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string
}

// RedpandaSchemaRegistryTlsRegisterLookupRoundTrip is the TLS-cluster
// counterpart of RedpandaSchemaRegistryRegisterLookupRoundTrip. A TLS
// Redpanda cluster terminates HTTPS on its bundled Schema Registry REST
// endpoint, reusing the broker's server leaf (configured through the
// separately-rendered redpanda.yaml schema_registry_api_tls block), so this
// exercises that YAML path end-to-end and drives register/lookup over HTTPS,
// verifying the SR cert against the cluster CA truststore.
func (r *Tests) RedpandaSchemaRegistryTLSRegisterLookupRoundTrip(ctx context.Context, opts ...TestsRedpandaSchemaRegistryTLSRegisterLookupRoundTripOpts) error {
	if r.redpandaSchemaRegistryTlsRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("redpandaSchemaRegistryTlsRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsRedpandaThreeBrokerReplicationFactorThreeProduceConsumeOpts contains options for Tests.RedpandaThreeBrokerReplicationFactorThreeProduceConsume
type TestsRedpandaThreeBrokerReplicationFactorThreeProduceConsumeOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string
}

// RedpandaThreeBrokerReplicationFactorThreeProduceConsume stands up a real
// three-node Redpanda cluster and drives a produce → consume round-trip over
// an RF=3 topic. A successful round-trip proves the three nodes formed a
// single Raft group over the internal RPC listener (seed-driven bootstrap) and
// that inter-node replication is actually exercised — an RF=3 topic can only be
// created and written if all three brokers reach each other.
func (r *Tests) RedpandaThreeBrokerReplicationFactorThreeProduceConsume(ctx context.Context, opts ...TestsRedpandaThreeBrokerReplicationFactorThreeProduceConsumeOpts) error {
	if r.redpandaThreeBrokerReplicationFactorThreeProduceConsume != nil {
		return nil
	}
	q := r.query.Select("redpandaThreeBrokerReplicationFactorThreeProduceConsume")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsRedpandaThreeBrokerTLSReplicationFactorThreeProduceConsumeOpts contains options for Tests.RedpandaThreeBrokerTLSReplicationFactorThreeProduceConsume
type TestsRedpandaThreeBrokerTLSReplicationFactorThreeProduceConsumeOpts struct {

	// Default: "v26.1.7"
	RedpandaImageTag string
}

// RedpandaThreeBrokerTlsReplicationFactorThreeProduceConsume is the TLS
// counterpart: a three-node Redpanda cluster with TLS on every node's external
// Kafka listener, driving a produce → consume round-trip over an RF=3 topic.
// The franz-go client verifies whichever node it is routed to against the
// cluster truststore, proving every node's leaf is SAN'd to its own hostname.
func (r *Tests) RedpandaThreeBrokerTLSReplicationFactorThreeProduceConsume(ctx context.Context, opts ...TestsRedpandaThreeBrokerTLSReplicationFactorThreeProduceConsumeOpts) error {
	if r.redpandaThreeBrokerTlsReplicationFactorThreeProduceConsume != nil {
		return nil
	}
	q := r.query.Select("redpandaThreeBrokerTlsReplicationFactorThreeProduceConsume")
	for i := len(opts) - 1; i >= 0; i-- {
		// `redpandaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].RedpandaImageTag) {
			q = q.Arg("redpandaImageTag", opts[i].RedpandaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsSchemaRegistryOpts contains options for Tests.SchemaRegistry
type TestsSchemaRegistryOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string

	Parallel int
}

// SchemaRegistry runs the Schema Registry tests — ConfluentSchemaRegistry,
// ApicurioSchemaRegistry, and KarapaceSchemaRegistry — as one group. Each test
// owns the cluster (and, for the round-trips, the registry service) it boots,
// so the group's only lifetime guarantee is that both are torn down once it
// returns.
func (r *Tests) SchemaRegistry(ctx context.Context, opts ...TestsSchemaRegistryOpts) error {
	if r.schemaRegistry != nil {
		return nil
	}
	q := r.query.Select("schemaRegistry")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// TestsSchemaRegistryFramedProduceConsumeRoundTripOpts contains options for Tests.SchemaRegistryFramedProduceConsumeRoundTrip
type TestsSchemaRegistryFramedProduceConsumeRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// SchemaRegistryFramedProduceConsumeRoundTrip exercises the data path of the
// Client with Confluent wire-format framing: register a schema to get an ID,
// produce a record whose value is framed with that ID, then consume with
// schemaRegistryAware=true and assert the parsed ID matches and the value
// bytes are stripped back to the original payload.
func (r *Tests) SchemaRegistryFramedProduceConsumeRoundTrip(ctx context.Context, opts ...TestsSchemaRegistryFramedProduceConsumeRoundTripOpts) error {
	if r.schemaRegistryFramedProduceConsumeRoundTrip != nil {
		return nil
	}
	q := r.query.Select("schemaRegistryFramedProduceConsumeRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsSchemaRegistryJsonframedProduceConsumeRoundTripOpts contains options for Tests.SchemaRegistryJsonframedProduceConsumeRoundTrip
type TestsSchemaRegistryJsonframedProduceConsumeRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// SchemaRegistryJSONFramedProduceConsumeRoundTrip composes the framing
// primitive (valueSchemaID) with the JSON serde (valueSerializeAs /
// valueDeserializeAs). A JSON document is registered as a JSON-schema-typed
// subject, produced with both opts on, then consumed with both opts on; the
// asserted invariant is byte-equality of the consumed value to the canonical
// JSON form of the original input — proving frame strip and JSON validation
// compose without corrupting the payload.
func (r *Tests) SchemaRegistryJsonframedProduceConsumeRoundTrip(ctx context.Context, opts ...TestsSchemaRegistryJsonframedProduceConsumeRoundTripOpts) error {
	if r.schemaRegistryJsonframedProduceConsumeRoundTrip != nil {
		return nil
	}
	q := r.query.Select("schemaRegistryJsonframedProduceConsumeRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// SchemaRegistryJSONSerializeRejectsMalformedInput pins the up-front
// validation contract of valueSerializeAs="JSON": Produce must reject a
// malformed JSON payload before any broker I/O. dag.Kafka().Client(...)
// builds without I/O, so no cluster boots — the failure is purely a
// payload-validation failure on the canonicalising serializer.
func (r *Tests) SchemaRegistryJsonserializeRejectsMalformedInput(ctx context.Context) error {
	if r.schemaRegistryJsonserializeRejectsMalformedInput != nil {
		return nil
	}
	q := r.query.Select("schemaRegistryJsonserializeRejectsMalformedInput")

	return q.Execute(ctx)
}

// TestsSchemaRegistryPlaintextConsumeUnframedOpts contains options for Tests.SchemaRegistryPlaintextConsumeUnframed
type TestsSchemaRegistryPlaintextConsumeUnframedOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// SchemaRegistryPlaintextConsumeUnframed verifies the negative path: a record
// produced without framing, consumed with schemaRegistryAware=true, must
// surface ValueSchemaID=0 and pass the value bytes through unchanged.
func (r *Tests) SchemaRegistryPlaintextConsumeUnframed(ctx context.Context, opts ...TestsSchemaRegistryPlaintextConsumeUnframedOpts) error {
	if r.schemaRegistryPlaintextConsumeUnframed != nil {
		return nil
	}
	q := r.query.Select("schemaRegistryPlaintextConsumeUnframed")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsSchemaRegistryRegisterLookupRoundTripOpts contains options for Tests.SchemaRegistryRegisterLookupRoundTrip
type TestsSchemaRegistryRegisterLookupRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// SchemaRegistryRegisterLookupRoundTrip is the PLAINTEXT happy-path test
// for Kafka.ConfluentSchemaRegistry: stand a cp-schema-registry up next to
// a fresh cluster, then exercise register → lookup-by-id →
// lookup-latest-by-subject → list-subjects → set/get-compatibility →
// delete against it — covering every SchemaRegistryClient operation.
func (r *Tests) SchemaRegistryRegisterLookupRoundTrip(ctx context.Context, opts ...TestsSchemaRegistryRegisterLookupRoundTripOpts) error {
	if r.schemaRegistryRegisterLookupRoundTrip != nil {
		return nil
	}
	q := r.query.Select("schemaRegistryRegisterLookupRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsSchemaRegistryRejectsClusterModeMismatchOpts contains options for Tests.SchemaRegistryRejectsClusterModeMismatch
type TestsSchemaRegistryRejectsClusterModeMismatchOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// SchemaRegistryRejectsClusterModeMismatch pins the mode-match contract: a
// registry's security profile must match its backing cluster's client-listener
// mode, so the registry's kafka-storage connection authenticates against the
// broker. Here a PLAINTEXT registry security is paired with a TLS cluster and
// must be rejected with an error naming both modes. The constructor errors
// before any service boots, so the TLS cluster never has to start.
func (r *Tests) SchemaRegistryRejectsClusterModeMismatch(ctx context.Context, opts ...TestsSchemaRegistryRejectsClusterModeMismatchOpts) error {
	if r.schemaRegistryRejectsClusterModeMismatch != nil {
		return nil
	}
	q := r.query.Select("schemaRegistryRejectsClusterModeMismatch")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsSingleNodeClusterStartsOpts contains options for Tests.SingleNodeClusterStarts
type TestsSingleNodeClusterStartsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// SingleNodeClusterStarts spins up the smallest split-role cluster (one
// controller + one broker) and forces the server-side Cluster constructor
// to run by resolving BootstrapServers, asserting only that the broker
// hostname is non-empty. End-to-end reachability is covered by sibling
// tests that exercise ListTopics / produce / consume.
func (r *Tests) SingleNodeClusterStarts(ctx context.Context, opts ...TestsSingleNodeClusterStartsOpts) error {
	if r.singleNodeClusterStarts != nil {
		return nil
	}
	q := r.query.Select("singleNodeClusterStarts")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsThreeControllerQuorumProduceConsumeOpts contains options for Tests.ThreeControllerQuorumProduceConsume
type TestsThreeControllerQuorumProduceConsumeOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// ThreeControllerQuorumProduceConsume stands up a real three-node KRaft
// controller quorum (plus one broker) and drives a produce → consume
// round-trip through it, mirroring the single-controller round-trip. A
// successful round-trip proves the three controllers formed a quorum and
// elected a leader over their (always-mTLS) CONTROLLER listeners — which in
// turn proves the internal CA minted a verifying leaf for every controller
// host and that all three discovered each other over session-wide DNS with
// no controller-to-controller WithServiceBinding. Cluster.Stop then tears
// down all three controller services plus the broker.
func (r *Tests) ThreeControllerQuorumProduceConsume(ctx context.Context, opts ...TestsThreeControllerQuorumProduceConsumeOpts) error {
	if r.threeControllerQuorumProduceConsume != nil {
		return nil
	}
	q := r.query.Select("threeControllerQuorumProduceConsume")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsTLSClientWithWrongCaFailsOpts contains options for Tests.TLSClientWithWrongCaFails
type TestsTLSClientWithWrongCaFailsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// TlsClientWithWrongCaFails verifies that pointing the client at a
// truststore for an unrelated CA fails the handshake — i.e. the broker is
// genuinely presenting a cert chained to its own CA, not skipping
// verification.
func (r *Tests) TLSClientWithWrongCaFails(ctx context.Context, opts ...TestsTLSClientWithWrongCaFailsOpts) error {
	if r.tlsClientWithWrongCaFails != nil {
		return nil
	}
	q := r.query.Select("tlsClientWithWrongCaFails")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsTLSClusterStartsOpts contains options for Tests.TLSClusterStarts
type TestsTLSClusterStartsOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// TlsClusterStarts forces the lazy Cluster construction to run under
// TlsServerSecurity and confirms BootstrapServers reports a non-empty,
// non-zero-port broker address. No client connection attempted — this
// proves caller's CA loads, leaf signing succeeds, the keystore mounts,
// and the broker doesn't crash on startup.
func (r *Tests) TLSClusterStarts(ctx context.Context, opts ...TestsTLSClusterStartsOpts) error {
	if r.tlsClusterStarts != nil {
		return nil
	}
	q := r.query.Select("tlsClusterStarts")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}

// TestsTLSRoundTripOpts contains options for Tests.TLSRoundTrip
type TestsTLSRoundTripOpts struct {

	// Default: "4.2.0"
	KafkaImageTag string
}

// TlsRoundTrip produces and consumes a single record over a TLS-only
// external listener with TlsClientSecurity holding the CA's truststore.
// Exercises: SAN matching the bootstrap address, kgo dialer + TLS, broker
// SSL listener, end-to-end encryption.
func (r *Tests) TLSRoundTrip(ctx context.Context, opts ...TestsTLSRoundTripOpts) error {
	if r.tlsRoundTrip != nil {
		return nil
	}
	q := r.query.Select("tlsRoundTrip")
	for i := len(opts) - 1; i >= 0; i-- {
		// `kafkaImageTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].KafkaImageTag) {
			q = q.Arg("kafkaImageTag", opts[i].KafkaImageTag)
		}
	}

	return q.Execute(ctx)
}
