// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

func (r *Query) Tests() *Tests { // tests (../../../../../:0:0)
	q := r.query.Select("tests")

	return &Tests{
		query: q,
	}
}

type Tests struct { // tests (../../../../../:0:0)
	query *querybuilder.Selection

	all                                     *Void
	bindsCollectorIntoFreshContainer        *Void
	conflictingComponentBodiesAreRejected   *Void
	contrib                                 *Void
	contribForwardsLogsToLoki               *Void
	core                                    *Void
	coreForwardsLogsToLoki                  *Void
	coreForwardsMetricsToMimir              *Void
	coreForwardsTracesToTempo               *Void
	customComponentBodyIsSpliced            *Void
	debugPipelineAcceptsOtlpPush            *Void
	id                                      *ID
	otlpExporterRejectsClientCertWithoutKey *Void
	otlpExporterTlsRendersCaFile            *Void
	rejectsInvalidComponentName             *Void
	rejectsUnknownPipelineSignal            *Void
	serviceWithoutPipelinesOrConfigFails    *Void
	sharedReceiverIsDedupedInRenderedYaml   *Void
	tls                                     *Void
	tlsPipelineForwardsToLoki               *Void
	validation                              *Void
	withMtlsRejectsWithoutClientCert        *Void
	withTlsAcceptsTlsRejectsPlaintext       *Void
	withTlsInjectsReceiverTls               *Void
}

func (r *Tests) WithGraphQLQuery(q *querybuilder.Selection) *Tests {
	return &Tests{
		query: q,
	}
}

// TestsAllOpts contains options for Tests.All
type TestsAllOpts struct {

	// Default: "0.130.1"
	CollectorTag string

	// Default: "3.4.1"
	LokiTag string

	// Default: "2.7.1"
	TempoTag string

	// Default: "2.15.1"
	MimirTag string

	Parallel int
}

// All runs every otel test inside this suite as a convenience for local
// `dagger call all` invocations. CI does NOT call All: each of the three
// sub-aggregators below (Validation, Core, Contrib) carries its own
// `+check` directive, so GH Actions schedules each onto its own runner
// in parallel — running All on top would double-bill the same work.
//
// collectorTag picks the otel/opentelemetry-collector{,-contrib} tag
// every spawned collector runs against; lokiTag/tempoTag/mimirTag pick
// the grafana/{loki,tempo,mimir} tags for the round-trip backends.
//
// parallel caps how many tests run concurrently. Defaults to 0 (unbounded).
func (r *Tests) All(ctx context.Context, opts ...TestsAllOpts) error {
	if r.all != nil {
		return nil
	}
	q := r.query.Select("all")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
		// `lokiTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].LokiTag) {
			q = q.Arg("lokiTag", opts[i].LokiTag)
		}
		// `tempoTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].TempoTag) {
			q = q.Arg("tempoTag", opts[i].TempoTag)
		}
		// `mimirTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].MimirTag) {
			q = q.Arg("mimirTag", opts[i].MimirTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// TestsBindsCollectorIntoFreshContainerOpts contains options for Tests.BindsCollectorIntoFreshContainer
type TestsBindsCollectorIntoFreshContainerOpts struct {

	// Default: "0.130.1"
	CollectorTag string
}

// BindsCollectorIntoFreshContainer asserts that a collector configured
// with the DebugPipeline can be reached on :4317 and :4318 from a
// vanilla alpine container via WithServiceBinding.
func (r *Tests) BindsCollectorIntoFreshContainer(ctx context.Context, opts ...TestsBindsCollectorIntoFreshContainerOpts) error {
	if r.bindsCollectorIntoFreshContainer != nil {
		return nil
	}
	q := r.query.Select("bindsCollectorIntoFreshContainer")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
	}

	return q.Execute(ctx)
}

// TestsConflictingComponentBodiesAreRejectedOpts contains options for Tests.ConflictingComponentBodiesAreRejected
type TestsConflictingComponentBodiesAreRejectedOpts struct {

	// Default: "0.130.1"
	CollectorTag string
}

// ConflictingComponentBodiesAreRejected asserts that wiring two
// distinct *CustomExporter instances sharing the same <kind>/<name>
// but with different bodies into the pipeline graph surfaces a
// non-nil error from the rendered-config path, rather than silently
// keeping whichever was registered first.
func (r *Tests) ConflictingComponentBodiesAreRejected(ctx context.Context, opts ...TestsConflictingComponentBodiesAreRejectedOpts) error {
	if r.conflictingComponentBodiesAreRejected != nil {
		return nil
	}
	q := r.query.Select("conflictingComponentBodiesAreRejected")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
	}

	return q.Execute(ctx)
}

// TestsContribOpts contains options for Tests.Contrib
type TestsContribOpts struct {

	// Default: "0.130.1"
	CollectorTag string

	// Default: "3.4.1"
	LokiTag string

	Parallel int
}

// Contrib runs the otel-collector-contrib-image round-trip tests.
func (r *Tests) Contrib(ctx context.Context, opts ...TestsContribOpts) error {
	if r.contrib != nil {
		return nil
	}
	q := r.query.Select("contrib")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
		// `lokiTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].LokiTag) {
			q = q.Arg("lokiTag", opts[i].LokiTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// TestsContribForwardsLogsToLokiOpts contains options for Tests.ContribForwardsLogsToLoki
type TestsContribForwardsLogsToLokiOpts struct {

	// Default: "0.130.1"
	CollectorTag string

	// Default: "3.4.1"
	LokiTag string
}

// ContribForwardsLogsToLoki — smoke check on the contrib distribution.
func (r *Tests) ContribForwardsLogsToLoki(ctx context.Context, opts ...TestsContribForwardsLogsToLokiOpts) error {
	if r.contribForwardsLogsToLoki != nil {
		return nil
	}
	q := r.query.Select("contribForwardsLogsToLoki")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
		// `lokiTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].LokiTag) {
			q = q.Arg("lokiTag", opts[i].LokiTag)
		}
	}

	return q.Execute(ctx)
}

// TestsCoreOpts contains options for Tests.Core
type TestsCoreOpts struct {

	// Default: "0.130.1"
	CollectorTag string

	// Default: "3.4.1"
	LokiTag string

	// Default: "2.7.1"
	TempoTag string

	// Default: "2.15.1"
	MimirTag string

	Parallel int
}

// Core runs the otel-collector core-image round-trip tests. Each test
// boots one Loki/Tempo/Mimir alongside the collector, so this group's
// per-runner footprint is the heaviest in the suite.
func (r *Tests) Core(ctx context.Context, opts ...TestsCoreOpts) error {
	if r.core != nil {
		return nil
	}
	q := r.query.Select("core")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
		// `lokiTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].LokiTag) {
			q = q.Arg("lokiTag", opts[i].LokiTag)
		}
		// `tempoTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].TempoTag) {
			q = q.Arg("tempoTag", opts[i].TempoTag)
		}
		// `mimirTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].MimirTag) {
			q = q.Arg("mimirTag", opts[i].MimirTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// TestsCoreForwardsLogsToLokiOpts contains options for Tests.CoreForwardsLogsToLoki
type TestsCoreForwardsLogsToLokiOpts struct {

	// Default: "0.130.1"
	CollectorTag string

	// Default: "3.4.1"
	LokiTag string
}

// CoreForwardsLogsToLoki asserts a Core collector forwards an OTLP/HTTP
// log push through to the grafana-stack Loki backend, where it is
// queryable via LogQL.
func (r *Tests) CoreForwardsLogsToLoki(ctx context.Context, opts ...TestsCoreForwardsLogsToLokiOpts) error {
	if r.coreForwardsLogsToLoki != nil {
		return nil
	}
	q := r.query.Select("coreForwardsLogsToLoki")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
		// `lokiTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].LokiTag) {
			q = q.Arg("lokiTag", opts[i].LokiTag)
		}
	}

	return q.Execute(ctx)
}

// TestsCoreForwardsMetricsToMimirOpts contains options for Tests.CoreForwardsMetricsToMimir
type TestsCoreForwardsMetricsToMimirOpts struct {

	// Default: "0.130.1"
	CollectorTag string

	// Default: "2.15.1"
	MimirTag string
}

// CoreForwardsMetricsToMimir asserts an OTLP/HTTP metric pushed to the
// collector lands in Mimir via the OTLP/HTTP exporter and is
// queryable via the Prometheus API.
func (r *Tests) CoreForwardsMetricsToMimir(ctx context.Context, opts ...TestsCoreForwardsMetricsToMimirOpts) error {
	if r.coreForwardsMetricsToMimir != nil {
		return nil
	}
	q := r.query.Select("coreForwardsMetricsToMimir")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
		// `mimirTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].MimirTag) {
			q = q.Arg("mimirTag", opts[i].MimirTag)
		}
	}

	return q.Execute(ctx)
}

// TestsCoreForwardsTracesToTempoOpts contains options for Tests.CoreForwardsTracesToTempo
type TestsCoreForwardsTracesToTempoOpts struct {

	// Default: "0.130.1"
	CollectorTag string

	// Default: "2.7.1"
	TempoTag string
}

// CoreForwardsTracesToTempo asserts an OTLP/HTTP trace pushed to the
// collector lands in Tempo via the OTLP/gRPC exporter.
func (r *Tests) CoreForwardsTracesToTempo(ctx context.Context, opts ...TestsCoreForwardsTracesToTempoOpts) error {
	if r.coreForwardsTracesToTempo != nil {
		return nil
	}
	q := r.query.Select("coreForwardsTracesToTempo")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
		// `tempoTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].TempoTag) {
			q = q.Arg("tempoTag", opts[i].TempoTag)
		}
	}

	return q.Execute(ctx)
}

// TestsCustomComponentBodyIsSplicedOpts contains options for Tests.CustomComponentBodyIsSpliced
type TestsCustomComponentBodyIsSplicedOpts struct {

	// Default: "0.130.1"
	CollectorTag string
}

// CustomComponentBodyIsSpliced asserts that a Custom* component's
// caller-supplied YAML body lands structurally under the rendered
// config (not as a quoted scalar).
func (r *Tests) CustomComponentBodyIsSpliced(ctx context.Context, opts ...TestsCustomComponentBodyIsSplicedOpts) error {
	if r.customComponentBodyIsSpliced != nil {
		return nil
	}
	q := r.query.Select("customComponentBodyIsSpliced")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
	}

	return q.Execute(ctx)
}

// TestsDebugPipelineAcceptsOtlpPushOpts contains options for Tests.DebugPipelineAcceptsOtlpPush
type TestsDebugPipelineAcceptsOtlpPushOpts struct {

	// Default: "0.130.1"
	CollectorTag string
}

// DebugPipelineAcceptsOtlpPush asserts a collector configured with
// DebugPipeline("logs") accepts an OTLP/HTTP log push without
// erroring (HTTP 200/204).
func (r *Tests) DebugPipelineAcceptsOtlpPush(ctx context.Context, opts ...TestsDebugPipelineAcceptsOtlpPushOpts) error {
	if r.debugPipelineAcceptsOtlpPush != nil {
		return nil
	}
	q := r.query.Select("debugPipelineAcceptsOtlpPush")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
	}

	return q.Execute(ctx)
}

// A unique identifier for this Tests.
func (r *Tests) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *Tests) XXX_GraphQLType() string {
	return "Tests"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *Tests) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *Tests) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *Tests) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *Tests) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = Tests{query: selectNode(dag.query, id, "Tests")}
	return nil
}

// OtlpExporterRejectsClientCertWithoutKey asserts the factories reject a
// client cert supplied without its key (and vice versa), since an mTLS
// identity needs both halves.
func (r *Tests) OtlpExporterRejectsClientCertWithoutKey(ctx context.Context) error {
	if r.otlpExporterRejectsClientCertWithoutKey != nil {
		return nil
	}
	q := r.query.Select("otlpExporterRejectsClientCertWithoutKey")

	return q.Execute(ctx)
}

// TestsOtlpExporterTLSRendersCaFileOpts contains options for Tests.OtlpExporterTLSRendersCaFile
type TestsOtlpExporterTLSRendersCaFileOpts struct {

	// Default: "0.130.1"
	CollectorTag string
}

// OtlpExporterTlsRendersCaFile asserts that supplying CaCert to
// OtlpHttpExporter renders a tls.ca_file pointing at the per-component
// mount path and drops the plaintext insecure flag.
func (r *Tests) OtlpExporterTLSRendersCaFile(ctx context.Context, opts ...TestsOtlpExporterTLSRendersCaFileOpts) error {
	if r.otlpExporterTlsRendersCaFile != nil {
		return nil
	}
	q := r.query.Select("otlpExporterTlsRendersCaFile")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
	}

	return q.Execute(ctx)
}

// RejectsInvalidComponentName asserts every component factory and the
// Custom* escape hatches reject empty / non-conforming names (and
// non-conforming kinds, where applicable) with a non-nil error.
func (r *Tests) RejectsInvalidComponentName(ctx context.Context) error {
	if r.rejectsInvalidComponentName != nil {
		return nil
	}
	q := r.query.Select("rejectsInvalidComponentName")

	return q.Execute(ctx)
}

// RejectsUnknownPipelineSignal asserts Otel.Pipeline rejects signals
// outside {logs, traces, metrics}, and Otel.DebugPipeline does the same.
func (r *Tests) RejectsUnknownPipelineSignal(ctx context.Context) error {
	if r.rejectsUnknownPipelineSignal != nil {
		return nil
	}
	q := r.query.Select("rejectsUnknownPipelineSignal")

	return q.Execute(ctx)
}

// TestsServiceWithoutPipelinesOrConfigFailsOpts contains options for Tests.ServiceWithoutPipelinesOrConfigFails
type TestsServiceWithoutPipelinesOrConfigFailsOpts struct {

	// Default: "0.130.1"
	CollectorTag string
}

// ServiceWithoutPipelinesOrConfigFails asserts that calling Service()
// on a collector with no pipelines and no override produces a
// container whose exec exits non-zero — the collector binary refuses
// to start without --config.
func (r *Tests) ServiceWithoutPipelinesOrConfigFails(ctx context.Context, opts ...TestsServiceWithoutPipelinesOrConfigFailsOpts) error {
	if r.serviceWithoutPipelinesOrConfigFails != nil {
		return nil
	}
	q := r.query.Select("serviceWithoutPipelinesOrConfigFails")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
	}

	return q.Execute(ctx)
}

// TestsSharedReceiverIsDedupedInRenderedYamlOpts contains options for Tests.SharedReceiverIsDedupedInRenderedYaml
type TestsSharedReceiverIsDedupedInRenderedYamlOpts struct {

	// Default: "0.130.1"
	CollectorTag string
}

// SharedReceiverIsDedupedInRenderedYaml asserts that wiring one
// receiver into three pipelines emits a single top-level
// receivers.otlp/primary entry rather than three.
func (r *Tests) SharedReceiverIsDedupedInRenderedYaml(ctx context.Context, opts ...TestsSharedReceiverIsDedupedInRenderedYamlOpts) error {
	if r.sharedReceiverIsDedupedInRenderedYaml != nil {
		return nil
	}
	q := r.query.Select("sharedReceiverIsDedupedInRenderedYaml")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
	}

	return q.Execute(ctx)
}

// TestsTLSOpts contains options for Tests.TLS
type TestsTLSOpts struct {

	// Default: "0.130.1"
	CollectorTag string

	// Default: "3.4.1"
	LokiTag string

	Parallel int
}

// Tls runs the otel TLS/mTLS suite: receiver-side TLS enforcement,
// exporter-side TLS rendering, and an end-to-end TLS pipeline hop that
// lands in Loki. Carries its own +check so CI schedules it on its own
// runner alongside Validation/Core/Contrib.
func (r *Tests) TLS(ctx context.Context, opts ...TestsTLSOpts) error {
	if r.tls != nil {
		return nil
	}
	q := r.query.Select("tls")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
		// `lokiTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].LokiTag) {
			q = q.Arg("lokiTag", opts[i].LokiTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// TestsTLSPipelineForwardsToLokiOpts contains options for Tests.TLSPipelineForwardsToLoki
type TestsTLSPipelineForwardsToLokiOpts struct {

	// Default: "0.130.1"
	CollectorTag string

	// Default: "3.4.1"
	LokiTag string
}

// TlsPipelineForwardsToLoki is the end-to-end TLS pipeline test: a
// plaintext edge collector forwards over a TLS hop (its OtlpHttpExporter
// pins the downstream CA) to a WithTls receiver collector, which relays
// the logs to Loki where they are queryable. Exercises exporter-side TLS
// (criterion 3) and a full TLS pipeline (criterion 4) with an observable
// sink.
func (r *Tests) TLSPipelineForwardsToLoki(ctx context.Context, opts ...TestsTLSPipelineForwardsToLokiOpts) error {
	if r.tlsPipelineForwardsToLoki != nil {
		return nil
	}
	q := r.query.Select("tlsPipelineForwardsToLoki")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
		// `lokiTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].LokiTag) {
			q = q.Arg("lokiTag", opts[i].LokiTag)
		}
	}

	return q.Execute(ctx)
}

// TestsValidationOpts contains options for Tests.Validation
type TestsValidationOpts struct {

	// Default: "0.130.1"
	CollectorTag string

	Parallel int
}

// Validation runs the otel validation + rendering tests that don't need
// a backend service stood up. Pure-unit + collector-only spin-ups.
func (r *Tests) Validation(ctx context.Context, opts ...TestsValidationOpts) error {
	if r.validation != nil {
		return nil
	}
	q := r.query.Select("validation")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
		// `parallel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Parallel) {
			q = q.Arg("parallel", opts[i].Parallel)
		}
	}

	return q.Execute(ctx)
}

// TestsWithMtlsRejectsWithoutClientCertOpts contains options for Tests.WithMtlsRejectsWithoutClientCert
type TestsWithMtlsRejectsWithoutClientCertOpts struct {

	// Default: "0.130.1"
	CollectorTag string
}

// WithMtlsRejectsWithoutClientCert asserts a WithTls+WithMtls collector
// accepts a push presenting a client cert signed by the trusted CA and
// rejects one that presents none.
func (r *Tests) WithMtlsRejectsWithoutClientCert(ctx context.Context, opts ...TestsWithMtlsRejectsWithoutClientCertOpts) error {
	if r.withMtlsRejectsWithoutClientCert != nil {
		return nil
	}
	q := r.query.Select("withMtlsRejectsWithoutClientCert")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
	}

	return q.Execute(ctx)
}

// TestsWithTLSAcceptsTLSRejectsPlaintextOpts contains options for Tests.WithTLSAcceptsTLSRejectsPlaintext
type TestsWithTLSAcceptsTLSRejectsPlaintextOpts struct {

	// Default: "0.130.1"
	CollectorTag string
}

// WithTlsAcceptsTlsRejectsPlaintext asserts a WithTls collector accepts an
// OTLP/HTTP push over TLS (client verifying against the issuing CA) and
// rejects a plaintext push on the same port.
func (r *Tests) WithTLSAcceptsTLSRejectsPlaintext(ctx context.Context, opts ...TestsWithTLSAcceptsTLSRejectsPlaintextOpts) error {
	if r.withTlsAcceptsTlsRejectsPlaintext != nil {
		return nil
	}
	q := r.query.Select("withTlsAcceptsTlsRejectsPlaintext")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
	}

	return q.Execute(ctx)
}

// TestsWithTLSInjectsReceiverTLSOpts contains options for Tests.WithTLSInjectsReceiverTLS
type TestsWithTLSInjectsReceiverTLSOpts struct {

	// Default: "0.130.1"
	CollectorTag string
}

// WithTlsInjectsReceiverTls asserts WithTls (and WithMtls) splice a `tls:`
// block referencing the fixed server cert/key (and client CA) mount paths
// into both the grpc and http protocols of every otlp receiver in the
// rendered config. Render-only: no service is stood up, so dummy material
// suffices.
func (r *Tests) WithTLSInjectsReceiverTLS(ctx context.Context, opts ...TestsWithTLSInjectsReceiverTLSOpts) error {
	if r.withTlsInjectsReceiverTls != nil {
		return nil
	}
	q := r.query.Select("withTlsInjectsReceiverTls")
	for i := len(opts) - 1; i >= 0; i-- {
		// `collectorTag` optional argument
		if !querybuilder.IsZeroValue(opts[i].CollectorTag) {
			q = q.Arg("collectorTag", opts[i].CollectorTag)
		}
	}

	return q.Execute(ctx)
}
