// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"
	"fmt"

	"dagger.io/dagger/querybuilder"
)

// The `QemuID` scalar type represents an identifier for an object of type Qemu.
type QemuID string // qemu (../../../../../daggerverse/qemu/main.go:29:6)

// The `QemuMachineID` scalar type represents an identifier for an object of type QemuMachine.
type QemuMachineID string // qemu (../../../../../daggerverse/qemu/machine.go:32:6)

// Retrieve the binding value, as type Qemu
func (r *Binding) AsQemu() *Qemu { // qemu (../../../../../daggerverse/qemu/main.go:29:6)
	q := r.query.Select("asQemu")

	return &Qemu{
		query: q,
	}
}

// Retrieve the binding value, as type QemuMachine
func (r *Binding) AsQemuMachine() *QemuMachine { // qemu (../../../../../daggerverse/qemu/machine.go:32:6)
	q := r.query.Select("asQemuMachine")

	return &QemuMachine{
		query: q,
	}
}

// Create or update a binding of type Qemu in the environment
func (r *Env) WithQemuInput(name string, value *Qemu, description string) *Env { // qemu (../../../../../daggerverse/qemu/main.go:29:6)
	assertNotNil("value", value)
	q := r.query.Select("withQemuInput")
	q = q.Arg("name", name)
	q = q.Arg("value", value)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Create or update a binding of type QemuMachine in the environment
func (r *Env) WithQemuMachineInput(name string, value *QemuMachine, description string) *Env { // qemu (../../../../../daggerverse/qemu/machine.go:32:6)
	assertNotNil("value", value)
	q := r.query.Select("withQemuMachineInput")
	q = q.Arg("name", name)
	q = q.Arg("value", value)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Declare a desired QemuMachine output to be assigned in the environment
func (r *Env) WithQemuMachineOutput(name string, description string) *Env { // qemu (../../../../../daggerverse/qemu/machine.go:32:6)
	q := r.query.Select("withQemuMachineOutput")
	q = q.Arg("name", name)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Declare a desired Qemu output to be assigned in the environment
func (r *Env) WithQemuOutput(name string, description string) *Env { // qemu (../../../../../daggerverse/qemu/main.go:29:6)
	q := r.query.Select("withQemuOutput")
	q = q.Arg("name", name)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Qemu is the root namespace for every exported function in this module.
// The machine constructors hang off *Qemu so the generated Dagger SDK
// surfaces them under `dag.Qemu().<Func>(...)`.
type Qemu struct { // qemu (../../../../../daggerverse/qemu/main.go:29:6)
	query *querybuilder.Selection

	id *QemuID
}

func (r *Qemu) WithGraphQLQuery(q *querybuilder.Selection) *Qemu {
	return &Qemu{
		query: q,
	}
}

// QemuDiskOpts contains options for Qemu.Disk
type QemuDiskOpts struct {

	// Default: AARCH_64
	Arch QemuArch // qemu (../../../../../daggerverse/qemu/machine.go:127:2)

	Machine string // qemu (../../../../../daggerverse/qemu/machine.go:129:2)

	CPU string // qemu (../../../../../daggerverse/qemu/machine.go:131:2)

	// Default: 1024
	MemoryMb int // qemu (../../../../../daggerverse/qemu/machine.go:133:2)

	// Default: TCG
	Accel QemuAccel // qemu (../../../../../daggerverse/qemu/machine.go:135:2)

	// Default: RAW
	Format QemuDiskFormat // qemu (../../../../../daggerverse/qemu/machine.go:137:2)

	// Default: VIRTIO
	Iface QemuDiskInterface // qemu (../../../../../daggerverse/qemu/machine.go:139:2)

	Bios *File // qemu (../../../../../daggerverse/qemu/machine.go:141:2)

	Cmdline string // qemu (../../../../../daggerverse/qemu/machine.go:143:2)

	TCPPorts []int // qemu (../../../../../daggerverse/qemu/machine.go:145:2)

	// Default: "docker.io"
	Registry string // qemu (../../../../../daggerverse/qemu/machine.go:147:2)

	Name string // qemu (../../../../../daggerverse/qemu/machine.go:149:2)
}

// Disk boots a guest from a bootable disk image as a long-running machine —
// the full-OS path. `format` / `iface` map to `-drive format=`/`if=`; an
// optional `bios` supplies firmware. Empty `machine` / `cpu` resolve to the
// per-arch default. Rejects a nil image and an unknown arch.
func (r *Qemu) Disk(image *File, opts ...QemuDiskOpts) *QemuMachine { // qemu (../../../../../daggerverse/qemu/machine.go:123:1)
	assertNotNil("image", image)
	q := r.query.Select("disk")
	for i := len(opts) - 1; i >= 0; i-- {
		// `arch` optional argument
		if !querybuilder.IsZeroValue(opts[i].Arch) {
			q = q.Arg("arch", opts[i].Arch)
		}
		// `machine` optional argument
		if !querybuilder.IsZeroValue(opts[i].Machine) {
			q = q.Arg("machine", opts[i].Machine)
		}
		// `cpu` optional argument
		if !querybuilder.IsZeroValue(opts[i].CPU) {
			q = q.Arg("cpu", opts[i].CPU)
		}
		// `memoryMb` optional argument
		if !querybuilder.IsZeroValue(opts[i].MemoryMb) {
			q = q.Arg("memoryMb", opts[i].MemoryMb)
		}
		// `accel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Accel) {
			q = q.Arg("accel", opts[i].Accel)
		}
		// `format` optional argument
		if !querybuilder.IsZeroValue(opts[i].Format) {
			q = q.Arg("format", opts[i].Format)
		}
		// `iface` optional argument
		if !querybuilder.IsZeroValue(opts[i].Iface) {
			q = q.Arg("iface", opts[i].Iface)
		}
		// `bios` optional argument
		if !querybuilder.IsZeroValue(opts[i].Bios) {
			q = q.Arg("bios", opts[i].Bios)
		}
		// `cmdline` optional argument
		if !querybuilder.IsZeroValue(opts[i].Cmdline) {
			q = q.Arg("cmdline", opts[i].Cmdline)
		}
		// `tcpPorts` optional argument
		if !querybuilder.IsZeroValue(opts[i].TCPPorts) {
			q = q.Arg("tcpPorts", opts[i].TCPPorts)
		}
		// `registry` optional argument
		if !querybuilder.IsZeroValue(opts[i].Registry) {
			q = q.Arg("registry", opts[i].Registry)
		}
		// `name` optional argument
		if !querybuilder.IsZeroValue(opts[i].Name) {
			q = q.Arg("name", opts[i].Name)
		}
	}
	q = q.Arg("image", image)

	return &QemuMachine{
		query: q,
	}
}

// A unique identifier for this Qemu.
func (r *Qemu) ID(ctx context.Context) (QemuID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response QemuID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *Qemu) XXX_GraphQLType() string {
	return "Qemu"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *Qemu) XXX_GraphQLIDType() string {
	return "QemuID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *Qemu) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *Qemu) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *Qemu) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = *dag.LoadQemuFromID(QemuID(id))
	return nil
}

// QemuLinuxOpts contains options for Qemu.Linux
type QemuLinuxOpts struct {
	Dtb *File // qemu (../../../../../daggerverse/qemu/machine.go:60:2)

	Initrd *File // qemu (../../../../../daggerverse/qemu/machine.go:62:2)

	Rootfs *File // qemu (../../../../../daggerverse/qemu/machine.go:64:2)

	// Default: AARCH_64
	Arch QemuArch // qemu (../../../../../daggerverse/qemu/machine.go:66:2)

	Machine string // qemu (../../../../../daggerverse/qemu/machine.go:68:2)

	CPU string // qemu (../../../../../daggerverse/qemu/machine.go:70:2)

	// Default: 512
	MemoryMb int // qemu (../../../../../daggerverse/qemu/machine.go:72:2)

	// Default: TCG
	Accel QemuAccel // qemu (../../../../../daggerverse/qemu/machine.go:74:2)

	Cmdline string // qemu (../../../../../daggerverse/qemu/machine.go:76:2)

	TCPPorts []int // qemu (../../../../../daggerverse/qemu/machine.go:78:2)

	// Default: "docker.io"
	Registry string // qemu (../../../../../daggerverse/qemu/machine.go:80:2)

	Name string // qemu (../../../../../daggerverse/qemu/machine.go:82:2)
}

// Linux boots a guest from a raw kernel (plus optional dtb / initrd / rootfs)
// — the firmware / SBC path. `arch` selects the qemu-system-<arch> binary;
// empty `machine` / `cpu` resolve to the per-arch default (AARCH64 => virt +
// cortex-a53). Each `tcpPorts` entry is forwarded `hostfwd=tcp::P-:P` and
// exposed at the same number. Rejects a nil kernel and an unknown arch.
//
// Session-cached on `name` so parallel callers get independent backing
// services; every *Machine method is never-cached so each Run / Service
// re-executes. Pass a unique `name` per parallel test for isolation.
func (r *Qemu) Linux(kernel *File, opts ...QemuLinuxOpts) *QemuMachine { // qemu (../../../../../daggerverse/qemu/machine.go:56:1)
	assertNotNil("kernel", kernel)
	q := r.query.Select("linux")
	for i := len(opts) - 1; i >= 0; i-- {
		// `dtb` optional argument
		if !querybuilder.IsZeroValue(opts[i].Dtb) {
			q = q.Arg("dtb", opts[i].Dtb)
		}
		// `initrd` optional argument
		if !querybuilder.IsZeroValue(opts[i].Initrd) {
			q = q.Arg("initrd", opts[i].Initrd)
		}
		// `rootfs` optional argument
		if !querybuilder.IsZeroValue(opts[i].Rootfs) {
			q = q.Arg("rootfs", opts[i].Rootfs)
		}
		// `arch` optional argument
		if !querybuilder.IsZeroValue(opts[i].Arch) {
			q = q.Arg("arch", opts[i].Arch)
		}
		// `machine` optional argument
		if !querybuilder.IsZeroValue(opts[i].Machine) {
			q = q.Arg("machine", opts[i].Machine)
		}
		// `cpu` optional argument
		if !querybuilder.IsZeroValue(opts[i].CPU) {
			q = q.Arg("cpu", opts[i].CPU)
		}
		// `memoryMb` optional argument
		if !querybuilder.IsZeroValue(opts[i].MemoryMb) {
			q = q.Arg("memoryMb", opts[i].MemoryMb)
		}
		// `accel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Accel) {
			q = q.Arg("accel", opts[i].Accel)
		}
		// `cmdline` optional argument
		if !querybuilder.IsZeroValue(opts[i].Cmdline) {
			q = q.Arg("cmdline", opts[i].Cmdline)
		}
		// `tcpPorts` optional argument
		if !querybuilder.IsZeroValue(opts[i].TCPPorts) {
			q = q.Arg("tcpPorts", opts[i].TCPPorts)
		}
		// `registry` optional argument
		if !querybuilder.IsZeroValue(opts[i].Registry) {
			q = q.Arg("registry", opts[i].Registry)
		}
		// `name` optional argument
		if !querybuilder.IsZeroValue(opts[i].Name) {
			q = q.Arg("name", opts[i].Name)
		}
	}
	q = q.Arg("kernel", kernel)

	return &QemuMachine{
		query: q,
	}
}

// Machine is a configured-but-not-necessarily-running QEMU guest. It carries
// both drive modes: a long-running *dagger.Service (mode A, for OS images
// reached over forwarded ports) and a finite run argv replayed per call (mode
// B, for firmware that prints to serial and powers off). See drive.go.
type QemuMachine struct { // qemu (../../../../../daggerverse/qemu/machine.go:32:6)
	query *querybuilder.Selection

	endpoint    *string
	host        *string
	id          *QemuMachineID
	run         *string
	stop        *Void
	waitForLine *string
}

func (r *QemuMachine) WithGraphQLQuery(q *querybuilder.Selection) *QemuMachine {
	return &QemuMachine{
		query: q,
	}
}

// Bind attaches the guest service to ctr under the machine's hostname so ctr
// can dial the forwarded ports at `Host()`.
func (r *QemuMachine) Bind(ctr *Container) *Container { // qemu (../../../../../daggerverse/qemu/drive.go:51:1)
	assertNotNil("ctr", ctr)
	q := r.query.Select("bind")
	q = q.Arg("ctr", ctr)

	return &Container{
		query: q,
	}
}

// Endpoint returns `host:port` for a forwarded TCP port, erroring if the port
// was not in the machine's `tcpPorts`.
func (r *QemuMachine) Endpoint(ctx context.Context, port int) (string, error) { // qemu (../../../../../daggerverse/qemu/drive.go:30:1)
	if r.endpoint != nil {
		return *r.endpoint, nil
	}
	q := r.query.Select("endpoint")
	q = q.Arg("port", port)

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// Host returns the per-machine hostname the backing service is reachable at.
func (r *QemuMachine) Host(ctx context.Context) (string, error) { // qemu (../../../../../daggerverse/qemu/drive.go:22:1)
	if r.host != nil {
		return *r.host, nil
	}
	q := r.query.Select("host")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// A unique identifier for this QemuMachine.
func (r *QemuMachine) ID(ctx context.Context) (QemuMachineID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response QemuMachineID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *QemuMachine) XXX_GraphQLType() string {
	return "QemuMachine"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *QemuMachine) XXX_GraphQLIDType() string {
	return "QemuMachineID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *QemuMachine) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *QemuMachine) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *QemuMachine) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = *dag.LoadQemuMachineFromID(QemuMachineID(id))
	return nil
}

// QemuMachineRunOpts contains options for QemuMachine.Run
type QemuMachineRunOpts struct {

	// Default: 300
	TimeoutSeconds int // qemu (../../../../../daggerverse/qemu/drive.go:80:2)
}

// Run boots the guest to completion (`-no-reboot`) and returns the captured
// serial console. A guest that powers off exits before `timeoutSeconds`; one
// that doesn't is killed at the deadline and whatever serial it produced is
// returned.
func (r *QemuMachine) Run(ctx context.Context, opts ...QemuMachineRunOpts) (string, error) { // qemu (../../../../../daggerverse/qemu/drive.go:77:1)
	if r.run != nil {
		return *r.run, nil
	}
	q := r.query.Select("run")
	for i := len(opts) - 1; i >= 0; i-- {
		// `timeoutSeconds` optional argument
		if !querybuilder.IsZeroValue(opts[i].TimeoutSeconds) {
			q = q.Arg("timeoutSeconds", opts[i].TimeoutSeconds)
		}
	}

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// QemuMachineSerialLogOpts contains options for QemuMachine.SerialLog
type QemuMachineSerialLogOpts struct {

	// Default: 300
	TimeoutSeconds int // qemu (../../../../../daggerverse/qemu/drive.go:112:2)
}

// SerialLog runs the guest and materializes the captured serial console as a
// *dagger.File via the module workdir — no helper container.
func (r *QemuMachine) SerialLog(opts ...QemuMachineSerialLogOpts) *File { // qemu (../../../../../daggerverse/qemu/drive.go:109:1)
	q := r.query.Select("serialLog")
	for i := len(opts) - 1; i >= 0; i-- {
		// `timeoutSeconds` optional argument
		if !querybuilder.IsZeroValue(opts[i].TimeoutSeconds) {
			q = q.Arg("timeoutSeconds", opts[i].TimeoutSeconds)
		}
	}

	return &File{
		query: q,
	}
}

// Service returns the long-running guest as a *dagger.Service. Consumers can
// bind it (see Bind) and reach forwarded ports at Endpoint.
func (r *QemuMachine) Service() *Service { // qemu (../../../../../daggerverse/qemu/drive.go:43:1)
	q := r.query.Select("service")

	return &Service{
		query: q,
	}
}

// Stop tears down the backing service. Tests should defer this so the service
// span closes when the test returns. SIGKILL skips graceful shutdown.
func (r *QemuMachine) Stop(ctx context.Context) error { // qemu (../../../../../daggerverse/qemu/drive.go:59:1)
	if r.stop != nil {
		return nil
	}
	q := r.query.Select("stop")

	return q.Execute(ctx)
}

// QemuMachineWaitForLineOpts contains options for QemuMachine.WaitForLine
type QemuMachineWaitForLineOpts struct {

	// Default: 300
	TimeoutSeconds int // qemu (../../../../../daggerverse/qemu/drive.go:93:2)
}

// WaitForLine runs the guest and returns the serial console only if it
// contains substr, erroring otherwise.
func (r *QemuMachine) WaitForLine(ctx context.Context, substr string, opts ...QemuMachineWaitForLineOpts) (string, error) { // qemu (../../../../../daggerverse/qemu/drive.go:89:1)
	if r.waitForLine != nil {
		return *r.waitForLine, nil
	}
	q := r.query.Select("waitForLine")
	for i := len(opts) - 1; i >= 0; i-- {
		// `timeoutSeconds` optional argument
		if !querybuilder.IsZeroValue(opts[i].TimeoutSeconds) {
			q = q.Arg("timeoutSeconds", opts[i].TimeoutSeconds)
		}
	}
	q = q.Arg("substr", substr)

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// Load a Qemu from its ID.
func (r *Query) LoadQemuFromID(id QemuID) *Qemu { // qemu (../../../../../daggerverse/qemu/main.go:29:6)
	q := r.query.Select("loadQemuFromID")
	q = q.Arg("id", id)

	return &Qemu{
		query: q,
	}
}

// Load a QemuMachine from its ID.
func (r *Query) LoadQemuMachineFromID(id QemuMachineID) *QemuMachine { // qemu (../../../../../daggerverse/qemu/machine.go:32:6)
	q := r.query.Select("loadQemuMachineFromID")
	q = q.Arg("id", id)

	return &QemuMachine{
		query: q,
	}
}

// Qemu is the root namespace for every exported function in this module.
// The machine constructors hang off *Qemu so the generated Dagger SDK
// surfaces them under `dag.Qemu().<Func>(...)`.
func (r *Query) Qemu() *Qemu { // qemu (../../../../../daggerverse/qemu/main.go:29:6)
	q := r.query.Select("qemu")

	return &Qemu{
		query: q,
	}
}

// Accel is the QEMU acceleration mode. TCG is pure-software emulation and
// needs no host support; KVM requires an engine exposing /dev/kvm, which
// this module does not provision.
type QemuAccel string // qemu (../../../../../daggerverse/qemu/enums.go:32:6)

func (QemuAccel) IsEnum() {}

func (v QemuAccel) Name() string {
	switch v {
	case QemuAccelKvm:
		return "KVM"
	case QemuAccelTcg:
		return "TCG"
	default:
		return ""
	}
}

func (v QemuAccel) Value() string {
	return string(v)
}

func (v *QemuAccel) MarshalJSON() ([]byte, error) {
	if *v == "" {
		return []byte(`""`), nil
	}
	name := v.Name()
	if name == "" {
		return nil, fmt.Errorf("invalid enum value %q", *v)
	}
	return json.Marshal(name)
}

func (v *QemuAccel) UnmarshalJSON(dt []byte) error {
	var s string
	if err := json.Unmarshal(dt, &s); err != nil {
		return err
	}
	switch s {
	case "":
		*v = ""
	case "KVM":
		*v = QemuAccelKvm
	case "TCG":
		*v = QemuAccelTcg
	default:
		return fmt.Errorf("invalid enum value %q", s)
	}
	return nil
}

const (
	QemuAccelKvm QemuAccel = "KVM" // qemu (../../../../../daggerverse/qemu/enums.go:36:2)

	QemuAccelTcg QemuAccel = "TCG" // qemu (../../../../../daggerverse/qemu/enums.go:35:2)

)

// Arch is the guest CPU architecture QEMU emulates. The enum value (e.g.
// "AARCH64") maps to QEMU's lowercase token internally (`qemu-system-aarch64`);
// invalid architectures are unrepresentable through the SDK.
//
// Note on rendered names: the Dagger Go SDK derives each GraphQL enum member
// from the *constant identifier* in SCREAMING_SNAKE_CASE and inserts an
// underscore at every letter↔digit boundary, so these surface as `AARCH_64`,
// `RISCV_64`, `X_86_64`, etc. The underscores are unavoidable SDK behavior; the
// internal Value() (the string literal below) is what drives the qemu mapping.
type QemuArch string // qemu (../../../../../daggerverse/qemu/enums.go:14:6)

func (QemuArch) IsEnum() {}

func (v QemuArch) Name() string {
	switch v {
	case QemuArchAarch64:
		return "AARCH_64"
	case QemuArchArm:
		return "ARM"
	case QemuArchI386:
		return "I_386"
	case QemuArchMips:
		return "MIPS"
	case QemuArchMipsel:
		return "MIPSEL"
	case QemuArchPpc:
		return "PPC"
	case QemuArchPpc64:
		return "PPC_64"
	case QemuArchRiscv32:
		return "RISCV_32"
	case QemuArchRiscv64:
		return "RISCV_64"
	case QemuArchX8664:
		return "X_86_64"
	default:
		return ""
	}
}

func (v QemuArch) Value() string {
	return string(v)
}

func (v *QemuArch) MarshalJSON() ([]byte, error) {
	if *v == "" {
		return []byte(`""`), nil
	}
	name := v.Name()
	if name == "" {
		return nil, fmt.Errorf("invalid enum value %q", *v)
	}
	return json.Marshal(name)
}

func (v *QemuArch) UnmarshalJSON(dt []byte) error {
	var s string
	if err := json.Unmarshal(dt, &s); err != nil {
		return err
	}
	switch s {
	case "":
		*v = ""
	case "AARCH_64":
		*v = QemuArchAarch64
	case "ARM":
		*v = QemuArchArm
	case "I_386":
		*v = QemuArchI386
	case "MIPS":
		*v = QemuArchMips
	case "MIPSEL":
		*v = QemuArchMipsel
	case "PPC":
		*v = QemuArchPpc
	case "PPC_64":
		*v = QemuArchPpc64
	case "RISCV_32":
		*v = QemuArchRiscv32
	case "RISCV_64":
		*v = QemuArchRiscv64
	case "X_86_64":
		*v = QemuArchX8664
	default:
		return fmt.Errorf("invalid enum value %q", s)
	}
	return nil
}

const (
	QemuArchAarch64 QemuArch = "AARCH64" // qemu (../../../../../daggerverse/qemu/enums.go:19:2)

	QemuArchArm QemuArch = "ARM" // qemu (../../../../../daggerverse/qemu/enums.go:20:2)

	QemuArchI386 QemuArch = "I386" // qemu (../../../../../daggerverse/qemu/enums.go:18:2)

	QemuArchMips QemuArch = "MIPS" // qemu (../../../../../daggerverse/qemu/enums.go:23:2)

	QemuArchMipsel QemuArch = "MIPSEL" // qemu (../../../../../daggerverse/qemu/enums.go:24:2)

	QemuArchPpc QemuArch = "PPC" // qemu (../../../../../daggerverse/qemu/enums.go:25:2)

	QemuArchPpc64 QemuArch = "PPC64" // qemu (../../../../../daggerverse/qemu/enums.go:26:2)

	QemuArchRiscv32 QemuArch = "RISCV32" // qemu (../../../../../daggerverse/qemu/enums.go:22:2)

	QemuArchRiscv64 QemuArch = "RISCV64" // qemu (../../../../../daggerverse/qemu/enums.go:21:2)

	QemuArchX8664 QemuArch = "X86_64" // qemu (../../../../../daggerverse/qemu/enums.go:17:2)

)

// DiskFormat is the on-disk image format passed to `-drive format=`.
type QemuDiskFormat string // qemu (../../../../../daggerverse/qemu/enums.go:40:6)

func (QemuDiskFormat) IsEnum() {}

func (v QemuDiskFormat) Name() string {
	switch v {
	case QemuDiskFormatQcow2:
		return "QCOW_2"
	case QemuDiskFormatRaw:
		return "RAW"
	default:
		return ""
	}
}

func (v QemuDiskFormat) Value() string {
	return string(v)
}

func (v *QemuDiskFormat) MarshalJSON() ([]byte, error) {
	if *v == "" {
		return []byte(`""`), nil
	}
	name := v.Name()
	if name == "" {
		return nil, fmt.Errorf("invalid enum value %q", *v)
	}
	return json.Marshal(name)
}

func (v *QemuDiskFormat) UnmarshalJSON(dt []byte) error {
	var s string
	if err := json.Unmarshal(dt, &s); err != nil {
		return err
	}
	switch s {
	case "":
		*v = ""
	case "QCOW_2":
		*v = QemuDiskFormatQcow2
	case "RAW":
		*v = QemuDiskFormatRaw
	default:
		return fmt.Errorf("invalid enum value %q", s)
	}
	return nil
}

const (
	QemuDiskFormatQcow2 QemuDiskFormat = "QCOW2" // qemu (../../../../../daggerverse/qemu/enums.go:44:2)

	QemuDiskFormatRaw QemuDiskFormat = "RAW" // qemu (../../../../../daggerverse/qemu/enums.go:43:2)

)

// DiskInterface is the guest-visible disk bus passed to `-drive if=`.
type QemuDiskInterface string // qemu (../../../../../daggerverse/qemu/enums.go:48:6)

func (QemuDiskInterface) IsEnum() {}

func (v QemuDiskInterface) Name() string {
	switch v {
	case QemuDiskInterfaceIde:
		return "IDE"
	case QemuDiskInterfaceNvme:
		return "NVME"
	case QemuDiskInterfaceSd:
		return "SD"
	case QemuDiskInterfaceVirtio:
		return "VIRTIO"
	default:
		return ""
	}
}

func (v QemuDiskInterface) Value() string {
	return string(v)
}

func (v *QemuDiskInterface) MarshalJSON() ([]byte, error) {
	if *v == "" {
		return []byte(`""`), nil
	}
	name := v.Name()
	if name == "" {
		return nil, fmt.Errorf("invalid enum value %q", *v)
	}
	return json.Marshal(name)
}

func (v *QemuDiskInterface) UnmarshalJSON(dt []byte) error {
	var s string
	if err := json.Unmarshal(dt, &s); err != nil {
		return err
	}
	switch s {
	case "":
		*v = ""
	case "IDE":
		*v = QemuDiskInterfaceIde
	case "NVME":
		*v = QemuDiskInterfaceNvme
	case "SD":
		*v = QemuDiskInterfaceSd
	case "VIRTIO":
		*v = QemuDiskInterfaceVirtio
	default:
		return fmt.Errorf("invalid enum value %q", s)
	}
	return nil
}

const (
	QemuDiskInterfaceIde QemuDiskInterface = "IDE" // qemu (../../../../../daggerverse/qemu/enums.go:52:2)

	QemuDiskInterfaceNvme QemuDiskInterface = "NVME" // qemu (../../../../../daggerverse/qemu/enums.go:54:2)

	QemuDiskInterfaceSd QemuDiskInterface = "SD" // qemu (../../../../../daggerverse/qemu/enums.go:53:2)

	QemuDiskInterfaceVirtio QemuDiskInterface = "VIRTIO" // qemu (../../../../../daggerverse/qemu/enums.go:51:2)

)
