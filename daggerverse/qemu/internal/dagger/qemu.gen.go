// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"
	"fmt"

	"github.com/dagger/querybuilder"
)

// Qemu is the root namespace for every exported function in this module.
// The machine constructors hang off *Qemu so the generated Dagger SDK
// surfaces them under `dag.Qemu().<Func>(...)`.
type Qemu struct { // qemu (../../../../:0:0)
	query *querybuilder.Selection

	id *ID
}

func (r *Qemu) WithGraphQLQuery(q *querybuilder.Selection) *Qemu {
	return &Qemu{
		query: q,
	}
}

// QemuBareMetalOpts contains options for Qemu.BareMetal
type QemuBareMetalOpts struct {

	// Default: "Arm"
	Arch QemuArch

	Machine string

	CPU string

	// Default: 16
	MemoryMb int

	DisableSemihosting bool

	Cmdline string

	// Default: "docker.io"
	Registry string

	Name string
}

// BareMetal boots a bare-metal microcontroller firmware directly — no Linux
// kernel, initrd, rootfs, or device tree — on an MCU-class machine, with
// ARM/RISC-V semihosting enabled. This is the off-device unit-test path for
// embedded firmware: a test build can write to the host console (semihosting
// SYS_WRITE0) and report pass/fail as a process exit code (semihosting
// SYS_EXIT) via Machine.RunStatus — neither of which the kernel-oriented Linux
// path can surface (it captures serial text only).
//
// `arch` selects the qemu-system-<arch> binary and an MCU-class machine
// default distinct from the SoC defaults Linux/Disk use (ARM => lm3s6965evb +
// cortex-m3, RISC-V => virt); an explicit `machine` / `cpu` overrides it.
// Acceleration is always TCG — MCU targets have no KVM analog. Semihosting is
// on by default (`-semihosting-config enable=on,target=native` lets the guest's
// semihosting calls reach the host so SYS_EXIT maps to the QEMU process exit
// code); pass `disableSemihosting` for firmware that drives a real UART instead
// and wants neither the host console route nor SYS_EXIT wiring. The option is
// inverted so the Go SDK can actually turn semihosting off — a `bool can't be set false through the generated bindings (false is the zero
// value and is dropped). Rejects a nil firmware and an arch with no bare-metal
// profile.
//
// Session-cached on `name` like Linux/Disk; every *Machine method is
// never-cached so each Run / RunStatus re-executes.
func (r *Qemu) BareMetal(firmware *File, opts ...QemuBareMetalOpts) *QemuMachine {
	assertNotNil("firmware", firmware)
	q := r.query.Select("bareMetal")
	for i := len(opts) - 1; i >= 0; i-- {
		// `arch` optional argument
		if !querybuilder.IsZeroValue(opts[i].Arch) {
			q = q.Arg("arch", opts[i].Arch)
		}
		// `machine` optional argument
		if !querybuilder.IsZeroValue(opts[i].Machine) {
			q = q.Arg("machine", opts[i].Machine)
		}
		// `cpu` optional argument
		if !querybuilder.IsZeroValue(opts[i].CPU) {
			q = q.Arg("cpu", opts[i].CPU)
		}
		// `memoryMb` optional argument
		if !querybuilder.IsZeroValue(opts[i].MemoryMb) {
			q = q.Arg("memoryMb", opts[i].MemoryMb)
		}
		// `disableSemihosting` optional argument
		if !querybuilder.IsZeroValue(opts[i].DisableSemihosting) {
			q = q.Arg("disableSemihosting", opts[i].DisableSemihosting)
		}
		// `cmdline` optional argument
		if !querybuilder.IsZeroValue(opts[i].Cmdline) {
			q = q.Arg("cmdline", opts[i].Cmdline)
		}
		// `registry` optional argument
		if !querybuilder.IsZeroValue(opts[i].Registry) {
			q = q.Arg("registry", opts[i].Registry)
		}
		// `name` optional argument
		if !querybuilder.IsZeroValue(opts[i].Name) {
			q = q.Arg("name", opts[i].Name)
		}
	}
	q = q.Arg("firmware", firmware)

	return &QemuMachine{
		query: q,
	}
}

// QemuDiskOpts contains options for Qemu.Disk
type QemuDiskOpts struct {

	// Default: "Aarch64"
	Arch QemuArch

	Machine string

	CPU string

	// Default: 1024
	MemoryMb int

	// Default: "Tcg"
	Accel QemuAccel

	// Default: "Raw"
	Format QemuDiskFormat

	// Default: "Virtio"
	Iface QemuDiskInterface

	Bios *File

	Cmdline string

	TCPPorts []int

	// Default: "docker.io"
	Registry string

	Name string
}

// Disk boots a guest from a bootable disk image as a long-running machine —
// the full-OS path. `format` / `iface` map to `-drive format=`/`if=`; an
// optional `bios` supplies firmware. Empty `machine` / `cpu` resolve to the
// per-arch default. Rejects a nil image and an unknown arch.
func (r *Qemu) Disk(image *File, opts ...QemuDiskOpts) *QemuMachine {
	assertNotNil("image", image)
	q := r.query.Select("disk")
	for i := len(opts) - 1; i >= 0; i-- {
		// `arch` optional argument
		if !querybuilder.IsZeroValue(opts[i].Arch) {
			q = q.Arg("arch", opts[i].Arch)
		}
		// `machine` optional argument
		if !querybuilder.IsZeroValue(opts[i].Machine) {
			q = q.Arg("machine", opts[i].Machine)
		}
		// `cpu` optional argument
		if !querybuilder.IsZeroValue(opts[i].CPU) {
			q = q.Arg("cpu", opts[i].CPU)
		}
		// `memoryMb` optional argument
		if !querybuilder.IsZeroValue(opts[i].MemoryMb) {
			q = q.Arg("memoryMb", opts[i].MemoryMb)
		}
		// `accel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Accel) {
			q = q.Arg("accel", opts[i].Accel)
		}
		// `format` optional argument
		if !querybuilder.IsZeroValue(opts[i].Format) {
			q = q.Arg("format", opts[i].Format)
		}
		// `iface` optional argument
		if !querybuilder.IsZeroValue(opts[i].Iface) {
			q = q.Arg("iface", opts[i].Iface)
		}
		// `bios` optional argument
		if !querybuilder.IsZeroValue(opts[i].Bios) {
			q = q.Arg("bios", opts[i].Bios)
		}
		// `cmdline` optional argument
		if !querybuilder.IsZeroValue(opts[i].Cmdline) {
			q = q.Arg("cmdline", opts[i].Cmdline)
		}
		// `tcpPorts` optional argument
		if !querybuilder.IsZeroValue(opts[i].TCPPorts) {
			q = q.Arg("tcpPorts", opts[i].TCPPorts)
		}
		// `registry` optional argument
		if !querybuilder.IsZeroValue(opts[i].Registry) {
			q = q.Arg("registry", opts[i].Registry)
		}
		// `name` optional argument
		if !querybuilder.IsZeroValue(opts[i].Name) {
			q = q.Arg("name", opts[i].Name)
		}
	}
	q = q.Arg("image", image)

	return &QemuMachine{
		query: q,
	}
}

// A unique identifier for this Qemu.
func (r *Qemu) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *Qemu) XXX_GraphQLType() string {
	return "Qemu"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *Qemu) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *Qemu) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *Qemu) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *Qemu) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = Qemu{query: selectNode(dag.query, id, "Qemu")}
	return nil
}

// QemuLinuxOpts contains options for Qemu.Linux
type QemuLinuxOpts struct {
	Dtb *File

	Initrd *File

	Rootfs *File

	// Default: "Aarch64"
	Arch QemuArch

	Machine string

	CPU string

	// Default: 512
	MemoryMb int

	// Default: "Tcg"
	Accel QemuAccel

	Cmdline string

	TCPPorts []int

	// Default: "docker.io"
	Registry string

	Name string
}

// Linux boots a guest from a raw kernel (plus optional dtb / initrd / rootfs)
// — the firmware / SBC path. `arch` selects the qemu-system-<arch> binary;
// empty `machine` / `cpu` resolve to the per-arch default (AARCH64 => virt +
// cortex-a53). Each `tcpPorts` entry is forwarded `hostfwd=tcp::P-:P` and
// exposed at the same number. Rejects a nil kernel and an unknown arch.
//
// Session-cached on `name` so parallel callers get independent backing
// services; every *Machine method is never-cached so each Run / Service
// re-executes. Pass a unique `name` per parallel test for isolation.
func (r *Qemu) Linux(kernel *File, opts ...QemuLinuxOpts) *QemuMachine {
	assertNotNil("kernel", kernel)
	q := r.query.Select("linux")
	for i := len(opts) - 1; i >= 0; i-- {
		// `dtb` optional argument
		if !querybuilder.IsZeroValue(opts[i].Dtb) {
			q = q.Arg("dtb", opts[i].Dtb)
		}
		// `initrd` optional argument
		if !querybuilder.IsZeroValue(opts[i].Initrd) {
			q = q.Arg("initrd", opts[i].Initrd)
		}
		// `rootfs` optional argument
		if !querybuilder.IsZeroValue(opts[i].Rootfs) {
			q = q.Arg("rootfs", opts[i].Rootfs)
		}
		// `arch` optional argument
		if !querybuilder.IsZeroValue(opts[i].Arch) {
			q = q.Arg("arch", opts[i].Arch)
		}
		// `machine` optional argument
		if !querybuilder.IsZeroValue(opts[i].Machine) {
			q = q.Arg("machine", opts[i].Machine)
		}
		// `cpu` optional argument
		if !querybuilder.IsZeroValue(opts[i].CPU) {
			q = q.Arg("cpu", opts[i].CPU)
		}
		// `memoryMb` optional argument
		if !querybuilder.IsZeroValue(opts[i].MemoryMb) {
			q = q.Arg("memoryMb", opts[i].MemoryMb)
		}
		// `accel` optional argument
		if !querybuilder.IsZeroValue(opts[i].Accel) {
			q = q.Arg("accel", opts[i].Accel)
		}
		// `cmdline` optional argument
		if !querybuilder.IsZeroValue(opts[i].Cmdline) {
			q = q.Arg("cmdline", opts[i].Cmdline)
		}
		// `tcpPorts` optional argument
		if !querybuilder.IsZeroValue(opts[i].TCPPorts) {
			q = q.Arg("tcpPorts", opts[i].TCPPorts)
		}
		// `registry` optional argument
		if !querybuilder.IsZeroValue(opts[i].Registry) {
			q = q.Arg("registry", opts[i].Registry)
		}
		// `name` optional argument
		if !querybuilder.IsZeroValue(opts[i].Name) {
			q = q.Arg("name", opts[i].Name)
		}
	}
	q = q.Arg("kernel", kernel)

	return &QemuMachine{
		query: q,
	}
}

// Machine is a configured-but-not-necessarily-running QEMU guest. It carries
// both drive modes: a long-running *dagger.Service (mode A, for OS images
// reached over forwarded ports) and a finite run argv replayed per call (mode
// B, for firmware that prints to serial and powers off). See drive.go.
type QemuMachine struct { // qemu (../../../../:0:0)
	query *querybuilder.Selection

	endpoint    *string
	host        *string
	id          *ID
	run         *string
	stop        *Void
	waitForLine *string
}

func (r *QemuMachine) WithGraphQLQuery(q *querybuilder.Selection) *QemuMachine {
	return &QemuMachine{
		query: q,
	}
}

// Bind attaches the guest service to ctr under the machine's hostname so ctr
// can dial the forwarded ports at `Host()`.
func (r *QemuMachine) Bind(ctr *Container) *Container {
	assertNotNil("ctr", ctr)
	q := r.query.Select("bind")
	q = q.Arg("ctr", ctr)

	return &Container{
		query: q,
	}
}

// Endpoint returns `host:port` for a forwarded TCP port, erroring if the port
// was not in the machine's `tcpPorts`.
func (r *QemuMachine) Endpoint(ctx context.Context, port int) (string, error) {
	if r.endpoint != nil {
		return *r.endpoint, nil
	}
	q := r.query.Select("endpoint")
	q = q.Arg("port", port)

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// Host returns the per-machine hostname the backing service is reachable at.
func (r *QemuMachine) Host(ctx context.Context) (string, error) {
	if r.host != nil {
		return *r.host, nil
	}
	q := r.query.Select("host")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// A unique identifier for this QemuMachine.
func (r *QemuMachine) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *QemuMachine) XXX_GraphQLType() string {
	return "QemuMachine"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *QemuMachine) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *QemuMachine) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *QemuMachine) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *QemuMachine) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = QemuMachine{query: selectNode(dag.query, id, "QemuMachine")}
	return nil
}

// QemuMachineRunOpts contains options for QemuMachine.Run
type QemuMachineRunOpts struct {

	// Default: 300
	TimeoutSeconds int
}

// Run boots the guest to completion (`-no-reboot`) and returns the captured
// serial console. A guest that powers off exits before `timeoutSeconds`; one
// that doesn't is killed at the deadline and whatever serial it produced is
// returned.
func (r *QemuMachine) Run(ctx context.Context, opts ...QemuMachineRunOpts) (string, error) {
	if r.run != nil {
		return *r.run, nil
	}
	q := r.query.Select("run")
	for i := len(opts) - 1; i >= 0; i-- {
		// `timeoutSeconds` optional argument
		if !querybuilder.IsZeroValue(opts[i].TimeoutSeconds) {
			q = q.Arg("timeoutSeconds", opts[i].TimeoutSeconds)
		}
	}

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// QemuMachineRunStatusOpts contains options for QemuMachine.RunStatus
type QemuMachineRunStatusOpts struct {

	// Default: 300
	TimeoutSeconds int
}

// RunStatus boots the guest to completion like Run, but returns both the
// captured serial console and the guest exit code in a single boot — the
// bare-metal counterpart to Run, where the semihosting SYS_EXIT code carries
// pass/fail that serial text alone can't. Run / WaitForLine / SerialLog are
// unchanged and still return serial only.
func (r *QemuMachine) RunStatus(opts ...QemuMachineRunStatusOpts) *QemuRunResult {
	q := r.query.Select("runStatus")
	for i := len(opts) - 1; i >= 0; i-- {
		// `timeoutSeconds` optional argument
		if !querybuilder.IsZeroValue(opts[i].TimeoutSeconds) {
			q = q.Arg("timeoutSeconds", opts[i].TimeoutSeconds)
		}
	}

	return &QemuRunResult{
		query: q,
	}
}

// QemuMachineSerialLogOpts contains options for QemuMachine.SerialLog
type QemuMachineSerialLogOpts struct {

	// Default: 300
	TimeoutSeconds int
}

// SerialLog runs the guest and materializes the captured serial console as a
// *dagger.File via the module workdir — no helper container.
func (r *QemuMachine) SerialLog(opts ...QemuMachineSerialLogOpts) *File {
	q := r.query.Select("serialLog")
	for i := len(opts) - 1; i >= 0; i-- {
		// `timeoutSeconds` optional argument
		if !querybuilder.IsZeroValue(opts[i].TimeoutSeconds) {
			q = q.Arg("timeoutSeconds", opts[i].TimeoutSeconds)
		}
	}

	return &File{
		query: q,
	}
}

// Service returns the long-running guest as a *dagger.Service. Consumers can
// bind it (see Bind) and reach forwarded ports at Endpoint.
func (r *QemuMachine) Service() *Service {
	q := r.query.Select("service")

	return &Service{
		query: q,
	}
}

// Stop tears down the backing service. Tests should defer this so the service
// span closes when the test returns. SIGKILL skips graceful shutdown.
func (r *QemuMachine) Stop(ctx context.Context) error {
	if r.stop != nil {
		return nil
	}
	q := r.query.Select("stop")

	return q.Execute(ctx)
}

// QemuMachineWaitForLineOpts contains options for QemuMachine.WaitForLine
type QemuMachineWaitForLineOpts struct {

	// Default: 300
	TimeoutSeconds int
}

// WaitForLine runs the guest and returns the serial console only if it
// contains substr, erroring otherwise.
func (r *QemuMachine) WaitForLine(ctx context.Context, substr string, opts ...QemuMachineWaitForLineOpts) (string, error) {
	if r.waitForLine != nil {
		return *r.waitForLine, nil
	}
	q := r.query.Select("waitForLine")
	for i := len(opts) - 1; i >= 0; i-- {
		// `timeoutSeconds` optional argument
		if !querybuilder.IsZeroValue(opts[i].TimeoutSeconds) {
			q = q.Arg("timeoutSeconds", opts[i].TimeoutSeconds)
		}
	}
	q = q.Arg("substr", substr)

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// RunResult pairs the serial console output of a finite boot with the guest's
// exit code. On the bare-metal path the exit code is the semihosting SYS_EXIT
// value (0 = success); see Machine.RunStatus.
type QemuRunResult struct { // qemu (../../../../:0:0)
	query *querybuilder.Selection

	exitCode *int
	id       *ID
	output   *string
}

func (r *QemuRunResult) WithGraphQLQuery(q *querybuilder.Selection) *QemuRunResult {
	return &QemuRunResult{
		query: q,
	}
}

// ExitCode is the guest exit code (semihosting SYS_EXIT; 0 = success). A
// guest that doesn't power off before the deadline is killed and yields the
// timeout code (124) — see runSerialStatus for why it isn't the raw SIGKILL
// code (137).
func (r *QemuRunResult) ExitCode(ctx context.Context) (int, error) {
	if r.exitCode != nil {
		return *r.exitCode, nil
	}
	q := r.query.Select("exitCode")

	var response int

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// A unique identifier for this QemuRunResult.
func (r *QemuRunResult) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *QemuRunResult) XXX_GraphQLType() string {
	return "QemuRunResult"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *QemuRunResult) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *QemuRunResult) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *QemuRunResult) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *QemuRunResult) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = QemuRunResult{query: selectNode(dag.query, id, "QemuRunResult")}
	return nil
}

// Output is the captured serial console output.
func (r *QemuRunResult) Output(ctx context.Context) (string, error) {
	if r.output != nil {
		return *r.output, nil
	}
	q := r.query.Select("output")

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

func (r *Query) Qemu() *Qemu { // qemu (../../../../:0:0)
	q := r.query.Select("qemu")

	return &Qemu{
		query: q,
	}
}

// Accel is the QEMU acceleration mode. TCG is pure-software emulation and
// needs no host support; KVM requires an engine exposing /dev/kvm, which
// this module does not provision.
type QemuAccel string // qemu (../../../../:0:0)

func (QemuAccel) IsEnum() {}

func (v QemuAccel) Name() string {
	switch v {
	case QemuAccelKvm:
		return "KVM"
	case QemuAccelTcg:
		return "TCG"
	default:
		return ""
	}
}

func (v QemuAccel) Value() string {
	return string(v)
}

func (v *QemuAccel) MarshalJSON() ([]byte, error) {
	if *v == "" {
		return []byte(`""`), nil
	}
	name := v.Name()
	if name == "" {
		return nil, fmt.Errorf("invalid enum value %q", *v)
	}
	return json.Marshal(name)
}

func (v *QemuAccel) UnmarshalJSON(dt []byte) error {
	var s string
	if err := json.Unmarshal(dt, &s); err != nil {
		return err
	}
	switch s {
	case "":
		*v = ""
	case "KVM":
		*v = QemuAccelKvm
	case "TCG":
		*v = QemuAccelTcg
	default:
		return fmt.Errorf("invalid enum value %q", s)
	}
	return nil
}

const (
	QemuAccelKvm QemuAccel = "KVM"

	QemuAccelTcg QemuAccel = "TCG"
)

// Arch is the guest CPU architecture QEMU emulates. The enum value (e.g.
// "AARCH64") maps to QEMU's lowercase token internally (`qemu-system-aarch64`);
// invalid architectures are unrepresentable through the SDK.
//
// Note on rendered names: the Dagger Go SDK derives each GraphQL enum member
// from the *constant identifier* in SCREAMING_SNAKE_CASE and inserts an
// underscore at every letter↔digit boundary, so these surface as `AARCH_64`,
// `RISCV_64`, `X_86_64`, etc. The underscores are unavoidable SDK behavior; the
// internal Value() (the string literal below) is what drives the qemu mapping.
type QemuArch string // qemu (../../../../:0:0)

func (QemuArch) IsEnum() {}

func (v QemuArch) Name() string {
	switch v {
	case QemuArchAarch64:
		return "AARCH_64"
	case QemuArchArm:
		return "ARM"
	case QemuArchI386:
		return "I386"
	case QemuArchMips:
		return "MIPS"
	case QemuArchMipsel:
		return "MIPSEL"
	case QemuArchPpc:
		return "PPC"
	case QemuArchPpc64:
		return "PPC_64"
	case QemuArchRiscv32:
		return "RISCV_32"
	case QemuArchRiscv64:
		return "RISCV_64"
	case QemuArchX8664:
		return "X86_64"
	default:
		return ""
	}
}

func (v QemuArch) Value() string {
	return string(v)
}

func (v *QemuArch) MarshalJSON() ([]byte, error) {
	if *v == "" {
		return []byte(`""`), nil
	}
	name := v.Name()
	if name == "" {
		return nil, fmt.Errorf("invalid enum value %q", *v)
	}
	return json.Marshal(name)
}

func (v *QemuArch) UnmarshalJSON(dt []byte) error {
	var s string
	if err := json.Unmarshal(dt, &s); err != nil {
		return err
	}
	switch s {
	case "":
		*v = ""
	case "AARCH_64":
		*v = QemuArchAarch64
	case "ARM":
		*v = QemuArchArm
	case "I386":
		*v = QemuArchI386
	case "MIPS":
		*v = QemuArchMips
	case "MIPSEL":
		*v = QemuArchMipsel
	case "PPC":
		*v = QemuArchPpc
	case "PPC_64":
		*v = QemuArchPpc64
	case "RISCV_32":
		*v = QemuArchRiscv32
	case "RISCV_64":
		*v = QemuArchRiscv64
	case "X86_64":
		*v = QemuArchX8664
	default:
		return fmt.Errorf("invalid enum value %q", s)
	}
	return nil
}

const (
	QemuArchAarch64 QemuArch = "AARCH_64"

	QemuArchArm QemuArch = "ARM"

	QemuArchI386 QemuArch = "I386"

	QemuArchMips QemuArch = "MIPS"

	QemuArchMipsel QemuArch = "MIPSEL"

	QemuArchPpc QemuArch = "PPC"

	QemuArchPpc64 QemuArch = "PPC_64"

	QemuArchRiscv32 QemuArch = "RISCV_32"

	QemuArchRiscv64 QemuArch = "RISCV_64"

	QemuArchX8664 QemuArch = "X86_64"
)

// DiskFormat is the on-disk image format passed to `-drive format=`.
type QemuDiskFormat string // qemu (../../../../:0:0)

func (QemuDiskFormat) IsEnum() {}

func (v QemuDiskFormat) Name() string {
	switch v {
	case QemuDiskFormatQcow2:
		return "QCOW_2"
	case QemuDiskFormatRaw:
		return "RAW"
	default:
		return ""
	}
}

func (v QemuDiskFormat) Value() string {
	return string(v)
}

func (v *QemuDiskFormat) MarshalJSON() ([]byte, error) {
	if *v == "" {
		return []byte(`""`), nil
	}
	name := v.Name()
	if name == "" {
		return nil, fmt.Errorf("invalid enum value %q", *v)
	}
	return json.Marshal(name)
}

func (v *QemuDiskFormat) UnmarshalJSON(dt []byte) error {
	var s string
	if err := json.Unmarshal(dt, &s); err != nil {
		return err
	}
	switch s {
	case "":
		*v = ""
	case "QCOW_2":
		*v = QemuDiskFormatQcow2
	case "RAW":
		*v = QemuDiskFormatRaw
	default:
		return fmt.Errorf("invalid enum value %q", s)
	}
	return nil
}

const (
	QemuDiskFormatQcow2 QemuDiskFormat = "QCOW_2"

	QemuDiskFormatRaw QemuDiskFormat = "RAW"
)

// DiskInterface is the guest-visible disk bus passed to `-drive if=`.
type QemuDiskInterface string // qemu (../../../../:0:0)

func (QemuDiskInterface) IsEnum() {}

func (v QemuDiskInterface) Name() string {
	switch v {
	case QemuDiskInterfaceIde:
		return "IDE"
	case QemuDiskInterfaceNvme:
		return "NVME"
	case QemuDiskInterfaceSd:
		return "SD"
	case QemuDiskInterfaceVirtio:
		return "VIRTIO"
	default:
		return ""
	}
}

func (v QemuDiskInterface) Value() string {
	return string(v)
}

func (v *QemuDiskInterface) MarshalJSON() ([]byte, error) {
	if *v == "" {
		return []byte(`""`), nil
	}
	name := v.Name()
	if name == "" {
		return nil, fmt.Errorf("invalid enum value %q", *v)
	}
	return json.Marshal(name)
}

func (v *QemuDiskInterface) UnmarshalJSON(dt []byte) error {
	var s string
	if err := json.Unmarshal(dt, &s); err != nil {
		return err
	}
	switch s {
	case "":
		*v = ""
	case "IDE":
		*v = QemuDiskInterfaceIde
	case "NVME":
		*v = QemuDiskInterfaceNvme
	case "SD":
		*v = QemuDiskInterfaceSd
	case "VIRTIO":
		*v = QemuDiskInterfaceVirtio
	default:
		return fmt.Errorf("invalid enum value %q", s)
	}
	return nil
}

const (
	QemuDiskInterfaceIde QemuDiskInterface = "IDE"

	QemuDiskInterfaceNvme QemuDiskInterface = "NVME"

	QemuDiskInterfaceSd QemuDiskInterface = "SD"

	QemuDiskInterfaceVirtio QemuDiskInterface = "VIRTIO"
)
