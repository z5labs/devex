// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

func (r *Query) Tests() *Tests { // tests (../../../../../:0:0)
	q := r.query.Select("tests")

	return &Tests{
		query: q,
	}
}

type Tests struct { // tests (../../../../../:0:0)
	query *querybuilder.Selection

	affectedModulesReportsWhatChangeReached         *Void
	all                                             *Void
	id                                              *ID
	memoStoreSelfTestPasses                         *Void
	newRejectsAnUnknownMemoStore                    *Void
	newRejectsMalformedTimeouts                     *Void
	newRejectsMemoTokenWithoutRepo                  *Void
	planAcceptsSymbolicRevisions                    *Void
	planAlwaysRunsUnhashableLeg                     *Void
	planAppliesTimeoutOverrides                     *Void
	planAttributesDeletedPathsToTheirModule         *Void
	planDropsKnownGoodLeg                           *Void
	planEmitsGithubActionsMatrix                    *Void
	planEmitsJenkinsParallelStages                  *Void
	planErrorsOnWorkspaceWithNoModules              *Void
	planFromRepoMatchesPlanFromWorkspace            *Void
	planGlobalInputsAreRootDependencyClosure        *Void
	planIgnoresPathsInNoSourceContext               *Void
	planLoadsOnlyAffectedModules                    *Void
	planRecordsPassesFromJenkinsBranches            *Void
	planRefusesRecordCommandForDataFormats          *Void
	planRefusesRecordedPassesWhenGlobalInputChanged *Void
	planRunsEverythingOnAnUnresolvableRevision      *Void
	planRunsEverythingOnAnUnusableDiffRange         *Void
	planRunsEverythingOnGlobalPathChange            *Void
	planSelectsAffectedModuleChecks                 *Void
	planSplitsNamedModulesOnTheRunEverythingPath    *Void
	recordPassNeedsTheRunsRef                       *Void
	recordPassNeverFailsThePassingCheck             *Void
	recordPassReachesTheStoreFromTrustedRef         *Void
	recordPassRefusesAnUntrustedRef                 *Void
	recordPassSaysTheActionsCacheIsUnwritable       *Void
	recordPassSkipsAnUnhashableLeg                  *Void
	recordPassSkipsWithNoStoreConfigured            *Void
	selectionSelfTestPasses                         *Void
}

func (r *Tests) WithGraphQLQuery(q *querybuilder.Selection) *Tests {
	return &Tests{
		query: q,
	}
}

// AffectedModulesReportsWhatChangeReached proves the attribution half of the plan
// is available on its own, for a caller that wants to know what a change reached
// without paying to enumerate checks.
func (r *Tests) AffectedModulesReportsWhatChangeReached(ctx context.Context) error {
	if r.affectedModulesReportsWhatChangeReached != nil {
		return nil
	}
	q := r.query.Select("affectedModulesReportsWhatChangeReached")

	return q.Execute(ctx)
}

// All runs every test.
//
// The concurrency cap keeps a burst of fixture uploads and module loads from
// crowding out the engine; the tests themselves are independent.
func (r *Tests) All(ctx context.Context) error {
	if r.all != nil {
		return nil
	}
	q := r.query.Select("all")

	return q.Execute(ctx)
}

// A unique identifier for this Tests.
func (r *Tests) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *Tests) XXX_GraphQLType() string {
	return "Tests"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *Tests) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *Tests) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *Tests) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *Tests) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = Tests{query: selectNode(dag.query, id, "Tests")}
	return nil
}

// MemoStoreSelfTestPasses runs the module's own store check the way a consumer's
// CI would, so a regression in recording, idempotence, TTL filtering or scope
// isolation fails here too rather than only where it is installed.
func (r *Tests) MemoStoreSelfTestPasses(ctx context.Context) error {
	if r.memoStoreSelfTestPasses != nil {
		return nil
	}
	q := r.query.Select("memoStoreSelfTestPasses")

	return q.Execute(ctx)
}

// NewRejectsAnUnknownMemoStore proves a misspelled store is a configuration
// error rather than a silent fall back to the read-only one, which would record
// nothing and look like a store nobody has written to.
func (r *Tests) NewRejectsAnUnknownMemoStore(ctx context.Context) error {
	if r.newRejectsAnUnknownMemoStore != nil {
		return nil
	}
	q := r.query.Select("newRejectsAnUnknownMemoStore")

	return q.Execute(ctx)
}

// NewRejectsMalformedTimeouts proves a timeout table that cannot be read is an
// error. A typo'd key already fails quietly — the default applies — so the one
// thing left to catch loudly is a table nothing could be read from.
func (r *Tests) NewRejectsMalformedTimeouts(ctx context.Context) error {
	if r.newRejectsMalformedTimeouts != nil {
		return nil
	}
	q := r.query.Select("newRejectsMalformedTimeouts")

	return q.Execute(ctx)
}

// NewRejectsMemoTokenWithoutRepo proves a credential with nothing to scope it is
// an error rather than a store that silently reads nothing — which would look
// exactly like a workspace with no recorded passes.
func (r *Tests) NewRejectsMemoTokenWithoutRepo(ctx context.Context) error {
	if r.newRejectsMemoTokenWithoutRepo != nil {
		return nil
	}
	q := r.query.Select("newRejectsMemoTokenWithoutRepo")

	return q.Execute(ctx)
}

// PlanAcceptsSymbolicRevisions proves a range named the way a person names one —
// a branch, a tag, HEAD-relative, or a mixture of those and a SHA — plans exactly
// what the equivalent pair of SHAs plans. CI passes SHAs because that is what the
// event payload carries; anyone running Plan by hand has `main` and `HEAD`.
//
// The range is the single commit that touches mods/a, so the expected plan is a
// strict subset of the workspace. That matters: a revision that does not resolve
// falls back to running everything, which an assertion against a full plan could
// not tell from success.
func (r *Tests) PlanAcceptsSymbolicRevisions(ctx context.Context) error {
	if r.planAcceptsSymbolicRevisions != nil {
		return nil
	}
	q := r.query.Select("planAcceptsSymbolicRevisions")

	return q.Execute(ctx)
}

// PlanAlwaysRunsUnhashableLeg proves the fail-safe half of memoization: a module
// with an input that has no object id at HEAD — an untracked file, or a dirty
// working tree — is never memoized, because a hash built from what git can see
// would not describe what the check reads.
func (r *Tests) PlanAlwaysRunsUnhashableLeg(ctx context.Context) error {
	if r.planAlwaysRunsUnhashableLeg != nil {
		return nil
	}
	q := r.query.Select("planAlwaysRunsUnhashableLeg")

	return q.Execute(ctx)
}

// PlanAppliesTimeoutOverrides proves the timeout table: an override keyed by a
// leg's name beats one keyed by its module, both beat the default, and the job
// budget always follows the step budget.
func (r *Tests) PlanAppliesTimeoutOverrides(ctx context.Context) error {
	if r.planAppliesTimeoutOverrides != nil {
		return nil
	}
	q := r.query.Select("planAppliesTimeoutOverrides")

	return q.Execute(ctx)
}

// PlanAttributesDeletedPathsToTheirModule proves a deleted file is attributed to
// its module rather than dropped. No source context can contain a path that no
// longer exists, so a deletion is indistinguishable from a file declared out —
// which is why deletions are attributed to their module instead.
func (r *Tests) PlanAttributesDeletedPathsToTheirModule(ctx context.Context) error {
	if r.planAttributesDeletedPathsToTheirModule != nil {
		return nil
	}
	q := r.query.Select("planAttributesDeletedPathsToTheirModule")

	return q.Execute(ctx)
}

// PlanDropsKnownGoodLeg proves the point of memoization: a leg whose whole input
// closure hashes to a value some earlier run already passed on is dropped, and
// only that leg is.
func (r *Tests) PlanDropsKnownGoodLeg(ctx context.Context) error {
	if r.planDropsKnownGoodLeg != nil {
		return nil
	}
	q := r.query.Select("planDropsKnownGoodLeg")

	return q.Execute(ctx)
}

// PlanEmitsGithubActionsMatrix proves the one non-canonical format carries the
// same legs on a single line, which is what a GITHUB_OUTPUT assignment and fromJSON
// need.
func (r *Tests) PlanEmitsGithubActionsMatrix(ctx context.Context) error {
	if r.planEmitsGithubActionsMatrix != nil {
		return nil
	}
	q := r.query.Select("planEmitsGithubActionsMatrix")

	return q.Execute(ctx)
}

// PlanEmitsJenkinsParallelStages proves the Jenkins form is what a declarative
// pipeline's parallel step actually takes — a Map of branch name to Closure — and
// that running a branch runs that leg's checks under that leg's budget.
//
// It evaluates the output in a real Groovy runtime rather than matching it as
// text, because escaping is the half that breaks: a plan is handed to `parallel`
// unread, so a mis-escaped quote is a pipeline that does not parse, and nothing
// between the renderer and Jenkins would report it.
func (r *Tests) PlanEmitsJenkinsParallelStages(ctx context.Context) error {
	if r.planEmitsJenkinsParallelStages != nil {
		return nil
	}
	q := r.query.Select("planEmitsJenkinsParallelStages")

	return q.Execute(ctx)
}

// PlanErrorsOnWorkspaceWithNoModules proves a workspace it cannot read is an
// error rather than an empty plan. An empty matrix skips the run job and passes the
// gate having run nothing, which is the one failure mode worth failing closed for.
func (r *Tests) PlanErrorsOnWorkspaceWithNoModules(ctx context.Context) error {
	if r.planErrorsOnWorkspaceWithNoModules != nil {
		return nil
	}
	q := r.query.Select("planErrorsOnWorkspaceWithNoModules")

	return q.Execute(ctx)
}

// PlanFromRepoMatchesPlanFromWorkspace proves the two ways of naming the
// repository are the same repository.
//
// The rest of this suite passes only a workspace, because that is what a module
// caller can build out of a directory. repo is the other seam — the escape hatch
// for a caller whose .git is a file rather than a directory — and it overrides
// the workspace rather than sitting beside it, so the two have to agree.
func (r *Tests) PlanFromRepoMatchesPlanFromWorkspace(ctx context.Context) error {
	if r.planFromRepoMatchesPlanFromWorkspace != nil {
		return nil
	}
	q := r.query.Select("planFromRepoMatchesPlanFromWorkspace")

	return q.Execute(ctx)
}

// PlanGlobalInputsAreRootDependencyClosure proves that the global inputs folded
// into every leg's hash are the root module's whole dependency closure and not
// just its own source context.
//
// The fixture's root module depends on mods/global, which nothing else depends
// on. Changing only that module must move the input hash of an unrelated module's
// leg — otherwise moving the CI engine itself into a dependency (which is exactly
// what adopting this module does) would leave it outside the trust boundary of
// the hashes it computes.
func (r *Tests) PlanGlobalInputsAreRootDependencyClosure(ctx context.Context) error {
	if r.planGlobalInputsAreRootDependencyClosure != nil {
		return nil
	}
	q := r.query.Select("planGlobalInputsAreRootDependencyClosure")

	return q.Execute(ctx)
}

// PlanIgnoresPathsInNoSourceContext proves a change to a file no module ships —
// a module's own README, declared out via dagger.json "include" — selects nothing.
// Only the root module's checks, which always run, remain.
func (r *Tests) PlanIgnoresPathsInNoSourceContext(ctx context.Context) error {
	if r.planIgnoresPathsInNoSourceContext != nil {
		return nil
	}
	q := r.query.Select("planIgnoresPathsInNoSourceContext")

	return q.Execute(ctx)
}

// PlanLoadsOnlyAffectedModules proves the performance property the whole design
// rests on, by counting rather than timing: producing a plan for a narrow change
// loads the affected modules and nothing else.
func (r *Tests) PlanLoadsOnlyAffectedModules(ctx context.Context) error {
	if r.planLoadsOnlyAffectedModules != nil {
		return nil
	}
	q := r.query.Select("planLoadsOnlyAffectedModules")

	return q.Execute(ctx)
}

// PlanRecordsPassesFromJenkinsBranches proves the memoization half of the Jenkins
// form: a branch records its own leg's hash when the leg passes, and records
// nothing when it fails.
//
// Both outcomes are asserted because only one of them is a property of what was
// rendered. That the recording runs on success is visible in the text; that it
// does *not* run on failure is a property of Groovy — `sh` throws, the closure
// unwinds, the step after it is never reached — and rearranging the render into
// something that still looks right would break it silently. So the plan is
// evaluated in a real Groovy runtime with a failing step, exactly as with a
// failing check.
//
// The leg the plan refuses to hash is the other half: a plan says "never memoize
// this" with an empty hash, and a branch that recorded one anyway would write an
// entry no later run could ever match — or, worse, one keyed on nothing.
func (r *Tests) PlanRecordsPassesFromJenkinsBranches(ctx context.Context) error {
	if r.planRecordsPassesFromJenkinsBranches != nil {
		return nil
	}
	q := r.query.Select("planRecordsPassesFromJenkinsBranches")

	return q.Execute(ctx)
}

// PlanRefusesRecordCommandForDataFormats proves the option is not silently
// dropped by the formats that cannot render one. JSON and GITHUB_ACTIONS carry
// each leg's hash as data for a surrounding job to record; accepting a record
// command there would hand back a plan that records nothing, which a consumer
// would discover as a memoization store that never fills up.
func (r *Tests) PlanRefusesRecordCommandForDataFormats(ctx context.Context) error {
	if r.planRefusesRecordCommandForDataFormats != nil {
		return nil
	}
	q := r.query.Select("planRefusesRecordCommandForDataFormats")

	return q.Execute(ctx)
}

// PlanRefusesRecordedPassesWhenGlobalInputChanged proves the trust boundary. Pass
// records are written by the same CI run that produced them, so a change that
// could alter the recording machinery must retire every recorded pass — even the
// ones whose hashes still match.
func (r *Tests) PlanRefusesRecordedPassesWhenGlobalInputChanged(ctx context.Context) error {
	if r.planRefusesRecordedPassesWhenGlobalInputChanged != nil {
		return nil
	}
	q := r.query.Select("planRefusesRecordedPassesWhenGlobalInputChanged")

	return q.Execute(ctx)
}

// PlanRunsEverythingOnAnUnresolvableRevision proves a revision that names no
// commit — a typo, a branch that was deleted, a shallow clone that never fetched
// the base — runs everything rather than erroring or, worse, planning nothing.
//
// It is the same fail-safe an all-zeros base takes, and it is why resolution
// failure is deliberately not promoted to an error: a name the repository cannot
// resolve must cost a run its time, never its coverage.
func (r *Tests) PlanRunsEverythingOnAnUnresolvableRevision(ctx context.Context) error {
	if r.planRunsEverythingOnAnUnresolvableRevision != nil {
		return nil
	}
	q := r.query.Select("planRunsEverythingOnAnUnresolvableRevision")

	return q.Execute(ctx)
}

// PlanRunsEverythingOnAnUnusableDiffRange proves the fail-safe: a base that cannot
// be diffed — a new branch, whose before-SHA GitHub sends as all zeros — runs
// everything rather than nothing.
//
// The all-zeros SHA is a sentinel and not a revision, so it keeps that meaning
// however the other side is spelled: it is rejected before the repository is
// consulted, and a symbolic head cannot turn it into a range worth diffing.
func (r *Tests) PlanRunsEverythingOnAnUnusableDiffRange(ctx context.Context) error {
	if r.planRunsEverythingOnAnUnusableDiffRange != nil {
		return nil
	}
	q := r.query.Select("planRunsEverythingOnAnUnusableDiffRange")

	return q.Execute(ctx)
}

// PlanRunsEverythingOnGlobalPathChange proves a change to the paths that govern
// how CI runs at all runs everything — and does it the cheap way: one leg per
// module, with no module loaded to produce the plan.
func (r *Tests) PlanRunsEverythingOnGlobalPathChange(ctx context.Context) error {
	if r.planRunsEverythingOnGlobalPathChange != nil {
		return nil
	}
	q := r.query.Select("planRunsEverythingOnGlobalPathChange")

	return q.Execute(ctx)
}

// PlanSelectsAffectedModuleChecks proves the core promise: a change to one module
// plans that module's checks and every check that legitimately depends on it, and
// nothing else.
//
// The fixture's b depends on a, so touching a must reach both; c and dirty depend
// on neither and must be absent. The root module is always there — its checks
// answer questions about the workspace as a whole.
func (r *Tests) PlanSelectsAffectedModuleChecks(ctx context.Context) error {
	if r.planSelectsAffectedModuleChecks != nil {
		return nil
	}
	q := r.query.Select("planSelectsAffectedModuleChecks")

	return q.Execute(ctx)
}

// PlanSplitsNamedModulesOnTheRunEverythingPath proves the escape hatch for a
// module whose checks must not share a leg: named modules are enumerated even when
// everything runs, and every other module still gets one coarse leg and is still
// never loaded.
func (r *Tests) PlanSplitsNamedModulesOnTheRunEverythingPath(ctx context.Context) error {
	if r.planSplitsNamedModulesOnTheRunEverythingPath != nil {
		return nil
	}
	q := r.query.Select("planSplitsNamedModulesOnTheRunEverythingPath")

	return q.Execute(ctx)
}

// RecordPassNeedsTheRunsRef is the one hard error. With no ref there is no scope
// to judge, so a silent refusal would be indistinguishable from a scope that was
// judged and rejected — and a CI system that never passes its ref would record
// nothing forever while reading as if it were.
func (r *Tests) RecordPassNeedsTheRunsRef(ctx context.Context) error {
	if r.recordPassNeedsTheRunsRef != nil {
		return nil
	}
	q := r.query.Select("recordPassNeedsTheRunsRef")

	return q.Execute(ctx)
}

// RecordPassNeverFailsThePassingCheck pins the contract the whole function
// hangs on: recording runs after the work is already green, so a store that
// cannot be reached at all reports itself in the return value and never as an
// error. A store outage that turned a passing suite red would be strictly worse
// than no memoization.
func (r *Tests) RecordPassNeverFailsThePassingCheck(ctx context.Context) error {
	if r.recordPassNeverFailsThePassingCheck != nil {
		return nil
	}
	q := r.query.Select("recordPassNeverFailsThePassingCheck")

	return q.Execute(ctx)
}

// RecordPassReachesTheStoreFromTrustedRef is the other half of the same
// boundary, and the reason the test above proves anything: from a nominated ref
// the module really does go on to write, so REFUSED is a judgement about the ref
// and not a planner that never writes at all. The store here cannot answer, so
// the outcome is FAILED — which is also the point of the next test.
func (r *Tests) RecordPassReachesTheStoreFromTrustedRef(ctx context.Context) error {
	if r.recordPassReachesTheStoreFromTrustedRef != nil {
		return nil
	}
	q := r.query.Select("recordPassReachesTheStoreFromTrustedRef")

	return q.Execute(ctx)
}

// RecordPassRefusesAnUntrustedRef is the trust boundary for module-side writes.
// A run whose ref the caller never nominated must record nothing, and it must
// refuse before it reaches the store rather than relying on the store to say no —
// so this points it at an API that cannot answer, and still expects REFUSED.
func (r *Tests) RecordPassRefusesAnUntrustedRef(ctx context.Context) error {
	if r.recordPassRefusesAnUntrustedRef != nil {
		return nil
	}
	q := r.query.Select("recordPassRefusesAnUntrustedRef")

	return q.Execute(ctx)
}

// RecordPassSaysTheActionsCacheIsUnwritable keeps the old constraint honest. The
// Actions cache still needs ACTIONS_RUNTIME_TOKEN, so a consumer who configures it
// and then calls RecordPass has to be told, in the return value, rather than
// getting a silent no-op that looks exactly like a store nobody has recorded into.
func (r *Tests) RecordPassSaysTheActionsCacheIsUnwritable(ctx context.Context) error {
	if r.recordPassSaysTheActionsCacheIsUnwritable != nil {
		return nil
	}
	q := r.query.Select("recordPassSaysTheActionsCacheIsUnwritable")

	return q.Execute(ctx)
}

// RecordPassSkipsAnUnhashableLeg proves the empty hash means the same thing on
// the write side as on the read side. A leg the planner could not hash must never
// have anything recorded for it — a hash of "" would otherwise become an entry
// every later unhashable leg matched.
func (r *Tests) RecordPassSkipsAnUnhashableLeg(ctx context.Context) error {
	if r.recordPassSkipsAnUnhashableLeg != nil {
		return nil
	}
	q := r.query.Select("recordPassSkipsAnUnhashableLeg")

	return q.Execute(ctx)
}

// RecordPassSkipsWithNoStoreConfigured covers the default posture: memoization
// off is a planner that records nothing, not one that errors.
func (r *Tests) RecordPassSkipsWithNoStoreConfigured(ctx context.Context) error {
	if r.recordPassSkipsWithNoStoreConfigured != nil {
		return nil
	}
	q := r.query.Select("recordPassSkipsWithNoStoreConfigured")

	return q.Execute(ctx)
}

// SelectionSelfTestPasses runs the module's own check the way a consumer's CI
// would, so a regression in the pure selection and hashing rules fails here too
// rather than only where it is installed.
func (r *Tests) SelectionSelfTestPasses(ctx context.Context) error {
	if r.selectionSelfTestPasses != nil {
		return nil
	}
	q := r.query.Select("selectionSelfTestPasses")

	return q.Execute(ctx)
}
