meta {
  name: token
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/token
  body: none
  auth: none
}

headers {
  X-Api-Key: {{apiKey}}
  X-Session-Token: {{sessionToken}}
  X-Refresh-Token: {{refreshToken}}
}

assert {
  res.status: eq 200
}
