// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

// Retrieve the binding value, as type CryptoExamples
func (r *Binding) AsCryptoExamples() *CryptoExamples { // crypto-examples (../../../../../daggerverse/crypto/examples/go/main.go:16:6)
	q := r.query.Select("asCryptoExamples")

	return &CryptoExamples{
		query: q,
	}
}

// CryptoExamples is the module's main object: a namespace for the crypto usage
// recipes.
type CryptoExamples struct { // crypto-examples (../../../../../daggerverse/crypto/examples/go/main.go:16:6)
	query *querybuilder.Selection

	hashSourceFile *string
	hashWithSha3   *string
	id             *ID
}

func (r *CryptoExamples) WithGraphQLQuery(q *querybuilder.Selection) *CryptoExamples {
	return &CryptoExamples{
		query: q,
	}
}

// GenerateEd25519SshKey returns a directory holding a fresh Ed25519 SSH
// identity under the names ssh expects: the PKCS#8 private key as
// `id_ed25519` and the OpenSSH-formatted public key as `id_ed25519.pub`, the
// single line you paste into an `authorized_keys` file or a Git host.
//
//	dagger call generate-ed25519-ssh-key export --path ~/.ssh
//
// This is the SSH flow in miniature: crypto emits the private key as PEM and
// the public key in OpenSSH wire format from the same generated key, so no
// ssh-keygen container is needed anywhere in the pipeline. As in
// GenerateRsaKeypair, the key is resolved to one instance before both files
// are derived from it.
func (r *CryptoExamples) GenerateEd25519SshKey() *Directory { // crypto-examples (../../../../../daggerverse/crypto/examples/go/main.go:112:1)
	q := r.query.Select("generateEd25519SshKey")

	return &Directory{
		query: q,
	}
}

// CryptoExamplesGenerateRsaKeypairOpts contains options for CryptoExamples.GenerateRsaKeypair
type CryptoExamplesGenerateRsaKeypairOpts struct {
	//
	// Modulus size in bits. Matches crypto's own default; drop to 2048 when
	// you want the recipe to finish quickly.
	//
	//
	// Default: 4096
	Bits int // crypto-examples (../../../../../daggerverse/crypto/examples/go/main.go:85:2)
}

// GenerateRsaKeypair returns a directory holding a freshly generated RSA
// keypair: the PKCS#8 private key as `key.pem` and its PKIX public half as
// `key.pub.pem`. Export it to see both files at once:
//
//	dagger call generate-rsa-keypair export --path ./keys
//
// The two halves are matched because the recipe resolves the generated key to
// a single instance (via its ID) before deriving files from it. Selecting
// .pem() and .publicKeyPem() straight off the generator would build two
// independent queries, and because key generation carries `each would run its own generator and hand back halves of two different keys.
func (r *CryptoExamples) GenerateRsaKeypair(opts ...CryptoExamplesGenerateRsaKeypairOpts) *Directory { // crypto-examples (../../../../../daggerverse/crypto/examples/go/main.go:79:1)
	q := r.query.Select("generateRsaKeypair")
	for i := len(opts) - 1; i >= 0; i-- {
		// `bits` optional argument
		if !querybuilder.IsZeroValue(opts[i].Bits) {
			q = q.Arg("bits", opts[i].Bits)
		}
	}

	return &Directory{
		query: q,
	}
}

// CryptoExamplesHashSourceFileOpts contains options for CryptoExamples.HashSourceFile
type CryptoExamplesHashSourceFileOpts struct {
	//
	// The file to digest. Defaults to a small built-in sample so the recipe
	// runs with no arguments.
	//
	File *File // crypto-examples (../../../../../daggerverse/crypto/examples/go/main.go:45:2)
}

// HashSourceFile returns the SHA-256 hex digest of a caller-supplied file --
// the everyday recipe for fingerprinting a build artifact, a lockfile, or a
// downloaded release before you trust it.
//
// Hashing is pure, so this function is deliberately left on Dagger's default
// caching: the same bytes always digest to the same string, and a repeat call
// should replay rather than re-read the file.
func (r *CryptoExamples) HashSourceFile(ctx context.Context, opts ...CryptoExamplesHashSourceFileOpts) (string, error) { // crypto-examples (../../../../../daggerverse/crypto/examples/go/main.go:39:1)
	if r.hashSourceFile != nil {
		return *r.hashSourceFile, nil
	}
	q := r.query.Select("hashSourceFile")
	for i := len(opts) - 1; i >= 0; i-- {
		// `file` optional argument
		if !querybuilder.IsZeroValue(opts[i].File) {
			q = q.Arg("file", opts[i].File)
		}
	}

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// CryptoExamplesHashWithSha3Opts contains options for CryptoExamples.HashWithSha3
type CryptoExamplesHashWithSha3Opts struct {
	//
	// The file to digest. Defaults to a small built-in sample so the recipe
	// runs with no arguments.
	//
	File *File // crypto-examples (../../../../../daggerverse/crypto/examples/go/main.go:61:2)
}

// HashWithSha3 returns the SHA3-512 hex digest of the same file
// HashSourceFile digests. Run both to see that SHA-2 and SHA-3 are different
// algorithms, not different sizes of one: SHA3-512 is a Keccak sponge, so its
// digest shares no bytes with the SHA-256 one even though the input is
// identical. Reach for it when a policy mandates the SHA-3 family.
func (r *CryptoExamples) HashWithSha3(ctx context.Context, opts ...CryptoExamplesHashWithSha3Opts) (string, error) { // crypto-examples (../../../../../daggerverse/crypto/examples/go/main.go:55:1)
	if r.hashWithSha3 != nil {
		return *r.hashWithSha3, nil
	}
	q := r.query.Select("hashWithSha3")
	for i := len(opts) - 1; i >= 0; i-- {
		// `file` optional argument
		if !querybuilder.IsZeroValue(opts[i].File) {
			q = q.Arg("file", opts[i].File)
		}
	}

	var response string

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// A unique identifier for this CryptoExamples.
func (r *CryptoExamples) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *CryptoExamples) XXX_GraphQLType() string {
	return "CryptoExamples"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *CryptoExamples) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *CryptoExamples) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *CryptoExamples) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *CryptoExamples) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = CryptoExamples{query: selectNode(dag.query, id, "CryptoExamples")}
	return nil
}

// AsNode returns this CryptoExamples as a Node.
// This is a local type conversion — no GraphQL call.
func (r *CryptoExamples) AsNode() Node {
	return &NodeClient{
		query: r.query,
	}
}

// Create or update a binding of type CryptoExamples in the environment
func (r *Env) WithCryptoExamplesInput(name string, value *CryptoExamples, description string) *Env { // crypto-examples (../../../../../daggerverse/crypto/examples/go/main.go:16:6)
	assertNotNil("value", value)
	q := r.query.Select("withCryptoExamplesInput")
	q = q.Arg("name", name)
	q = q.Arg("value", value)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Declare a desired CryptoExamples output to be assigned in the environment
func (r *Env) WithCryptoExamplesOutput(name string, description string) *Env { // crypto-examples (../../../../../daggerverse/crypto/examples/go/main.go:16:6)
	q := r.query.Select("withCryptoExamplesOutput")
	q = q.Arg("name", name)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// CryptoExamples is the module's main object: a namespace for the crypto usage
// recipes.
func (r *Query) CryptoExamples() *CryptoExamples { // crypto-examples (../../../../../daggerverse/crypto/examples/go/main.go:16:6)
	q := r.query.Select("cryptoExamples")

	return &CryptoExamples{
		query: q,
	}
}
