// Code generated by dagger. DO NOT EDIT.

package dagger

import (
	"context"
	"encoding/json"

	"github.com/dagger/querybuilder"
)

// Retrieve the binding value, as type SkillGen
func (r *Binding) AsSkillGen() *SkillGen { // skill-gen (../../../../../daggerverse/skill-gen/main.go:22:6)
	q := r.query.Select("asSkillGen")

	return &SkillGen{
		query: q,
	}
}

// Create or update a binding of type SkillGen in the environment
func (r *Env) WithSkillGenInput(name string, value *SkillGen, description string) *Env { // skill-gen (../../../../../daggerverse/skill-gen/main.go:22:6)
	assertNotNil("value", value)
	q := r.query.Select("withSkillGenInput")
	q = q.Arg("name", name)
	q = q.Arg("value", value)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// Declare a desired SkillGen output to be assigned in the environment
func (r *Env) WithSkillGenOutput(name string, description string) *Env { // skill-gen (../../../../../daggerverse/skill-gen/main.go:22:6)
	q := r.query.Select("withSkillGenOutput")
	q = q.Arg("name", name)
	q = q.Arg("description", description)

	return &Env{
		query: q,
	}
}

// SkillGen is the module's root object.
func (r *Query) SkillGen() *SkillGen { // skill-gen (../../../../../daggerverse/skill-gen/main.go:22:6)
	q := r.query.Select("skillGen")

	return &SkillGen{
		query: q,
	}
}

// SkillGen is the module's root object.
type SkillGen struct { // skill-gen (../../../../../daggerverse/skill-gen/main.go:22:6)
	query *querybuilder.Selection

	id *ID
}

func (r *SkillGen) WithGraphQLQuery(q *querybuilder.Selection) *SkillGen {
	return &SkillGen{
		query: q,
	}
}

// A unique identifier for this SkillGen.
func (r *SkillGen) ID(ctx context.Context) (ID, error) {
	if r.id != nil {
		return *r.id, nil
	}
	q := r.query.Select("id")

	var response ID

	q = q.Bind(&response)
	return response, q.Execute(ctx)
}

// XXX_GraphQLType is an internal function. It returns the native GraphQL type name
func (r *SkillGen) XXX_GraphQLType() string {
	return "SkillGen"
}

// XXX_GraphQLIDType is an internal function. It returns the native GraphQL type name for the ID of this object
func (r *SkillGen) XXX_GraphQLIDType() string {
	return "ID"
}

// XXX_GraphQLID is an internal function. It returns the underlying type ID
func (r *SkillGen) XXX_GraphQLID(ctx context.Context) (string, error) {
	id, err := r.ID(ctx)
	if err != nil {
		return "", err
	}
	return string(id), nil
}

func (r *SkillGen) MarshalJSON() ([]byte, error) {
	id, err := r.ID(marshalCtx)
	if err != nil {
		return nil, err
	}
	return json.Marshal(id)
}
func (r *SkillGen) UnmarshalJSON(bs []byte) error {
	var id string
	err := json.Unmarshal(bs, &id)
	if err != nil {
		return err
	}
	*r = SkillGen{query: selectNode(dag.query, id, "SkillGen")}
	return nil
}

// SkillGenPostgresOpts contains options for SkillGen.Postgres
type SkillGenPostgresOpts struct {

	// Default: 5432
	Port int // skill-gen (../../../../../daggerverse/skill-gen/main.go:57:2)
	//
	// serverCa pins the server's CA (sslmode=verify-full). Required for TLS/mTLS; omit for plaintext.
	//
	ServerCa *File // skill-gen (../../../../../daggerverse/skill-gen/main.go:63:2)
	//
	// clientCert is the PEM client leaf for mTLS; its CN must equal `user`. Provide with clientKey.
	//
	ClientCert *File // skill-gen (../../../../../daggerverse/skill-gen/main.go:66:2)
	//
	// clientKey is the PEM PKCS#8 client private key for mTLS. Provide with clientCert.
	//
	ClientKey *Secret // skill-gen (../../../../../daggerverse/skill-gen/main.go:69:2)
}

// Postgres introspects the PostgreSQL database at host:port and returns a
// generated `pg-<db>` Claude Code skill as a *dagger.Directory. The returned
// tree is the skill directory itself (SKILL.md at its root) with no enclosing
// `.claude/skills/` wrapper, so it can be dropped straight into Claude Code,
// Copilot, or any other gen-AI environment. The function never touches the
// host filesystem — the caller exports the tree wherever they want (e.g.
// `export --path pg-<db>` for Copilot, or `export --path .claude/skills/pg-<db>`
// for Claude Code).
//
// Introspection is delegated to the postgres module's pgx-backed
// Client.QueryJSON; only core types cross this module's boundary
// (*dagger.Secret/*dagger.File in, *dagger.Directory out). `db` is validated
// against ^[A-Za-z0-9_-]+$ before any network I/O, because it flows into the
// skill's `name: pg-<db>` frontmatter and into filenames. Any introspection
// failure aborts with a non-zero error and no partial output.
//
// The transport security mode is inferred from the supplied cert params, so it
// can never disagree with the material actually provided:
//
//   - none → plaintext (scram-sha-256 over an unencrypted TCP connection).
//   - serverCa only → one-way TLS (sslmode=verify-full against serverCa).
//   - serverCa + clientCert + clientKey → mTLS; the client presents its leaf
//     to satisfy the server's clientcert=verify-full. The client cert's CN
//     must equal `user`, or the server rejects it with a misleading 28P01.
//
// serverCa and clientCert are public PEM certs (*dagger.File); clientKey is the
// PEM PKCS#8 private key kept as a *dagger.Secret end-to-end.
func (r *SkillGen) Postgres(host string, user string, db string, password *Secret, opts ...SkillGenPostgresOpts) *Directory { // skill-gen (../../../../../daggerverse/skill-gen/main.go:53:1)
	assertNotNil("password", password)
	q := r.query.Select("postgres")
	for i := len(opts) - 1; i >= 0; i-- {
		// `port` optional argument
		if !querybuilder.IsZeroValue(opts[i].Port) {
			q = q.Arg("port", opts[i].Port)
		}
		// `serverCa` optional argument
		if !querybuilder.IsZeroValue(opts[i].ServerCa) {
			q = q.Arg("serverCa", opts[i].ServerCa)
		}
		// `clientCert` optional argument
		if !querybuilder.IsZeroValue(opts[i].ClientCert) {
			q = q.Arg("clientCert", opts[i].ClientCert)
		}
		// `clientKey` optional argument
		if !querybuilder.IsZeroValue(opts[i].ClientKey) {
			q = q.Arg("clientKey", opts[i].ClientKey)
		}
	}
	q = q.Arg("host", host)
	q = q.Arg("user", user)
	q = q.Arg("db", db)
	q = q.Arg("password", password)

	return &Directory{
		query: q,
	}
}

// AsNode returns this SkillGen as a Node.
// This is a local type conversion — no GraphQL call.
func (r *SkillGen) AsNode() Node {
	return &NodeClient{
		query: r.query,
	}
}
